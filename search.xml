<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>FTP断开连接，并返回421错误码</title>
    <url>/2020/11/21/FTP%E6%96%AD%E5%BC%80%E8%BF%9E%E6%8E%A5%EF%BC%8C%E5%B9%B6%E8%BF%94%E5%9B%9E421%E9%94%99%E8%AF%AF%E7%A0%81/</url>
    <content><![CDATA[<h1 id="FTP断开连接，并返回421错误码"><a href="#FTP断开连接，并返回421错误码" class="headerlink" title="FTP断开连接，并返回421错误码"></a>FTP断开连接，并返回421错误码</h1><blockquote>
<p>这个问题搞了好久。办公地点机房搬迁，导致网络及其不稳定。<br>421 received. Server closed connection<br>FTP 无法远程连接<br><a id="more"></a></p>
</blockquote>
<h2 id="报错情况与解决思路"><a href="#报错情况与解决思路" class="headerlink" title="报错情况与解决思路"></a>报错情况与解决思路</h2><p>一开始的报错情况是可以在本机连接自己的 FTP，在其他服务器上不行。使用命令<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">service iptables stop</span><br></pre></td></tr></table></figure></p>
<p>关闭了防火墙后，成功解决问题。</p>
<p>这边使用的是 hutool 提供的 FTP 工具类。在返回错误时，只返回了<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">421 received. Server closed connection</span><br></pre></td></tr></table></figure></p>
<p>一开始百度到的回答是说连接数过多，我查了一下FTP配置文件，连接数是默认无限制的。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">max_per_ip </span><br><span class="line">如果vsftpd处于独立模式，则这是可以从同一源Internet地址连接的最大客户端数。</span><br><span class="line">如果客户端超过此限制，则会收到错误消息。</span><br><span class="line">默认值：0（无限制）</span><br></pre></td></tr></table></figure></p>
<p>然后经过程序多次尝试，发现和FTP连接时间有关。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">data_connection_timeout</span><br><span class="line">超时（以秒为单位），大约是我们允许数据传输暂停而没有任何进展的最长时间。</span><br><span class="line">如果超时触发，则启动远程客户端。</span><br><span class="line">默认值：300</span><br><span class="line"></span><br><span class="line">accept_timeout</span><br><span class="line">远程客户端与PASV样式数据连接建立连接的超时时间（以秒为单位）。</span><br><span class="line">默认值：60</span><br></pre></td></tr></table></figure></p>
<p>尝试提高连接时间并将不需要 FTP 处理的程序抽离出来，成功解决问题。</p>
]]></content>
      <tags>
        <tag>ftp</tag>
      </tags>
  </entry>
  <entry>
    <title>Idea报错 【cannot access com.xxx】的【解决办法】</title>
    <url>/2020/11/28/Idea%E6%8A%A5%E9%94%99%20%E3%80%90cannot%20access%20com.xxx%E3%80%91%E7%9A%84%E3%80%90%E8%A7%A3%E5%86%B3%E5%8A%9E%E6%B3%95%E3%80%91/</url>
    <content><![CDATA[<h1 id="Idea报错-【cannot-access-com-xxx】的【解决办法】"><a href="#Idea报错-【cannot-access-com-xxx】的【解决办法】" class="headerlink" title="Idea报错 【cannot access com.xxx】的【解决办法】"></a>Idea报错 【cannot access com.xxx】的【解决办法】</h1><blockquote>
<p>本文来自 <a href="https://blog.csdn.net/qq_27818541/article/details/105078428" target="_blank" rel="noopener">https://blog.csdn.net/qq_27818541/article/details/105078428</a><br>我也没搞明白为啥会这样，但是这个文章中的方法我尝试后解决了问题</p>
</blockquote>
<a id="more"></a>
<h2 id="起因"><a href="#起因" class="headerlink" title="起因"></a>起因</h2><p>从别的项目中抽取代码，将代码包路径改动后出现。具体表现为工具类正常但是无法调用，实体类正常也无法使用。直接 import 路径也不行。</p>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><p> 执行下面操作，执行完后项目变为正常。<br><img src="/2020/11/28/Idea报错 【cannot access com.xxx】的【解决办法】/b1.png" alt="1"><br><img src="/2020/11/28/Idea报错 【cannot access com.xxx】的【解决办法】/b2.png" alt="2"></p>
]]></content>
  </entry>
  <entry>
    <title>InnoDB 中一棵 B+ 树能存多少行数据？</title>
    <url>/2020/02/22/InnoDB%20%E4%B8%AD%E4%B8%80%E6%A3%B5%20B+%E6%A0%91%E8%83%BD%E5%AD%98%E5%A4%9A%E5%B0%91%E8%A1%8C%E6%95%B0%E6%8D%AE%EF%BC%9F/</url>
    <content><![CDATA[<h1 id="InnoDB-中一棵-B-树能存多少行数据？"><a href="#InnoDB-中一棵-B-树能存多少行数据？" class="headerlink" title="InnoDB 中一棵 B+ 树能存多少行数据？"></a>InnoDB 中一棵 B+ 树能存多少行数据？</h1><blockquote>
<p>来自<a href="https://www.cnblogs.com/leefreeman/p/8315844.html" target="_blank" rel="noopener">https://www.cnblogs.com/leefreeman/p/8315844.html</a> ，看到这篇文章后感觉对自己理解mysql索引存储方式有所帮助，记录下来。</p>
</blockquote>
<a id="more"></a>
<h2 id="一个问题？"><a href="#一个问题？" class="headerlink" title="一个问题？"></a>一个问题？</h2><p>InnoDB一棵B+树可以存放多少行数据？这个问题的简单回答是：约2千万。为什么是这么多呢？因为这是可以算出来的，要搞清楚这个问题，我们先从InnoDB索引数据结构、数据组织方式说起。</p>
<p>我们都知道计算机在存储数据的时候，有最小存储单元，这就好比我们今天进行现金的流通最小单位是一毛。在计算机中磁盘存储数据最小单元是扇区，一个扇区的大小是512字节，而文件系统（例如XFS/EXT4）他的最小单元是块，一个块的大小是4k，而对于我们的InnoDB存储引擎也有自己的最小储存单元——页（Page），一个页的大小是16K。</p>
<p>下面几张图可以帮你理解最小存储单元：</p>
<p>文件系统中一个文件大小只有1个字节，但不得不占磁盘上4KB的空间。<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b1.png" alt=""><br>innodb的所有数据文件（后缀为ibd的文件），他的大小始终都是16384（16k）的整数倍。<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b2.png" alt=""><br>磁盘扇区、文件系统、InnoDB存储引擎都有各自的最小存储单元。<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b3.png" alt=""><br>在MySQL中我们的InnoDB页的大小默认是16k，当然也可以通过参数设置：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; show variables like &apos;innodb_page_size&apos;;</span><br><span class="line"></span><br><span class="line">+------------------+-------+</span><br><span class="line"></span><br><span class="line">| Variable_name    | Value |</span><br><span class="line"></span><br><span class="line">+------------------+-------+</span><br><span class="line"></span><br><span class="line">| innodb_page_size | 16384 |</span><br><span class="line"></span><br><span class="line">+------------------+-------+</span><br><span class="line"></span><br><span class="line">1 row in set (0.00 sec)</span><br></pre></td></tr></table></figure></p>
<p>数据表中的数据都是存储在页中的，所以一个页中能存储多少行数据呢？假设一行数据的大小是1k，那么一个页可以存放16行这样的数据。</p>
<p>如果数据库只按这样的方式存储，那么如何查找数据就成为一个问题，因为我们不知道要查找的数据存在哪个页中，也不可能把所有的页遍历一遍，那样太慢了。所以人们想了一个办法，用B+树的方式组织这些数据。如图所示：<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b4.png" alt=""></p>
<p>我们先将数据记录按主键进行排序，分别存放在不同的页中（为了便于理解我们这里一个页中只存放3条记录，实际情况可以存放很多），除了存放数据的页以外，还有存放键值+指针的页，如图中page number=3的页，该页存放键值和指向数据页的指针，这样的页由N个键值+指针组成。当然它也是排好序的。这样的数据组织形式，我们称为索引组织表。现在来看下，要查找一条数据，怎么查？</p>
<p>如select * from user where id=5;</p>
<p>这里id是主键,我们通过这棵B+树来查找，首先找到根页，你怎么知道user表的根页在哪呢？其实每张表的根页位置在表空间文件中是固定的，即page number=3的页（这点我们下文还会进一步证明），找到根页后通过二分查找法，定位到id=5的数据应该在指针P5指向的页中，那么进一步去page number=5的页中查找，同样通过二分查询法即可找到id=5的记录：<br><strong>|</strong> 5 <strong>|</strong> zhao2 <strong>|</strong> 27 <strong>|</strong></p>
<p>现在我们清楚了InnoDB中主键索引B+树是如何组织数据、查询数据的，我们总结一下：</p>
<p>1、InnoDB存储引擎的最小存储单元是页，页可以用于存放数据也可以用于存放键值+指针，在B+树中叶子节点存放数据，非叶子节点存放键值+指针。</p>
<p>2、索引组织表通过非叶子节点的二分查找法以及指针确定数据在哪个页中，进而在去数据页中查找到需要的数据；</p>
<p>那么回到我们开始的问题，通常一棵B+树可以存放多少行数据？</p>
<p>这里我们先假设B+树高为2，即存在一个根节点和若干个叶子节点，那么这棵B+树的存放总记录数为：根节点指针数*单个叶子节点记录行数。</p>
<p>上文我们已经说明单个叶子节点（页）中的记录数=16K/1K=16。（这里假设一行记录的数据大小为1k，实际上现在很多互联网业务数据记录大小通常就是1K左右）。</p>
<p>那么现在我们需要计算出非叶子节点能存放多少指针，其实这也很好算，我们假设主键ID为bigint类型，长度为8字节，而指针大小在InnoDB源码中设置为6字节，这样一共14字节，我们一个页中能存放多少这样的单元，其实就代表有多少指针，即16384/14=1170。那么可以算出一棵高度为2的B+树，能存放1170*16=18720条这样的数据记录。</p>
<p>根据同样的原理我们可以算出一个高度为3的B+树可以存放：1170*1170*16=21902400条这样的记录。所以在InnoDB中B+树高度一般为1-3层，它就能满足千万级的数据存储。在查找数据时一次页的查找代表一次IO，所以通过主键索引查询通常只需要1-3次IO操作即可查找到数据。</p>
<h2 id="怎么得到InnoDB主键索引B-树的高度？"><a href="#怎么得到InnoDB主键索引B-树的高度？" class="headerlink" title="怎么得到InnoDB主键索引B+树的高度？"></a>怎么得到InnoDB主键索引B+树的高度？</h2><p>上面我们通过推断得出B+树的高度通常是1-3，下面我们从另外一个侧面证明这个结论。在InnoDB的表空间文件中，约定page number为3的代表主键索引的根页，而在根页偏移量为64的地方存放了该B+树的page level。如果page level为1，树高为2，page level为2，则树高为3。即B+树的高度=page level+1；下面我们将从实际环境中尝试找到这个page level。</p>
<p>在实际操作之前，你可以通过InnoDB元数据表确认主键索引根页的page number为3，你也可以从《InnoDB存储引擎》这本书中得到确认。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SELECT</span><br><span class="line">b.name, a.name, index_id, type, a.space, a.PAGE_NO</span><br><span class="line">FROM</span><br><span class="line">information_schema.INNODB_SYS_INDEXES a,</span><br><span class="line">information_schema.INNODB_SYS_TABLES b</span><br><span class="line">WHERE</span><br><span class="line">a.table_id = b.table_id AND a.space &lt;&gt; 0;</span><br></pre></td></tr></table></figure>
<p>执行结果：<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b5.png" alt=""></p>
<p>可以看出数据库dbt3下的customer表、lineitem表主键索引根页的page number均为3，而其他的二级索引page number为4。关于二级索引与主键索引的区别请参考MySQL相关书籍，本文不在此介绍。</p>
<p>下面我们对数据库表空间文件做想相关的解析：<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b6.png" alt=""><br>因为主键索引B+树的根页在整个表空间文件中的第3个页开始，所以可以算出它在文件中的偏移量：16384*3=49152（16384为页大小）。</p>
<p>另外根据《InnoDB存储引擎》中描述在根页的64偏移量位置前2个字节，保存了page level的值，因此我们想要的page level的值在整个文件中的偏移量为：16384*3+64=49152+64=49216，前2个字节中。</p>
<p>接下来我们用hexdump工具，查看表空间文件指定偏移量上的数据：<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b7.png" alt=""></p>
<p>linetem表的page level为2，B+树高度为page level+1=3；</p>
<p>region表的page level为0，B+树高度为page level+1=1；</p>
<p>customer表的page level为2，B+树高度为page level+1=3；</p>
<p>这三张表的数据量如下：<br><img src="/2020/02/22/InnoDB 中一棵 B+树能存多少行数据？/b8.png" alt=""></p>
<p>总结：</p>
<p>lineitem表的数据行数为600多万，B+树高度为3，customer表数据行数只有15万，B+树高度也为3。可以看出尽管数据量差异较大，这两个表树的高度都是3，换句话说这两个表通过索引查询效率并没有太大差异，因为都只需要做3次IO。那么如果有一张表行数是一千万，那么他的B+树高度依旧是3，查询效率仍然不会相差太大。</p>
<p>region表只有5行数据，当然他的B+树高度为1。</p>
<h2 id="最后回顾一道面试题"><a href="#最后回顾一道面试题" class="headerlink" title="最后回顾一道面试题"></a>最后回顾一道面试题</h2><p>有一道MySQL的面试题，为什么MySQL的索引要使用B+树而不是其它树形结构?比如B树？</p>
<p>现在这个问题的复杂版本可以参考本文；</p>
<p>他的简单版本回答是：</p>
<p>因为B树不管叶子节点还是非叶子节点，都会保存数据，这样导致在非叶子节点中能保存的指针数量变少（有些资料也称为扇出），指针少的情况下要保存大量数据，只能增加树的高度，导致IO操作变多，查询性能变低；</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本文从一个问题出发，逐步介绍了InnoDB索引组织表的原理、查询方式，并结合已有知识，回答该问题，结合实践来证明。当然为了表述简单易懂，文中忽略了一些细枝末节，比如一个页中不可能所有空间都用于存放数据，它还会存放一些少量的其他字段比如page level，index number等等，另外还有页的填充因子也导致一个页不可能全部用于保存数据。关于二级索引数据存取方式可以参考MySQL相关书籍，他的要点是结合主键索引进行回表查询。</p>
]]></content>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>JPA配置多数据源</title>
    <url>/2020/12/05/JPA%E9%85%8D%E7%BD%AE%E5%A4%9A%E6%95%B0%E6%8D%AE%E6%BA%90/</url>
    <content><![CDATA[<h1 id="JPA配置多数据源"><a href="#JPA配置多数据源" class="headerlink" title="JPA配置多数据源"></a>JPA配置多数据源</h1><blockquote>
<p>本文来自多个博文的综合。<br>主要为：<a href="https://blog.csdn.net/blowbob_666/article/details/110670713" target="_blank" rel="noopener">https://blog.csdn.net/blowbob_666/article/details/110670713</a><br>本文顺带解决一个 Jpa 不自动将驼峰（大写字母）命名转下划线的问题。</p>
</blockquote>
<a id="more"></a>
<h2 id="yml配置"><a href="#yml配置" class="headerlink" title="yml配置"></a>yml配置</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">spring:</span><br><span class="line">  datasource:</span><br><span class="line">    druid:</span><br><span class="line">      db:</span><br><span class="line">        primary:</span><br><span class="line">          name: primary</span><br><span class="line">          url: jdbc:mysql:<span class="comment">//127.0.0.1:3307/primary?useSSL=false&amp;allowPublicKeyRetrieval=true&amp;serverTimezone=Asia/Shanghai</span></span><br><span class="line">          username: root</span><br><span class="line">          password: <span class="number">123456</span></span><br><span class="line">          initialSize: <span class="number">5</span></span><br><span class="line">          minIdle: <span class="number">5</span></span><br><span class="line">          maxActive: <span class="number">20</span></span><br><span class="line">          maxWait: <span class="number">60000</span></span><br><span class="line">          timeBetweenEvictionRunsMillis: <span class="number">60000</span></span><br><span class="line">          minEvictableIdleTimeMillis: <span class="number">60000</span></span><br><span class="line">          validationQuery: SELECT <span class="number">1</span></span><br><span class="line">          validationQueryTimeout: <span class="number">10000</span></span><br><span class="line">          testWhileIdle: <span class="keyword">true</span></span><br><span class="line">          testOnBorrow: <span class="keyword">false</span></span><br><span class="line">          testOnReturn: <span class="keyword">false</span></span><br><span class="line">          poolPreparedStatements: <span class="keyword">true</span></span><br><span class="line">          maxPoolPreparedStatementPerConnectionSize: <span class="number">20</span></span><br><span class="line">          filters: stat,wall</span><br><span class="line">          connectionProperties: druid.stat.mergeSql=<span class="keyword">true</span>;druid.stat.slowSqlMillis=<span class="number">5000</span></span><br><span class="line">          useGlobalDataSourceStat: <span class="keyword">true</span></span><br><span class="line">        test:</span><br><span class="line">          name: test</span><br><span class="line">          url: jdbc:mysql:<span class="comment">//127.0.0.1:3308/test?useSSL=false</span></span><br><span class="line">          username: root</span><br><span class="line">          password: <span class="number">123456</span></span><br><span class="line">          initialSize: <span class="number">5</span></span><br><span class="line">          minIdle: <span class="number">5</span></span><br><span class="line">          maxActive: <span class="number">20</span></span><br><span class="line">          maxWait: <span class="number">60000</span></span><br><span class="line">          timeBetweenEvictionRunsMillis: <span class="number">60000</span></span><br><span class="line">          minEvictableIdleTimeMillis: <span class="number">60000</span></span><br><span class="line">          validationQuery: SELECT <span class="number">1</span></span><br><span class="line">          validationQueryTimeout: <span class="number">10000</span></span><br><span class="line">          testWhileIdle: <span class="keyword">true</span></span><br><span class="line">          testOnBorrow: <span class="keyword">false</span></span><br><span class="line">          testOnReturn: <span class="keyword">false</span></span><br><span class="line">          poolPreparedStatements: <span class="keyword">true</span></span><br><span class="line">          maxPoolPreparedStatementPerConnectionSize: <span class="number">20</span></span><br><span class="line">          filters: stat,wall</span><br><span class="line">          connectionProperties: druid.stat.mergeSql=<span class="keyword">true</span>;druid.stat.slowSqlMillis=<span class="number">5000</span></span><br><span class="line">          useGlobalDataSourceStat: <span class="keyword">true</span></span><br><span class="line">  jpa:</span><br><span class="line">    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect</span><br><span class="line">    show-sql: <span class="keyword">false</span></span><br><span class="line">    open-in-view: <span class="keyword">true</span></span><br><span class="line">    hibernate:</span><br><span class="line">      ddl-auto: update</span><br><span class="line">      primary-dialect: org.hibernate.dialect.MySQL5InnoDBDialect</span><br><span class="line">      test-dialect: org.hibernate.dialect.MySQL5InnoDBDialect</span><br></pre></td></tr></table></figure>
<h2 id="多数据源配置类"><a href="#多数据源配置类" class="headerlink" title="多数据源配置类"></a>多数据源配置类</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.alibaba.druid.spring.boot.autoconfigure.DruidDataSourceBuilder;</span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Qualifier;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.context.properties.ConfigurationProperties;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Primary;</span><br><span class="line"><span class="keyword">import</span> org.springframework.jdbc.core.JdbcTemplate;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.annotation.EnableTransactionManagement;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 数据源管理</span></span><br><span class="line"><span class="comment"> * 1. 自动载入多数据源&lt;br&gt;</span></span><br><span class="line"><span class="comment"> * 2. 支持数据源参数重载&lt;br&gt;</span></span><br><span class="line"><span class="comment"> * &lt;br&gt;</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableTransactionManagement</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DataSourceConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Primary</span></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"dataSource"</span>)</span><br><span class="line">    <span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.datasource.druid.db.primary"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">dataSource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> DruidDataSourceBuilder.create().build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Bean</span>(name = <span class="string">"dataSourceTest"</span>)</span><br><span class="line">	<span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.datasource.druid.db.test"</span>)</span><br><span class="line">	<span class="function"><span class="keyword">public</span> DataSource <span class="title">dataSourceTest</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> DruidDataSourceBuilder.create().build();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Primary</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> JdbcTemplate <span class="title">jdbcTemplate</span><span class="params">(@Qualifier(<span class="string">"dataSource"</span>)</span> DataSource ds) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> JdbcTemplate(ds);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Bean</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> JdbcTemplate <span class="title">jdbcTemplateTest</span><span class="params">(@Qualifier(<span class="string">"dataSourceTest"</span>)</span> DataSource ds) </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> JdbcTemplate(ds);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="主数据源的详细配置（其它数据源类比配置即可，不要加-Primary）包含驼峰转下划线配置"><a href="#主数据源的详细配置（其它数据源类比配置即可，不要加-Primary）包含驼峰转下划线配置" class="headerlink" title="主数据源的详细配置（其它数据源类比配置即可，不要加@Primary）包含驼峰转下划线配置"></a>主数据源的详细配置（其它数据源类比配置即可，不要加@Primary）包含驼峰转下划线配置</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.HashMap;</span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.EntityManager;</span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Qualifier;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.orm.jpa.EntityManagerFactoryBuilder;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Primary;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.env.Environment;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.jpa.repository.config.EnableJpaRepositories;</span><br><span class="line"><span class="keyword">import</span> org.springframework.orm.jpa.JpaTransactionManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.PlatformTransactionManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.annotation.EnableTransactionManagement;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 主数据源配置</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableTransactionManagement</span></span><br><span class="line"><span class="meta">@EnableJpaRepositories</span>(</span><br><span class="line">    entityManagerFactoryRef=<span class="string">"entityManagerFactoryPrimary"</span>,</span><br><span class="line">    transactionManagerRef=<span class="string">"transactionManagerPrimary"</span>,</span><br><span class="line">    basePackages= &#123; <span class="string">"com.yiring.primary.repository"</span> &#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PrimaryConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> Environment env;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="meta">@Qualifier</span>(<span class="string">"dataSource"</span>)</span><br><span class="line">    <span class="keyword">private</span> DataSource primaryDataSource;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Primary</span></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"entityManagerPrimary"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> EntityManager <span class="title">entityManager</span><span class="params">(EntityManagerFactoryBuilder builder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> entityManagerFactoryPrimary(builder).getObject().createEntityManager();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Primary</span></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"entityManagerFactoryPrimary"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> LocalContainerEntityManagerFactoryBean <span class="title">entityManagerFactoryPrimary</span> <span class="params">(EntityManagerFactoryBuilder builder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> builder</span><br><span class="line">            .dataSource(primaryDataSource)</span><br><span class="line">            .properties(getVendorProperties())</span><br><span class="line">            .packages(<span class="string">"com.yiring.primary.domain"</span>)</span><br><span class="line">            .persistenceUnit(<span class="string">"primaryPersistenceUnit"</span>)</span><br><span class="line">            .build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> Map&lt;String, String&gt; <span class="title">getVendorProperties</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Map&lt;String, String&gt; jpaProperties = <span class="keyword">new</span> HashMap&lt;&gt;(<span class="number">16</span>);</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.hbm2ddl.auto"</span>, <span class="string">"update"</span>);</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.show_sql"</span>, env.getProperty(<span class="string">"spring.jpa.show-sql"</span>));</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.format_sql"</span>, env.getProperty(<span class="string">"spring.jpa.hibernate.format_sql"</span>));</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.dialect"</span>, env.getProperty(<span class="string">"spring.jpa.hibernate.primary-dialect"</span>));</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.current_session_context_class"</span>, <span class="string">"org.springframework.orm.hibernate5.SpringSessionContext"</span>);</span><br><span class="line">        <span class="comment">//设置驼峰转下划线</span></span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.physical_naming_strategy"</span>, SpringPhysicalNamingStrategy.class.getName());</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.implicit_naming_strategy"</span>, SpringImplicitNamingStrategy.class.getName());</span><br><span class="line">        <span class="keyword">return</span> jpaProperties;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Primary</span></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"transactionManagerPrimary"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> PlatformTransactionManager <span class="title">transactionManagerPrimary</span><span class="params">(EntityManagerFactoryBuilder builder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> JpaTransactionManager(entityManagerFactoryPrimary(builder).getObject());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="副数据源"><a href="#副数据源" class="headerlink" title="副数据源"></a>副数据源</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.HashMap;</span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"><span class="keyword">import</span> javax.annotation.Resource;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.EntityManager;</span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Qualifier;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.orm.jpa.EntityManagerFactoryBuilder;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.env.Environment;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.jpa.repository.config.EnableJpaRepositories;</span><br><span class="line"><span class="keyword">import</span> org.springframework.orm.jpa.JpaTransactionManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.PlatformTransactionManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.annotation.EnableTransactionManagement;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 副数据源配置</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> zhangshuai</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2021/2/24 15:45</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableTransactionManagement</span></span><br><span class="line"><span class="meta">@EnableJpaRepositories</span>(</span><br><span class="line">    <span class="comment">//实体管理</span></span><br><span class="line">    entityManagerFactoryRef=<span class="string">"entityManagerFactorySecond"</span>,</span><br><span class="line">    <span class="comment">//事务管理</span></span><br><span class="line">    transactionManagerRef=<span class="string">"transactionManagerSecond"</span>,</span><br><span class="line">    <span class="comment">//实体扫描,设置Repository所在位置</span></span><br><span class="line">    basePackages= &#123; <span class="string">"com.yiring.test.repository"</span> &#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ZjtqConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="meta">@Qualifier</span>(<span class="string">"dataSourceZjtq"</span>)</span><br><span class="line">    <span class="keyword">private</span> DataSource secondDataSource;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> Environment env;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"entityManagerSecond"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> EntityManager <span class="title">entityManager</span><span class="params">(EntityManagerFactoryBuilder builder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> entityManagerFactorySecond(builder).getObject().createEntityManager();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"entityManagerFactorySecond"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> LocalContainerEntityManagerFactoryBean <span class="title">entityManagerFactorySecond</span> <span class="params">(EntityManagerFactoryBuilder builder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> builder</span><br><span class="line">            .dataSource(secondDataSource)</span><br><span class="line">            .properties(getVendorProperties())</span><br><span class="line">            .packages(<span class="string">"com.yiring.test.domain"</span>)</span><br><span class="line">            .persistenceUnit(<span class="string">"secondPersistenceUnit"</span>)</span><br><span class="line">            .build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> Map&lt;String, String&gt; <span class="title">getVendorProperties</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Map&lt;String, String&gt; jpaProperties = <span class="keyword">new</span> HashMap&lt;&gt;(<span class="number">16</span>);</span><br><span class="line">        <span class="comment">//这里取消对副数据源的修改</span></span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.show_sql"</span>, env.getProperty(<span class="string">"spring.jpa.show-sql"</span>));</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.dialect"</span>, env.getProperty(<span class="string">"spring.jpa.hibernate.Second-dialect"</span>));</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.format_sql"</span>, env.getProperty(<span class="string">"spring.jpa.hibernate.format_sql"</span>));</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.current_session_context_class"</span>, <span class="string">"org.springframework.orm.hibernate5.SpringSessionContext"</span>);</span><br><span class="line">        <span class="comment">//设置驼峰转下划线</span></span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.physical_naming_strategy"</span>, SpringPhysicalNamingStrategy.class.getName());</span><br><span class="line">        jpaProperties.put(<span class="string">"hibernate.implicit_naming_strategy"</span>, SpringImplicitNamingStrategy.class.getName());</span><br><span class="line">        <span class="keyword">return</span> jpaProperties;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"transactionManagerSecond"</span>)</span><br><span class="line">    <span class="function">PlatformTransactionManager <span class="title">transactionManagerSecond</span><span class="params">(EntityManagerFactoryBuilder builder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> JpaTransactionManager(entityManagerFactorySecond(builder).getObject());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>通过不同的包路径读取来区分不同的数据源。尽量不要用同名类，否则在使用时注入会报错。<br>使用方法和平时的单数据源使用方法一样。</p>
]]></content>
      <tags>
        <tag>jpa</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode第4题结题思路</title>
    <url>/2020/06/19/LeetCode%E7%AC%AC4%E9%A2%98%E7%BB%93%E9%A2%98%E6%80%9D%E8%B7%AF/</url>
    <content><![CDATA[<h1 id="LeetCode第4题结题思路"><a href="#LeetCode第4题结题思路" class="headerlink" title="LeetCode第4题结题思路"></a>LeetCode第4题结题思路</h1><blockquote>
<p>最近开始刷起了算法题，发现在白天做完业务功能后。晚上刷刷题有种换换思路的感觉。<br>下面全是从原文哪里搬来的，来自(wind-liang的github)[<a href="https://leetcode.wang/leetCode-4-Median-of-Two-Sorted-Arrays.html]" target="_blank" rel="noopener">https://leetcode.wang/leetCode-4-Median-of-Two-Sorted-Arrays.html]</a> 。我自己的解法就是第一种的思路。</p>
</blockquote>
<a id="more"></a>
<h2 id="第4题"><a href="#第4题" class="headerlink" title="第4题"></a>第4题</h2><h3 id="1-题目描述（困难难度）"><a href="#1-题目描述（困难难度）" class="headerlink" title="1. 题目描述（困难难度）"></a>1. 题目描述（困难难度）</h3><p><img src="/2020/06/19/LeetCode第4题结题思路/b1.png" alt="第4题"></p>
<p>已知两个有序数组，找到两个数组合并后的中位数。</p>
<h3 id="2-解法一"><a href="#2-解法一" class="headerlink" title="2.解法一"></a>2.解法一</h3><p>简单粗暴，先将两个数组合并，两个有序数组的合并也是归并排序中的一部分。然后根据奇数，还是偶数，返回中位数。</p>
<h3 id="3-代码"><a href="#3-代码" class="headerlink" title="3. 代码"></a>3. 代码</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public double findMedianSortedArrays(int[] nums1, int[] nums2) &#123;</span><br><span class="line">    int[] nums;</span><br><span class="line">    int m = nums1.length;</span><br><span class="line">    int n = nums2.length;</span><br><span class="line">    nums = new int[m + n];</span><br><span class="line">    if (m == 0) &#123;</span><br><span class="line">        if (n % 2 == 0) &#123;</span><br><span class="line">            return (nums2[n / 2 - 1] + nums2[n / 2]) / 2.0;</span><br><span class="line">        &#125; else &#123;</span><br><span class="line"></span><br><span class="line">            return nums2[n / 2];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    if (n == 0) &#123;</span><br><span class="line">        if (m % 2 == 0) &#123;</span><br><span class="line">            return (nums1[m / 2 - 1] + nums1[m / 2]) / 2.0;</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            return nums1[m / 2];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    int count = 0;</span><br><span class="line">    int i = 0, j = 0;</span><br><span class="line">    while (count != (m + n)) &#123;</span><br><span class="line">        if (i == m) &#123;</span><br><span class="line">            while (j != n) &#123;</span><br><span class="line">                nums[count++] = nums2[j++];</span><br><span class="line">            &#125;</span><br><span class="line">            break;</span><br><span class="line">        &#125;</span><br><span class="line">        if (j == n) &#123;</span><br><span class="line">            while (i != m) &#123;</span><br><span class="line">                nums[count++] = nums1[i++];</span><br><span class="line">            &#125;</span><br><span class="line">            break;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        if (nums1[i] &lt; nums2[j]) &#123;</span><br><span class="line">            nums[count++] = nums1[i++];</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            nums[count++] = nums2[j++];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (count % 2 == 0) &#123;</span><br><span class="line">        return (nums[count / 2 - 1] + nums[count / 2]) / 2.0;</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        return nums[count / 2];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：遍历全部数组，O（m + n）</p>
<p>空间复杂度：开辟了一个数组，保存合并后的两个数组，O（m + n）</p>
<h3 id="4-解法二"><a href="#4-解法二" class="headerlink" title="4. 解法二"></a>4. 解法二</h3><p>其实，我们不需要将两个数组真的合并，我们只需要找到中位数在哪里就可以了。</p>
<p>开始的思路是写一个循环，然后里边判断是否到了中位数的位置，到了就返回结果，但这里对偶数和奇数的分类会很麻烦。当其中一个数组遍历完后，出了 for 循环对边界的判断也会分几种情况。总体来说，虽然复杂度不影响，但代码会看起来很乱。然后在 这里 找到了另一种思路。</p>
<p>首先是怎么将奇数和偶数的情况合并一下。</p>
<p>用 len 表示合并后数组的长度，如果是奇数，我们需要知道第 （len + 1）/ 2 个数就可以了，如果遍历的话需要遍历 int ( len / 2 ) + 1 次。如果是偶数，我们需要知道第 len / 2 和 len / 2 + 1 个数，也是需要遍历 len / 2 + 1 次。所以遍历的话，奇数和偶数都是 len / 2 + 1 次。</p>
<p>返回中位数的话，奇数需要最后一次遍历的结果就可以了，偶数需要最后一次和上一次遍历的结果。所以我们用两个变量 left 和 right ，right 保存当前循环的结果，在每次循环前将 right 的值赋给 left 。这样在最后一次循环的时候，left 将得到 right 的值，也就是上一次循环的结果，接下来 right 更新为最后一次的结果。</p>
<p>循环中该怎么写，什么时候 A 数组后移，什么时候 B 数组后移。用 aStart 和 bStart 分别表示当前指向 A 数组和 B 数组的位置。如果 aStart 还没有到最后并且此时 A 位置的数字小于 B 位置的数组，那么就可以后移了。也就是aStart ＜ m &amp;&amp; A[aStart] &lt; B[bStart]。</p>
<p>但如果 B 数组此刻已经没有数字了，继续取数字B [ bStart ]，则会越界，所以判断下 bStart 是否大于数组长度了，这样 || 后边的就不会执行了，也就不会导致错误了，所以增加为 aStart ＜ m &amp;&amp; ( bStart &gt;= n || A [ aStart ] &lt; B [ bStart ] ) 。</p>
<h3 id="5-代码"><a href="#5-代码" class="headerlink" title="5. 代码"></a>5. 代码</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public double findMedianSortedArrays(int[] A, int[] B) &#123;</span><br><span class="line">    int m = A.length;</span><br><span class="line">    int n = B.length;</span><br><span class="line">    int len = m + n;</span><br><span class="line">    int left = -1, right = -1;</span><br><span class="line">    int aStart = 0, bStart = 0;</span><br><span class="line">    for (int i = 0; i &lt;= len / 2; i++) &#123;</span><br><span class="line">        left = right;</span><br><span class="line">        if (aStart &lt; m &amp;&amp; (bStart &gt;= n || A[aStart] &lt; B[bStart])) &#123;</span><br><span class="line">            right = A[aStart++];</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            right = B[bStart++];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    if ((len &amp; 1) == 0)</span><br><span class="line">        return (left + right) / 2.0;</span><br><span class="line">    else</span><br><span class="line">        return right;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：遍历 len/2 + 1 次，len = m + n ，所以时间复杂度依旧是 O（m + n）。</p>
<p>空间复杂度：我们申请了常数个变量，也就是 m，n，len，left，right，aStart，bStart 以及 i 。</p>
<p>总共 8 个变量，所以空间复杂度是 O（1）。</p>
<h3 id="6-解法三"><a href="#6-解法三" class="headerlink" title="6. 解法三"></a>6. 解法三</h3><p>上边的两种思路，时间复杂度都达不到题目的要求 O ( log ( m + n ) )。看到 log ，很明显，我们只有用到二分的方法才能达到。我们不妨用另一种思路，题目是求中位数，其实就是求第 k 小数的一种特殊情况，而求第 k 小数有一种算法。</p>
<p>解法二中，我们一次遍历就相当于去掉不可能是中位数的一个值，也就是一个一个排除。由于数列是有序的，其实我们完全可以一半儿一半儿的排除。假设我们要找第 k 小数，我们可以每次循环排除掉 k / 2 个数。看下边一个例子。</p>
<p>假设我们要找第 7 小的数字。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b2.png" alt=""></p>
<p>我们比较两个数组的第 k / 2 个数字，如果 k 是奇数，向下取整。也就是比较第 3 个数字，上边数组中的 4 和 下边数组中的 3 ，如果哪个小，就表明该数组的前 k / 2 个数字都不是第 k 小数字，所以可以排除。也就是 1，2，3 这三个数字不可能是第 7 小的数字，我们可以把它排除掉。将 1349 和 45678910 两个数组作为新的数组进行比较。</p>
<p>更一般的情况 A [ 1 ]，A [ 2 ]，A [ 3 ]，A [ k / 2] … ，B[ 1 ]，B [ 2 ]，B [ 3 ]，B[ k / 2] … ，如果 A [ k / 2 ] &lt; B [ k / 2 ] ，那么 A [ 1 ]，A [ 2 ]，A [ 3 ]，A [ k / 2] 都不可能是第 k 小的数字。</p>
<p>A 数组中比 A [ k / 2 ] 小的数有 k / 2 - 1 个，B 数组中，B [ k / 2 ] 比 A [ k / 2 ] 大，假设 B [ k / 2 ] 前边的数字都比 A [ k / 2 ] 小，也只有 k / 2 - 1 个，所以比 A [ k / 2 ] 小的数字最多有 k / 2 - 1 + k / 2 - 1 = k - 2 个，所以 A [ k / 2 ] 最多是第 k - 1 小的数。而比 A [ k / 2 ] 小的数更不可能是第 k 小的数了，所以可以把它们排除。</p>
<p>橙色的部分表示已经去掉的数字。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b3.png" alt=""></p>
<p>由于我们已经排除掉了 3 个数字，就是这 3 个数字一定在最前边，所以在两个新数组中，我们只需要找第 7 - 3 = 4 小的数字就可以了，也就是 k = 4 。此时两个数组，比较第 2 个数字，3 &lt; 5，所以我们可以把小的那个数组中的 1 ，3 排除掉了。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b4.png" alt=""></p>
<p>我们又排除掉 2 个数字，所以现在找第 4 - 2 = 2 小的数字就可以了。此时比较两个数组中的第 k / 2 = 1 个数，4 == 4 ，怎么办呢？由于两个数相等，所以我们无论去掉哪个数组中的都行，因为去掉 1 个总会保留 1 个的，所以没有影响。为了统一，我们就假设 4 &gt; 4 吧，所以此时将下边的 4 去掉。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b5.png" alt=""></p>
<p>由于又去掉 1 个数字，此时我们要找第 1 小的数字，所以只需判断两个数组中第一个数字哪个小就可以了，也就是 4 。</p>
<p>所以第 7 小的数字是 4 。</p>
<p>我们每次都是取 k / 2 的数进行比较，有时候可能会遇到数组长度小于 k / 2 的时候。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b6.png" alt=""></p>
<p>此时 k / 2 等于 3 ，而上边的数组长度是 2 ，我们此时将箭头指向它的末尾就可以了。这样的话，由于 2 &lt; 3 ，所以就会导致上边的数组 1，2 都被排除。造成下边的情况。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b7.png" alt=""></p>
<p>由于 2 个元素被排除，所以此时 k = 5 ，又由于上边的数组已经空了，我们只需要返回下边的数组的第 5 个数字就可以了。</p>
<p>从上边可以看到，无论是找第奇数个还是第偶数个数字，对我们的算法并没有影响，而且在算法进行中，k 的值都有可能从奇数变为偶数，最终都会变为 1 或者由于一个数组空了，直接返回结果。</p>
<p>所以我们采用递归的思路，为了防止数组长度小于 k / 2 ，所以每次比较 min ( k / 2，len ( 数组 ) ) 对应的数字，把小的那个对应的数组的数字排除，将两个新数组进入递归，并且 k 要减去排除的数字的个数。递归出口就是当 k = 1 或者其中一个数字长度是 0 了。</p>
<h3 id="7-代码"><a href="#7-代码" class="headerlink" title="7. 代码"></a>7. 代码</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public double findMedianSortedArrays(int[] nums1, int[] nums2) &#123;</span><br><span class="line">    int n = nums1.length;</span><br><span class="line">    int m = nums2.length;</span><br><span class="line">    int left = (n + m + 1) / 2;</span><br><span class="line">    int right = (n + m + 2) / 2;</span><br><span class="line">    //将偶数和奇数的情况合并，如果是奇数，会求两次同样的 k 。</span><br><span class="line">    return (getKth(nums1, 0, n - 1, nums2, 0, m - 1, left) + getKth(nums1, 0, n - 1, nums2, 0, m - 1, right)) * 0.5;  </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    private int getKth(int[] nums1, int start1, int end1, int[] nums2, int start2, int end2, int k) &#123;</span><br><span class="line">        int len1 = end1 - start1 + 1;</span><br><span class="line">        int len2 = end2 - start2 + 1;</span><br><span class="line">        //让 len1 的长度小于 len2，这样就能保证如果有数组空了，一定是 len1 </span><br><span class="line">        if (len1 &gt; len2) return getKth(nums2, start2, end2, nums1, start1, end1, k);</span><br><span class="line">        if (len1 == 0) return nums2[start2 + k - 1];</span><br><span class="line"></span><br><span class="line">        if (k == 1) return Math.min(nums1[start1], nums2[start2]);</span><br><span class="line"></span><br><span class="line">        int i = start1 + Math.min(len1, k / 2) - 1;</span><br><span class="line">        int j = start2 + Math.min(len2, k / 2) - 1;</span><br><span class="line"></span><br><span class="line">        if (nums1[i] &gt; nums2[j]) &#123;</span><br><span class="line">            return getKth(nums1, start1, end1, nums2, j + 1, end2, k - (j - start2 + 1));</span><br><span class="line">        &#125;</span><br><span class="line">        else &#123;</span><br><span class="line">            return getKth(nums1, i + 1, end1, nums2, start2, end2, k - (i - start1 + 1));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：每进行一次循环，我们就减少 k / 2 个元素，所以时间复杂度是 O（log（k）），而 k = （m + n）/ 2 ，所以最终的复杂也就是 O（log（m + n））。</p>
<p>空间复杂度：虽然我们用到了递归，但是可以看到这个递归属于尾递归，所以编译器不需要不停地堆栈，所以空间复杂度为 O（1）。</p>
<h3 id="8-解法四"><a href="#8-解法四" class="headerlink" title="8. 解法四"></a>8. 解法四</h3><p>我们首先理一下中位数的定义是什么</p>
<blockquote>
<p>中位数（又称中值，英语：Median），统计学中的专有名词，代表一个样本、种群或概率分布中的一个数值，其可将数值集合划分为相等的上下两部分。</p>
</blockquote>
<p>所以我们只需要将数组进行切。</p>
<p>一个长度为 m 的数组，有 0 到 m 总共 m + 1 个位置可以切。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b8.png" alt=""></p>
<p>我们把数组 A 和数组 B 分别在 i 和 j 进行切割。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b9.png" alt=""></p>
<p>将 i 的左边和 j 的左边组合成「左半部分」，将 i 的右边和 j 的右边组合成「右半部分」。</p>
<ul>
<li><p>当 A 数组和 B 数组的总长度是偶数时，如果我们能够保证</p>
<ul>
<li><p>左半部分的长度等于右半部分<br>i + j = m - i + n - j , 也就是 j = ( m + n ) / 2 - i</p>
</li>
<li><p>左半部分最大的值小于等于右半部分最小的值 max ( A [ i - 1 ] , B [ j - 1 ]）） &lt;= min ( A [ i ] , B [ j ]））</p>
<p>那么，中位数就可以表示如下</p>
<p>（左半部分最大值 + 右半部分最小值 ）/ 2 。</p>
<p>（max ( A [ i - 1 ] , B [ j - 1 ]）+ min ( A [ i ] , B [ j ]）） / 2</p>
</li>
</ul>
</li>
<li><p>当 A 数组和 B 数组的总长度是奇数时，如果我们能够保证</p>
<ul>
<li><p>左半部分的长度比右半部分大 1</p>
<p>i + j = m - i + n - j + 1也就是 j = ( m + n + 1) / 2 - i</p>
</li>
<li><p>左半部分最大的值小于等于右半部分最小的值 max ( A [ i - 1 ] , B [ j - 1 ]）） &lt;= min ( A [ i ] , B [ j ]））</p>
<p>那么，中位数就是</p>
<p>左半部分最大值，也就是左半部比右半部分多出的那一个数。</p>
<p>max ( A [ i - 1 ] , B [ j - 1 ]）</p>
</li>
</ul>
</li>
</ul>
<p>上边的第一个条件我们其实可以合并为 j = ( m + n + 1) / 2 - i，因为如果 m + n 是偶数，由于我们取的是 int 值，所以加 1 也不会影响结果。当然，由于 0 &lt;= i &lt;= m ，为了保证 0 &lt;= j &lt;= n ，我们必须保证 m &lt;= n 。</p>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b10.png" alt=""></p>
<p>最后一步由于是 int 间的运算，所以 1 / 2 = 0。</p>
<p>而对于第二个条件，奇数和偶数的情况是一样的，我们进一步分析。为了保证 max ( A [ i - 1 ] , B [ j - 1 ]）） &lt;= min ( A [ i ] , B [ j ]）），因为 A 数组和 B 数组是有序的，所以 A [ i - 1 ] &lt;= A [ i ]，B [ i - 1 ] &lt;= B [ i ] 这是天然的，所以我们只需要保证 B [ j - 1 ] &lt; = A [ i ] 和 A [ i - 1 ] &lt;= B [ j ] 所以我们分两种情况讨论：</p>
<ul>
<li>B [ j - 1 ] &gt; A [ i ]，并且为了不越界，要保证 j != 0，i != m</li>
</ul>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b11.png" alt=""></p>
<p>​    此时很明显，我们需要增加 i ，为了数量的平衡还要减少 j ，幸运的是 j = ( m + n + 1) / 2 - i，i 增    大，j 自然会减少。</p>
<ul>
<li>A [ i - 1 ] &gt; B [ j ] ，并且为了不越界，要保证 i != 0，j != n</li>
</ul>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b12.png" alt=""></p>
<p>​    此时和上边的情况相反，我们要减少 i ，增大 j 。</p>
<p>上边两种情况，我们把边界都排除了，需要单独讨论。</p>
<ul>
<li>当 i = 0 , 或者 j = 0 ，也就是切在了最前边。 </li>
</ul>
<p><img src="/2020/06/19/LeetCode第4题结题思路/b13.png" alt=""></p>
<p>​    此时左半部分当 j = 0 时，最大的值就是 A [ i - 1 ] ；当 i = 0 时 最大的值就是 B [ j - 1] 。右半部分最小值和之前一样。 </p>
<ul>
<li>当 i = m 或者 j = n ，也就是切在了最后边<br><img src="/2020/06/19/LeetCode第4题结题思路/b14.png" alt=""></li>
</ul>
<p>此时左半部分最大值和之前一样。右半部分当 j = n 时，最小值就是 A [ i ] ；当 i = m 时，最小值就是B [ j ] 。</p>
<p>所有的思路都理清了，最后一个问题，增加 i 的方式。当然用二分了。初始化 i 为中间的值，然后减半找中间的，减半找中间的，减半找中间的直到答案。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">    public double findMedianSortedArrays(int[] A, int[] B) &#123;</span><br><span class="line">        int m = A.length;</span><br><span class="line">        int n = B.length;</span><br><span class="line">        if (m &gt; n) &#123; </span><br><span class="line">            return findMedianSortedArrays(B,A); // 保证 m &lt;= n</span><br><span class="line">        &#125;</span><br><span class="line">        int iMin = 0, iMax = m;</span><br><span class="line">        while (iMin &lt;= iMax) &#123;</span><br><span class="line">            int i = (iMin + iMax) / 2;</span><br><span class="line">            int j = (m + n + 1) / 2 - i;</span><br><span class="line">            if (j != 0 &amp;&amp; i != m &amp;&amp; B[j-1] &gt; A[i])&#123; // i 需要增大</span><br><span class="line">                iMin = i + 1; </span><br><span class="line">            &#125;</span><br><span class="line">            else if (i != 0 &amp;&amp; j != n &amp;&amp; A[i-1] &gt; B[j]) &#123; // i 需要减小</span><br><span class="line">                iMax = i - 1; </span><br><span class="line">            &#125;</span><br><span class="line">            else &#123; // 达到要求，并且将边界条件列出来单独考虑</span><br><span class="line">                int maxLeft = 0;</span><br><span class="line">                if (i == 0) &#123; maxLeft = B[j-1]; &#125;</span><br><span class="line">                else if (j == 0) &#123; maxLeft = A[i-1]; &#125;</span><br><span class="line">                else &#123; maxLeft = Math.max(A[i-1], B[j-1]); &#125;</span><br><span class="line">                if ( (m + n) % 2 == 1 ) &#123; return maxLeft; &#125; // 奇数的话不需要考虑右半部分</span><br><span class="line"></span><br><span class="line">                int minRight = 0;</span><br><span class="line">                if (i == m) &#123; minRight = B[j]; &#125;</span><br><span class="line">                else if (j == n) &#123; minRight = A[i]; &#125;</span><br><span class="line">                else &#123; minRight = Math.min(B[j], A[i]); &#125;</span><br><span class="line"></span><br><span class="line">                return (maxLeft + minRight) / 2.0; //如果是偶数的话返回结果</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return 0.0;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：我们对较短的数组进行了二分查找，所以时间复杂度是 O（log（min（m，n）））。</p>
<p>空间复杂度：只有一些固定的变量，和数组长度无关，所以空间复杂度是 O ( 1 ) 。</p>
<h3 id="9-总结"><a href="#9-总结" class="headerlink" title="9. 总结"></a>9. 总结</h3><p>解法二中体会到了对情况的转换，有时候即使有了思路，代码也不一定写的优雅，需要多锻炼才可以。解法三和解法四充分发挥了二分查找的优势，将时间复杂度降为 log 级别。</p>
]]></content>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 8 中的 Streams API</title>
    <url>/2019/03/12/Java-8-%E4%B8%AD%E7%9A%84-Streams-API/</url>
    <content><![CDATA[<h1 id="Java-8-中的-Streams-API-详解"><a href="#Java-8-中的-Streams-API-详解" class="headerlink" title="Java 8 中的 Streams API 详解"></a>Java 8 中的 Streams API 详解</h1><blockquote>
<p>本文是我在学习JAVA8新特性时使用的，原文地址<a href="https://www.ibm.com/developerworks/cn/java/j-lo-java8streamapi/" target="_blank" rel="noopener">https://www.ibm.com/developerworks/cn/java/j-lo-java8streamapi/</a> 。这里会加入我自己使用过得一些经验，如有不对还请见谅。</p>
</blockquote>
<a id="more"></a>
<h2 id="为什么需要-Stream"><a href="#为什么需要-Stream" class="headerlink" title="为什么需要 Stream"></a>为什么需要 Stream</h2><p>Stream 作为 Java 8 的一大亮点，它与 java.io 包里的 InputStream 和 OutputStream 是完全不同的概念。它也不同于 StAX 对 XML 解析的 Stream，也不是 Amazon Kinesis 对大数据实时处理的 Stream。Java 8 中的 Stream 是对集合（Collection）对象功能的增强，它专注于对集合对象进行各种非常便利、高效的聚合操作（aggregate operation），或者大批量数据操作 (bulk data operation)。Stream API 借助于同样新出现的 Lambda 表达式，极大的提高编程效率和程序可读性。同时它提供串行和并行两种模式进行汇聚操作，并发模式能够充分利用多核处理器的优势，使用 fork/join 并行方式来拆分任务和加速处理过程。通常编写并行代码很难而且容易出错, 但使用 Stream API 无需编写一行多线程的代码，就可以很方便地写出高性能的并发程序。所以说，Java 8 中首次出现的 java.util.stream 是一个函数式语言+多核时代综合影响的产物。</p>
<blockquote>
<p>自我使用过的感觉是比之前的代码格式简洁了很多，很多处理用一行代码就可以搞定。提高了编写效率和阅读愉悦感</p>
</blockquote>
<h3 id="什么是聚合操作"><a href="#什么是聚合操作" class="headerlink" title="什么是聚合操作"></a>什么是聚合操作</h3><p>在传统的 J2EE 应用中，Java 代码经常不得不依赖于关系型数据库的聚合操作来完成诸如：</p>
<ul>
<li>客户每月平均消费金额</li>
<li>最昂贵的在售商品</li>
<li>本周完成的有效订单（排除了无效的）</li>
<li>取十个数据样本作为首页推荐</li>
</ul>
<p>这类的操作。</p>
<p>但在当今这个数据大爆炸的时代，在数据来源多样化、数据海量化的今天，很多时候不得不脱离 RDBMS，或者以底层返回的数据为基础进行更上层的数据统计。而 Java 的集合 API 中，仅仅有极少量的辅助型方法，更多的时候是程序员需要用 Iterator 来遍历集合，完成相关的聚合应用逻辑。这是一种远不够高效、笨拙的方法。在 Java 7 中，如果要发现 type 为 grocery 的所有交易，然后返回以交易值降序排序好的交易 ID 集合，我们需要这样写：</p>
<p>清单 1. Java 7 的排序、取值实现<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;Transaction&gt; groceryTransactions = new Arraylist&lt;&gt;();</span><br><span class="line">for(Transaction t: transactions)&#123;</span><br><span class="line"> if(t.getType() == Transaction.GROCERY)&#123;</span><br><span class="line"> groceryTransactions.add(t);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line">Collections.sort(groceryTransactions, new Comparator()&#123;</span><br><span class="line"> public int compare(Transaction t1, Transaction t2)&#123;</span><br><span class="line"> return t2.getValue().compareTo(t1.getValue());</span><br><span class="line"> &#125;</span><br><span class="line">&#125;);</span><br><span class="line">List&lt;Integer&gt; transactionIds = new ArrayList&lt;&gt;();</span><br><span class="line">for(Transaction t: groceryTransactions)&#123;</span><br><span class="line"> transactionsIds.add(t.getId());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>而在 Java 8 使用 Stream，代码更加简洁易读；而且使用并发模式，程序执行速度更快。</p>
<p>清单 2. Java 8 的排序、取值实现<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; transactionsIds = transactions.parallelStream().</span><br><span class="line"> filter(t -&gt; t.getType() == Transaction.GROCERY).</span><br><span class="line"> sorted(comparing(Transaction::getValue).reversed()).</span><br><span class="line"> map(Transaction::getId).</span><br><span class="line"> collect(toList());</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>这也是我日常使用中变化最大的一种编码方法，这种流式处理数据的api，使得整个代码块干净整洁。</p>
</blockquote>
<h2 id="Stream-总览"><a href="#Stream-总览" class="headerlink" title="Stream 总览"></a>Stream 总览</h2><h3 id="什么是流"><a href="#什么是流" class="headerlink" title="什么是流"></a>什么是流</h3><p>Stream 不是集合元素，它不是数据结构并不保存数据，它是有关算法和计算的，它更像一个高级版本的 Iterator。原始版本的 Iterator，用户只能显式地一个一个遍历元素并对其执行某些操作；高级版本的 Stream，用户只要给出需要对其包含的元素执行什么操作，比如 “过滤掉长度大于 10 的字符串”、“获取每个字符串的首字母”等，Stream 会隐式地在内部进行遍历，做出相应的数据转换。</p>
<p>Stream 就如同一个迭代器（Iterator），单向，不可往复，数据只能遍历一次，遍历过一次后即用尽了，就好比流水从面前流过，一去不复返。</p>
<p>而和迭代器又不同的是，Stream 可以并行化操作，迭代器只能命令式地、串行化操作。顾名思义，当使用串行方式去遍历时，每个 item 读完后再读下一个 item。而使用并行去遍历时，数据会被分成多个段，其中每一个都在不同的线程中处理，然后将结果一起输出。Stream 的并行操作依赖于 Java7 中引入的 Fork/Join 框架（JSR166y）来拆分任务和加速处理过程。Java 的并行 API 演变历程基本如下：</p>
<ol>
<li>1.0-1.4 中的 java.lang.Thread</li>
<li>5.0 中的 java.util.concurrent</li>
<li>6.0 中的 Phasers 等</li>
<li>7.0 中的 Fork/Join 框架</li>
<li>8.0 中的 Lambda</li>
</ol>
<p>Stream 的另外一大特点是，数据源本身可以是无限的。</p>
<h3 id="流的构成"><a href="#流的构成" class="headerlink" title="流的构成"></a>流的构成</h3><p>当我们使用一个流的时候，通常包括三个基本步骤：<br>获取一个数据源（source）→ 数据转换→执行操作获取想要的结果，每次转换原有 Stream 对象不改变，返回一个新的 Stream 对象（可以有多次转换），这就允许对其操作可以像链条一样排列，变成一个管道，如下图所示。</p>
<p>图 1. 流管道 (Stream Pipeline) 的构成<br><img src="/2019/03/12/Java-8-中的-Streams-API/b1.png" alt="1"></p>
<p>有多种方式生成 Stream Source：</p>
<ol>
<li><p>从 Collection 和数组</p>
<ul>
<li>Collection.stream()</li>
<li>Collection.parallelStream()</li>
<li>Arrays.stream(T array) or Stream.of()</li>
</ul>
</li>
<li><p>从 BufferedReader</p>
<ul>
<li>java.io.BufferedReader.lines()</li>
</ul>
</li>
<li>静态工厂<ul>
<li>java.util.stream.IntStream.range()</li>
<li>java.nio.file.Files.walk()    </li>
</ul>
</li>
<li>自己构建<ul>
<li>java.util.Spliterator</li>
</ul>
</li>
<li>其他<ul>
<li>Random.ints()</li>
<li>BitSet.stream()</li>
<li>Pattern.splitAsStream(java.lang.CharSequence)</li>
<li>JarFile.stream()</li>
</ul>
</li>
</ol>
<p>流的操作类型分为两种：</p>
<ul>
<li><strong>Intermediate：</strong>一个流可以后面跟随零个或多个 intermediate 操作。其目的主要是打开流，做出某种程度的数据映射/过滤，然后返回一个新的流，交给下一个操作使用。这类操作都是惰性化的（lazy），就是说，仅仅调用到这类方法，并没有真正开始流的遍历。</li>
<li><strong>Terminal：</strong>一个流只能有一个 terminal 操作，当这个操作执行后，流就被使用“光”了，无法再被操作。所以这必定是流的最后一个操作。Terminal 操作的执行，才会真正开始流的遍历，并且会生成一个结果，或者一个 side effect。</li>
</ul>
<p>在对于一个 Stream 进行多次转换操作 (Intermediate 操作)，每次都对 Stream 的每个元素进行转换，而且是执行多次，这样时间复杂度就是 N（转换次数）个 for 循环里把所有操作都做掉的总和吗？其实不是这样的，转换操作都是 lazy 的，多个转换操作只会在 Terminal 操作的时候融合起来，一次循环完成。我们可以这样简单的理解，Stream 里有个操作函数的集合，每次转换操作就是把转换函数放入这个集合中，在 Terminal 操作的时候循环 Stream 对应的集合，然后对每个元素执行所有的函数。</p>
<p>还有一种操作被称为 <strong>short-circuiting</strong>。用以指：</p>
<ul>
<li>对于一个 intermediate 操作，如果它接受的是一个无限大（infinite/unbounded）的 Stream，但返回一个有限的新 Stream。</li>
<li>对于一个 terminal 操作，如果它接受的是一个无限大的 Stream，但能在有限的时间计算出结果。</li>
</ul>
<p>当操作一个无限大的 Stream，而又希望在有限时间内完成操作，则在管道内拥有一个 short-circuiting 操作是必要非充分条件。</p>
<p>清单 3. 一个流操作的示例<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int sum = widgets.stream()</span><br><span class="line">.filter(w -&gt; w.getColor() == RED)</span><br><span class="line"> .mapToInt(w -&gt; w.getWeight())</span><br><span class="line"> .sum();</span><br></pre></td></tr></table></figure></p>
<p>stream() 获取当前小物件的 source，filter 和 mapToInt 为 intermediate 操作，进行数据筛选和转换，最后一个 sum() 为 terminal 操作，对符合条件的全部小物件作重量求和。</p>
<h2 id="流的使用详解"><a href="#流的使用详解" class="headerlink" title="流的使用详解"></a>流的使用详解</h2><p>简单说，对 Stream 的使用就是实现一个 filter-map-reduce 过程，产生一个最终结果，或者导致一个副作用（side effect）。</p>
<h3 id="流的构造与转换"><a href="#流的构造与转换" class="headerlink" title="流的构造与转换"></a>流的构造与转换</h3><p>下面提供最常见的几种构造 Stream 的样例。<br>清单 4. 构造流的几种常见方法<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 1. Individual values</span><br><span class="line">Stream stream = Stream.of(&quot;a&quot;, &quot;b&quot;, &quot;c&quot;);</span><br><span class="line">// 2. Arrays</span><br><span class="line">String [] strArray = new String[] &#123;&quot;a&quot;, &quot;b&quot;, &quot;c&quot;&#125;;</span><br><span class="line">stream = Stream.of(strArray);</span><br><span class="line">stream = Arrays.stream(strArray);</span><br><span class="line">// 3. Collections</span><br><span class="line">List&lt;String&gt; list = Arrays.asList(strArray);</span><br><span class="line">stream = list.stream();</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>在这里我比较常用的还是后面两种构造流的方法</p>
</blockquote>
<p>需要注意的是，对于基本数值型，目前有三种对应的包装类型 Stream：</p>
<p>IntStream、LongStream、DoubleStream。当然我们也可以用 Stream\&lt;Integer>、Stream\&lt;Long>、Stream\&lt;Double>，但是 boxing 和 unboxing 会很耗时，所以特别为这三种基本数值型提供了对应的 Stream。</p>
<p>Java 8 中还没有提供其它数值型 Stream，因为这将导致扩增的内容较多。而常规的数值型聚合运算可以通过上面三种 Stream 进行。</p>
<p>清单 5. 数值流的构造<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">IntStream.of(new int[]&#123;1, 2, 3&#125;).forEach(System.out::println);</span><br><span class="line">IntStream.range(1, 3).forEach(System.out::println);</span><br><span class="line">IntStream.rangeClosed(1, 3).forEach(System.out::println);</span><br></pre></td></tr></table></figure></p>
<p>清单 6. 流转换为其它数据结构<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 1. Array</span><br><span class="line">String[] strArray1 = stream.toArray(String[]::new);</span><br><span class="line">// 2. Collection</span><br><span class="line">List&lt;String&gt; list1 = stream.collect(Collectors.toList());</span><br><span class="line">List&lt;String&gt; list2 = stream.collect(Collectors.toCollection(ArrayList::new));</span><br><span class="line">Set set1 = stream.collect(Collectors.toSet());</span><br><span class="line">Stack stack1 = stream.collect(Collectors.toCollection(Stack::new));</span><br><span class="line">// 3. String</span><br><span class="line">String str = stream.collect(Collectors.joining()).toString();</span><br></pre></td></tr></table></figure></p>
<p>一个 Stream 只可以使用一次，上面的代码为了简洁而重复使用了数次。</p>
<blockquote>
<p>这里当转换为其他数据结构后，这个数据就不能在用流的方式去操作了。这里只是忽略掉了新建流的过程，仅仅展示出最后转换的方法</p>
</blockquote>
<h3 id="流的操作"><a href="#流的操作" class="headerlink" title="流的操作"></a>流的操作</h3><p>接下来，当把一个数据结构包装成 Stream 后，就要开始对里面的元素进行各类操作了。常见的操作可以归类如下。</p>
<ul>
<li>Intermediate：<br>map (mapToInt, flatMap 等)、 filter、 distinct、 sorted、 peek、 limit、 skip、 parallel、 sequential、 unordered</li>
<li>Terminal：<br>forEach、 forEachOrdered、 toArray、 reduce、 collect、 min、 max、 count、 anyMatch、 allMatch、 noneMatch、 findFirst、 findAny、 iterator</li>
<li>Short-circuiting：<br>anyMatch、 allMatch、 noneMatch、 findFirst、 findAny、 limit<br>我们下面看一下 Stream 的比较典型用法。<br><strong>map/flatMap</strong></li>
</ul>
<p>我们先来看 map。如果你熟悉 scala 这类函数式语言，对这个方法应该很了解，它的作用就是把 input Stream 的每一个元素，映射成 output Stream 的另外一个元素。</p>
<p>清单 7. 转换大写<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; output = wordList.stream().</span><br><span class="line">map(String::toUpperCase).</span><br><span class="line">collect(Collectors.toList());</span><br></pre></td></tr></table></figure></p>
<p>这段代码把所有的单词转换为大写。</p>
<p>清单 8. 平方数<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; nums = Arrays.asList(1, 2, 3, 4);</span><br><span class="line">List&lt;Integer&gt; squareNums = nums.stream().</span><br><span class="line">map(n -&gt; n * n).</span><br><span class="line">collect(Collectors.toList());</span><br></pre></td></tr></table></figure></p>
<p>这段代码生成一个整数 list 的平方数 {1, 4, 9, 16}。</p>
<blockquote>
<p>这里我最常用的处理方式还是提取出一个对象中的某个属性作为一个新的数组使用</p>
</blockquote>
<p>从上面例子可以看出，map 生成的是个 1:1 映射，每个输入元素，都按照规则转换成为另外一个元素。还有一些场景，是一对多映射关系的，这时需要 flatMap。</p>
<p>清单 9. 一对多<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Stream&lt;List&lt;Integer&gt;&gt; inputStream = Stream.of(</span><br><span class="line"> Arrays.asList(1),</span><br><span class="line"> Arrays.asList(2, 3),</span><br><span class="line"> Arrays.asList(4, 5, 6)</span><br><span class="line"> );</span><br><span class="line">Stream&lt;Integer&gt; outputStream = inputStream.</span><br><span class="line">flatMap((childList) -&gt; childList.stream());</span><br></pre></td></tr></table></figure></p>
<p>flatMap 把 input Stream 中的层级结构扁平化，就是将最底层元素抽出来放到一起，最终 output 的新 Stream 里面已经没有 List 了，都是直接的数字。</p>
<p><strong>filter</strong></p>
<p>filter 对原始 Stream 进行某项测试，通过测试的元素被留下来生成一个新 Stream。</p>
<p>清单 10. 留下偶数<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Integer[] sixNums = &#123;1, 2, 3, 4, 5, 6&#125;;</span><br><span class="line">Integer[] evens =</span><br><span class="line">Stream.of(sixNums).filter(n -&gt; n%2 == 0).toArray(Integer[]::new);</span><br></pre></td></tr></table></figure></p>
<p>经过条件“被 2 整除”的 filter，剩下的数字为 {2, 4, 6}。</p>
<p>清单 11. 把单词挑出来<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; output = reader.lines().</span><br><span class="line"> flatMap(line -&gt; Stream.of(line.split(REGEXP))).</span><br><span class="line"> filter(word -&gt; word.length() &gt; 0).</span><br><span class="line"> collect(Collectors.toList());</span><br></pre></td></tr></table></figure></p>
<p>这段代码首先把每行的单词用 flatMap 整理到新的 Stream，然后保留长度不为 0 的，就是整篇文章中的全部单词了。</p>
<blockquote>
<p>实际就是针对流中的每一个元素做出一次过滤</p>
</blockquote>
<p><strong>forEach</strong></p>
<p>forEach 方法接收一个 Lambda 表达式，然后在 Stream 的每一个元素上执行该表达式。</p>
<p>清单 12. 打印姓名（forEach 和 pre-java8 的对比）<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// Java 8</span><br><span class="line">roster.stream()</span><br><span class="line"> .filter(p -&gt; p.getGender() == Person.Sex.MALE)</span><br><span class="line"> .forEach(p -&gt; System.out.println(p.getName()));</span><br><span class="line">// Pre-Java 8</span><br><span class="line">for (Person p : roster) &#123;</span><br><span class="line"> if (p.getGender() == Person.Sex.MALE) &#123;</span><br><span class="line"> System.out.println(p.getName());</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></p>
<p>对一个人员集合遍历，找出男性并打印姓名。可以看出来，forEach 是为 Lambda 而设计的，保持了最紧凑的风格。而且 Lambda 表达式本身是可以重用的，非常方便。当需要为多核系统优化时，可以 parallelStream().forEach()，只是此时原有元素的次序没法保证，并行的情况下将改变串行时操作的行为，此时 forEach 本身的实现不需要调整，而 Java8 以前的 for 循环 code 可能需要加入额外的多线程逻辑。</p>
<p>但一般认为，forEach 和常规 for 循环的差异不涉及到性能，它们仅仅是函数式风格与传统 Java 风格的差别。</p>
<p>另外一点需要注意，forEach 是 terminal 操作，因此它执行后，Stream 的元素就被“消费”掉了，你无法对一个 Stream 进行两次 terminal 运算。下面的代码是错误的：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">stream.forEach(element -&gt; doOneThing(element));</span><br><span class="line">stream.forEach(element -&gt; doAnotherThing(element));</span><br></pre></td></tr></table></figure></p>
<p>相反，具有相似功能的 intermediate 操作 peek 可以达到上述目的。如下是出现在该 api javadoc 上的一个示例。</p>
<p>清单 13. peek 对每个元素执行操作并返回一个新的 Stream<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Stream.of(&quot;one&quot;, &quot;two&quot;, &quot;three&quot;, &quot;four&quot;)</span><br><span class="line"> .filter(e -&gt; e.length() &gt; 3)</span><br><span class="line"> .peek(e -&gt; System.out.println(&quot;Filtered value: &quot; + e))</span><br><span class="line"> .map(String::toUpperCase)</span><br><span class="line"> .peek(e -&gt; System.out.println(&quot;Mapped value: &quot; + e))</span><br><span class="line"> .collect(Collectors.toList());</span><br></pre></td></tr></table></figure></p>
<p>forEach 不能修改自己包含的本地变量值，也不能用 break/return 之类的关键字提前结束循环。</p>
<blockquote>
<p>forEach这个方法我在使用时一般都是放在最后进行，而且确定不需要中途退出时。</p>
</blockquote>
<p><strong>findFirst</strong><br>这是一个 termimal 兼 short-circuiting 操作，它总是返回 Stream 的第一个元素，或者空。</p>
<p>这里比较重点的是它的返回值类型：Optional。这也是一个模仿 Scala 语言中的概念，作为一个容器，它可能含有某值，或者不包含。使用它的目的是尽可能避免 NullPointerException。</p>
<p>清单 14. Optional 的两个用例<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">String strA = &quot; abcd &quot;, strB = null;</span><br><span class="line">print(strA);</span><br><span class="line">print(&quot;&quot;);</span><br><span class="line">print(strB);</span><br><span class="line">getLength(strA);</span><br><span class="line">getLength(&quot;&quot;);</span><br><span class="line">getLength(strB);</span><br><span class="line">public static void print(String text) &#123;</span><br><span class="line"> // Java 8</span><br><span class="line"> Optional.ofNullable(text).ifPresent(System.out::println);</span><br><span class="line"> // Pre-Java 8</span><br><span class="line"> if (text != null) &#123;</span><br><span class="line"> System.out.println(text);</span><br><span class="line"> &#125;</span><br><span class="line"> &#125;</span><br><span class="line">public static int getLength(String text) &#123;</span><br><span class="line"> // Java 8</span><br><span class="line">return Optional.ofNullable(text).map(String::length).orElse(-1);</span><br><span class="line"> // Pre-Java 8</span><br><span class="line">// return if (text != null) ? text.length() : -1;</span><br><span class="line"> &#125;;</span><br></pre></td></tr></table></figure></p>
<p>在更复杂的 if (xx != null) 的情况中，使用 Optional 代码的可读性更好，而且它提供的是编译时检查，能极大的降低 NPE 这种 Runtime Exception 对程序的影响，或者迫使程序员更早的在编码阶段处理空值问题，而不是留到运行时再发现和调试。</p>
<p>Stream 中的 findAny、max/min、reduce 等方法等返回 Optional 值。还有例如 IntStream.average() 返回 OptionalDouble 等等。</p>
<p><strong>reduce</strong></p>
<p>这个方法的主要作用是把 Stream 元素组合起来。它提供一个起始值（种子），然后依照运算规则（BinaryOperator），和前面 Stream 的第一个、第二个、第 n 个元素组合。从这个意义上说，字符串拼接、数值的 sum、min、max、average 都是特殊的 reduce。例如 Stream 的 sum 就相当于</p>
<p>Integer sum = integers.reduce(0, (a, b) -&gt; a+b); 或</p>
<p>Integer sum = integers.reduce(0, Integer::sum);</p>
<p>也有没有起始值的情况，这时会把 Stream 的前面两个元素组合起来，返回的是 Optional。</p>
<p>清单 15. reduce 的用例<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 字符串连接，concat = &quot;ABCD&quot;</span><br><span class="line">String concat = Stream.of(&quot;A&quot;, &quot;B&quot;, &quot;C&quot;, &quot;D&quot;).reduce(&quot;&quot;, String::concat); </span><br><span class="line">// 求最小值，minValue = -3.0</span><br><span class="line">double minValue = Stream.of(-1.5, 1.0, -3.0, -2.0).reduce(Double.MAX_VALUE, Double::min); </span><br><span class="line">// 求和，sumValue = 10, 有起始值</span><br><span class="line">int sumValue = Stream.of(1, 2, 3, 4).reduce(0, Integer::sum);</span><br><span class="line">// 求和，sumValue = 10, 无起始值</span><br><span class="line">sumValue = Stream.of(1, 2, 3, 4).reduce(Integer::sum).get();</span><br><span class="line">// 过滤，字符串连接，concat = &quot;ace&quot;</span><br><span class="line">concat = Stream.of(&quot;a&quot;, &quot;B&quot;, &quot;c&quot;, &quot;D&quot;, &quot;e&quot;, &quot;F&quot;).</span><br><span class="line"> filter(x -&gt; x.compareTo(&quot;Z&quot;) &gt; 0).</span><br><span class="line"> reduce(&quot;&quot;, String::concat);</span><br></pre></td></tr></table></figure></p>
<p>上面代码例如第一个示例的 reduce()，第一个参数（空白字符）即为起始值，第二个参数（String::concat）为 BinaryOperator。这类有起始值的 reduce() 都返回具体的对象。而对于第四个示例没有起始值的 reduce()，由于可能没有足够的元素，返回的是 Optional，请留意这个区别。</p>
<p><strong>limit/skip</strong></p>
<p>limit 返回 Stream 的前面 n 个元素；skip 则是扔掉前 n 个元素（它是由一个叫 subStream 的方法改名而来）。</p>
<p>清单 16. limit 和 skip 对运行次数的影响<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public void testLimitAndSkip() &#123;</span><br><span class="line"> List&lt;Person&gt; persons = new ArrayList();</span><br><span class="line"> for (int i = 1; i &lt;= 10000; i++) &#123;</span><br><span class="line"> Person person = new Person(i, &quot;name&quot; + i);</span><br><span class="line"> persons.add(person);</span><br><span class="line"> &#125;</span><br><span class="line">List&lt;String&gt; personList2 = persons.stream().</span><br><span class="line">map(Person::getName).limit(10).skip(3).collect(Collectors.toList());</span><br><span class="line"> System.out.println(personList2);</span><br><span class="line">&#125;</span><br><span class="line">private class Person &#123;</span><br><span class="line"> public int no;</span><br><span class="line"> private String name;</span><br><span class="line"> public Person (int no, String name) &#123;</span><br><span class="line"> this.no = no;</span><br><span class="line"> this.name = name;</span><br><span class="line"> &#125;</span><br><span class="line"> public String getName() &#123;</span><br><span class="line"> System.out.println(name);</span><br><span class="line"> return name;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出结果为：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">name1</span><br><span class="line">name2</span><br><span class="line">name3</span><br><span class="line">name4</span><br><span class="line">name5</span><br><span class="line">name6</span><br><span class="line">name7</span><br><span class="line">name8</span><br><span class="line">name9</span><br><span class="line">name10</span><br><span class="line">[name4, name5, name6, name7, name8, name9, name10]</span><br></pre></td></tr></table></figure></p>
<p>这是一个有 10，000 个元素的 Stream，但在 short-circuiting 操作 limit 和 skip 的作用下，管道中 map 操作指定的 getName() 方法的执行次数为 limit 所限定的 10 次，而最终返回结果在跳过前 3 个元素后只有后面 7 个返回。</p>
<p>有一种情况是 limit/skip 无法达到 short-circuiting 目的的，就是把它们放在 Stream 的排序操作后，原因跟 sorted 这个 intermediate 操作有关：此时系统并不知道 Stream 排序后的次序如何，所以 sorted 中的操作看上去就像完全没有被 limit 或者 skip 一样。</p>
<p>清单 17. limit 和 skip 对 sorted 后的运行次数无影响<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;Person&gt; persons = new ArrayList();</span><br><span class="line"> for (int i = 1; i &lt;= 5; i++) &#123;</span><br><span class="line"> Person person = new Person(i, &quot;name&quot; + i);</span><br><span class="line"> persons.add(person);</span><br><span class="line"> &#125;</span><br><span class="line">List&lt;Person&gt; personList2 = persons.stream().sorted((p1, p2) -&gt; </span><br><span class="line">p1.getName().compareTo(p2.getName())).limit(2).collect(Collectors.toList());</span><br><span class="line">System.out.println(personList2);</span><br></pre></td></tr></table></figure></p>
<p>上面的示例对清单 13 做了微调，首先对 5 个元素的 Stream 排序，然后进行 limit 操作。输出结果为：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">name2</span><br><span class="line">name1</span><br><span class="line">name3</span><br><span class="line">name2</span><br><span class="line">name4</span><br><span class="line">name3</span><br><span class="line">name5</span><br><span class="line">name4</span><br><span class="line">[stream.StreamDW$Person@816f27d, stream.StreamDW$Person@87aac27]</span><br></pre></td></tr></table></figure></p>
<p>即虽然最后的返回元素数量是 2，但整个管道中的 sorted 表达式执行次数没有像前面例子相应减少。</p>
<p>最后有一点需要注意的是，对一个 parallel 的 Steam 管道来说，如果其元素是有序的，那么 limit 操作的成本会比较大，因为它的返回对象必须是前 n 个也有一样次序的元素。取而代之的策略是取消元素间的次序，或者不要用 parallel Stream。</p>
<p><strong>sorted</strong></p>
<p>对 Stream 的排序通过 sorted 进行，它比数组的排序更强之处在于你可以首先对 Stream 进行各类 map、filter、limit、skip 甚至 distinct 来减少元素数量后，再排序，这能帮助程序明显缩短执行时间。我们对清单 14 进行优化：</p>
<p>清单 18. 优化：排序前进行 limit 和 skip<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;Person&gt; persons = new ArrayList();</span><br><span class="line"> for (int i = 1; i &lt;= 5; i++) &#123;</span><br><span class="line"> Person person = new Person(i, &quot;name&quot; + i);</span><br><span class="line"> persons.add(person);</span><br><span class="line"> &#125;</span><br><span class="line">List&lt;Person&gt; personList2 = persons.stream().limit(2).sorted((p1, p2) -&gt; p1.getName().compareTo(p2.getName())).collect(Collectors.toList());</span><br><span class="line">System.out.println(personList2);</span><br></pre></td></tr></table></figure></p>
<p>结果会简单很多：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">name2</span><br><span class="line">name1</span><br><span class="line">[stream.StreamDW$Person@6ce253f1, stream.StreamDW$Person@53d8d10a]</span><br></pre></td></tr></table></figure></p>
<p>当然，这种优化是有 business logic 上的局限性的：即不要求排序后再取值。</p>
<p><strong>min/max/distinct</strong></p>
<p>min 和 max 的功能也可以通过对 Stream 元素先排序，再 findFirst 来实现，但前者的性能会更好，为 O(n)，而 sorted 的成本是 O(n log n)。同时它们作为特殊的 reduce 方法被独立出来也是因为求最大最小值是很常见的操作。</p>
<p>清单 19. 找出最长一行的长度<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">BufferedReader br = new BufferedReader(new FileReader(&quot;c:\\SUService.log&quot;));</span><br><span class="line">int longest = br.lines().</span><br><span class="line"> mapToInt(String::length).</span><br><span class="line"> max().</span><br><span class="line"> getAsInt();</span><br><span class="line">br.close();</span><br><span class="line">System.out.println(longest);</span><br></pre></td></tr></table></figure></p>
<p>下面的例子则使用 distinct 来找出不重复的单词。</p>
<p>清单 20. 找出全文的单词，转小写，并排序<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; words = br.lines().</span><br><span class="line"> flatMap(line -&gt; Stream.of(line.split(&quot; &quot;))).</span><br><span class="line"> filter(word -&gt; word.length() &gt; 0).</span><br><span class="line"> map(String::toLowerCase).</span><br><span class="line"> distinct().</span><br><span class="line"> sorted().</span><br><span class="line"> collect(Collectors.toList());</span><br><span class="line">br.close();</span><br><span class="line">System.out.println(words);</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>这两个就是单纯的两种筛选方法</p>
</blockquote>
<p><strong>Match</strong></p>
<p>Stream 有三个 match 方法，从语义上说：</p>
<ul>
<li>allMatch：Stream 中全部元素符合传入的 predicate，返回 true</li>
<li>anyMatch：Stream 中只要有一个元素符合传入的 predicate，返回 true</li>
<li>noneMatch：Stream 中没有一个元素符合传入的 predicate，返回 true</li>
</ul>
<p>它们都不是要遍历全部元素才能返回结果。例如 allMatch 只要一个元素不满足条件，就 skip 剩下的所有元素，返回 false。对清单 13 中的 Person 类稍做修改，加入一个 age 属性和 getAge 方法。</p>
<p>清单 21. 使用 Match<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List&lt;Person&gt; persons = new ArrayList();</span><br><span class="line">persons.add(new Person(1, &quot;name&quot; + 1, 10));</span><br><span class="line">persons.add(new Person(2, &quot;name&quot; + 2, 21));</span><br><span class="line">persons.add(new Person(3, &quot;name&quot; + 3, 34));</span><br><span class="line">persons.add(new Person(4, &quot;name&quot; + 4, 6));</span><br><span class="line">persons.add(new Person(5, &quot;name&quot; + 5, 55));</span><br><span class="line">boolean isAllAdult = persons.stream().</span><br><span class="line"> allMatch(p -&gt; p.getAge() &gt; 18);</span><br><span class="line">System.out.println(&quot;All are adult? &quot; + isAllAdult);</span><br><span class="line">boolean isThereAnyChild = persons.stream().</span><br><span class="line"> anyMatch(p -&gt; p.getAge() &lt; 12);</span><br><span class="line">System.out.println(&quot;Any child? &quot; + isThereAnyChild);</span><br></pre></td></tr></table></figure></p>
<p>输出结果：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">All are adult? false</span><br><span class="line">Any child? true</span><br></pre></td></tr></table></figure></p>
<h3 id="进阶：自己生成流"><a href="#进阶：自己生成流" class="headerlink" title="进阶：自己生成流"></a>进阶：自己生成流</h3><p><strong>Stream.generate</strong></p>
<p>通过实现 Supplier 接口，你可以自己来控制流的生成。这种情形通常用于随机数、常量的 Stream，或者需要前后元素间维持着某种状态信息的 Stream。把 Supplier 实例传递给 Stream.generate() 生成的 Stream，默认是串行（相对 parallel 而言）但无序的（相对 ordered 而言）。由于它是无限的，在管道中，必须利用 limit 之类的操作限制 Stream 大小。</p>
<p>清单 22. 生成 10 个随机整数<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Random seed = new Random();</span><br><span class="line">Supplier&lt;Integer&gt; random = seed::nextInt;</span><br><span class="line">Stream.generate(random).limit(10).forEach(System.out::println);</span><br><span class="line">//Another way</span><br><span class="line">IntStream.generate(() -&gt; (int) (System.nanoTime() % 100)).</span><br><span class="line">limit(10).forEach(System.out::println);</span><br></pre></td></tr></table></figure></p>
<p>Stream.generate() 还接受自己实现的 Supplier。例如在构造海量测试数据的时候，用某种自动的规则给每一个变量赋值；或者依据公式计算 Stream 的每个元素值。这些都是维持状态信息的情形。</p>
<p>清单 23. 自实现 Supplier<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Stream.generate(new PersonSupplier()).</span><br><span class="line">limit(10).</span><br><span class="line">forEach(p -&gt; System.out.println(p.getName() + &quot;, &quot; + p.getAge()));</span><br><span class="line">private class PersonSupplier implements Supplier&lt;Person&gt; &#123;</span><br><span class="line"> private int index = 0;</span><br><span class="line"> private Random random = new Random();</span><br><span class="line"> @Override</span><br><span class="line"> public Person get() &#123;</span><br><span class="line"> return new Person(index++, &quot;StormTestUser&quot; + index, random.nextInt(100));</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出结果：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">StormTestUser1, 9</span><br><span class="line">StormTestUser2, 12</span><br><span class="line">StormTestUser3, 88</span><br><span class="line">StormTestUser4, 51</span><br><span class="line">StormTestUser5, 22</span><br><span class="line">StormTestUser6, 28</span><br><span class="line">StormTestUser7, 81</span><br><span class="line">StormTestUser8, 51</span><br><span class="line">StormTestUser9, 4</span><br><span class="line">StormTestUser10, 76</span><br></pre></td></tr></table></figure></p>
<p><strong>Stream.iterate</strong><br>iterate 跟 reduce 操作很像，接受一个种子值，和一个 UnaryOperator（例如 f）。然后种子值成为 Stream 的第一个元素，f(seed) 为第二个，f(f(seed)) 第三个，以此类推。</p>
<p>清单 24. 生成一个等差数列<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Stream.iterate(0, n -&gt; n + 3).limit(10). forEach(x -&gt; System.out.print(x + &quot; &quot;));.</span><br></pre></td></tr></table></figure></p>
<p>输出结果：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">0 3 6 9 12 15 18 21 24 27</span><br></pre></td></tr></table></figure></p>
<p>与 Stream.generate 相仿，在 iterate 时候管道必须有 limit 这样的操作来限制 Stream 大小。</p>
<h3 id="进阶：用-Collectors-来进行-reduction-操作"><a href="#进阶：用-Collectors-来进行-reduction-操作" class="headerlink" title="进阶：用 Collectors 来进行 reduction 操作"></a>进阶：用 Collectors 来进行 reduction 操作</h3><p>java.util.stream.Collectors 类的主要作用就是辅助进行各类有用的 reduction 操作，例如转变输出为 Collection，把 Stream 元素进行归组。</p>
<p><strong>groupingBy/partitioningBy</strong></p>
<p>清单 25. 按照年龄归组<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Map&lt;Integer, List&lt;Person&gt;&gt; personGroups = Stream.generate(new PersonSupplier()).</span><br><span class="line"> limit(100).</span><br><span class="line"> collect(Collectors.groupingBy(Person::getAge));</span><br><span class="line">Iterator it = personGroups.entrySet().iterator();</span><br><span class="line">while (it.hasNext()) &#123;</span><br><span class="line"> Map.Entry&lt;Integer, List&lt;Person&gt;&gt; persons = (Map.Entry) it.next();</span><br><span class="line"> System.out.println(&quot;Age &quot; + persons.getKey() + &quot; = &quot; + persons.getValue().size());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面的 code，首先生成 100 人的信息，然后按照年龄归组，相同年龄的人放到同一个 list 中，可以看到如下的输出：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Age 0 = 2</span><br><span class="line">Age 1 = 2</span><br><span class="line">Age 5 = 2</span><br><span class="line">Age 8 = 1</span><br><span class="line">Age 9 = 1</span><br><span class="line">Age 11 = 2</span><br><span class="line">……</span><br></pre></td></tr></table></figure>
<p>清单 26. 按照未成年人和成年人归组<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Map&lt;Boolean, List&lt;Person&gt;&gt; children = Stream.generate(new PersonSupplier()).</span><br><span class="line"> limit(100).</span><br><span class="line"> collect(Collectors.partitioningBy(p -&gt; p.getAge() &lt; 18));</span><br><span class="line">System.out.println(&quot;Children number: &quot; + children.get(true).size());</span><br><span class="line">System.out.println(&quot;Adult number: &quot; + children.get(false).size());</span><br></pre></td></tr></table></figure></p>
<p>输出结果：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Children number: 23 </span><br><span class="line">Adult number: 77</span><br></pre></td></tr></table></figure></p>
<p>在使用条件“年龄小于 18”进行分组后可以看到，不到 18 岁的未成年人是一组，成年人是另外一组。partitioningBy 其实是一种特殊的 groupingBy，它依照条件测试的是否两种结果来构造返回的数据结构，get(true) 和 get(false) 能即为全部的元素对象。</p>
<blockquote>
<p>这里我只是在业务中使用过groupingBy，这个方法常用的就是分组后筛选，再更换数据结构输出</p>
</blockquote>
<h2 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h2><p>总之，Stream 的特性可以归纳为：</p>
<ul>
<li>不是数据结构</li>
<li>它没有内部存储，它只是用操作管道从 source（数据结构、数组、generator function、IO channel）抓取数据。</li>
<li>它也绝不修改自己所封装的底层数据结构的数据。例如 Stream 的 filter 操作会产生一个不包含被过滤元素的新 Stream，而不是从 source 删除那些元素。</li>
<li>所有 Stream 的操作必须以 lambda 表达式为参数</li>
<li>不支持索引访问</li>
<li>你可以请求第一个元素，但无法请求第二个，第三个，或最后一个。不过请参阅下一项。</li>
<li>很容易生成数组或者 List</li>
<li>惰性化</li>
<li>很多 Stream 操作是向后延迟的，一直到它弄清楚了最后需要多少数据才会开始。</li>
<li>Intermediate 操作永远是惰性化的。</li>
<li>并行能力</li>
<li>当一个 Stream 是并行化的，就不需要再写多线程代码，所有对它的操作会自动并行进行的。</li>
<li>可以是无限的</li>
<li>集合有固定大小，Stream 则不必。limit(n) 和 findFirst() 这类的 short-circuiting 操作可以对无限的 Stream 进行运算并很快完成。</li>
</ul>
<hr>
<h3 id="相关主题"><a href="#相关主题" class="headerlink" title="相关主题"></a>相关主题</h3><ul>
<li>Oracle Java 8 官方文档对<a href="https://docs.oracle.com/javase/8/docs/api/java/util/stream/package-summary.html" target="_blank" rel="noopener"> java.util.stream package</a> 的说明。</li>
<li>一篇教程：Java 8 Tutorials, Resources, Books and Examples to learn Lambdas, Stream API and Functional Interfaces。(这个的原文已经丢失了，可惜我当时并没有能把这个文章保存下来)</li>
<li>关于这篇 <a href="http://www.drdobbs.com/jvm/lambdas-and-streams-in-java-8-libraries/240166818" target="_blank" rel="noopener">Lambda 和 Stream</a> 更多介绍的教程。(这个是英文的，需要一些耐心才能流畅阅读)</li>
<li>访问 <a href="https://www.ibm.com/developerworks/cn/java/" target="_blank" rel="noopener">developerWorks Java</a> 专区，了解关于信息管理的更多信息，获取技术文档、how-to 文章、培训、下载、产品信息以及其他资源。</li>
</ul>
<hr>
<blockquote>
<p>这篇文章讲解的非常详细透彻，让我受益颇多。 英文文档因为我自身的英文水平原因导致阅读起来的效率并不如这个高</p>
</blockquote>
]]></content>
      <tags>
        <tag>java8</tag>
      </tags>
  </entry>
  <entry>
    <title>LxRunOffline安装CentOS</title>
    <url>/2020/08/01/LxRunOffline%E5%AE%89%E8%A3%85CentOS/</url>
    <content><![CDATA[<h1 id="LxRunOffline安装CentOS"><a href="#LxRunOffline安装CentOS" class="headerlink" title="LxRunOffline安装CentOS"></a>LxRunOffline安装CentOS</h1><blockquote>
<p>首先，本机已经是 windows2004 版本，安装了阿里云提供的<a href="http://mirrors.aliyun.com/docker-toolbox/windows/docker-for-windows/" target="_blank" rel="noopener">Docker for Windows</a> 。<br>本文承接上文的《在win上使用VMware安装虚拟机运行tippecanoe切片》,本质都是在 Windows10 上安装Linux虚拟机。</p>
</blockquote>
<a id="more"></a>
<h2 id="安装LxRunOffline"><a href="#安装LxRunOffline" class="headerlink" title="安装LxRunOffline"></a>安装LxRunOffline</h2><ul>
<li>普通手动安装：下载解压 <a href="https://github.com/DDoSolitary/LxRunOffline/releases" target="_blank" rel="noopener">LxRunOffline</a> ，并设置环境变量。</li>
<li><p>使用 <a href="https://chocolatey.org/" target="_blank" rel="noopener">Chocolatey</a> （windows 中的一个软件包管理工具）安装。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">choco install lxrunoffline</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用 <a href="https://scoop.sh/" target="_blank" rel="noopener">Scoop</a> （windows 中的一个软件包管理工具） 安装。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scoop bucket add extras</span><br><span class="line">scoop install lxrunoffline</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>我本人使用的是第一种，下载解压后把<code>LxRunOffline.exe</code>的本地路径（例：<code>D:\LxRunOffine\LxRunOffline.exe</code> ）放在 Path 环境变量中。</p>
<h2 id="下载Centos镜像"><a href="#下载Centos镜像" class="headerlink" title="下载Centos镜像"></a>下载Centos镜像</h2><p>直接下载下面的链接<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https://raw.githubusercontent.com/CentOS/sig-cloud-instance-images/a77b36c6c55559b0db5bf9e74e61d32ea709a179/docker/centos-7-docker.tar.xz</span><br></pre></td></tr></table></figure></p>
<h2 id="使用-LxRunOffline-部署-Centos-到WSL"><a href="#使用-LxRunOffline-部署-Centos-到WSL" class="headerlink" title="使用 LxRunOffline 部署 Centos 到WSL"></a>使用 LxRunOffline 部署 Centos 到WSL</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">LxRunOffline.exe  install -n centos -d E:\ProgramData\Microsoft\Windows\WSL\CentOS -f  E:\Progra</span><br><span class="line">mData\Microsoft\Windows\WSL\centos-7-docker.tar.xz</span><br></pre></td></tr></table></figure>
<p>其中 -d 后面是要安装到的目录，-f 是前面下载的镜像， -n 用来指定名称。</p>
<p>使用微软应用商店安装的 WSL 会在开始菜单添加应用图标（快捷方式），而使用 LxRunOf­fline 安装 WSL 时可以通过添加 -s 参数在桌面创建快捷方式。如果你安装时忘记添加参数，可以使用以下命令进行创建。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">LxRunOffline s -n &lt;WSL名称&gt; -f &lt;快捷方式路径&gt;.lnk</span><br></pre></td></tr></table></figure></p>
<p>然后使用  LxRunOffine 来开启 Centos</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">LxRunOffline  run  -n centos</span><br></pre></td></tr></table></figure>
<h2 id="在-Centos-中部署-tippecanoe"><a href="#在-Centos-中部署-tippecanoe" class="headerlink" title="在 Centos 中部署 tippecanoe"></a>在 Centos 中部署 tippecanoe</h2><p>更新yum配置<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install gcc-c++</span><br><span class="line">yum install sqlite-devel</span><br><span class="line">yum install -y zlib zlib-devel</span><br></pre></td></tr></table></figure></p>
<p>下载解压 <a href="https://github.com/mapbox/tippecanoe" target="_blank" rel="noopener">tippecanoe</a> 包，在解压后的文件夹里<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">make &amp;&amp; make install</span><br></pre></td></tr></table></figure></p>
<p>如果碰到<code>-bash: make: command not found</code>这个错误，安装make命令即可<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum -y install gcc automake autoconf libtool make</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>LxRunOffline</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL主从复制，启动slave时报错Slave failed to initialize relay log info structure from the repository</title>
    <url>/2019/12/21/MySQL%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6%EF%BC%8C%E5%90%AF%E5%8A%A8slave%E6%97%B6%E6%8A%A5%E9%94%99/</url>
    <content><![CDATA[<h1 id="MySQL主从复制，启动slave时报错Slave-failed-to-initialize-relay-log-info-structure-from-the-repository"><a href="#MySQL主从复制，启动slave时报错Slave-failed-to-initialize-relay-log-info-structure-from-the-repository" class="headerlink" title="MySQL主从复制，启动slave时报错Slave failed to initialize relay log info structure from the repository"></a>MySQL主从复制，启动slave时报错Slave failed to initialize relay log info structure from the repository</h1><blockquote>
<p>本机的主从同步再次崩溃了。</p>
</blockquote>
<a id="more"></a>
<h2 id="报错信息"><a href="#报错信息" class="headerlink" title="报错信息"></a>报错信息</h2><p>mysql&gt; start slave;<br>ERROR 1872 (HY000): Slave failed to initialize relay log info structure from the repository</p>
<h2 id="处理流程"><a href="#处理流程" class="headerlink" title="处理流程"></a>处理流程</h2><p>查看了一下错误日值<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Failed to open the relay log &apos;.\WIN-DOUOFQR7DBP-relay-bin.000040&apos; (relay_log_pos 333357).</span><br><span class="line"></span><br><span class="line">Could not find target log file mentioned in relay log info in the index file &apos;.\myhbase-relay-bin.index&apos; during relay log initialization.</span><br></pre></td></tr></table></figure></p>
<p>在百度中找到一个类似的文章<a href="https://blog.csdn.net/weixin_37998647/article/details/79950133" target="_blank" rel="noopener">https://blog.csdn.net/weixin_37998647/article/details/79950133</a> </p>
<p>使用命令<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; reset slave;</span><br><span class="line">Query OK, 0 rows affected (0.01 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; change master to ......</span><br><span class="line"></span><br><span class="line">mysql&gt; start slave;</span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br></pre></td></tr></table></figure></p>
<p>即可成功解决问题。</p>
]]></content>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode第5题解体思路</title>
    <url>/2020/06/27/LeetCode%E7%AC%AC5%E9%A2%98%E8%A7%A3%E4%BD%93%E6%80%9D%E8%B7%AF/</url>
    <content><![CDATA[<h1 id="LeetCode第5题解体思路"><a href="#LeetCode第5题解体思路" class="headerlink" title="LeetCode第5题解体思路"></a>LeetCode第5题解体思路</h1><blockquote>
<p>最近这忙于换工作电脑，换工作场地消耗了不少的时间。通勤时间也从10分钟增长到了来回1小时，每天也要7点钟起来去吃早餐了。<br>原文地址<a href="https://leetcode.wang/leetCode-5-Longest-Palindromic-Substring.html" target="_blank" rel="noopener">https://leetcode.wang</a> 感觉我的排版不行，可以选择直接去看原版。</p>
</blockquote>
<a id="more"></a>
<h2 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h2><p>给定一个字符串，输出最长的回文子串。回文串指的是正的读和反的读是一样的字符串，例如 “aba”，”ccbbcc”。</p>
<h2 id="解法"><a href="#解法" class="headerlink" title="解法"></a>解法</h2><h3 id="解法一：暴力破解"><a href="#解法一：暴力破解" class="headerlink" title="解法一：暴力破解"></a>解法一：暴力破解</h3><p>暴力求解，列举所有的子串，判断是否为回文串，保存最长的回文串。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public boolean isPalindromic(String s) &#123;</span><br><span class="line">        int len = s.length();</span><br><span class="line">        for (int i = 0; i &lt; len / 2; i++) &#123;</span><br><span class="line">            if (s.charAt(i) != s.charAt(len - i - 1)) &#123;</span><br><span class="line">                return false;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">// 暴力解法</span><br><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    String ans = &quot;&quot;;</span><br><span class="line">    int max = 0;</span><br><span class="line">    int len = s.length();</span><br><span class="line">    for (int i = 0; i &lt; len; i++)</span><br><span class="line">        for (int j = i + 1; j &lt;= len; j++) &#123;</span><br><span class="line">            String test = s.substring(i, j);</span><br><span class="line">            if (isPalindromic(test) &amp;&amp; test.length() &gt; max) &#123;</span><br><span class="line">                ans = s.substring(i, j);</span><br><span class="line">                max = Math.max(max, ans.length());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    return ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>时间复杂度：两层 for 循环 O（n²），for 循环里边判断是否为回文，O（n），所以时间复杂度为 O（n³）。</p>
<p>空间复杂度：O（1），常数个变量。</p>
<h3 id="解法二：最长公共子串"><a href="#解法二：最长公共子串" class="headerlink" title="解法二：最长公共子串"></a>解法二：最长公共子串</h3><p>根据回文串的定义，正着和反着读一样，那我们是不是把原来的字符串倒置了，然后找最长的公共子串就可以了。例如，S = “ caba”，S’ = “ abac”，最长公共子串是 “aba”，所以原字符串的最长回文串就是 “aba”。</p>
<p>关于求最长公共子串（不是公共子序列），有很多方法，这里用动态规划的方法，可以先阅读下边的链接。</p>
<p><a href="https://blog.csdn.net/u010397369/article/details/38979077" target="_blank" rel="noopener">https://blog.csdn.net/u010397369/article/details/38979077</a><br><a href="https://www.kancloud.cn/digest/pieces-algorithm/163624" target="_blank" rel="noopener">https://www.kancloud.cn/digest/pieces-algorithm/163624</a></p>
<p>整体思想就是，申请一个二维的数组初始化为 0，然后判断对应的字符是否相等，相等的话</p>
<p>arr [ i ][ j ] = arr [ i - 1 ][ j - 1] + 1 。</p>
<p>当 i = 0 或者 j = 0 的时候单独分析，字符相等的话 arr [ i ][ j ] 就赋为 1 。</p>
<p>arr [ i ][ j ] 保存的就是公共子串的长度。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    if (s.equals(&quot;&quot;))</span><br><span class="line">        return &quot;&quot;;</span><br><span class="line">    String origin = s;</span><br><span class="line">    String reverse = new StringBuffer(s).reverse().toString(); //字符串倒置</span><br><span class="line">    int length = s.length();</span><br><span class="line">    int[][] arr = new int[length][length];</span><br><span class="line">    int maxLen = 0;</span><br><span class="line">    int maxEnd = 0;</span><br><span class="line">    for (int i = 0; i &lt; length; i++)</span><br><span class="line">        for (int j = 0; j &lt; length; j++) &#123;</span><br><span class="line">            if (origin.charAt(i) == reverse.charAt(j)) &#123;</span><br><span class="line">                if (i == 0 || j == 0) &#123;</span><br><span class="line">                    arr[i][j] = 1;</span><br><span class="line">                &#125; else &#123;</span><br><span class="line">                    arr[i][j] = arr[i - 1][j - 1] + 1;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            if (arr[i][j] &gt; maxLen) &#123; </span><br><span class="line">                maxLen = arr[i][j];</span><br><span class="line">                maxEnd = i; //以 i 位置结尾的字符</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return s.substring(maxEnd - maxLen + 1, maxEnd + 1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>再看一个例子，S = “abc435cba”，S’ = “abc534cba” ，最长公共子串是 “abc” 和 “cba” ，但很明显这两个字符串都不是回文串。</p>
<p>所以我们求出最长公共子串后，并不一定是回文串，我们还需要判断该字符串倒置前的下标和当前的字符串下标是不是匹配。</p>
<p>比如 S = “ caba “，S’ = “ abac “ ，S’ 中 aba 的下标是 0 1 2 ，倒置前是 3 2 1，和 S 中 aba 的下标符合，所以 aba 就是我们需要找的。当然我们不需要每个字符都判断，我们只需要判断末尾字符就可以。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b1.png" alt="b1"></p>
<p>首先 i ，j 始终指向子串的末尾字符。所以 j 指向的红色的 a 倒置前的下标是 beforeRev = length - 1 - j = 4 - 1 - 2 = 1，对应的是字符串首位的下标，我们还需要加上字符串的长度才是末尾字符的下标，也就是 beforeRev + arr[ i ] [ j ] - 1 = 1 + 3 - 1 = 3，因为 arr[ i ] [ j ] 保存的就是当前子串的长度，也就是图中的数字 3 。此时再和它与 i 比较，如果相等，则说明它是我们要找的回文串。</p>
<p>之前的 S = “abc435cba”，S’ = “abc534cba” ，可以看一下图示，为什么不符合。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b2.png" alt="b2"></p>
<p>当前 j 指向的 c ，倒置前的下标是 beforeRev = length - 1 - j = 9 - 1 - 2 = 6，对应的末尾下标是 beforeRev + arr[ i ] [ j ] - 1 = 6 + 3 - 1 = 8 ，而此时 i = 2 ，所以当前的子串不是回文串。</p>
<p>代码的话，在上边的基础上，保存 maxLen 前判断一下下标匹不匹配就可以了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    if (s.equals(&quot;&quot;))</span><br><span class="line">        return &quot;&quot;;</span><br><span class="line">    String origin = s;</span><br><span class="line">    String reverse = new StringBuffer(s).reverse().toString();</span><br><span class="line">    int length = s.length();</span><br><span class="line">    int[][] arr = new int[length][length];</span><br><span class="line">    int maxLen = 0;</span><br><span class="line">    int maxEnd = 0;</span><br><span class="line">    for (int i = 0; i &lt; length; i++)</span><br><span class="line">        for (int j = 0; j &lt; length; j++) &#123;</span><br><span class="line">            if (origin.charAt(i) == reverse.charAt(j)) &#123;</span><br><span class="line">                if (i == 0 || j == 0) &#123;</span><br><span class="line">                    arr[i][j] = 1;</span><br><span class="line">                &#125; else &#123;</span><br><span class="line">                    arr[i][j] = arr[i - 1][j - 1] + 1;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            /**********修改的地方*******************/</span><br><span class="line">            if (arr[i][j] &gt; maxLen) &#123;</span><br><span class="line">                int beforeRev = length - 1 - j;</span><br><span class="line">                if (beforeRev + arr[i][j] - 1 == i) &#123; //判断下标是否对应</span><br><span class="line">                    maxLen = arr[i][j];</span><br><span class="line">                    maxEnd = i;</span><br><span class="line">                &#125;</span><br><span class="line">                /*************************************/</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    return s.substring(maxEnd - maxLen + 1, maxEnd + 1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：两层循环，O（n²）。</p>
<p>空间复杂度：一个二维数组，O（n²）。</p>
<p>空间复杂度其实可以再优化一下。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b3.png" alt="b3"></p>
<p>我们分析一下循环，i = 0 ，j = 0，1，2 … 8 更新一列，然后 i = 1 ，再更新一列，而更新的时候我们其实只需要上一列的信息，更新第 3 列的时候，第 1 列的信息是没有用的。所以我们只需要一个一维数组就可以了。但是更新 arr [ i ] 的时候我们需要 arr [ i - 1 ] 的信息，假设 a [ 3 ] = a [ 2 ] + 1，更新 a [ 4 ] 的时候， 我们需要 a [ 3 ] 的信息，但是 a [ 3 ] 在之前已经被更新了，所以 j 不能从 0 到 8 ，应该倒过来，a [ 8 ] = a [ 7 ] + 1，a [ 7 ] = a [ 6 ] + 1 , 这样更新 a [ 8 ] 的时候用 a [ 7 ] ，用完后才去更新 a [ 7 ]，保证了不会出错。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    if (s.equals(&quot;&quot;))</span><br><span class="line">        return &quot;&quot;;</span><br><span class="line">    String origin = s;</span><br><span class="line">    String reverse = new StringBuffer(s).reverse().toString();</span><br><span class="line">    int length = s.length();</span><br><span class="line">    int[] arr = new int[length];</span><br><span class="line">    int maxLen = 0;</span><br><span class="line">    int maxEnd = 0;</span><br><span class="line">    for (int i = 0; i &lt; length; i++)</span><br><span class="line">        /**************修改的地方***************************/</span><br><span class="line">        for (int j = length - 1; j &gt;= 0; j--) &#123;</span><br><span class="line">        /**************************************************/</span><br><span class="line">            if (origin.charAt(i) == reverse.charAt(j)) &#123;</span><br><span class="line">                if (i == 0 || j == 0) &#123;</span><br><span class="line">                    arr[j] = 1;</span><br><span class="line">                &#125; else &#123;</span><br><span class="line">                    arr[j] = arr[j - 1] + 1;</span><br><span class="line">                &#125;</span><br><span class="line">            /**************修改的地方***************************/</span><br><span class="line">            //之前二维数组，每次用的是不同的列，所以不用置 0 。</span><br><span class="line">            &#125; else &#123;</span><br><span class="line">                arr[j] = 0;</span><br><span class="line">            &#125;</span><br><span class="line">            /**************************************************/</span><br><span class="line">            if (arr[j] &gt; maxLen) &#123;</span><br><span class="line">                int beforeRev = length - 1 - j;</span><br><span class="line">                if (beforeRev + arr[j] - 1 == i) &#123;</span><br><span class="line">                    maxLen = arr[j];</span><br><span class="line">                    maxEnd = i;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    return s.substring(maxEnd - maxLen + 1, maxEnd + 1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：O（n²）。</p>
<p>空间复杂度：降为 O（n）。</p>
<h3 id="解法三：暴力破解优化"><a href="#解法三：暴力破解优化" class="headerlink" title="解法三：暴力破解优化"></a>解法三：暴力破解优化</h3><p>解法一的暴力解法时间复杂度太高，在 leetCode 上并不能 AC 。我们可以考虑，去掉一些暴力解法中重复的判断。我们可以基于下边的发现，进行改进。</p>
<p>首先定义 P（i，j）。<br><img src="/2020/06/27/LeetCode第5题解体思路/b4.png" alt="b4"></p>
<p>接下来<br><img src="/2020/06/27/LeetCode第5题解体思路/b5.png" alt="b5"></p>
<p>所以如果我们想知道 P（i，j）的情况，不需要调用判断回文串的函数了，只需要知道 P（i + 1，j - 1）的情况就可以了，这样时间复杂度就少了 O（n）。因此我们可以用动态规划的方法，空间换时间，把已经求出的 P（i，j）存储起来。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b6.png" alt="b6"></p>
<p>如果 S[i+1,j-1]S[i+1,j−1] 是回文串，那么只要 S [ i ] == S [ j ] ，就可以确定 S [ i , j ] 也是回文串了。</p>
<p>求 长度为 1 和长度为 2 的 P ( i , j ) 时不能用上边的公式，因为我们代入公式后会遇到 P[i][j]P[i][j] 中 i &gt; j 的情况，比如求 P[1][2]P[1][2] 的话，我们需要知道 P[1+1][2-1]=P[2][1]P[1+1][2−1]=P[2][1] ，而 P[2][1]P[2][1] 代表着 S[2,1]S[2,1] 是不是回文串，显然是不对的，所以我们需要单独判断。</p>
<p>所以我们先初始化长度是 1 的回文串的 P [ i , j ]，这样利用上边提出的公式 P(i,j)=(P(i+1,j-1)\&amp;\&amp;S[i]==S[j])P(i,j)=(P(i+1,j−1)&amp;&amp;S[i]==S[j])，然后两边向外各扩充一个字符，长度为 3 的，为 5 的，所有奇数长度的就都求出来了。</p>
<p>同理，初始化长度是 2 的回文串 P [ i , i + 1 ]，利用公式，长度为 4 的，6 的所有偶数长度的就都求出来了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    int length = s.length();</span><br><span class="line">    boolean[][] P = new boolean[length][length];</span><br><span class="line">    int maxLen = 0;</span><br><span class="line">    String maxPal = &quot;&quot;;</span><br><span class="line">    for (int len = 1; len &lt;= length; len++) //遍历所有的长度</span><br><span class="line">        for (int start = 0; start &lt; length; start++) &#123;</span><br><span class="line">            int end = start + len - 1;</span><br><span class="line">            if (end &gt;= length) //下标已经越界，结束本次循环</span><br><span class="line">                break;</span><br><span class="line">            P[start][end] = (len == 1 || len == 2 || P[start + 1][end - 1]) &amp;&amp; s.charAt(start) == s.charAt(end); //长度为 1 和 2 的单独判断下</span><br><span class="line">            if (P[start][end] &amp;&amp; len &gt; maxLen) &#123;</span><br><span class="line">                maxPal = s.substring(start, end + 1);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    return maxPal;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：两层循环，O（n²）。</p>
<p>空间复杂度：用二维数组 P 保存每个子串的情况，O（n²）。</p>
<p>我们分析下每次循环用到的 P（i，j），看一看能不能向解法二一样优化一下空间复杂度。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b7.png" alt="b7"></p>
<p>当我们求长度为 6 和 5 的子串的情况时，其实只用到了 4 ， 3 长度的情况，而长度为 1 和 2 的子串情况其实已经不需要了。但是由于我们并不是用 P 数组的下标进行的循环，暂时没有想到优化的方法。</p>
<p>之后看到了另一种动态规划的思路<br><a href="https://leetcode.com/problems/longest-palindromic-substring/discuss/2921/Share-my-Java-solution-using-dynamic-programming" target="_blank" rel="noopener">https://leetcode.com/problems/longest-palindromic-substring/discuss/2921/Share-my-Java-solution-using-dynamic-programming</a></p>
<p>公式还是这个不变</p>
<p>首先定义 P（i，j）。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b8.png" alt="b8"><br>接下来</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b9.png" alt="b9"><br>递推公式中我们可以看到，我们首先知道了 i +1 才会知道 i ，所以我们只需要倒着遍历就行了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    int n = s.length();</span><br><span class="line">    String res = &quot;&quot;;</span><br><span class="line">    boolean[][] dp = new boolean[n][n];</span><br><span class="line">    for (int i = n - 1; i &gt;= 0; i--) &#123;</span><br><span class="line">        for (int j = i; j &lt; n; j++) &#123;</span><br><span class="line">            dp[i][j] = s.charAt(i) == s.charAt(j) &amp;&amp; (j - i &lt; 2 || dp[i + 1][j - 1]); //j - i 代表长度减去 1        </span><br><span class="line">            if (dp[i][j] &amp;&amp;  j - i + 1 &gt; res.length()) &#123;</span><br><span class="line">                res = s.substring(i, j + 1);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度和空间复杂和之前都没有变化，我们来看看可不可以优化空间复杂度。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b10.png" alt="b10"></p>
<p>当求第 i 行的时候我们只需要第 i + 1 行的信息，并且 j 的话需要 j - 1 的信息，所以和之前一样 j 也需要倒叙。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String longestPalindrome7(String s) &#123;</span><br><span class="line">        int n = s.length();</span><br><span class="line">        String res = &quot;&quot;;</span><br><span class="line">        boolean[] P = new boolean[n];</span><br><span class="line">        for (int i = n - 1; i &gt;= 0; i--) &#123;</span><br><span class="line">            for (int j = n - 1; j &gt;= i; j--) &#123;</span><br><span class="line">                P[j] = s.charAt(i) == s.charAt(j) &amp;&amp; (j - i &lt; 3 || P[j - 1]);</span><br><span class="line">                if (P[j] &amp;&amp; j - i + 1 &gt; res.length()) &#123;</span><br><span class="line">                    res = s.substring(i, j + 1);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return res;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：不变，O（n²）。</p>
<p>空间复杂度：降为 O（n ) 。</p>
<h3 id="解法四：扩展中心"><a href="#解法四：扩展中心" class="headerlink" title="解法四：扩展中心"></a>解法四：扩展中心</h3><p>我们知道回文串一定是对称的，所以我们可以每次循环选择一个中心，进行左右扩展，判断左右字符是否相等即可。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b11.png" alt="b11"></p>
<p>由于存在奇数的字符串和偶数的字符串，所以我们需要从一个字符开始扩展，或者从两个字符之间开始扩展，所以总共有 n + n - 1 个中心。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    if (s == null || s.length() &lt; 1) return &quot;&quot;;</span><br><span class="line">    int start = 0, end = 0;</span><br><span class="line">    for (int i = 0; i &lt; s.length(); i++) &#123;</span><br><span class="line">        int len1 = expandAroundCenter(s, i, i);</span><br><span class="line">        int len2 = expandAroundCenter(s, i, i + 1);</span><br><span class="line">        int len = Math.max(len1, len2);</span><br><span class="line">        if (len &gt; end - start) &#123;</span><br><span class="line">            start = i - (len - 1) / 2;</span><br><span class="line">            end = i + len / 2;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return s.substring(start, end + 1);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">private int expandAroundCenter(String s, int left, int right) &#123;</span><br><span class="line">    int L = left, R = right;</span><br><span class="line">    while (L &gt;= 0 &amp;&amp; R &lt; s.length() &amp;&amp; s.charAt(L) == s.charAt(R)) &#123;</span><br><span class="line">        L--;</span><br><span class="line">        R++;</span><br><span class="line">    &#125;</span><br><span class="line">    return R - L - 1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：O（n²）。</p>
<p>空间复杂度：O（1）。</p>
<h3 id="解法五：Manacher’s-Algorithm-马拉车算法。"><a href="#解法五：Manacher’s-Algorithm-马拉车算法。" class="headerlink" title="解法五：Manacher’s Algorithm 马拉车算法。"></a>解法五：Manacher’s Algorithm 马拉车算法。</h3><blockquote>
<p>马拉车算法 Manacher‘s Algorithm 是用来查找一个字符串的最长回文子串的线性方法，由一个叫Manacher的人在1975年发明的，这个方法的最大贡献是在于将时间复杂度提升到了线性。</p>
</blockquote>
<p>主要参考了下边链接进行讲解。</p>
<p><a href="https://segmentfault.com/a/1190000008484167" target="_blank" rel="noopener">https://segmentfault.com/a/1190000008484167</a><br><a href="https://blog.crimx.com/2017/07/06/manachers-algorithm/" target="_blank" rel="noopener">https://blog.crimx.com/2017/07/06/manachers-algorithm/</a><br><a href="http://ju.outofmemory.cn/entry/130005" target="_blank" rel="noopener">http://ju.outofmemory.cn/entry/130005</a><br><a href="https://articles.leetcode.com/longest-palindromic-substring-part-ii/" target="_blank" rel="noopener">https://articles.leetcode.com/longest-palindromic-substring-part-ii/</a> （已经404了）</p>
<p>首先我们解决下奇数和偶数的问题，在每个字符间插入”#”，并且为了使得扩展的过程中，到边界后自动结束，在两端分别插入 “^” 和 “$”，两个不可能在字符串中出现的字符，这样中心扩展的时候，判断两端字符是否相等的时候，如果到了边界就一定会不相等，从而出了循环。经过处理，字符串的长度永远都是奇数了。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b12.png" alt="b12"></p>
<p>首先我们用一个数组 P 保存从中心扩展的最大个数，而它刚好也是去掉 “#” 的原字符串的总长度。例如下图中下标是 6 的地方。可以看到 P[ 6 ] 等于 5，所以它是从左边扩展 5 个字符，相应的右边也是扩展 5 个字符，也就是 “#c#b#c#b#c#”。而去掉 # 恢复到原来的字符串，变成 “cbcbc”，它的长度刚好也就是 5。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b13.png" alt="b13"></p>
<h4 id="1-求原字符串下标"><a href="#1-求原字符串下标" class="headerlink" title="1.求原字符串下标"></a>1.求原字符串下标</h4><p>用 P 的下标 i 减去 P [ i ]，再除以 2 ，就是原字符串的开头下标了。</p>
<p>例如我们找到 P[ i ] 的最大值为 5 ，也就是回文串的最大长度是 5 ，对应的下标是 6 ，所以原字符串的开头下标是 （6 - 5 ）/ 2 = 0 。所以我们只需要返回原字符串的第 0 到 第 （5 - 1）位就可以了。</p>
<h4 id="2-求每个-P-i"><a href="#2-求每个-P-i" class="headerlink" title="2.求每个 P [ i ]"></a>2.求每个 P [ i ]</h4><p>接下来是算法的关键了，它充分利用了回文串的对称性。</p>
<p>我们用 C 表示回文串的中心，用 R 表示回文串的右边半径坐标，所以 R = C + P[ C ] 。C 和 R 所对应的回文串是当前循环中 R 最靠右的回文串。</p>
<p>让我们考虑求 P [ i ] 的时候，如下图。</p>
<p>用 i_mirror 表示当前需要求的第 i 个字符关于 C 对应的下标。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b15.png" alt="b15"></p>
<p>我们现在要求 P [ i ]， 如果是用中心扩展法，那就向两边扩展比对就行了。但是我们其实可以利用回文串 C 的对称性。i 关于 C 的对称点是 i_mirror ，P [ i_mirror ] = 3，所以 P [ i ] 也等于 3 。</p>
<p>但是有三种情况将会造成直接赋值为 P [ i_mirror ] 是不正确的，下边一一讨论。</p>
<p><strong>2.1 超出了 R</strong><br><img src="/2020/06/27/LeetCode第5题解体思路/b15.png" alt="b15"></p>
<p>当我们要求 P [ i ] 的时候，P [ mirror ] = 7，而此时 P [ i ] 并不等于 7 ，为什么呢，因为我们从 i 开始往后数 7 个，等于 22 ，已经超过了最右的 R ，此时不能利用对称性了，但我们一定可以扩展到 R 的，所以 P [ i ] 至少等于 R - i = 20 - 15 = 5，会不会更大呢，我们只需要比较 T [ R+1 ] 和 T [ R+1 ]关于 i 的对称点就行了，就像中心扩展法一样一个个扩展。</p>
<p><strong>2.2 P [ i_mirror ] 遇到了原字符串的左边界</strong><br><img src="/2020/06/27/LeetCode第5题解体思路/b16.png" alt="b16"></p>
<p>此时P [ i_mirror ] = 1，但是 P [ i ] 赋值成 1 是不正确的，出现这种情况的原因是 P [ i_mirror ] 在扩展的时候首先是 “#” == “#” ，之后遇到了 “^”和另一个字符比较，也就是到了边界，才终止循环的。而 P [ i ] 并没有遇到边界，所以我们可以继续通过中心扩展法一步一步向两边扩展就行了。</p>
<p><strong>2.3 i 等于了 R</strong><br>此时我们先把 P [ i ] 赋值为 0 ，然后通过中心扩展法一步一步扩展就行了。</p>
<h4 id="3-考虑-C-和-R-的更新"><a href="#3-考虑-C-和-R-的更新" class="headerlink" title="3.考虑 C 和 R 的更新"></a>3.考虑 C 和 R 的更新</h4><p>就这样一步一步的求出每个 P [ i ]，当求出的 P [ i ] 的右边界大于当前的 R 时，我们就需要更新 C 和 R 为当前的回文串了。因为我们必须保证 i 在 R 里面，所以一旦有更右边的 R 就要更新 R。</p>
<p><img src="/2020/06/27/LeetCode第5题解体思路/b17.png" alt="b17"></p>
<p>此时的 P [ i ] 求出来将会是 3 ，P [ i ] 对应的右边界将是 10 + 3 = 13，所以大于当前的 R ，我们需要把 C 更新成 i 的值，也就是 10 ，R 更新成 13。继续下边的循环。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String preProcess(String s) &#123;</span><br><span class="line">    int n = s.length();</span><br><span class="line">    if (n == 0) &#123;</span><br><span class="line">        return &quot;^$&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">    String ret = &quot;^&quot;;</span><br><span class="line">    for (int i = 0; i &lt; n; i++)</span><br><span class="line">        ret += &quot;#&quot; + s.charAt(i);</span><br><span class="line">    ret += &quot;#$&quot;;</span><br><span class="line">    return ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 马拉车算法</span><br><span class="line">public String longestPalindrome(String s) &#123;</span><br><span class="line">    String T = preProcess(s);</span><br><span class="line">    int n = T.length();</span><br><span class="line">    int[] P = new int[n];</span><br><span class="line">    int C = 0, R = 0;</span><br><span class="line">    for (int i = 1; i &lt; n - 1; i++) &#123;</span><br><span class="line">        int i_mirror = 2 * C - i;</span><br><span class="line">        if (R &gt; i) &#123;</span><br><span class="line">            P[i] = Math.min(R - i, P[i_mirror]);// 防止超出 R</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            P[i] = 0;// 等于 R 的情况</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        // 碰到之前讲的三种情况时候，需要利用中心扩展法</span><br><span class="line">        while (T.charAt(i + 1 + P[i]) == T.charAt(i - 1 - P[i])) &#123;</span><br><span class="line">            P[i]++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        // 判断是否需要更新 R</span><br><span class="line">        if (i + P[i] &gt; R) &#123;</span><br><span class="line">            C = i;</span><br><span class="line">            R = i + P[i];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 找出 P 的最大值</span><br><span class="line">    int maxLen = 0;</span><br><span class="line">    int centerIndex = 0;</span><br><span class="line">    for (int i = 1; i &lt; n - 1; i++) &#123;</span><br><span class="line">        if (P[i] &gt; maxLen) &#123;</span><br><span class="line">            maxLen = P[i];</span><br><span class="line">            centerIndex = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    int start = (centerIndex - maxLen) / 2; //最开始讲的求原字符串下标</span><br><span class="line">    return s.substring(start, start + maxLen);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>时间复杂度：for 循环里边套了一层 while 循环，难道不是 O ( n² )？不！其实是 O ( n )。不严谨的想一下，因为 while 循环访问 R 右边的数字用来扩展，也就是那些还未求出的节点，然后不断扩展，而期间访问的节点下次就不会再进入 while 了，可以利用对称得到自己的解，所以每个节点访问都是常数次，所以是 O ( n )。</p>
<p>空间复杂度：O（n）。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>时间复杂度从三次方降到了一次，美妙！这里两次用到了动态规划去求解，初步认识了动态规划，就是将之前求的值保存起来，方便后边的计算，使得一些多余的计算消失了。并且在动态规划中，通过观察数组的利用情况，从而降低了空间复杂度。而 Manacher 算法对回文串对称性的充分利用，不得不让人叹服，自己加油啦！</p>
]]></content>
  </entry>
  <entry>
    <title>Navicat Premium 12激活</title>
    <url>/2019/02/27/Navicat-Premium-12%E6%BF%80%E6%B4%BB/</url>
    <content><![CDATA[<h1 id="Navicat-Premium-12激活"><a href="#Navicat-Premium-12激活" class="headerlink" title="Navicat Premium 12激活"></a>Navicat Premium 12激活</h1><blockquote>
<p>原博主地址是<a href="https://www.jianshu.com/p/5f693b4c9468" target="_blank" rel="noopener">https://www.jianshu.com/p/5f693b4c9468</a>。这里仅仅只是做一份备份。</p>
</blockquote>
<a id="more"></a>
<h2 id="Navicat注册机"><a href="#Navicat注册机" class="headerlink" title="Navicat注册机"></a>Navicat注册机</h2><p>　　<strong>注册机下载：</strong><a href="https://pan.baidu.com/s/1jZO5ie22hMFLPk-2SepMgw" target="_blank" rel="noopener">Navicat Premium 12注册机</a>，提取码：cx68。</p>
<p>　　<strong>重要提示：</strong>该注册机来源于<a href="https://dfox.it/DeFconX/files/file/79-navicat-software/" target="_blank" rel="noopener">DeltaFoX</a>（<a href="https://github.com/Deltafox79/Navicat_Keygen" target="_blank" rel="noopener">GitHub</a>）。一般来说，由于注册机会修改.exe文件或.dll文件，加壳并且没有数字签名，所以杀毒软件会报毒，自行决定是否使用本注册机。如需使用本注册机或者下载后找不到文件，需要关闭杀毒软件或将本注册机添加至杀毒软件白名单。(本人用的就是这方式，win764位系统，i5-4590,内存8g)</p>
<p>　　<strong>备用方案：</strong>命令行激活工具，来自<a href="https://github.com/DoubleLabyrinth" target="_blank" rel="noopener">DoubleLabyrinth</a>，分别适用于<a href="https://github.com/DoubleLabyrinth/navicat-keygen/blob/windows/README.zh-CN.md" target="_blank" rel="noopener">Windows</a>和<a href="https://github.com/DoubleLabyrinth/navicat-keygen/blob/mac/README.zh-CN.md" target="_blank" rel="noopener">Mac</a>。请仔细阅读README.md，有问题请到GitHub向该作者提issue。</p>
<p>　　若曾安装过Navicat Premium且多次激活失败，请先卸载已安装的Navicat Premium并清理文件残留和注册表残留，重启电脑，再次尝试激活。请勿无脑提问！确保Navicat Premium程序已关闭。无需断网，无需将注册机放到Navicat Premium安装目录下。</p>
<p>　　<strong><em>以管理员身份运行</em></strong>此注册机：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog.png" alt="">    </p>
<p>​    打开注册机后，在<strong>1) Patch</strong>，勾选Backup、Host和Navicat v12，然后点击Patch按钮：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog1.png" alt="">    </p>
<p>​    找到Navicat Premium 12安装路径下的<strong>navicat.exe</strong>，选中并点击打开：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog2.png" alt="">    </p>
<p>​    此时出现如下弹窗，提示<code>navicat.exe - x64 -&gt; Cracked.</code>，提示已破解（别高兴，还没结束）。若提示<code>libcc.dll</code>或<code>navicat.exe</code>出错，检查是否未关闭Navicat Premium，或到安装目录下将<code>libcc.dll</code>和<code>navicat.exe</code>删除，并将<code>libcc.dll.BAK</code>或<code>navicat.exe.BAK</code>去掉<code>.BAK</code>后缀名。否则卸载已安装的Navicat Premium并清理文件残留和注册表残留：<br>​<br><img src="/2019/02/27/Navicat-Premium-12激活/blog3.png" alt="">    </p>
<p>​    2) License, Product and Language确保License为Enterprise，Products为Premium，Languages为Simplified Chinese（即简体中文，其它语言版本如英文版请选择English）；</p>
<p>​    3) Resale License确保Resale Version为Site license；</p>
<p>​    4) Keygen / Offline Activation中Your Name和Your Organization可以任意填写或者默认:</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog4.png" alt="">    </p>
<p>​    在<strong>4) Keygen / Offline Activation</strong>，点击Generate，将自动生成Serial Keygen（即注册码）：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog5.png" alt="">    </p>
<p>​    打开Navicat Premium 12，点击菜单栏的<strong>帮助</strong>，选择<strong>注册</strong>，在注册窗口<strong>键</strong>处填入上一步生成的Serial Keygen（即注册码），然后点击<strong>激活</strong>：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog6.png" alt="">    </p>
<p>​    点击<strong>手动激活</strong>：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog7.png" alt="">    </p>
<p>​    将Navicat手动激活窗口的请求码框中内容复制到注册机Request Code框中，点击Activation Code下面的Generate按钮（若此处出现错误，要么你未完全按照教程来，要么你所安装的版本高于注册机所支持的版本）：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog8.png" alt="">    </p>
<p>​    将注册机Activation Code处生成的激活码内容复制到Navicat手动激活窗口的激活码框中（或点击Activation Code处下面的Copy按钮，这样会自动粘贴到Navicat手动激活窗口的激活码框中），然后点击激活按钮：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog9.png" alt="">    </p>
<p>​    提示Navicat现已激活：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog10.png" alt="">    </p>
<p>​    Navicat Premium 12激活成功：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog11.png" alt="">    </p>
<blockquote>
<p>这是经过测试确认好用的方法，感谢原博主的分享。本人破解的版本为：</p>
<p><img src="/2019/02/27/Navicat-Premium-12激活/blog12.png" alt="">    </p>
</blockquote>
]]></content>
      <tags>
        <tag>sql</tag>
      </tags>
  </entry>
  <entry>
    <title>RabbitMQ中的stomp初步部署</title>
    <url>/2019/10/19/RabbitMQ%E4%B8%AD%E7%9A%84stomp%E5%88%9D%E6%AD%A5%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h1 id="RabbitMQ中的stomp初步部署"><a href="#RabbitMQ中的stomp初步部署" class="headerlink" title="RabbitMQ中的stomp初步部署"></a>RabbitMQ中的stomp初步部署</h1><blockquote>
<p>本文是为了防止我使用的参考网页404，而做的备份。原网页地址为：<a href="https://blog.csdn.net/weixin_40461281/article/details/81806921" target="_blank" rel="noopener">https://blog.csdn.net/weixin_40461281/article/details/81806921</a></p>
</blockquote>
<a id="more"></a>
<h2 id="非docker安装RabbitMQ的stomp部署"><a href="#非docker安装RabbitMQ的stomp部署" class="headerlink" title="非docker安装RabbitMQ的stomp部署"></a>非docker安装RabbitMQ的stomp部署</h2><ol>
<li>进入到RabbitMQ安装目录下的sbin文件夹内。</li>
<li>Shift加右键进入命令行</li>
<li>执行命令 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rabbitmq-plugins enable rabbitmq_web_stomp</span><br></pre></td></tr></table></figure>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rabbitmq-plugins enable rabbitmq_web_stomp_examples</span><br></pre></td></tr></table></figure>
<ol start="4">
<li>成功（没有报错消息）后重启RabbitMQ</li>
<li>尝试使用stomp.js进行连接</li>
<li>连接成功<h2 id="docker安装RabbitMQ的stomp部署"><a href="#docker安装RabbitMQ的stomp部署" class="headerlink" title="docker安装RabbitMQ的stomp部署"></a>docker安装RabbitMQ的stomp部署</h2></li>
<li><p>执行docker命令，容器id可以通过docker ps查询</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker exec -it &#123;容器id&#125; /bin/sh -c &quot;[ -e /bin/bash ] &amp;&amp; /bin/bash || /bin/sh&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>进入容器后的命令和上面的非docker一致</p>
</li>
<li>开启后需要修改容器的开放端口。我个人为了省事，直接删除容器后新建容器时就开放了15674、61613。</li>
<li><p>停止容器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker stop &#123;容器名&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>删除容器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker rm &#123;容器名&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <tags>
        <tag>RabbitMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis缓存三大问题</title>
    <url>/2020/06/05/Redis%E7%BC%93%E5%AD%98%E4%B8%89%E5%A4%A7%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="Redis缓存三大问题"><a href="#Redis缓存三大问题" class="headerlink" title="Redis缓存三大问题"></a>Redis缓存三大问题</h1><blockquote>
<p>最近发现微信公众号里的文章和我自己写的文章还是有很大的不同，我的文章有些表达不清。公众号的文章穿插了一些图片和日常语言，还是让阅读的感觉生动有趣一些。<br>本文来源于公众号非科班的科班，我是从猿天地的转载上看到的<a href="https://mp.weixin.qq.com/s/IBlWjCnmLm9o6QXxa2rOPw" target="_blank" rel="noopener">看完这篇Redis缓存三大问题，保你能和面试官互扯</a> 。标题有点夸张，有些格式问题，但内容还是有点东西的。</p>
</blockquote>
<a id="more"></a>
<h2 id="序言"><a href="#序言" class="headerlink" title="序言"></a>序言</h2><p>日常的开发中，无不都是使用数据库来进行数据的存储，由于一般的系统任务中通常不会存在高并发的情况，所以这样看起来并没有什么问题。</p>
<p>一旦涉及大数据量的需求，如一些<strong>商品抢购</strong>的情景，或者<strong>主页访问量</strong>瞬间较大的时候，单一使用数据库来保存数据的系统会因为<strong>面向磁盘，磁盘读/写速度</strong>问题有严重的性能弊端。</p>
<p>在这一瞬间成千上万的请求到来，需要系统在极短的时间内完成成千上万次的读/写操作，这个时候往往不是数据库能够承受的，极其容易造成数据库系统瘫痪，最终导致服务宕机的严重生产问题。</p>
<p>为了克服上述的问题，项目通常会引入 NoSQL 技术，这是一种基于内存的数据库，并且提供一定的持久化功能。</p>
<p>Redis 技术就是 NoSQL 技术中的一种。Redis 缓存的使用，极大的提升了应用程序的性能和效率，特别是数据查询方面。</p>
<p>但同时，它也带来了一些问题。其中，最要害的问题，就是数据的一致性问题，从严格意义上讲，这个问题无解。如果对数据的一致性要求很高，那么就不能使用缓存。</p>
<p>另外的一些典型问题就是，缓存穿透、缓存击穿和缓存雪崩。本篇文章从实际代码操作，来提出解决这三个缓存问题的方案，毕竟 Redis 的缓存问题是实际面试中高频问点，理论和实操要兼得。</p>
<h2 id="缓存穿透"><a href="#缓存穿透" class="headerlink" title="缓存穿透"></a>缓存穿透</h2><p>缓存穿透是指查询一条数据库和缓存都没有的一条数据，就会一直查询数据库，对数据库的访问压力就会增大，缓存穿透的解决方案，有以下两种：</p>
<ol>
<li>缓存空对象：代码维护较简单，但是效果不好。</li>
<li>布隆过滤器：代码维护复杂，效果很好。</li>
</ol>
<h3 id="缓存空对象"><a href="#缓存空对象" class="headerlink" title="缓存空对象"></a>缓存空对象</h3><p>缓存空对象是指当一个请求过来缓存中和数据库中都不存在该请求的数据，第一次请求就会跳过缓存进行数据库的访问，并且访问数据库后返回为空，此时也将该空对象进行缓存。</p>
<p>若是再次进行访问该空对象的时候，就会直接击中缓存，而不是再次数据库，缓存空对象实现的原理图如下：</p>
<p><img src="/2020/06/05/Redis缓存三大问题/b1.png" alt=""></p>
<p>缓存空对象的实现代码如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class UserServiceImpl &#123;</span><br><span class="line">     @Autowired</span><br><span class="line">     UserDAO userDAO;</span><br><span class="line">     @Autowired</span><br><span class="line">     RedisCache redisCache;</span><br><span class="line"></span><br><span class="line">     public User findUser(Integer id) &#123;</span><br><span class="line">          Object object = redisCache.get(Integer.toString(id));</span><br><span class="line">          // 缓存中存在，直接返回</span><br><span class="line">          if(object != null) &#123;</span><br><span class="line">               // 检验该对象是否为缓存空对象，是则直接返回null</span><br><span class="line">               if(object instanceof NullValueResultDO) &#123;</span><br><span class="line">                    return null;</span><br><span class="line">               &#125;</span><br><span class="line">               return (User)object;</span><br><span class="line">          &#125; else &#123;  </span><br><span class="line">               // 缓存中不存在，查询数据库</span><br><span class="line">               User user = userDAO.getUser(id);</span><br><span class="line">               // 存入缓存</span><br><span class="line">               if(user != null) &#123;</span><br><span class="line">                    redisCache.put(Integer.toString(id),user);</span><br><span class="line">               &#125; else &#123;</span><br><span class="line">                    // 将空对象存进缓存</span><br><span class="line">                    redisCache.put(Integer.toString(id), new NullValueResultDO());</span><br><span class="line">               &#125;</span><br><span class="line">               return user;</span><br><span class="line">          &#125;</span><br><span class="line">     &#125;          </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>缓存空对象的实现代码很简单，但是缓存空对象会带来比较大的问题，就是缓存中会存在很多空对象，占用内存的空间，浪费资源，一个解决的办法就是设置空对象的较短的过期时间，代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 在缓存的时候，添加多一个该空对象的过期时间60秒</span><br><span class="line">redisCache.put(Integer.toString(id), new NullValueResultDO(),60);</span><br></pre></td></tr></table></figure>
<h3 id="布隆过滤器"><a href="#布隆过滤器" class="headerlink" title="布隆过滤器"></a>布隆过滤器</h3><p>布隆过滤器是一种基于概率的数据结构，主要用来判断某个元素是否在集合内，它具有运行速度快（时间效率），占用内存小的优点（空间效率），但是有一定的误识别率和删除困难的问题。它只能告诉你某个元素一定不在集合内或可能在集合内。</p>
<p>在计算机科学中有一种思想：空间换时间，时间换空间。一般两者是不可兼得，而布隆过滤器运行效率和空间大小都兼得，它是怎么做到的呢？</p>
<p>在布隆过滤器中引用了一个误判率的概念，即它可能会把不属于这个集合的元素认为可能属于这个集合，但是不会把属于这个集合的认为不属于这个集合，布隆过滤器的特点如下：</p>
<ol>
<li>一个非常大的二进制位数组 （数组里只有0和1）</li>
<li>若干个哈希函数</li>
<li>空间效率和查询效率高</li>
<li>不存在漏报（False Negative）：某个元素在某个集合中，肯定能报出来。</li>
<li>可能存在误报（False Positive）：某个元素不在某个集合中，可能也被爆出来。</li>
<li>不提供删除方法，代码维护困难。</li>
<li>位数组初始化都为0，它不存元素的具体值，当元素经过哈希函数哈希后的值（也就是数组下标）对应的数组位置值改为1。</li>
</ol>
<p>实际布隆过滤器存储数据和查询数据的原理图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b2.png" alt=""></p>
<p>可能很多读者看完上面的特点和原理图，还是看不懂，别急下面通过图解一步一步的讲解布隆过滤器，总而言之一句简单的话概括就是布隆过滤器是一个很大二进制的位数组，数组里面只存0和1。</p>
<p>初始化的布隆过滤器的结构图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b3.png" alt=""><br>以上只是画了布隆过滤器的很小很小的一部分，实际布隆过滤器是非常大的数组（这里的大是指它的长度大，并不是指它所占的内存空间大）。</p>
<p><strong>那么一个数据是怎么存进布隆过滤器的呢？</strong></p>
<p>当一个数据进行存入布隆过滤器的时候，会经过如干个哈希函数进行哈希，得到对应的哈希值作为数组的下标，然后将初始化的位数组对应的下标的值修改为1，结果图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b4.png" alt=""></p>
<p>当再次进行存入第二个值的时候，修改后的结果的原理图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b5.png" alt=""><br>所以每次存入一个数据，就会哈希函数的计算，计算的结果就会作为下标，在布隆过滤器中有多少个哈希函数就会计算出多少个下标，布隆过滤器插入的流程如下：</p>
<ol>
<li>将要添加的元素给m个哈希函数</li>
<li>得到对应于位数组上的m个位置</li>
<li>将这m个位置设为1</li>
</ol>
<p>假设在我们多次存入值后，在布隆过滤器中存在x、y、z这三个值，布隆过滤器的存储结构图如下所示：<br><img src="/2020/06/05/Redis缓存三大问题/b6.png" alt=""></p>
<p>当我们要查询的时候，比如查询a这个数，实际中a这个数是不存在布隆过滤器中的，经过2个哈希函数计算后得到a的哈希值分别为2和13，结构原理图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b7.png" alt=""></p>
<p>经过查询后，发现2和13位置所存储的值都为1，但是2和13的下标分别是x和z经过计算后的下标位置的修改，该布隆过滤器中实际不存在a，那么布隆过滤器就会误判改值可能存在，因为布隆过滤器不存元素值，所以存在误判率。</p>
<p>那么具体布隆过布隆过滤的判断的准确率和一下两个因素有关：</p>
<ol>
<li>布隆过滤器大小：越大，误判率就越小，所以说布隆过滤器一般长度都是非常大的。</li>
<li>哈希函数的个数：哈希函数的个数越多，那么误判率就越小。</li>
</ol>
<p><strong>那么为什么不能删除元素呢？</strong></p>
<p>原因很简单，因为删除元素后，将对应元素的下标设置为零，可能别的元素的下标也引用改下标，这样别的元素的判断就会收到影响，原理图如下：</p>
<p><img src="/2020/06/05/Redis缓存三大问题/b8.png" alt=""></p>
<p>当你删除z元素之后，将对应的下标10和13设置为0，这样导致x和y元素的下标受到影响，导致数据的判断不准确，所以直接不提供删除元素的api。</p>
<p>以上说的都是布隆过滤器的原理，只有理解了原理，在实际的运用才能如鱼得水，下面就来实操代码，手写一个简单的布隆过滤器。</p>
<p>对于要手写一个布隆过滤器，首先要明确布隆过滤器的核心：</p>
<ul>
<li>若干哈希函数</li>
<li>存值的Api</li>
<li>判断值得Api</li>
</ul>
<p>实现的代码如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class MyBloomFilter &#123;</span><br><span class="line">    // 布隆过滤器长度</span><br><span class="line">    private static final int SIZE = 2 &lt;&lt; 10;</span><br><span class="line">    // 模拟实现不同的哈希函数</span><br><span class="line">    private static final int[] num= new int[] &#123;5, 19, 23, 31,47, 71&#125;;   </span><br><span class="line">    // 初始化位数组</span><br><span class="line">    private BitSet bits = new BitSet(SIZE);</span><br><span class="line">    // 用于存储哈希函数</span><br><span class="line">    private MyHash[] function = new MyHash[num.length];</span><br><span class="line"></span><br><span class="line">    // 初始化哈希函数</span><br><span class="line">    public MyBloomFilter() &#123;</span><br><span class="line">        for (int i = 0; i &lt; num.length; i++) &#123;</span><br><span class="line">            function [i] = new MyHash(SIZE, num[i]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 存值Api </span><br><span class="line">    public void add(String value) &#123;</span><br><span class="line">        // 对存入得值进行哈希计算</span><br><span class="line">        for (MyHash f: function) &#123;</span><br><span class="line">            // 将为数组对应的哈希下标得位置得值改为1</span><br><span class="line">            bits.set(f.hash(value), true);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 判断是否存在该值得Api</span><br><span class="line">    public boolean contains(String value) &#123;</span><br><span class="line">        if (value == null) &#123;</span><br><span class="line">            return false;</span><br><span class="line">        &#125;</span><br><span class="line">        boolean result= true;</span><br><span class="line">        for (MyHash f : func) &#123;</span><br><span class="line">            result= result&amp;&amp; bits.get(f.hash(value));</span><br><span class="line">        &#125;</span><br><span class="line">        return result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>哈希函数代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static class MyHash &#123;</span><br><span class="line">        private int cap;</span><br><span class="line">        private int seed;</span><br><span class="line">        // 初始化数据</span><br><span class="line">        public MyHash(int cap, int seed) &#123;</span><br><span class="line">            this.cap = cap;</span><br><span class="line">            this.seed = seed;</span><br><span class="line">        &#125;</span><br><span class="line">        // 哈希函数</span><br><span class="line">        public int hash(String value) &#123;</span><br><span class="line">            int result = 0;</span><br><span class="line">            int len = value.length();</span><br><span class="line">            for (int i = 0; i &lt; len; i++) &#123;</span><br><span class="line">                result = seed * result + value.charAt(i);</span><br><span class="line">            &#125;</span><br><span class="line">            return (cap - 1) &amp; result;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>布隆过滤器测试代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static void test &#123;</span><br><span class="line">       String value = &quot;4243212355312&quot;;</span><br><span class="line">       MyBloomFilter filter = new MyBloomFilter();</span><br><span class="line">       System.out.println(filter.contains(value));</span><br><span class="line">       filter.add(value);</span><br><span class="line">       System.out.println(filter.contains(value));</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>以上就是手写了一个非常简单的布隆过滤器，但是实际项目中可能由牛人或者大公司已经帮你写好的，如谷歌的Google Guava，只需要在项目中引入一下依赖：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;com.google.guava&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;guava&lt;/artifactId&gt;</span><br><span class="line">    &lt;version&gt;27.0.1-jre&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>
<p>实际项目中具体的操作代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static void MyBloomFilterSysConfig &#123;</span><br><span class="line"></span><br><span class="line">     @Autowired</span><br><span class="line">     OrderMapper orderMapper</span><br><span class="line"></span><br><span class="line">    // 1.创建布隆过滤器  第二个参数为预期数据量10000000，第三个参数为错误率0.00001</span><br><span class="line">    BloomFilter&lt;CharSequence&gt; bloomFilter =  BloomFilter.create(Funnels.stringFunnel(Charset.forName(&quot;utf-8&quot;)),10000000, 0.00001);</span><br><span class="line">    // 2.获取所有的订单，并将订单的id放进布隆过滤器里面</span><br><span class="line">    List&lt;Order&gt; orderList = orderMapper.findAll()</span><br><span class="line">    for (Order order;orderList ) &#123;</span><br><span class="line">        Long id = order.getId();</span><br><span class="line">        bloomFilter.put(&quot;&quot; + id);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在实际项目中会启动一个系统任务或者定时任务，来初始化布隆过滤器，将热点查询数据的id放进布隆过滤器里面，当用户再次请求的时候，使用布隆过滤器进行判断，改订单的id是否在布隆过滤器中存在，不存在直接返回null，具体操作代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 判断订单id是否在布隆过滤器中存在</span><br><span class="line">bloomFilter.mightContain(&quot;&quot; + id)</span><br></pre></td></tr></table></figure>
<p>布隆过滤器的缺点就是要维持容器中的数据，因为订单数据肯定是频繁变化的，实时的要更新布隆过滤器中的数据为最新。</p>
<h2 id="缓存击穿"><a href="#缓存击穿" class="headerlink" title="缓存击穿"></a>缓存击穿</h2><p>缓存击穿是指一个key非常热点，在不停的扛着大并发，大并发集中对这一个点进行访问，当这个key在失效的瞬间，持续的大并发就穿破缓存，直接请求数据库，瞬间对数据库的访问压力增大。</p>
<p>缓存击穿这里强调的是并发，造成缓存击穿的原因有以下两个：</p>
<ol>
<li>该数据没有人查询过 ，第一次就大并发的访问。（冷门数据）</li>
<li>添加到了缓存，reids有设置数据失效的时间 ，这条数据刚好失效，大并发访问（热点数据）</li>
</ol>
<p>对于缓存击穿的解决方案就是加锁，具体实现的原理图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b9.png" alt=""><br>当用户出现大并发访问的时候，在查询缓存的时候和查询数据库的过程加锁，只能第一个进来的请求进行执行，当第一个请求把该数据放进缓存中，接下来的访问就会直接集中缓存，防止了缓存击穿。</p>
<p>业界比价普遍的一种做法，即根据 key 获取 value 值为空时，锁上，从数据库中 load 数据后再释放锁。若其它线程获取锁失败，则等待一段时间后重试。这里要注意，分布式环境中要使用分布式锁，单机的话用普通的锁（synchronized、Lock）就够了。</p>
<p>下面以一个获取商品库存的案例进行代码的演示，单机版的锁实现具体实现的代码如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 获取库存数量</span><br><span class="line">public String getProduceNum(String key) &#123;</span><br><span class="line">    try &#123;</span><br><span class="line">        synchronized (this) &#123;   //加锁</span><br><span class="line">            // 缓存中取数据，并存入缓存中</span><br><span class="line">            int num= Integer.parseInt(redisTemplate.opsForValue().get(key));</span><br><span class="line"></span><br><span class="line">            if (num&gt; 0) &#123;</span><br><span class="line">                //没查一次库存-1</span><br><span class="line">                redisTemplate.opsForValue().set(key, (num- 1) + &quot;&quot;);</span><br><span class="line">                System.out.println(&quot;剩余的库存为num：&quot; + (num- 1));</span><br><span class="line">            &#125; else &#123;</span><br><span class="line">                System.out.println(&quot;库存为0&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; catch (NumberFormatException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; finally &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    return &quot;OK&quot;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>分布式的锁实现具体实现的代码如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public String getProduceNum(String key) &#123;</span><br><span class="line">    // 获取分布式锁</span><br><span class="line">    RLock lock = redissonClient.getLock(key);</span><br><span class="line">    try &#123;</span><br><span class="line">        // 获取库存数</span><br><span class="line">        int num= Integer.parseInt(redisTemplate.opsForValue().get(key));  </span><br><span class="line">        // 上锁           </span><br><span class="line">        lock.lock();</span><br><span class="line">        if (num&gt; 0) &#123;</span><br><span class="line">            //减少库存，并存入缓存中</span><br><span class="line">            redisTemplate.opsForValue().set(key, (num - 1) + &quot;&quot;);</span><br><span class="line">            System.out.println(&quot;剩余库存为num：&quot; + (num- 1));</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            System.out.println(&quot;库存已经为0&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; catch (NumberFormatException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; finally &#123;</span><br><span class="line">        //解锁</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">    return &quot;OK&quot;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="缓存雪崩"><a href="#缓存雪崩" class="headerlink" title="缓存雪崩"></a>缓存雪崩</h2><p>缓存雪崩 是指在某一个时间段，缓存集中过期失效。此刻无数的请求直接绕开缓存，直接请求数据库。</p>
<p>造成缓存雪崩的原因，有以下两种：</p>
<ol>
<li>reids宕机</li>
<li>大部分数据失效</li>
</ol>
<p>比如天猫双11，马上就要到双11零点，很快就会迎来一波抢购，这波商品在23点集中的放入了缓存，假设缓存一个小时，那么到了凌晨24点的时候，这批商品的缓存就都过期了。</p>
<p>而对这批商品的访问查询，都落到了数据库上，对于数据库而言，就会产生周期性的压力波峰，对数据库造成压力，甚至压垮数据库。</p>
<p>缓存雪崩的原理图如下，当正常的情况下，key没有大量失效的用户访问原理图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b10.png" alt=""></p>
<p>当某一时间点，key大量失效，造成的缓存雪崩的原理图如下：<br><img src="/2020/06/05/Redis缓存三大问题/b10.png" alt=""></p>
<p>对于缓存雪崩的解决方案有以下两种：</p>
<ol>
<li>搭建高可用的集群，防止单机的redis宕机。</li>
<li>设置不同的过期时间，防止同一时间内大量的key失效。</li>
</ol>
]]></content>
      <tags>
        <tag>redis</tag>
      </tags>
  </entry>
  <entry>
    <title>side location conflict错误解决</title>
    <url>/2020/10/03/SideLocationConflict%E9%94%99%E8%AF%AF%E8%A7%A3%E5%86%B3/</url>
    <content><![CDATA[<h1 id="side-location-conflict错误解决"><a href="#side-location-conflict错误解决" class="headerlink" title="side location conflict错误解决"></a>side location conflict错误解决</h1><blockquote>
<p>在进行两个面集合之间的关系比较时出现的问题<br>有关博文 <a href="http://locationtech.github.io/jts/javadoc/org/locationtech/jts/geom/Geometry.html" target="_blank" rel="noopener">http://locationtech.github.io/jts/javadoc/org/locationtech/jts/geom/Geometry.html</a> jar 包的api 文档。和 <a href="https://github.com/nettopologysuite/nettopologysuite/issues/129" target="_blank" rel="noopener">https://github.com/nettopologysuite/nettopologysuite/issues/129</a> 的一句话。<br><a id="more"></a></p>
</blockquote>
<h2 id="错误"><a href="#错误" class="headerlink" title="错误"></a>错误</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">org.locationtech.jts.geom.TopologyException: side location conflict [ (112.094482, 25.851795, NaN) ]</span><br></pre></td></tr></table></figure>
<p>在判断从 geojson 格式转成的 org.locationtech.jts.geom.Polygon 面之间的关系时发生</p>
<h2 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h2><p>首先贴一下我的 geojson 转 Polygon 面的代码<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private List&lt;Polygon&gt; jsonToGeometry(String jsonFilePath) throws IOException &#123;</span><br><span class="line">        List&lt;Polygon&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">        try (InputStream is = GeoTools.class.getResourceAsStream(jsonFilePath)) &#123;</span><br><span class="line">            JSONObject city = JSON.parseObject(IoUtil.read(is, StandardCharsets.UTF_8));</span><br><span class="line">            list = city.getJSONArray(&quot;features&quot;).stream()</span><br><span class="line">                    .map(obj -&gt; &#123;</span><br><span class="line">                        JSONObject json = (JSONObject) obj;</span><br><span class="line">                        Polygon polygon = null;</span><br><span class="line">                        polygon = (Polygon) new GeoJSONReader().read(json.getJSONObject(&quot;geometry&quot;).toJSONString());</span><br><span class="line">                        polygon.setUserData(json.getJSONObject(&quot;properties&quot;));</span><br><span class="line">                        return polygon;</span><br><span class="line">                    &#125;)</span><br><span class="line">                    .collect(Collectors.toList());</span><br><span class="line">        &#125; catch (IOException e) &#123;</span><br><span class="line">            throw new HandleException(e);</span><br><span class="line">        &#125;</span><br><span class="line">        return list;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>jsonFilePath 参数就是一个 geojson 的文件。<br>所需引用 jar 包为<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">implementation group: &apos;org.locationtech.jts&apos;, name: &apos;jts-core&apos;, version: &apos;1.16.1&apos;</span><br></pre></td></tr></table></figure></p>
<p>我所使用的对比面与面之间关系的方法是<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line">     * 把县级所属城市标记出来</span><br><span class="line">     * @param polygons 县</span><br><span class="line">     * @param citypolygons 市</span><br><span class="line">     */</span><br><span class="line">    private List&lt;Polygon&gt; belongingCity(List&lt;Polygon&gt; polygons, List&lt;Polygon&gt; citypolygons) &#123;</span><br><span class="line">        polygons = polygons.stream().map(polygon -&gt; &#123;</span><br><span class="line">            List&lt;String&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">            citypolygons.stream().forEach(citypolygo -&gt; &#123;</span><br><span class="line">                try &#123;</span><br><span class="line">                    Polygon union = polygon.isValid() ? polygon : (Polygon)polygon.buffer(0);</span><br><span class="line">                    Polygon cityunion = citypolygo.isValid() ? citypolygo : (Polygon)citypolygo.buffer(0);</span><br><span class="line">                    if (cityunion.contains(union) || cityunion.overlaps(union)) &#123;</span><br><span class="line">                        String name = ((JSONObject)polygon.getUserData()).getString(&quot;name&quot;);</span><br><span class="line">                        String cityName = ((JSONObject)citypolygo.getUserData()).getString(&quot;name&quot;);</span><br><span class="line">                            name = cityName + name;</span><br><span class="line">                        ((JSONObject)polygon.getUserData()).put(&quot;name&quot;, name);</span><br><span class="line">                        list.add(name);</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                &#125; catch (TopologyException e) &#123;</span><br><span class="line">                   log.info(e.getMessage(), e);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">            return polygon;</span><br><span class="line">        &#125;).collect(Collectors.toList());</span><br><span class="line">        return polygons;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>这个判断面与面之间的关系这里还是有一点问题，将一个县分到了两个市所属下。<br>其中解决这次报错的方法是<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">polygon.isValid() ? polygon : (Polygon)polygon.buffer(0)</span><br></pre></td></tr></table></figure></p>
<p><code>isValid</code>方法是判断 Geometry 根据OGC SFS规范测试这在拓扑上是否有效。<br>然后再用<code>buffer</code>方法拿出一个面。其中有一些要注意的点，在原文回答中有写：</p>
<blockquote>
<p>There are two possible reasons that you are getting this exception:<br>Most likely you have invalid geometries to start with. You can test geometries for validity by either checking feature.Geometry.IsValid property or by using the NetTopologySuite.Operation.Valid.IsValidOp class. The later gives you the location where the validation test failed.<br>If this is the case you can attempt to fix the geometry by assigning feature.Geometry = feature.Geometry.Buffer(0);. While this works for most of the cases, it is not guaranteed to do so. An example where this might not work is a “bow-tie: or “figure-8” polygon, with one very small lobe and one large one. Depending on the orientations of the lobes, the Buffer(0) operation may keep the small lobe and discard the “valid” large lobe.<br>It can also happen during operations on geometries due to some subtle kinds of cross-geometry<br>topology, such as two line segments being almost but not quite coincident. This can be avoided by using the NetTopologySuite.Precision.GeometryPrecisionReducer.<br>I would need to see the geometries to tell more.</p>
</blockquote>
<p>要注意自相交面处理后的截取的面是否为小的那个面。<br>成功解决问题</p>
]]></content>
      <tags>
        <tag>jts</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot中使用Swagger2构建 API文档</title>
    <url>/2019/03/21/Spring-Boot%E4%B8%AD%E4%BD%BF%E7%94%A8Swagger2%E6%9E%84%E5%BB%BARESTful-API%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[<h1 id="Spring-Boot中使用Swagger2构建API文档"><a href="#Spring-Boot中使用Swagger2构建API文档" class="headerlink" title="Spring Boot中使用Swagger2构建API文档"></a>Spring Boot中使用Swagger2构建API文档</h1><blockquote>
<p>引用<a href="https://blog.csdn.net/liyanlei5858/article/details/80326995" target="_blank" rel="noopener">https://blog.csdn.net/liyanlei5858/article/details/80326995</a>内容和<a href="http://blog.didispace.com/springbootswagger2/" target="_blank" rel="noopener">http://blog.didispace.com/springbootswagger2/</a>部分内容,还有部分自我使用得出的经验。如有不对，还请多多指教。</p>
</blockquote>
<a id="more"></a>
<h2 id="Swagger2的好处"><a href="#Swagger2的好处" class="headerlink" title="Swagger2的好处"></a>Swagger2的好处</h2><p>Swagger2，它可以轻松的整合到Spring Boot中，并与Spring MVC程序配合组织出强大API文档。它既可以减少我们创建文档的工作量，同时说明内容又整合入实现代码中，让维护文档和修改代码整合为一体，可以让我们在修改代码逻辑的同时方便的修改文档说明。另外Swagger2也提供了强大的页面测试功能来调试每个API。具体效果如下图所示：</p>
<p><img src="/2019/03/21/Spring-Boot中使用Swagger2构建RESTful-API文档/b1.png" alt=""></p>
<p>将其集成到spring boot中时也很方便，只需要如下几步：</p>
<h3 id="1-jar包导入"><a href="#1-jar包导入" class="headerlink" title="1. jar包导入"></a>1. jar包导入</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;io.springfox&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;springfox-swagger2&lt;/artifactId&gt;</span><br><span class="line">    &lt;version&gt;2.9.2&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;io.springfox&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;springfox-swagger-ui&lt;/artifactId&gt;</span><br><span class="line">    &lt;version&gt;2.9.2&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>
<h3 id="2-添加配置类"><a href="#2-添加配置类" class="headerlink" title="2.添加配置类"></a>2.添加配置类</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import org.springframework.context.annotation.Bean;</span><br><span class="line">import org.springframework.context.annotation.Configuration;</span><br><span class="line">import org.springframework.context.annotation.Profile;</span><br><span class="line">import springfox.documentation.builders.ApiInfoBuilder;</span><br><span class="line">import springfox.documentation.builders.PathSelectors;</span><br><span class="line">import springfox.documentation.builders.RequestHandlerSelectors;</span><br><span class="line">import springfox.documentation.service.ApiInfo;</span><br><span class="line">import springfox.documentation.spi.DocumentationType;</span><br><span class="line">import springfox.documentation.spring.web.plugins.Docket;</span><br><span class="line">import springfox.documentation.swagger2.annotations.EnableSwagger2;</span><br><span class="line"></span><br><span class="line">@Configuration</span><br><span class="line">@EnableSwagger2</span><br><span class="line">public class SwaggerConfig &#123;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public Docket api() &#123;</span><br><span class="line">        return new Docket(DocumentationType.SWAGGER_2)</span><br><span class="line">                .apiInfo(apiInfo())</span><br><span class="line">                .select()</span><br><span class="line">                // 自行修改为自己的包路径</span><br><span class="line">                .apis(RequestHandlerSelectors.basePackage(&quot;com.yiring.meeting&quot;))</span><br><span class="line">                .paths(PathSelectors.any())</span><br><span class="line">                .build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private ApiInfo apiInfo() &#123;</span><br><span class="line">        return new ApiInfoBuilder()</span><br><span class="line">                .title(&quot;Swagger UI Description.&quot;)</span><br><span class="line">                .description(&quot;Swagger UI Description.&quot;)</span><br><span class="line">                .version(&quot;1.0&quot;)</span><br><span class="line">                .build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如上代码所示，通过@Configuration注解，让Spring来加载该类配置。再通过@EnableSwagger2注解来启用Swagger2。</p>
<p>再通过createRestApi函数创建Docket的Bean之后，apiInfo()用来创建该Api的基本信息（这些基本信息会展现在文档页面中）。select()函数返回一个ApiSelectorBuilder实例用来控制哪些接口暴露给Swagger来展现，本例采用指定扫描的包路径来定义，Swagger会扫描该包下所有Controller定义的API，并产生文档内容（除了被@ApiIgnore指定的请求）。</p>
<h3 id="3-添加文档内容"><a href="#3-添加文档内容" class="headerlink" title="3.添加文档内容"></a>3.添加文档内容</h3><p>在完成了上述配置后，其实已经可以生产文档内容，但是这样的文档主要针对请求本身，而描述主要来源于函数等命名产生，对用户并不友好，我们通常需要自己增加一些说明来丰富文档内容。如下所示，我们通过@ApiOperation注解来给API增加说明、通过@ApiImplicitParams、@ApiImplicitParam注解来给参数增加说明。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@RestController</span><br><span class="line">@RequestMapping(value=&quot;/users&quot;)     // 通过这里配置使下面的映射都在/users下，可去除</span><br><span class="line">public class UserController &#123;</span><br><span class="line"></span><br><span class="line">    static Map&lt;Long, User&gt; users = Collections.synchronizedMap(new HashMap&lt;Long, User&gt;());</span><br><span class="line"></span><br><span class="line">    @ApiOperation(value=&quot;获取用户列表&quot;, notes=&quot;&quot;)</span><br><span class="line">    @RequestMapping(value=&#123;&quot;&quot;&#125;, method=RequestMethod.GET)</span><br><span class="line">    public List&lt;User&gt; getUserList() &#123;</span><br><span class="line">        List&lt;User&gt; r = new ArrayList&lt;User&gt;(users.values());</span><br><span class="line">        return r;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ApiOperation(value=&quot;创建用户&quot;, notes=&quot;根据User对象创建用户&quot;)</span><br><span class="line">    @ApiImplicitParam(name = &quot;user&quot;, value = &quot;用户详细实体user&quot;, required = true, dataType = &quot;User&quot;)</span><br><span class="line">    @RequestMapping(value=&quot;&quot;, method=RequestMethod.POST)</span><br><span class="line">    public String postUser(@RequestBody User user) &#123;</span><br><span class="line">        users.put(user.getId(), user);</span><br><span class="line">        return &quot;success&quot;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ApiOperation(value=&quot;获取用户详细信息&quot;, notes=&quot;根据url的id来获取用户详细信息&quot;)</span><br><span class="line">    @ApiImplicitParam(name = &quot;id&quot;, value = &quot;用户ID&quot;, required = true, dataType = &quot;Long&quot;)</span><br><span class="line">    @RequestMapping(value=&quot;/&#123;id&#125;&quot;, method=RequestMethod.GET)</span><br><span class="line">    public User getUser(@PathVariable Long id) &#123;</span><br><span class="line">        return users.get(id);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ApiOperation(value=&quot;更新用户详细信息&quot;, notes=&quot;根据url的id来指定更新对象，并根据传过来的user信息来更新用户详细信息&quot;)</span><br><span class="line">    @ApiImplicitParams(&#123;</span><br><span class="line">            @ApiImplicitParam(name = &quot;id&quot;, value = &quot;用户ID&quot;, required = true, dataType = &quot;Long&quot;),</span><br><span class="line">            @ApiImplicitParam(name = &quot;user&quot;, value = &quot;用户详细实体user&quot;, required = true, dataType = &quot;User&quot;)</span><br><span class="line">    &#125;)</span><br><span class="line">    @RequestMapping(value=&quot;/&#123;id&#125;&quot;, method=RequestMethod.PUT)</span><br><span class="line">    public String putUser(@PathVariable Long id, @RequestBody User user) &#123;</span><br><span class="line">        User u = users.get(id);</span><br><span class="line">        u.setName(user.getName());</span><br><span class="line">        u.setAge(user.getAge());</span><br><span class="line">        users.put(id, u);</span><br><span class="line">        return &quot;success&quot;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ApiOperation(value=&quot;删除用户&quot;, notes=&quot;根据url的id来指定删除对象&quot;)</span><br><span class="line">    @ApiImplicitParam(name = &quot;id&quot;, value = &quot;用户ID&quot;, required = true, dataType = &quot;Long&quot;)</span><br><span class="line">    @RequestMapping(value=&quot;/&#123;id&#125;&quot;, method=RequestMethod.DELETE)</span><br><span class="line">    public String deleteUser(@PathVariable Long id) &#123;</span><br><span class="line">        users.remove(id);</span><br><span class="line">        return &quot;success&quot;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>@ApiOperation() </strong>用于方法；表示一个http请求的操作<br>value用于方法描述<br>notes用于提示内容<br>tags可以重新分组（视情况而用） </p>
<p><strong>@ApiParam() </strong>用于方法，参数，字段说明；表示对参数的添加元数据（说明或是否必填等）<br>name–参数名<br>value–参数说明<br>required–是否必填</p>
<p><strong>@ApiModel()</strong>用于类 ；表示对类进行说明，用于参数用实体类接收<br>value–表示对象名<br>description–描述<br>都可省略 </p>
<p><strong>@ApiModelProperty()</strong>用于方法，字段； 表示对model属性的说明或者数据操作更改<br>value–字段说明<br>name–重写属性名字<br>dataType–重写属性类型<br>required–是否必填<br>example–举例说明<br>hidden–隐藏</p>
<p><strong>@ApiIgnore()</strong>用于类或者方法上，可以不被swagger显示在页面上<br>更多的使用方式就不再介绍了</p>
<p>完成上述代码添加上，启动Spring Boot程序，访问：<a href="">http://localhost:8080/swagger-ui.html</a><br>。就能看到前文所展示的API的页面。我们可以再点开具体的API请求，以POST类型的/users请求为例，可找到上述代码中我们配置的Notes信息以及参数user的描述信息，如下图所示。</p>
<p><img src="/2019/03/21/Spring-Boot中使用Swagger2构建RESTful-API文档/b2.png" alt=""></p>
<h3 id="4-API文档访问与调试"><a href="#4-API文档访问与调试" class="headerlink" title="4.API文档访问与调试"></a>4.API文档访问与调试</h3><p>在上图请求的页面中，我们看到user的Value是个输入框？是的，Swagger除了查看接口功能外，还提供了调试测试功能，我们可以点击上图中右侧的Model Schema（黄色区域：它指明了User的数据结构），此时Value中就有了user对象的模板，我们只需要稍适修改，点击下方“Try it out！”按钮，即可完成了一次请求调用！</p>
<blockquote>
<p><strong>注意事项：</strong> 如果你的项目中使用了拦截器，请将swagger资源放行     </p>
</blockquote>
]]></content>
      <tags>
        <tag>Spring boot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot+Mybatis+Swagger2环境搭建</title>
    <url>/2019/11/23/SpringBoot-Mybatis-Swagger2%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h1 id="SpringBoot-Mybatis-Swagger2环境搭建"><a href="#SpringBoot-Mybatis-Swagger2环境搭建" class="headerlink" title="SpringBoot+Mybatis+Swagger2环境搭建"></a>SpringBoot+Mybatis+Swagger2环境搭建</h1><blockquote>
<p>这篇文章是我从公众号上找到的。现在大多数的公众号都喜欢互相转载文章，这篇也是我关注的人从别的哪里转载过来了。很喜欢这个文章的排版和内容，就在自己这里保存一下。<br>作者：calebman<br><a href="https://www.jianshu.com/p/95946d6b0c7d" target="_blank" rel="noopener">https://www.jianshu.com/p/95946d6b0c7d</a></p>
</blockquote>
<a id="more"></a>
<h2 id="本文简介"><a href="#本文简介" class="headerlink" title="本文简介"></a>本文简介</h2><ul>
<li>为什么使用SpringBoot</li>
<li>搭建怎样一个环境</li>
<li>开发环境</li>
<li>导入快速启动项目</li>
<li>集成前准备</li>
<li>集成Mybatis</li>
<li>集成Swagger2</li>
<li>多环境配置</li>
<li>多环境下的日志配置</li>
<li>常用配置</li>
</ul>
<h2 id="为什么使用SpringBoot"><a href="#为什么使用SpringBoot" class="headerlink" title="为什么使用SpringBoot"></a>为什么使用SpringBoot</h2><p>SpringBoot相对于传统的SSM框架的优点是提供了默认的样板化配置，简化了Spring应用的初始搭建过程，如果你不想被众多的xml配置文件困扰，可以考虑使用SpringBoot替代</p>
<h2 id="搭建怎样一个环境"><a href="#搭建怎样一个环境" class="headerlink" title="搭建怎样一个环境"></a>搭建怎样一个环境</h2><p>本文将基于Spring官方提供的快速启动项目模板集成Mybatis、Swagger2框架，并讲解mybatis generator一键生成代码插件、logback、一键生成文档以及多环境的配置方法，最后再介绍一下自定义配置的注解获取、全局异常处理等经常用到的东西。</p>
<h2 id="开发环境"><a href="#开发环境" class="headerlink" title="开发环境"></a>开发环境</h2><p>本人使用IDEA作为开发工具，IDEA下载时默认集成了SpringBoot的快速启动项目可以直接创建，如果使用Eclipse的同学可以考虑安装SpringBoot插件或者直接从这里配置并下载SpringBoot快速启动项目，需要注意的是本次环境搭建选择的是SpringBoot2.0的快速启动框架，SpringBoot2.0要求jdk版本必须要在1.8及以上。</p>
<blockquote>
<p><a href="https://start.spring.io/" target="_blank" rel="noopener">https://start.spring.io/</a></p>
</blockquote>
<h2 id="导入快速启动项目"><a href="#导入快速启动项目" class="headerlink" title="导入快速启动项目"></a>导入快速启动项目</h2><p>不管是由IDEA导入还是现实下载模板工程都需要初始化快速启动工程的配置，如果使用IDEA，在新建项目时选择Spring Initializr，主要配置如下图<br><img src="/2019/11/23/SpringBoot-Mybatis-Swagger2环境搭建/b1.png" alt="IDEA新建SpringBoot项目-填写项目/包名"></p>
<p><img src="/2019/11/23/SpringBoot-Mybatis-Swagger2环境搭建/b2.png" alt="IDEA新建SpringBoot项目-选择依赖包"></p>
<p>点击next之后finish之后IDEA显示正在下载模板工程，下载完成后会根据pom.xml下载包依赖，依赖下载完毕后模板项目就算创建成功了，如果是直接从官方网站配置下载快速启动项目可参考下图配置<br><img src="/2019/11/23/SpringBoot-Mybatis-Swagger2环境搭建/b3.png" alt="直接下载SpringBoot快速启动项目-项目配置"></p>
<p>从Search for dependencies 框中输入并选择Web、Mysql、Mybatis加入依赖，点击Generate Project下载快速启动项目，然后在IDE中选择导入Maven项目，项目导入完成后可见其目录结构如下图</p>
<p><img src="/2019/11/23/SpringBoot-Mybatis-Swagger2环境搭建/b4.png" alt="快速启动项目-项目结构"></p>
<p>需要关注红色方框圈起来的部分，由上往下第一个java类是用来启动项目的入口函数，第二个properties后缀的文件是项目的配置文件，第三个是项目的依赖包以及执行插件的配置</p>
<h2 id="集成前准备"><a href="#集成前准备" class="headerlink" title="集成前准备"></a>集成前准备</h2><h3 id="修改-properties为-yml"><a href="#修改-properties为-yml" class="headerlink" title="修改.properties为.yml"></a>修改.properties为.yml</h3><p>yml相对于properties更加精简而且很多官方给出的Demo都是yml的配置形式，在这里我们采用yml的形式代替properties，相对于properties形式主要有以下两点不同</p>
<ul>
<li>对于键的描述由原有的 “.” 分割变成了树的形状</li>
<li>对于所有的键的后面一个要跟一个空格，不然启动项目会报配置解析错误</li>
</ul>
<blockquote>
<p>这也是我现在的项目中使用的配置文件的方式，树状结构从显示上来说比properties的扁平化结构清晰的多。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># properties式语法描述</span><br><span class="line">spring.datasource.name = mysql</span><br><span class="line">spring.datasource.url = jdbc:mysql://localhost:3306/db?characterEncoding=utf-8</span><br><span class="line">spring.datasource.username = root</span><br><span class="line">spring.datasource.password = 123</span><br><span class="line"># yml式语法描述</span><br><span class="line">spring:</span><br><span class="line">    datasource:</span><br><span class="line">        name: mysql</span><br><span class="line">        url: jdbc:mysql://localhost:3306/db?characterEncoding=utf-8</span><br><span class="line">        username: root</span><br><span class="line">        password: 123</span><br></pre></td></tr></table></figure>
<h3 id="配置所需依赖"><a href="#配置所需依赖" class="headerlink" title="配置所需依赖"></a>配置所需依赖</h3><p>快速启动项目创建成功后我们观察其pom.xml文件中的依赖如下图，包含了我们选择的Web、Mybatis以及Mysql<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!-- spring web mvc --&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br><span class="line">&lt;!-- mybatis --&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;org.mybatis.spring.boot&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;mybatis-spring-boot-starter&lt;/artifactId&gt;</span><br><span class="line">    &lt;version&gt;1.3.1&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br><span class="line">&lt;!-- mysql --&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;mysql&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;</span><br><span class="line">    &lt;scope&gt;runtime&lt;/scope&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br><span class="line">&lt;!-- test --&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;spring-boot-starter-test&lt;/artifactId&gt;</span><br><span class="line">    &lt;scope&gt;test&lt;/scope&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure></p>
<p>但是我们使用ORM框架一般还会配合数据库连接池以及分页插件来使用，在这里我选择了阿里的druid以及pagehelper这个分页插件，再加上我们还需要整合swagger2文档自动化构建框架，所以增加了以下四个依赖项</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!-- 分页插件 --&gt;</span><br><span class="line">        &lt;dependency&gt;</span><br><span class="line">            &lt;groupId&gt;com.github.pagehelper&lt;/groupId&gt;</span><br><span class="line">            &lt;artifactId&gt;pagehelper-spring-boot-starter&lt;/artifactId&gt;</span><br><span class="line">            &lt;version&gt;1.2.3&lt;/version&gt;</span><br><span class="line">        &lt;/dependency&gt;</span><br><span class="line">        &lt;!-- alibaba的druid数据库连接池 --&gt;</span><br><span class="line">        &lt;dependency&gt;</span><br><span class="line">            &lt;groupId&gt;com.alibaba&lt;/groupId&gt;</span><br><span class="line">            &lt;artifactId&gt;druid-spring-boot-starter&lt;/artifactId&gt;</span><br><span class="line">            &lt;version&gt;1.1.1&lt;/version&gt;</span><br><span class="line">        &lt;/dependency&gt;</span><br><span class="line">        &lt;!-- alibaba的json格式化对象 --&gt;</span><br><span class="line">        &lt;dependency&gt;</span><br><span class="line">            &lt;groupId&gt;com.alibaba&lt;/groupId&gt;</span><br><span class="line">            &lt;artifactId&gt;fastjson&lt;/artifactId&gt;</span><br><span class="line">            &lt;version&gt;1.2.31&lt;/version&gt;</span><br><span class="line">        &lt;/dependency&gt;</span><br><span class="line">        &lt;!-- 自动生成API文档 --&gt;</span><br><span class="line">        &lt;dependency&gt;</span><br><span class="line">            &lt;groupId&gt;io.springfox&lt;/groupId&gt;</span><br><span class="line">            &lt;artifactId&gt;springfox-swagger2&lt;/artifactId&gt;</span><br><span class="line">            &lt;version&gt;2.5.0&lt;/version&gt;</span><br><span class="line">        &lt;/dependency&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>这里我现在使用的是gradle管理jar包。感觉比maven减少了行数。清晰一些。</p>
</blockquote>
<h3 id="集成Mybatis"><a href="#集成Mybatis" class="headerlink" title="集成Mybatis"></a>集成Mybatis</h3><p>Mybatis的配置主要包括了druid数据库连接池、pagehelper分页插件、mybatis-generator代码逆向生成插件以及mapper、pojo扫描配置</p>
<h3 id="配置druid数据库连接池"><a href="#配置druid数据库连接池" class="headerlink" title="配置druid数据库连接池"></a>配置druid数据库连接池</h3><p>添加以下配置至application.yml文件中<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">spring:</span><br><span class="line">    datasource:</span><br><span class="line">        # 如果存在多个数据源，监控的时候可以通过名字来区分开来</span><br><span class="line">        name: mysql</span><br><span class="line">        # 连接数据库的url</span><br><span class="line">        url: jdbc:mysql://localhost:3306/db?characterEncoding=utf-8</span><br><span class="line">        # 连接数据库的账号</span><br><span class="line">        username: root</span><br><span class="line">        #  连接数据库的密码</span><br><span class="line">        password: 123</span><br><span class="line">        # 使用druid数据源</span><br><span class="line">        type: com.alibaba.druid.pool.DruidDataSource</span><br><span class="line">        # 扩展插件</span><br><span class="line">        # 监控统计用的filter:stat 日志用的filter:log4j 防御sql注入的filter:wall</span><br><span class="line">        filters: stat</span><br><span class="line">        # 最大连接池数量</span><br><span class="line">        maxActive: 20</span><br><span class="line">        # 初始化时建立物理连接的个数。初始化发生在显示调用init方法，或者第一次getConnection时</span><br><span class="line">        initialSize: 1</span><br><span class="line">        # 获取连接时最大等待时间，单位毫秒</span><br><span class="line">        maxWait: 60000</span><br><span class="line">        # 最小连接池数量</span><br><span class="line">        minIdle: 1</span><br><span class="line">        timeBetweenEvictionRunsMillis: 60000</span><br><span class="line">        # 连接保持空闲而不被驱逐的最长时间</span><br><span class="line">        minEvictableIdleTimeMillis: 300000</span><br><span class="line">        # 用来检测连接是否有效的sql，要求是一个查询语句</span><br><span class="line">        # 如果validationQuery为null，testOnBorrow、testOnReturn、testWhileIdle都不会其作用</span><br><span class="line">        validationQuery: select count(1) from &apos;table&apos;</span><br><span class="line">        # 申请连接的时候检测，如果空闲时间大于timeBetweenEvictionRunsMillis，执行validationQuery检测连接是否有效</span><br><span class="line">        testWhileIdle: true</span><br><span class="line">        # 申请连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能</span><br><span class="line">        testOnBorrow: false</span><br><span class="line">        # 归还连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能</span><br><span class="line">        testOnReturn: false</span><br><span class="line">        # 是否缓存preparedStatement，即PSCache</span><br><span class="line">        poolPreparedStatements: false</span><br><span class="line">        # 要启用PSCache，必须配置大于0，当大于0时，poolPreparedStatements自动触发修改为true</span><br><span class="line">        maxOpenPreparedStatements: -1</span><br></pre></td></tr></table></figure></p>
<h3 id="配置pagehelper分页插件"><a href="#配置pagehelper分页插件" class="headerlink" title="配置pagehelper分页插件"></a>配置pagehelper分页插件</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># pagehelper分页插件</span><br><span class="line">pagehelper:</span><br><span class="line">    # 数据库的方言</span><br><span class="line">    helperDialect: mysql</span><br><span class="line">    # 启用合理化，如果pageNum &lt; 1会查询第一页，如果pageNum &gt; pages会查询最后一页</span><br><span class="line">    reasonable: true</span><br></pre></td></tr></table></figure>
<h3 id="代码逆向生成插件mybatis-generator的配置及运行"><a href="#代码逆向生成插件mybatis-generator的配置及运行" class="headerlink" title="代码逆向生成插件mybatis-generator的配置及运行"></a>代码逆向生成插件mybatis-generator的配置及运行</h3><p>mybatis-generator插件的使用主要分为以下三步</p>
<p><strong>1.pom.xml中添加mybatis-generator插件</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;build&gt;</span><br><span class="line">        &lt;plugins&gt;</span><br><span class="line">            &lt;!-- 将Spring Boot应用打包为可执行的jar或war文件 --&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">                &lt;artifactId&gt;spring-boot-maven-plugin&lt;/artifactId&gt;</span><br><span class="line">            &lt;/plugin&gt;</span><br><span class="line">            &lt;!-- mybatis generator 自动生成代码插件 --&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;groupId&gt;org.mybatis.generator&lt;/groupId&gt;</span><br><span class="line">                &lt;artifactId&gt;mybatis-generator-maven-plugin&lt;/artifactId&gt;</span><br><span class="line">                &lt;version&gt;1.3.2&lt;/version&gt;</span><br><span class="line">                &lt;configuration&gt;</span><br><span class="line">                    &lt;!-- 扫描resources/generator目录下的generatorConfig.xml配置 --&gt;</span><br><span class="line">                    &lt;configurationFile&gt;</span><br><span class="line">                        $&#123;basedir&#125;/src/main/resources/generator/generatorConfig.xml</span><br><span class="line">                    &lt;/configurationFile&gt;</span><br><span class="line">                    &lt;overwrite&gt;true&lt;/overwrite&gt;</span><br><span class="line">                    &lt;verbose&gt;true&lt;/verbose&gt;</span><br><span class="line">                &lt;/configuration&gt;</span><br><span class="line">            &lt;/plugin&gt;</span><br><span class="line">        &lt;/plugins&gt;</span><br><span class="line">    &lt;/build&gt;</span><br></pre></td></tr></table></figure></p>
<p><strong>2.创建逆向代码生成配置文件generatorConfig.xml</strong></p>
<p>参照pom.xml插件配置中的扫描位置，在resources目录下创建generator文件夹，在新建的文件夹中创建generatorConfig.xml配置文件，文件的详细配置信息如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;!DOCTYPE generatorConfiguration</span><br><span class="line">        PUBLIC &quot;-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN&quot;</span><br><span class="line">        &quot;http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd&quot;&gt;</span><br><span class="line">&lt;generatorConfiguration&gt;</span><br><span class="line">    &lt;!-- 运行方式:mvaen运行命令 mybatis-generator:generate -e --&gt;</span><br><span class="line">    &lt;!-- 数据库驱动:选择你的本地硬盘上面的数据库驱动包--&gt;</span><br><span class="line">    &lt;properties resource=&quot;generator/generator.properties&quot;/&gt;</span><br><span class="line">    &lt;classPathEntry location=&quot;$&#123;classPathEntry&#125;&quot;/&gt;</span><br><span class="line">    &lt;context id=&quot;DB2Tables&quot; targetRuntime=&quot;MyBatis3&quot;&gt;</span><br><span class="line">        &lt;!--数据库链接URL，用户名、密码 --&gt;</span><br><span class="line">        &lt;jdbcConnection</span><br><span class="line">                driverClass=&quot;com.mysql.jdbc.Driver&quot;</span><br><span class="line">                connectionURL=&quot;jdbc:mysql://localhost:3306/$&#123;db&#125;?characterEncoding=utf-8&quot;</span><br><span class="line">                userId=&quot;$&#123;userId&#125;&quot;</span><br><span class="line">                password=&quot;$&#123;password&#125;&quot;&gt;</span><br><span class="line">        &lt;/jdbcConnection&gt;</span><br><span class="line">        &lt;javaTypeResolver&gt;</span><br><span class="line">            &lt;property name=&quot;forceBigDecimals&quot; value=&quot;false&quot;/&gt;</span><br><span class="line">        &lt;/javaTypeResolver&gt;</span><br><span class="line">        &lt;javaModelGenerator targetPackage=&quot;$&#123;pojoTargetPackage&#125;&quot; targetProject=&quot;src/main/java&quot;&gt;</span><br><span class="line">            &lt;property name=&quot;enableSubPackages&quot; value=&quot;true&quot;/&gt;</span><br><span class="line">            &lt;property name=&quot;trimStrings&quot; value=&quot;true&quot;/&gt;</span><br><span class="line">        &lt;/javaModelGenerator&gt;</span><br><span class="line">        &lt;!-- 生成映射文件的包名和位置--&gt;</span><br><span class="line">        &lt;sqlMapGenerator targetPackage=&quot;$&#123;mapperTargetPackage&#125;&quot; targetProject=&quot;src/main/resources&quot;&gt;</span><br><span class="line">            &lt;property name=&quot;enableSubPackages&quot; value=&quot;true&quot;/&gt;</span><br><span class="line">        &lt;/sqlMapGenerator&gt;</span><br><span class="line">        &lt;!-- 生成DAO的包名和位置--&gt;</span><br><span class="line">        &lt;javaClientGenerator type=&quot;XMLMAPPER&quot; targetPackage=&quot;$&#123;daoTargetPackage&#125;&quot; targetProject=&quot;src/main/java&quot;&gt;</span><br><span class="line">            &lt;property name=&quot;enableSubPackages&quot; value=&quot;true&quot;/&gt;</span><br><span class="line">        &lt;/javaClientGenerator&gt;</span><br><span class="line">        &lt;!-- 要生成的表 tableName是数据库中的表名或视图名 schema是数据库名称--&gt;</span><br><span class="line">        &lt;table tableName=&quot;%&quot; schema=&quot;$&#123;db&#125;&quot;/&gt;</span><br><span class="line">    &lt;/context&gt;</span><br><span class="line">&lt;/generatorConfiguration&gt;</span><br></pre></td></tr></table></figure>
<p>为了将generatorConfig.xml配置模板化，在这里将变动性较大的配置项单独提取出来作为一个generatorConfig.xml的配置文件，然后通过properties标签读取此文件的配置，这样做的好处是当需要多处复用此xml时只需要关注少量的配置项。</p>
<p>在generatorConfig.xml同级创建generator.properties文件，现只需要配置generator.properties文件即可，配置内容如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 请手动配置以下选项</span><br><span class="line"># 数据库驱动:选择你的本地硬盘上面的数据库驱动包</span><br><span class="line">classPathEntry = D:/CJH/maven-repository/mysql/mysql-connector-java/5.1.30/mysql-connector-java-5.1.30.jar</span><br><span class="line"># 数据库名称、用户名、密码</span><br><span class="line">db = db</span><br><span class="line">userId = root</span><br><span class="line">password = 123</span><br><span class="line"># 生成pojo的包名位置 在src/main/java目录下</span><br><span class="line">pojoTargetPackage = com.spring.demo.springbootexample.mybatis.po</span><br><span class="line"># 生成DAO的包名位置 在src/main/java目录下</span><br><span class="line">daoTargetPackage = com.spring.demo.springbootexample.mybatis.mapper</span><br><span class="line"># 生成Mapper的包名位置 位于src/main/resources目录下</span><br><span class="line">mapperTargetPackage = mapper</span><br></pre></td></tr></table></figure>
<p><strong>3.运行mybatis-generator插件生成Dao、Model、Mapping</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 打开命令行cd到项目pom.xml同级目录运行以下命令</span><br><span class="line">mvn mybatis-generator:generate -e</span><br></pre></td></tr></table></figure>
<h3 id="mybatis扫描包配置"><a href="#mybatis扫描包配置" class="headerlink" title="mybatis扫描包配置"></a>mybatis扫描包配置</h3><p>至此已经生成了指定数据库对应的实体、映射类，但是还不能直接使用，需要配置mybatis扫描地址后才能正常调用</p>
<p><strong>1.在application.yml配置mapper.xml以及pojo的包地址</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mybatis:</span><br><span class="line">    # mapper.xml包地址</span><br><span class="line">    mapper-locations: classpath:mapper/*.xml</span><br><span class="line">    # pojo生成包地址</span><br><span class="line">    type-aliases-package: com.spring.demo.springbootexample.mybatis.po</span><br></pre></td></tr></table></figure>
<p><strong>2.在SpringBootExampleApplication.java中开启Mapper扫描注解</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@SpringBootApplication</span><br><span class="line">@MapperScan(&quot;com.spring.demo.springbootexample.mybatis.mapper&quot;)</span><br><span class="line">public class SpringBootExampleApplication &#123;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        SpringApplication.run(SpringBootExampleApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="测试mapper的有效性"><a href="#测试mapper的有效性" class="headerlink" title="测试mapper的有效性"></a>测试mapper的有效性</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Controller</span><br><span class="line">public class TestController &#123;</span><br><span class="line">    //替换成自己生成的mapper</span><br><span class="line">    @Autowired</span><br><span class="line">    UserMapper userMapper;</span><br><span class="line"></span><br><span class="line">    @RequestMapping(&quot;/test&quot;)</span><br><span class="line">    @ResponseBody</span><br><span class="line">    public Object test()&#123;</span><br><span class="line">        //查询该表的所有数据</span><br><span class="line">        return userMapper.selectByExample(null);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>启动SpringBootExampleApplication.java的main函数，如果没有在application.yml特意配置server.port那么springboot会采用默认的8080端口运行，运行成功将打印如下日志<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Tomcat started on port(s): 8080 (http) with context path &apos;&apos;</span><br></pre></td></tr></table></figure></p>
<p>在浏览器输入地址如果返回表格的中的所有数据代表mybatis集成成功<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://localhost:8080/test</span><br></pre></td></tr></table></figure></p>
<h2 id="集成Swagger2"><a href="#集成Swagger2" class="headerlink" title="集成Swagger2"></a>集成Swagger2</h2><p>Swagger2是一个文档快速构建工具，能够通过注解自动生成一个Restful风格json形式的接口文档，并可以通过如swagger-ui等工具生成html网页形式的接口文档，swagger2的集成比较简单，使用需要稍微熟悉一下，集成、注解与使用分如下四步</p>
<h3 id="1-建立SwaggerConfig文件"><a href="#1-建立SwaggerConfig文件" class="headerlink" title="1.建立SwaggerConfig文件"></a>1.建立SwaggerConfig文件</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">public class SwaggerConfig &#123;</span><br><span class="line">    // 接口版本号</span><br><span class="line">    private final String version = &quot;1.0&quot;;</span><br><span class="line">    // 接口大标题</span><br><span class="line">    private final String title = &quot;SpringBoot示例工程&quot;;</span><br><span class="line">    // 具体的描述</span><br><span class="line">    private final String description = &quot;API文档自动生成示例&quot;;</span><br><span class="line">    // 服务说明url</span><br><span class="line">    private final String termsOfServiceUrl = &quot;http://www.kingeid.com&quot;;</span><br><span class="line">    // licence</span><br><span class="line">    private final String license = &quot;MIT&quot;;</span><br><span class="line">    // licnce url</span><br><span class="line">    private final String licenseUrl = &quot;https://mit-license.org/&quot;;</span><br><span class="line">    // 接口作者联系方式</span><br><span class="line">    private final Contact contact = new Contact(&quot;calebman&quot;, &quot;https://github.com/calebman&quot;, &quot;chenjianhui0428@gmail.com&quot;);</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public Docket buildDocket() &#123;</span><br><span class="line">        return new Docket(DocumentationType.SWAGGER_2).apiInfo(buildApiInf())</span><br><span class="line">                .select().build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private ApiInfo buildApiInf() &#123;</span><br><span class="line">        return new ApiInfoBuilder().title(title).termsOfServiceUrl(termsOfServiceUrl).description(description)</span><br><span class="line">                .version(version).license(license).licenseUrl(licenseUrl).contact(contact).build();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="2-在SpringBootExampleApplication-java中启用Swagger2注解"><a href="#2-在SpringBootExampleApplication-java中启用Swagger2注解" class="headerlink" title="2.在SpringBootExampleApplication.java中启用Swagger2注解"></a>2.在SpringBootExampleApplication.java中启用Swagger2注解</h3><p>在@SpringBootApplication注解下面加上@EnableSwagger2注解</p>
<h3 id="3-常用注解示例"><a href="#3-常用注解示例" class="headerlink" title="3.常用注解示例"></a>3.常用注解示例</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//Contorller中的注解示例</span><br><span class="line">@Controller</span><br><span class="line">@RequestMapping(&quot;/v1/product&quot;)</span><br><span class="line">// 表示标识这个类是swagger的资源 </span><br><span class="line">@Api(value = &quot;DocController&quot;, tags = &#123;&quot;restful api示例&quot;&#125;)</span><br><span class="line">public class DocController extends BaseController &#123;</span><br><span class="line"></span><br><span class="line">    @RequestMapping(value = &quot;/&#123;id&#125;&quot;, method = RequestMethod.PUT)</span><br><span class="line">    @ResponseBody</span><br><span class="line">    //表示一个http请求的操作</span><br><span class="line">    @ApiOperation(value = &quot;修改指定产品&quot;, httpMethod = &quot;PUT&quot;, produces = &quot;application/json&quot;)</span><br><span class="line">    //@ApiImplicitParams用于方法，包含多个@ApiImplicitParam表示单独的请求参数 </span><br><span class="line">    @ApiImplicitParams(&#123;@ApiImplicitParam(name = &quot;id&quot;, value = &quot;产品ID&quot;, required = true, paramType = &quot;path&quot;)&#125;)</span><br><span class="line">    public WebResult update(@PathVariable(&quot;id&quot;) Integer id, @ModelAttribute Product product) &#123;</span><br><span class="line">        logger.debug(&quot;修改指定产品接收产品id与产品信息=&gt;%d,&#123;&#125;&quot;, id, product);</span><br><span class="line">        if (id == null || &quot;&quot;.equals(id)) &#123;</span><br><span class="line">            logger.debug(&quot;产品id不能为空&quot;);</span><br><span class="line">            return WebResult.error(ERRORDetail.RC_0101001);</span><br><span class="line">        &#125;</span><br><span class="line">        return WebResult.success();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">//Model中的注解示例</span><br><span class="line">//表示对类进行说明，用于参数用实体类接收 </span><br><span class="line">@ApiModel(value = &quot;产品信息&quot;)</span><br><span class="line">public class Product &#123;</span><br><span class="line">    //表示对model属性的说明或者数据操作更改 </span><br><span class="line">    @ApiModelProperty(required = true, name = &quot;name&quot;, value = &quot;产品名称&quot;, dataType = &quot;query&quot;)</span><br><span class="line">    private String name;</span><br><span class="line">    @ApiModelProperty(name = &quot;type&quot;, value = &quot;产品类型&quot;, dataType = &quot;query&quot;)</span><br><span class="line">    private String type;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="4-生成json形式的文档"><a href="#4-生成json形式的文档" class="headerlink" title="4.生成json形式的文档"></a>4.生成json形式的文档</h3><p>集成成功后启动项目控制台会打印级别为INFO的日志，截取部分如下，表明可通过访问应用的v2/api-docs接口得到文档api的json格式数据，可在浏览器输入指定地址验证集成是否成功</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Mapped &quot;&#123;[/v2/api-docs],methods=[GET],produces=[application/json || application/hal+json]&#125;&quot; </span><br><span class="line"> http://localhost:8080/v2/api-docs</span><br></pre></td></tr></table></figure>
<h2 id="多环境配置"><a href="#多环境配置" class="headerlink" title="多环境配置"></a>多环境配置</h2><p>应用研发过程中多环境是不可避免的，假设我们现在有开发、演示、生产三个不同的环境其配置也不同，如果每次都在打包环节来进行配置难免出错，SpringBoot支持通过命令启动不同的环境，但是配置文件需要满足application-{profile}.properties的格式，profile代表对应环境的标识，加载时可通过不同命令加载不同环境。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">application-dev.properties：开发环境</span><br><span class="line">application-test.properties：演示环境</span><br><span class="line">application-prod.properties：生产环境</span><br><span class="line"># 运行演示环境命令</span><br><span class="line">java -jar spring-boot-example-0.0.1-SNAPSHOT --spring.profiles.active=test</span><br></pre></td></tr></table></figure>
<p>基于现在的项目实现多环境我们需要在application.yml同级目录新建application-dev.yml、application-test.yml、application-prod.yml三个不同环境的配置文件，将不变的公有配置如druid的大部分、pagehelper分页插件以及mybatis包扫描配置放置于application.yml中，并在application.yml中配置默认采用开发环境，那么如果不带–spring.profiles.active启动应用就默认为开发环境启动，变动较大的配置如数据库的账号密码分别写入不同环境的配置文件中</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">spring:</span><br><span class="line">    profiles:</span><br><span class="line">      # 默认使用开发环境</span><br><span class="line">      active: dev</span><br></pre></td></tr></table></figure>
<p>配置到这里我们的项目目录结构如下图所示<br><img src="/2019/11/23/SpringBoot-Mybatis-Swagger2环境搭建/b5.png" alt=""></p>
<p>至此我们分别完成了Mybatis、Swagger2以及多环境的集成，接下来我们配置多环境下的logger。对于logger我们总是希望在项目研发过程中越多越好，能够给予足够的信息定位bug，项目处于演示或者上线状态时为了不让日志打印影响程序性能我们只需要警告或者错误的日志，并且需要写入文件，那么接下来就基于logback实现多环境下的日志配置</p>
<h3 id="多环境下的日志配置"><a href="#多环境下的日志配置" class="headerlink" title="多环境下的日志配置"></a>多环境下的日志配置</h3><p>创建logback-spring.xml在application.yml的同级目录，springboot推荐使用logback-spring.xml而不是logback.xml文件，logback-spring.xml的配置内容如下所示</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;configuration scan=&quot;true&quot; scanPeriod=&quot;60 seconds&quot; debug=&quot;false&quot;&gt;</span><br><span class="line">    &lt;!--</span><br><span class="line">        简要描述</span><br><span class="line">        日志格式 =&gt; %d&#123;HH:mm:ss.SSS&#125;(时间) [%-5level](日志级别) %logger&#123;36&#125;(logger名字最长36个字符，否则按照句点分割) - %msg%n(具体日志信息并且换行)</span><br><span class="line"></span><br><span class="line">        开发环境 =&gt; $&#123;basepackage&#125;包下控制台打印DEBUG级别及以上、其他包控制台打印INFO级别及以上</span><br><span class="line">        演示（测试）环境 =&gt; $&#123;basepackage&#125;包下控制台打印INFO级别及以上、其他包控制台以及文件打印WARN级别及以上</span><br><span class="line">        生产环境 =&gt; 控制台以及文件打印ERROR级别及以上</span><br><span class="line"></span><br><span class="line">        日志文件生成规则如下：</span><br><span class="line">        文件生成目录 =&gt; $&#123;logdir&#125;</span><br><span class="line">        当日的log文件名称 =&gt; $&#123;appname&#125;.log</span><br><span class="line">        其他时候的log文件名称 =&gt; $&#123;appname&#125;.%d&#123;yyyy-MM-dd&#125;.log</span><br><span class="line">        日志文件最大 =&gt; $&#123;maxsize&#125;</span><br><span class="line">        最多保留 =&gt; $&#123;maxdays&#125;天</span><br><span class="line">    --&gt;</span><br><span class="line">    &lt;!--自定义参数 --&gt;</span><br><span class="line">    &lt;!--用来指定日志文件的上限大小，那么到了这个值，就会删除旧的日志--&gt;</span><br><span class="line">    &lt;property name=&quot;maxsize&quot; value=&quot;30MB&quot; /&gt;</span><br><span class="line">    &lt;!--只保留最近90天的日志--&gt;</span><br><span class="line">    &lt;property name=&quot;maxdays&quot; value=&quot;90&quot; /&gt;</span><br><span class="line">    &lt;!--application.yml 传递参数 --&gt;</span><br><span class="line">    &lt;!--log文件生成目录--&gt;</span><br><span class="line">    &lt;springProperty scope=&quot;context&quot; name=&quot;logdir&quot; source=&quot;resources.logdir&quot;/&gt;</span><br><span class="line">    &lt;!--应用名称--&gt;</span><br><span class="line">    &lt;springProperty scope=&quot;context&quot; name=&quot;appname&quot; source=&quot;resources.appname&quot;/&gt;</span><br><span class="line">    &lt;!--项目基础包--&gt;</span><br><span class="line">    &lt;springProperty scope=&quot;context&quot; name=&quot;basepackage&quot; source=&quot;resources.basepackage&quot;/&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--输出到控制台 ConsoleAppender--&gt;</span><br><span class="line">    &lt;appender name=&quot;consoleLog&quot; class=&quot;ch.qos.logback.core.ConsoleAppender&quot;&gt;</span><br><span class="line">        &lt;!--展示格式 layout--&gt;</span><br><span class="line">        &lt;layout class=&quot;ch.qos.logback.classic.PatternLayout&quot;&gt;</span><br><span class="line">            &lt;pattern&gt;</span><br><span class="line">                &lt;pattern&gt;%d&#123;HH:mm:ss.SSS&#125; [%-5level] %logger&#123;36&#125; - %msg%n&lt;/pattern&gt;</span><br><span class="line">            &lt;/pattern&gt;</span><br><span class="line">        &lt;/layout&gt;</span><br><span class="line">    &lt;/appender&gt;</span><br><span class="line">    &lt;!--输出到文件 FileAppender--&gt;</span><br><span class="line">    &lt;appender name=&quot;fileLog&quot; class=&quot;ch.qos.logback.core.rolling.RollingFileAppender&quot;&gt;</span><br><span class="line">        &lt;!--</span><br><span class="line">            日志名称，如果没有File 属性，那么只会使用FileNamePattern的文件路径规则</span><br><span class="line">            如果同时有&lt;File&gt;和&lt;FileNamePattern&gt;，那么当天日志是&lt;File&gt;，明天会自动把今天</span><br><span class="line">            的日志改名为今天的日期。即，&lt;File&gt; 的日志都是当天的。</span><br><span class="line">        --&gt;</span><br><span class="line">        &lt;File&gt;$&#123;logdir&#125;/$&#123;appname&#125;.log&lt;/File&gt;</span><br><span class="line">        &lt;!--滚动策略，按照时间滚动 TimeBasedRollingPolicy--&gt;</span><br><span class="line">        &lt;rollingPolicy class=&quot;ch.qos.logback.core.rolling.TimeBasedRollingPolicy&quot;&gt;</span><br><span class="line">            &lt;!--文件路径,定义了日志的切分方式——把每一天的日志归档到一个文件中,以防止日志填满整个磁盘空间--&gt;</span><br><span class="line">            &lt;FileNamePattern&gt;$&#123;logdir&#125;/$&#123;appname&#125;.%d&#123;yyyy-MM-dd&#125;.log&lt;/FileNamePattern&gt;</span><br><span class="line">            &lt;maxHistory&gt;$&#123;maxdays&#125;&lt;/maxHistory&gt;</span><br><span class="line">            &lt;totalSizeCap&gt;$&#123;maxsize&#125;&lt;/totalSizeCap&gt;</span><br><span class="line">        &lt;/rollingPolicy&gt;</span><br><span class="line">        &lt;!--日志输出编码格式化--&gt;</span><br><span class="line">        &lt;encoder&gt;</span><br><span class="line">            &lt;charset&gt;UTF-8&lt;/charset&gt;</span><br><span class="line">            &lt;pattern&gt;%d&#123;HH:mm:ss.SSS&#125; [%-5level] %logger&#123;36&#125; - %msg%n&lt;/pattern&gt;</span><br><span class="line">        &lt;/encoder&gt;</span><br><span class="line">    &lt;/appender&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!-- 开发环境--&gt;</span><br><span class="line">    &lt;springProfile name=&quot;dev&quot;&gt;</span><br><span class="line">        &lt;root level=&quot;INFO&quot;&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;consoleLog&quot;/&gt;</span><br><span class="line">        &lt;/root&gt;</span><br><span class="line">        &lt;!--</span><br><span class="line">            additivity是子Logger 是否继承 父Logger 的 输出源（appender） 的标志位</span><br><span class="line">            在这里additivity配置为false代表如果$&#123;basepackage&#125;中有INFO级别日志则子looger打印 root不打印</span><br><span class="line">        --&gt;</span><br><span class="line">        &lt;logger name=&quot;$&#123;basepackage&#125;&quot; level=&quot;DEBUG&quot; additivity=&quot;false&quot;&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;consoleLog&quot;/&gt;</span><br><span class="line">        &lt;/logger&gt;</span><br><span class="line">    &lt;/springProfile&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!-- 演示（测试）环境--&gt;</span><br><span class="line">    &lt;springProfile name=&quot;test&quot;&gt;</span><br><span class="line">        &lt;root level=&quot;WARN&quot;&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;consoleLog&quot;/&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;fileLog&quot;/&gt;</span><br><span class="line">        &lt;/root&gt;</span><br><span class="line">        &lt;logger name=&quot;$&#123;basepackage&#125;&quot; level=&quot;INFO&quot; additivity=&quot;false&quot;&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;consoleLog&quot;/&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;fileLog&quot;/&gt;</span><br><span class="line">        &lt;/logger&gt;</span><br><span class="line">    &lt;/springProfile&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!-- 生产环境 --&gt;</span><br><span class="line">    &lt;springProfile name=&quot;prod&quot;&gt;</span><br><span class="line">        &lt;root level=&quot;ERROR&quot;&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;consoleLog&quot;/&gt;</span><br><span class="line">            &lt;appender-ref ref=&quot;fileLog&quot;/&gt;</span><br><span class="line">        &lt;/root&gt;</span><br><span class="line">    &lt;/springProfile&gt;</span><br><span class="line">&lt;/configuration&gt;</span><br></pre></td></tr></table></figure>
<p>日志配置中引用了application.yml的配置信息，主要有logdir、appname、basepackage三项，logdir是日志文件的写入地址，可以传入相对路径，appname是应用名称，引入这项是为了通过日志文件名称区分是哪个应该输出的，basepackage是包过滤配置。</p>
<p>比如开发环境中需要打印debug级别以上的日志，但是又想使除我写的logger之外的DEBUG不打印，可过滤到本项目的包名才用DEBUG打印，此外包名使用INFO级别打印，在application.yml中新建这三项配置，也可在不同环境配置不同属性</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#应用配置</span><br><span class="line">resources:</span><br><span class="line">    # log文件写入地址</span><br><span class="line">    logdir: logs/</span><br><span class="line">    # 应用名称</span><br><span class="line">    appname: spring-boot-example</span><br><span class="line">    # 日志打印的基础扫描包</span><br><span class="line">    basepackage: com.spring.demo.springbootexample</span><br></pre></td></tr></table></figure>
<p>使用不同环境启动测试logger配置是否生效，在开发环境下将打印DEBUG级别以上的四条logger记录，在演示环境下降打印INFO级别以上的三条记录并写入文件，在生产环境下只打印ERROR级别以上的一条记录并写入文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@RequestMapping(&quot;/logger&quot;)</span><br><span class="line">@ResponseBody</span><br><span class="line">public WebResult logger() &#123;</span><br><span class="line">    logger.trace(&quot;日志输出 &#123;&#125;&quot;, &quot;trace&quot;);</span><br><span class="line">    logger.debug(&quot;日志输出 &#123;&#125;&quot;, &quot;debug&quot;);</span><br><span class="line">    logger.info(&quot;日志输出 &#123;&#125;&quot;, &quot;info&quot;);</span><br><span class="line">    logger.warn(&quot;日志输出 &#123;&#125;&quot;, &quot;warn&quot;);</span><br><span class="line">    logger.error(&quot;日志输出 &#123;&#125;&quot;, &quot;error&quot;);</span><br><span class="line">    return &quot;00&quot;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="常用配置"><a href="#常用配置" class="headerlink" title="常用配置"></a>常用配置</h2><h3 id="加载自定义配置"><a href="#加载自定义配置" class="headerlink" title="加载自定义配置"></a>加载自定义配置</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Component</span><br><span class="line">@PropertySource(value = &#123;&quot;classpath:application.yml&quot;&#125;, encoding = &quot;utf-8&quot;)</span><br><span class="line">public class Config &#123;</span><br><span class="line"></span><br><span class="line">    @Value(&quot;$&#123;resources.midpHost&#125;&quot;)</span><br><span class="line">    private String midpHost;</span><br><span class="line"></span><br><span class="line">    public String getMidpHost() &#123;</span><br><span class="line">        return midpHost;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>全局异常处理器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@ControllerAdvice</span><br><span class="line">public class GlobalExceptionResolver &#123;</span><br><span class="line"></span><br><span class="line">    Logger logger = LoggerFactory.getLogger(GlobalExceptionResolver.class);</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(value = Exception.class)</span><br><span class="line">    @ResponseBody</span><br><span class="line">    public WebResult exceptionHandle(HttpServletRequest req, Exception ex) &#123;</span><br><span class="line">        ex.printStackTrace();</span><br><span class="line">        logger.error(&quot;未知异常&quot;, ex);</span><br><span class="line">        return WebResult.error(ERRORDetail.RC_0401001);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p><a href="https://github.com/calebman/spring-boot-example" target="_blank" rel="noopener">https://github.com/calebman/spring-boot-example</a><br>这篇文章中的技术我都去使用过，但是和这个的配置方式有所不同。于是在这里保存一下。</p>
</blockquote>
]]></content>
      <tags>
        <tag>Spring boot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot-单元测试过程</title>
    <url>/2020/01/04/SpringBoot-%E5%8D%95%E5%85%83%E6%B5%8B%E8%AF%95%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="SpringBoot-单元测试过程"><a href="#SpringBoot-单元测试过程" class="headerlink" title="SpringBoot-单元测试过程"></a>SpringBoot-单元测试过程</h1><blockquote>
<p>记录一下在项目中使用过的spring-boot-starter-test</p>
</blockquote>
<a id="more"></a>
<h2 id="引入依赖"><a href="#引入依赖" class="headerlink" title="引入依赖"></a>引入依赖</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">         &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">         &lt;artifactId&gt;spring-boot-starter-test&lt;/artifactId&gt;</span><br><span class="line">         &lt;scope&gt;test&lt;/scope&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>
<p>包内自带了junit和mockito，如果自己引入过的话，最好使用包内自带的。</p>
<h2 id="基本注释"><a href="#基本注释" class="headerlink" title="基本注释"></a>基本注释</h2><p>说明一些我用到的注释和方法。</p>
<h3 id="类注释和抽象类"><a href="#类注释和抽象类" class="headerlink" title="类注释和抽象类"></a>类注释和抽象类</h3><ul>
<li><p><strong>@TestExecutionListeners：</strong>是一个类级别的注释，用于指定在测试类执行之前，可以做的一些动作。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@TestExecutionListeners(&#123;TransactionalTestExecutionListener.class, SqlScriptsTestExecutionListener.class&#125;)</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>@ActiveProfiles：</strong>是一个类级别的注释，用于声明在加载 for测试类时应使用哪些活动bean定义配置文件ApplicationContext。就是指定你这个测试类所用的配置文件。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@ActiveProfiles(&quot;test&quot;)</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>@SpringBootTest：</strong>是一个类级别的注释，指定启动类和web环境。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>AbstractTestNGSpringContextTests：</strong>抽象基础测试类，该类将Spring TestContext Framework 与TestNG 环境中的显式ApplicationContext测试支持集成在一起。</p>
</li>
</ul>
<h3 id="方法注释和实现方法"><a href="#方法注释和实现方法" class="headerlink" title="方法注释和实现方法"></a>方法注释和实现方法</h3><ul>
<li><strong>@BeforeMethod：</strong>在测试方法前运行的方法。</li>
<li><strong>@AfterMethod：</strong>在测试方法后运行的方法。</li>
<li><p><strong>@Test：</strong>测试方法注释。</p>
<ol>
<li><strong>groups：</strong>给方法分组执行。</li>
<li><strong>dependsOnGroups：</strong>说明这个方法依赖的组列表。这些组的方法都已经在此方法调用前被调用。如果被依赖的方法未执行成功，这个测试方法会被跳过。</li>
<li><strong>dependsOnMethods：</strong>和（2）类似，是说明依赖的方法列表。</li>
<li><strong>timeOut：</strong>方法应花费的最大毫秒数。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Test(groups = &#123;&quot;home&quot;&#125;, dependsOnGroups = &#123;&quot;login&quot;&#125;, dependsOnMethods = &#123;&#125;, timeOut = 300)</span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p><strong>@Ignore：</strong>这个可以放在类上，将忽略类和其子类的测试。我使用时放在方法上，仅仅只是需要忽略方法。</p>
</li>
</ul>
<h2 id="使用例子"><a href="#使用例子" class="headerlink" title="使用例子"></a>使用例子</h2><p>在这里放一个我使用时的例子。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@TestExecutionListeners(&#123;TransactionalTestExecutionListener.class, SqlScriptsTestExecutionListener.class&#125;)</span><br><span class="line">@ActiveProfiles(&quot;test&quot;)</span><br><span class="line">@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)</span><br><span class="line">public class AuthControllerTest extends AbstractTestNGSpringContextTests &#123;//可以使用AbstractTestNGSpringContextTests来取消事务</span><br><span class="line"></span><br><span class="line">    @Autowired</span><br><span class="line">    WebApplicationContext context;</span><br><span class="line"></span><br><span class="line">    private ManualRestDocumentation restDocumentation = new ManualRestDocumentation();</span><br><span class="line">    private MockMvc mockMvc;</span><br><span class="line">    private String token = TestsConstant.DEFAULT_TOKEN;</span><br><span class="line"></span><br><span class="line">    @BeforeMethod</span><br><span class="line">    public void setUp(Method method) &#123;</span><br><span class="line">        mockMvc = ApplicationTests.initMockMvc(context, restDocumentation);</span><br><span class="line">        this.restDocumentation.beforeTest(getClass(), method.getName());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @AfterMethod</span><br><span class="line">    public void tearDown() &#123;</span><br><span class="line">        this.restDocumentation.afterTest();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Transactional</span><br><span class="line">    //@Test(priority = -1)//权重（默认为0，此处需要先登录才能进行下面的操作，所以需要设置为-1，使login先进行测试，再进行下面测试）</span><br><span class="line">    @Test(groups = &#123;&quot;login&quot;&#125;)</span><br><span class="line">    public void login() throws Exception &#123;</span><br><span class="line">        getVerificationCode();</span><br><span class="line">        String url = &quot;/auth/login&quot;;</span><br><span class="line"></span><br><span class="line">        // 登录本身不需要Authorization, 为了统一处理&#123; Header - Authorization &#125;, 登录这里先加上请求头, 在文档输出时再过滤掉</span><br><span class="line">        RequestBuilder builder = post(url)//把所有元素拼接起来组成一个访问url</span><br><span class="line">                .header(JwtUtils.TOKEN_HEADER, JwtUtils.getTokenHeaderValue(this.token))//添加头</span><br><span class="line">                .param(&quot;username&quot;, TestsConstant.USERNAME)</span><br><span class="line">                .param(&quot;password&quot;, TestsConstant.PASSWORD)//参数名称和值</span><br><span class="line">                .param(&quot;code&quot;,TestsConstant.CODE)</span><br><span class="line">                .accept(MediaType.APPLICATION_JSON);</span><br><span class="line"></span><br><span class="line">        String result = mockMvc.perform(builder)</span><br><span class="line">                .andExpect(status().isOk())</span><br><span class="line">                .andExpect(jsonPath(&quot;$.status&quot;).value(HttpStatus.OK.value()))//状态</span><br><span class="line">                .andExpect(jsonPath(&quot;$.body&quot;).exists())//body详细内容</span><br><span class="line">                .andDo(document(TestsConstant.DOCUMENT_IDENTIFIER, preprocessRequest(//打印配置</span><br><span class="line">                        removeHeaders(JwtUtils.TOKEN_HEADER)</span><br><span class="line">                ), requestParameters(</span><br><span class="line">                        parameterWithName(&quot;username&quot;).description(&quot;用户名（手机号）&quot;),</span><br><span class="line">                        parameterWithName(&quot;password&quot;).description(&quot;密码&quot;),</span><br><span class="line">                        parameterWithName(&quot;code&quot;).description(&quot;验证码&quot;),</span><br><span class="line">                        beneathPath(&quot;body&quot;),//解析文件时只解析body内容</span><br><span class="line">                        fieldWithPath(&quot;roles&quot;).description(&quot;角色&quot;)</span><br><span class="line">                )))</span><br><span class="line">                .andDo(print())//打印出测试结果（详细参数以及结果等等）</span><br><span class="line">                .andReturn()</span><br><span class="line">                .getResponse()</span><br><span class="line">                .getContentAsString();</span><br><span class="line"></span><br><span class="line">        this.token = JSONObject.parseObject(result).getJSONObject(&quot;body&quot;).getString(&quot;token&quot;);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>Spring boot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot2.0集成WebSocket，实现推送信息</title>
    <url>/2019/04/02/SpringBoot2-0%E9%9B%86%E6%88%90WebSocket%EF%BC%8C%E5%AE%9E%E7%8E%B0%E5%90%8E%E5%8F%B0%E5%90%91%E5%89%8D%E7%AB%AF%E6%8E%A8%E9%80%81%E4%BF%A1%E6%81%AF/</url>
    <content><![CDATA[<h1 id="SpringBoot2-0集成WebSocket，实现推送信息"><a href="#SpringBoot2-0集成WebSocket，实现推送信息" class="headerlink" title="SpringBoot2.0集成WebSocket，实现推送信息"></a>SpringBoot2.0集成WebSocket，实现推送信息</h1><blockquote>
<p>原博客为<a href="https://blog.csdn.net/moshowgame/article/details/80275084" target="_blank" rel="noopener">https://blog.csdn.net/moshowgame/article/details/80275084</a>。这里是根据原博客搭建成功之后的记录，以后使用时可以直接套用。</p>
</blockquote>
<a id="more"></a>
<h2 id="什么是WebSocket"><a href="#什么是WebSocket" class="headerlink" title="什么是WebSocket?"></a>什么是WebSocket?</h2><p>WebSocket是HTML5下一种新的协议。它实现了浏览器与服务器全双工通信——允许服务器主动发送信息给客户端，能更好的节省服务器资源和带宽并达到实时通讯的目的。它与HTTP一样通过已建立的TCP连接来传输数据，但是它和HTTP最大不同是：</p>
<ul>
<li>WebSocket是一种双向通信协议。在建立连接后，WebSocket服务器端和客户端都能主动向对方发送或接收数据，就像Socket一样；</li>
<li>WebSocket需要像TCP一样，先建立连接，连接成功后才能相互通信。</li>
</ul>
<p><img src="/2019/04/02/SpringBoot2-0集成WebSocket，实现后台向前端推送信息/b1.jpg" alt="">    </p>
<h2 id="为什么需要-WebSocket？"><a href="#为什么需要-WebSocket？" class="headerlink" title="为什么需要 WebSocket？"></a>为什么需要 WebSocket？</h2><p>相比HTTP长连接，WebSocket有以下特点：</p>
<ul>
<li>是真正的全双工方式，建立连接后客户端与服务器端是完全平等的，可以互相主动请求。而HTTP长连接基于HTTP，是传统的客户端对服务器发起请求的模式。</li>
<li>HTTP长连接中，每次数据交换除了真正的数据部分外，服务器和客户端还要大量交换HTTP header，信息交换效率很低。Websocket协议通过第一个request建立了TCP连接之后，之后交换的数据都不需要发送 HTTP header就能交换数据，这显然和原有的HTTP协议有区别所以它需要对服务器和客户端都进行升级才能实现（主流浏览器都已支持HTML5）。此外还有 multiplexing、不同的URL可以复用同一个WebSocket连接等功能。这些都是HTTP长连接不能做到的。</li>
</ul>
<h2 id="spring-boot-如何使用WebSocket"><a href="#spring-boot-如何使用WebSocket" class="headerlink" title="spring boot 如何使用WebSocket"></a>spring boot 如何使用WebSocket</h2><h3 id="引入jar包"><a href="#引入jar包" class="headerlink" title="引入jar包"></a>引入jar包</h3><p>使用springboot的内置tomcat时，就不需要引入javaee-api了，spring-boot已经包含了。使用springboot的websocket功能首先引入springboot组件。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">            &lt;artifactId&gt;spring-boot-starter-websocket&lt;/artifactId&gt;</span><br><span class="line">            &lt;version&gt;1.3.5.RELEASE&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure></p>
<p>顺便说一句，springboot的高级组件会自动引用基础的组件，像spring-boot-starter-websocket就引入了spring-boot-starter-web和spring-boot-starter，所以不要重复引入。</p>
<h3 id="配置WebSocketConfig"><a href="#配置WebSocketConfig" class="headerlink" title="配置WebSocketConfig"></a>配置WebSocketConfig</h3><p>启用WebSocket的支持也是很简单，几句代码搞定<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import org.springframework.context.annotation.Bean;</span><br><span class="line">import org.springframework.context.annotation.Configuration;</span><br><span class="line">import org.springframework.web.socket.server.standard.ServerEndpointExporter;</span><br><span class="line"></span><br><span class="line">@Configuration</span><br><span class="line">public class WebSocketConfig &#123;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public ServerEndpointExporter serverEndpointExporter() &#123;</span><br><span class="line">        return new ServerEndpointExporter();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="实现WebSocketServer"><a href="#实现WebSocketServer" class="headerlink" title="实现WebSocketServer"></a>实现WebSocketServer</h3><p>因为WebSocket是类似客户端服务端的形式(采用ws协议)，那么这里的WebSocketServer其实就相当于一个ws协议的Controller<br>直接@ServerEndpoint(“/websocket”)@Component启用即可，然后在里面实现@OnOpen,@onClose,@onMessage等方法<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import java.io.IOException;</span><br><span class="line">import java.util.concurrent.CopyOnWriteArraySet;</span><br><span class="line"></span><br><span class="line">import javax.websocket.OnClose;</span><br><span class="line">import javax.websocket.OnError;</span><br><span class="line">import javax.websocket.OnMessage;</span><br><span class="line">import javax.websocket.OnOpen;</span><br><span class="line">import javax.websocket.Session;</span><br><span class="line">import javax.websocket.server.ServerEndpoint;</span><br><span class="line">import org.springframework.stereotype.Component;</span><br><span class="line">import cn.hutool.log.Log;</span><br><span class="line">import cn.hutool.log.LogFactory;</span><br><span class="line">import lombok.extern.slf4j.Slf4j;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">@ServerEndpoint(&quot;/websocket/&#123;sid&#125;&quot;)</span><br><span class="line">@Component</span><br><span class="line">public class WebSocketServer &#123;</span><br><span class="line">	</span><br><span class="line">	static Log log=LogFactory.get(WebSocketServer.class);</span><br><span class="line">    //静态变量，用来记录当前在线连接数。应该把它设计成线程安全的。</span><br><span class="line">    private static int onlineCount = 0;</span><br><span class="line">    //concurrent包的线程安全Set，用来存放每个客户端对应的MyWebSocket对象。</span><br><span class="line">    private static CopyOnWriteArraySet&lt;WebSocketServer&gt; webSocketSet = new CopyOnWriteArraySet&lt;WebSocketServer&gt;();</span><br><span class="line"></span><br><span class="line">    //与某个客户端的连接会话，需要通过它来给客户端发送数据</span><br><span class="line">    private Session session;</span><br><span class="line"></span><br><span class="line">    //接收sid</span><br><span class="line">    private String sid=&quot;&quot;;</span><br><span class="line">    /**</span><br><span class="line">     * 连接建立成功调用的方法*/</span><br><span class="line">    @OnOpen</span><br><span class="line">    public void onOpen(Session session,@PathParam(&quot;sid&quot;) String sid) &#123;</span><br><span class="line">        this.session = session;</span><br><span class="line">        //加入set中</span><br><span class="line">        webSocketSet.add(this);</span><br><span class="line">        //在线数加1</span><br><span class="line">        addOnlineCount();</span><br><span class="line">        log.info(&quot;有新窗口开始监听:&quot;+sid+&quot;,当前在线人数为&quot; + getOnlineCount());</span><br><span class="line">        this.sid=sid;</span><br><span class="line">        try &#123;</span><br><span class="line">        	 sendMessage(&quot;连接成功&quot;);</span><br><span class="line">        &#125; catch (IOException e) &#123;</span><br><span class="line">            log.error(&quot;websocket IO异常&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 连接关闭调用的方法</span><br><span class="line">     */</span><br><span class="line">    @OnClose</span><br><span class="line">    public void onClose() &#123;</span><br><span class="line">        //从set中删除</span><br><span class="line">        webSocketSet.remove(this); </span><br><span class="line">         //在线数减1</span><br><span class="line">        subOnlineCount();</span><br><span class="line">        log.info(&quot;有一连接关闭！当前在线人数为&quot; + getOnlineCount());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 收到客户端消息后调用的方法</span><br><span class="line">     *</span><br><span class="line">     * @param message 客户端发送过来的消息*/</span><br><span class="line">    @OnMessage</span><br><span class="line">    public void onMessage(String message, Session session) &#123;</span><br><span class="line">    	log.info(&quot;收到来自窗口&quot;+sid+&quot;的信息:&quot;+message);</span><br><span class="line">        //群发消息</span><br><span class="line">        for (WebSocketServer item : webSocketSet) &#123;</span><br><span class="line">            try &#123;</span><br><span class="line">                item.sendMessage(message);</span><br><span class="line">            &#125; catch (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	/**</span><br><span class="line">	 * </span><br><span class="line">	 * @param session</span><br><span class="line">	 * @param error</span><br><span class="line">	 */</span><br><span class="line">    @OnError</span><br><span class="line">    public void onError(Session session, Throwable error) &#123;</span><br><span class="line">        log.error(&quot;发生错误&quot;);</span><br><span class="line">        error.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">	/**</span><br><span class="line">	 * 实现服务器主动推送</span><br><span class="line">	 */</span><br><span class="line">    public void sendMessage(String message) throws IOException &#123;</span><br><span class="line">        this.session.getBasicRemote().sendText(message);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 群发自定义消息</span><br><span class="line">     * */</span><br><span class="line">    public static void sendInfo(String message,@PathParam(&quot;sid&quot;) String sid) throws IOException &#123;</span><br><span class="line">    	log.info(&quot;推送消息到窗口&quot;+sid+&quot;，推送内容:&quot;+message);</span><br><span class="line">        for (WebSocketServer item : webSocketSet) &#123;</span><br><span class="line">            try &#123;</span><br><span class="line">            	//这里可以设定只推送给这个sid的，为null则全部推送</span><br><span class="line">            	if(sid==null) &#123;</span><br><span class="line">            		item.sendMessage(message);</span><br><span class="line">            	&#125;else if(item.sid.equals(sid))&#123;</span><br><span class="line">            		item.sendMessage(message);</span><br><span class="line">            	&#125;</span><br><span class="line">            &#125; catch (IOException e) &#123;</span><br><span class="line">                continue;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">/**</span><br><span class="line">  * 获得计数器数量</span><br><span class="line">  * */</span><br><span class="line">    public static synchronized int getOnlineCount() &#123;</span><br><span class="line">        return onlineCount;</span><br><span class="line">    &#125;</span><br><span class="line">/**</span><br><span class="line">  * 增加计数器数量</span><br><span class="line">  * */</span><br><span class="line">    public static synchronized void addOnlineCount() &#123;</span><br><span class="line">        WebSocketServer.onlineCount++;</span><br><span class="line">    &#125;</span><br><span class="line">/**</span><br><span class="line">  * 删减计数器数量</span><br><span class="line">  * */</span><br><span class="line">    public static synchronized void subOnlineCount() &#123;</span><br><span class="line">        WebSocketServer.onlineCount--;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="消息推送-（并没有经过实际测试）"><a href="#消息推送-（并没有经过实际测试）" class="headerlink" title="消息推送 （并没有经过实际测试）"></a>消息推送 （并没有经过实际测试）</h3><p>推送新信息，可以再自己的Controller写个方法调用WebSocketServer.sendInfo();即可</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Controller</span><br><span class="line">@RequestMapping(&quot;/checkcenter&quot;)</span><br><span class="line">public class CheckCenterController &#123;</span><br><span class="line"></span><br><span class="line">	//页面请求</span><br><span class="line">	@GetMapping(&quot;/socket/&#123;cid&#125;&quot;)</span><br><span class="line">	public ModelAndView socket(@PathVariable String cid) &#123;</span><br><span class="line">		ModelAndView mav=new ModelAndView(&quot;/socket&quot;);</span><br><span class="line">		mav.addObject(&quot;cid&quot;, cid);</span><br><span class="line">		return mav;</span><br><span class="line">	&#125;</span><br><span class="line">	//推送数据接口</span><br><span class="line">	@ResponseBody</span><br><span class="line">	@RequestMapping(&quot;/socket/push/&#123;cid&#125;&quot;)</span><br><span class="line">	public ApiReturnObject pushToWeb(@PathVariable String cid,String message) &#123;  </span><br><span class="line">		try &#123;</span><br><span class="line">			WebSocketServer.sendInfo(message,cid);</span><br><span class="line">		&#125; catch (IOException e) &#123;</span><br><span class="line">			e.printStackTrace();</span><br><span class="line">			return ApiReturnUtil.error(cid+&quot;#&quot;+e.getMessage());</span><br><span class="line">		&#125;  </span><br><span class="line">		return ApiReturnUtil.success(cid);</span><br><span class="line">	&#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="页面发起socket请求"><a href="#页面发起socket请求" class="headerlink" title="页面发起socket请求"></a>页面发起socket请求</h3><p>然后在页面用js代码调用socket，当然，太古老的浏览器是不行的，一般新的浏览器或者谷歌浏览器是没问题的。还有一点，记得协议是ws的哦，如果像我这样封装了一些basePath的路径类，可以replace(“http”,“ws”)来替换协议</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script&gt; </span><br><span class="line">var socket;  </span><br><span class="line">if(typeof(WebSocket) == &quot;undefined&quot;) &#123;  </span><br><span class="line">    console.log(&quot;您的浏览器不支持WebSocket&quot;);  </span><br><span class="line">&#125;else&#123;  </span><br><span class="line">    console.log(&quot;您的浏览器支持WebSocket&quot;);  </span><br><span class="line">    	//实现化WebSocket对象，指定要连接的服务器地址与端口  建立连接  </span><br><span class="line">        //等同于socket = new WebSocket(&quot;ws://localhost:8083/checkcentersys/websocket/20&quot;);  </span><br><span class="line">        socket = new WebSocket(&quot;$&#123;basePath&#125;websocket/$&#123;cid&#125;&quot;.replace(&quot;http&quot;,&quot;ws&quot;));  </span><br><span class="line">        //打开事件  </span><br><span class="line">        socket.onopen = function() &#123;  </span><br><span class="line">            console.log(&quot;Socket 已打开&quot;);  </span><br><span class="line">            //socket.send(&quot;这是来自客户端的消息&quot; + location.href + new Date());  </span><br><span class="line">        &#125;;  </span><br><span class="line">        //获得消息事件  </span><br><span class="line">        socket.onmessage = function(msg) &#123;  </span><br><span class="line">            console.log(msg.data);  </span><br><span class="line">            //发现消息进入    开始处理前端触发逻辑</span><br><span class="line">        &#125;;  </span><br><span class="line">        //关闭事件  </span><br><span class="line">        socket.onclose = function() &#123;  </span><br><span class="line">            console.log(&quot;Socket已关闭&quot;);  </span><br><span class="line">        &#125;;  </span><br><span class="line">        //发生了错误事件  </span><br><span class="line">        socket.onerror = function() &#123;  </span><br><span class="line">            alert(&quot;Socket发生了错误&quot;);  </span><br><span class="line">            //此时可以尝试刷新页面</span><br><span class="line">        &#125;  </span><br><span class="line">        //离开页面时，关闭socket</span><br><span class="line">        //jquery1.8中已经被废弃，3.0中已经移除</span><br><span class="line">        // $(window).unload(function()&#123;  </span><br><span class="line">        //     socket.close();  </span><br><span class="line">        //&#125;);  </span><br><span class="line">&#125;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>
<h2 id="运行效果"><a href="#运行效果" class="headerlink" title="运行效果"></a>运行效果</h2><p><img src="/2019/04/02/SpringBoot2-0集成WebSocket，实现后台向前端推送信息/b2.jpg" alt="">    </p>
<p><img src="/2019/04/02/SpringBoot2-0集成WebSocket，实现后台向前端推送信息/b3.jpg" alt="">    </p>
]]></content>
      <tags>
        <tag>webSocket</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringIoc介绍</title>
    <url>/2020/01/11/SpringIoc%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<h1 id="SpringIoc介绍"><a href="#SpringIoc介绍" class="headerlink" title="SpringIoc介绍"></a>SpringIoc介绍</h1><blockquote>
<p>从多个博客和文章中摘抄出的ioc的介绍。为了让自己可以说明白什么是ioc。</p>
</blockquote>
<a id="more"></a>
<h2 id="ioc的概念"><a href="#ioc的概念" class="headerlink" title="ioc的概念"></a>ioc的概念</h2><p>:pencil2: 摘抄自<a href="http://c.biancheng.net/view/4627.html" target="_blank" rel="noopener">c语言中文网</a>。</p>
<p>IoC 其实有两种方式，一种就是 DI，而另一种是 DL，即 Dependency Lookup（依赖查找），前者是当前软件实体被动接受其依赖的其他组件被 IoC 容器注入，而后者则是当前软件实体主动去某个服务注册地查找其依赖的那些服务，概念之间的关系如图 1 所示可能更贴切些。<br><img src="/2020/01/11/SpringIoc介绍/b1.png" alt="图1  IoC相关概念示意图"></p>
<p>我们通常提到的 Spring IoC，实际上是指 Spring 框架提供的 IoC 容器实现（IoC Container），而使用 Spring IoC 容器的一个典型代码片段就是：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class App &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        ApplicationContext context = new FileSystemXmlApplication-Context(&quot;...&quot;);</span><br><span class="line">        // ...</span><br><span class="line">        MockService service = context.getBean(MockService.class);</span><br><span class="line">        service.doSomething();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>任何一个使用 Spring 框架构建的独立的 Java 应用（Standalone Java Application），通常都会存在一行类似于“context.getBean（..）；”的代码。</p>
<p>实际上，这行代码做的就是 DL 的工作，而构建的任何一种 IoC 容器背后（比如 BeanFactory 或者 ApplicationContext）发生的事情，则更多是 DI 的过程（也可能有部分 DL 的逻辑用于对接遗留系统）。</p>
<h2 id="ioc的工作流程"><a href="#ioc的工作流程" class="headerlink" title="ioc的工作流程"></a>ioc的工作流程</h2><p>Spring 的 IoC 容器中发生的事情其实也很简单，总结下来即两个阶段：</p>
<ul>
<li>采摘和收集“咖啡豆”（bean）</li>
<li>研磨和烹饪咖啡</li>
</ul>
<p>Spring IoC 容器的依赖注入工作可以分为两个阶段：<br><strong>1）收集和注册</strong><br>第一个阶段可以认为是构建和收集 bean 定义的阶段，在这个阶段中，我们可以通过 XML 或者 Java 代码的方式定义一些 bean，然后通过手动组装或者让容器基于某些机制自动扫描的形式，将这些 bean 定义收集到 IoC 容器中。</p>
<p>假设我们以 XML 配置的形式来收集并注册单一 bean，一般形式如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;bean id=&quot;mockService&quot; class=&quot;..MockServiceImpl&quot;&gt; ...&lt;/bean&gt;</span><br></pre></td></tr></table></figure></p>
<p>如果嫌逐个收集 bean 定义麻烦，想批量地收集并注册到 IoC 容器中，我们也可以通过 XML Schema 形式的配置进行批量扫描并采集和注册：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;context:component-scan base-package=&quot;com.keevol&quot;&gt;</span><br></pre></td></tr></table></figure></p>
<p>注意基于 JavaConfig 形式的收集和注册，不管是单一还是批量，后面我们都会单独提及。<br><strong>2）分析和组装</strong><br>当第一阶段工作完成后，我们可以先暂且认为 IoC 容器中充斥着一个个独立的 bean，它们之间没有任何关系。</p>
<p>但实际上，它们之间是有依赖关系的，所以，IoC 容器在第二阶段要干的事情就是分析这些已经在 IoC 容器之中的 bean，然后根据它们之间的依赖关系先后组装它们。</p>
<p>如果 IoC 容器发现某个 bean 依赖另一个 bean，它就会将这另一个 bean 注入给依赖它的那个 bean，直到所有 bean 的依赖都注入完成，所有 bean 都“整装待发”，整个 IoC 容器的工作即算完成。</p>
<p>至于分析和组装的依据，Spring 框架最早是通过 XML 配置文件的形式来描述 bean 与 bean 之间的关系的，随着 Java 业界研发技术和理念的转变，基于 Java 代码和 Annotation 元信息的描述方式也日渐兴盛（比如 @Autowired 和 @Inject），但不管使用哪种方式，都只是为了简化绑定逻辑描述的各种“表象”，最终都是为本阶段的最终目的服务。</p>
<p>很多 Java 开发者一定认为 Spring 的 XML 配置文件是一种配置（Configuration），但本质上，这些配置文件更应该是一种代码形式，XML 在这里其实可以看作一种 DSL，它用来表述的是 bean 与 bean 之间的依赖绑定关系，如果没有 IoC 容器就要自己写代码新建（new）对象并配置（set）依赖。</p>
<h2 id="依赖倒置原则介绍"><a href="#依赖倒置原则介绍" class="headerlink" title="依赖倒置原则介绍"></a>依赖倒置原则介绍</h2><p>:pencil2:摘抄自<a href="https://www.zhihu.com/question/23277575" target="_blank" rel="noopener">Spring IoC有什么好处呢？</a></p>
<p><strong>依赖倒置原则</strong>——把原本的高层建筑依赖底层建筑“倒置”过来，变成底层建筑依赖高层建筑。高层建筑决定需要什么，底层去实现这样的需求，但是高层并不用管底层是怎么实现的。这样就不会出现前面的“牵一发动全身”的情况。</p>
<h2 id="控制反转介绍"><a href="#控制反转介绍" class="headerlink" title="控制反转介绍"></a>控制反转介绍</h2><p><strong>控制反转（Inversion of Control）</strong> 就是依赖倒置原则的一种代码设计的思路。具体采用的方法就是所谓的<strong>依赖注入（Dependency Injection）</strong>。其实这些概念初次接触都会感到云里雾里的。说穿了，这几种概念的关系大概如下图：<br><img src="/2020/01/11/SpringIoc介绍/b2.png" alt="图2  关系图"></p>
<p><strong>控制反转（IoC）</strong>，及上层控制下层，而不是下层控制着上层。我们用依赖注入（Dependency Injection）这种方式来实现控制反转。所谓依赖注入，就是把底层类作为参数传入上层类，实现上层类对下层类的“控制”。</p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu的初次操作记录</title>
    <url>/2020/09/05/UBuntu%E7%9A%84%E5%88%9D%E6%AC%A1%E6%93%8D%E4%BD%9C%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="Ubuntu的初次操作记录"><a href="#Ubuntu的初次操作记录" class="headerlink" title="Ubuntu的初次操作记录"></a>Ubuntu的初次操作记录</h1><blockquote>
<p>简单的记录一下操作过程，做个存档。主要目的是排查服务器 ssh 关闭，并安装切片程序。<br>包含防火墙检查，ssh安装，更换数据源，解决域名无法解析。</p>
</blockquote>
<a id="more"></a>
<h2 id="检查防火墙"><a href="#检查防火墙" class="headerlink" title="检查防火墙"></a>检查防火墙</h2><p>拿到服务器后首先检查了一下防火墙是否开启。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo ufw status</span><br></pre></td></tr></table></figure></p>
<p>发现防火墙已经关闭。</p>
<h2 id="检查ssh是否安装"><a href="#检查ssh是否安装" class="headerlink" title="检查ssh是否安装"></a>检查ssh是否安装</h2><p>检查这个机器是否安装了 ssh-server 服务，一般都是只安装了 client。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dpkg -l | grep ssh</span><br></pre></td></tr></table></figure></p>
<p>发现并没有安装，使用命令开始安装。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install openssh-server</span><br></pre></td></tr></table></figure></p>
<p>报错 ubuntu 数据源无法下载。准备更换国内的阿里数据源。</p>
<h2 id="更换数据源之前"><a href="#更换数据源之前" class="headerlink" title="更换数据源之前"></a>更换数据源之前</h2><p>这个服务器不能解析域名，我还是在下面的步骤中的第四步执行时，发现无法解析。<br>修改 resolv.conf 文件<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo gedit /etc/resolv.conf</span><br></pre></td></tr></table></figure></p>
<p>打开后发现整个文件没有任何配置，在最后一行加入。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nameserver 8.8.8.8</span><br></pre></td></tr></table></figure></p>
<h2 id="更换-apt-默认数据源为阿里"><a href="#更换-apt-默认数据源为阿里" class="headerlink" title="更换 apt 默认数据源为阿里"></a>更换 apt 默认数据源为阿里</h2><p>这里就不得不吐槽一下，拿到得是一个从 ie 浏览器访问 vmware 管理界面，然后再从管理界面访问我们的机器。<br>这就导致无法复制命令，下面的数据源更换愣是靠手打的。</p>
<ol>
<li><p>备份源列表</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak</span><br></pre></td></tr></table></figure>
</li>
<li><p>命令行打开sources.list文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo gedit /etc/apt/sources.list</span><br></pre></td></tr></table></figure>
</li>
<li><p>修改sources,list文件【本例更改为阿里镜像源】</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#  阿里镜像源</span><br><span class="line"></span><br><span class="line">deb http://mirrors.aliyun.com/ubuntu/ bionic main restricted universe multiverse</span><br><span class="line">deb-src http://mirrors.aliyun.com/ubuntu/ bionic main restricted universe multiverse</span><br><span class="line"></span><br><span class="line">deb http://mirrors.aliyun.com/ubuntu/ bionic-security main restricted universe multiverse</span><br><span class="line">deb-src http://mirrors.aliyun.com/ubuntu/ bionic-security main restricted universe multiverse</span><br><span class="line"></span><br><span class="line">deb http://mirrors.aliyun.com/ubuntu/ bionic-updates main restricted universe multiverse</span><br><span class="line">deb-src http://mirrors.aliyun.com/ubuntu/ bionic-updates main restricted universe multiverse</span><br><span class="line"></span><br><span class="line">deb http://mirrors.aliyun.com/ubuntu/ bionic-proposed main restricted universe multiverse</span><br><span class="line">deb-src http://mirrors.aliyun.com/ubuntu/ bionic-proposed main restricted universe multiverse</span><br><span class="line"></span><br><span class="line">deb http://mirrors.aliyun.com/ubuntu/ bionic-backports main restricted universe multiverse</span><br><span class="line">deb-src http://mirrors.aliyun.com/ubuntu/ bionic-backports main restricted universe multiverse</span><br></pre></td></tr></table></figure>
</li>
<li><p>更新并升级</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get update &amp;&amp; sudo apt-get upgrade</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>在输入了这个更新命令后，等待了一段时间。其中有很多的忽略包，不知道后面会不会需要重新弄。</p>
<h2 id="结束"><a href="#结束" class="headerlink" title="结束"></a>结束</h2><p>更换完数据源后，即可成功安装 ssh。</p>
]]></content>
      <tags>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu上次操作后的继续操作</title>
    <url>/2020/09/12/Ubuntu%E4%B8%8A%E6%AC%A1%E6%93%8D%E4%BD%9C%E5%90%8E%E7%9A%84%E7%BB%A7%E7%BB%AD/</url>
    <content><![CDATA[<h1 id="Ubuntu上次操作后的继续操作"><a href="#Ubuntu上次操作后的继续操作" class="headerlink" title="Ubuntu上次操作后的继续操作"></a>Ubuntu上次操作后的继续操作</h1><blockquote>
<p>上次搞完哪个服务器后，再次使用发现无法用 sftp 传输文件。<br>包含查询系统版本号，更换清华软件数据源，处理文件无法上传。<br><a id="more"></a></p>
</blockquote>
<h2 id="apt-无法定位软件包"><a href="#apt-无法定位软件包" class="headerlink" title="apt 无法定位软件包"></a>apt 无法定位软件包</h2><p>当我使用服务器安装 gcc-c++ 时，发现报错。<br>查询后建议换下载数据源。<br>(<a href="https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu/)[https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu/]" target="_blank" rel="noopener">https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu/)[https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu/]</a> 通过这个地址来试图更换为清华数据源。<br>查询 Ubuntu 版本信息<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cat /proc/version</span><br></pre></td></tr></table></figure></p>
<p>显示<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Linux version 4.15.0-45-generic (buildd@lcy01-amd64-027) (gcc version 5.4.0 20160609 (Ubuntu 5.4.0-6ubuntu1~16.04.10)) #48~16.04.1-Ubuntu SMP Tue Jan 29 18:03:48 UTC 2019</span><br></pre></td></tr></table></figure></p>
<p>可以选择对应版本的数据源来更换。<br>但是，通过 Xshell 直接操作时发现 sftp 无法传递文件。</p>
<h2 id="sftp-无法传递文件"><a href="#sftp-无法传递文件" class="headerlink" title="sftp 无法传递文件"></a>sftp 无法传递文件</h2><p>主要问题是可以下载，但是不能上传。首先查看用户权限，发现登录的用户权限不足。无法修改文件夹，只能用命令修改权限后上传。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">chmod 777 /etc/apt/sources.list</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu安装tippecanoe</title>
    <url>/2020/09/19/Ubuntu%E5%AE%89%E8%A3%85tippecanoe/</url>
    <content><![CDATA[<h1 id="Ubuntu安装tippecanoe"><a href="#Ubuntu安装tippecanoe" class="headerlink" title="Ubuntu安装tippecanoe"></a>Ubuntu安装tippecanoe</h1><blockquote>
<p>Ubuntu 的命令还是和 centos 有很多出入，没法直接复制。再次整理一份安装过程。<br>参考博客 <a href="https://blog.csdn.net/zxzfcsu/article/details/80836277" target="_blank" rel="noopener">https://blog.csdn.net/zxzfcsu/article/details/80836277</a><br><a id="more"></a></p>
</blockquote>
<h2 id="安装依赖"><a href="#安装依赖" class="headerlink" title="安装依赖"></a>安装依赖</h2><p>安装 gcc<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get  install  build-essential</span><br></pre></td></tr></table></figure></p>
<p>安装 sqlite-devel<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install sqlite sqlite3</span><br></pre></td></tr></table></figure></p>
<p>同时还要安装 ruby<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install ruby</span><br></pre></td></tr></table></figure></p>
<p>安装 zlib zlib-devel<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install zlib1g</span><br><span class="line">sudo apt-get install zlib1g.dev</span><br></pre></td></tr></table></figure></p>
<p>后来在 <code>make -j</code> 的时候还报错<code>fatal error:sqlite3.h</code> 。又安装了<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install libsqlit3-dev</span><br></pre></td></tr></table></figure></p>
<p>并没有报<code>fatal error:zlib.h</code> 就没有使用<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install zlib1g-dev</span><br></pre></td></tr></table></figure></p>
<h2 id="解压"><a href="#解压" class="headerlink" title="解压"></a>解压</h2><p>剩下的步骤就是一样的了，解压后运行命令即可。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">make -j</span><br><span class="line">make install</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>tippecanoe</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows Server 2012 R2安装域控制器</title>
    <url>/2020/04/18/Windows%20Server%202012%20R2%E5%AE%89%E8%A3%85%E5%9F%9F%E6%8E%A7%E5%88%B6%E5%99%A8/</url>
    <content><![CDATA[<h1 id="Windows-Server-2012-R2安装域控制器"><a href="#Windows-Server-2012-R2安装域控制器" class="headerlink" title="Windows Server 2012 R2安装域控制器"></a>Windows Server 2012 R2安装域控制器</h1><blockquote>
<p>转载自<a href="https://blog.csdn.net/jiaqu2177/article/details/81943615" target="_blank" rel="noopener">https://blog.csdn.net/jiaqu2177/article/details/81943615</a> 本人已经使用过了。图片有点模糊，不影响使用。<br>主要是用于使用office web apps 和office online server搭建在线Word预览编辑！</p>
</blockquote>
<a id="more"></a>
<h2 id="一、准备工作"><a href="#一、准备工作" class="headerlink" title="一、准备工作"></a>一、准备工作</h2><p>原文介绍用Windows Server 2012 R2版本。我参考了别的文章后，从<a href="https://msdn.itellyou.cn/" target="_blank" rel="noopener">msdn</a> 下载了<code>cn_windows_server_2012_r2_with_update_x64_dvd_6052725</code>这个版本用来安装。</p>
<h2 id="二、安装域"><a href="#二、安装域" class="headerlink" title="二、安装域"></a>二、安装域</h2><p>这个步骤按照原文进行。转换服务器和域服务器最好设置成一样的密码。<br>（1）打开服务器管理器，添加角色和功能。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b1.png" alt="1"></p>
<p>（2）出现“添加角色和功能”界面，下一步  。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b2.png" alt="2"></p>
<p>（3）根据提示操作，下一步。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b3.png" alt="3"></p>
<p>（4）根据提示操作，下一步。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b4.png" alt="4"></p>
<p>（5）选择添加AD域服务（Active Directory 域服务），同时添加所需功能。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b5.png" alt="5"></p>
<p>（6）根据提示操作，下一步<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b6.png" alt=""></p>
<p>（7）安装完成 。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b7.png" alt=""></p>
<h2 id="三、配置域"><a href="#三、配置域" class="headerlink" title="三、配置域"></a>三、配置域</h2><p>（1）点击服务器管理器左侧“AD DS” 、点击黄色提示部分中的更多。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b8.png" alt=""></p>
<p>（2）点击“将此服务器升级为域控制器” 。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b9.png" alt=""></p>
<p>（3）进入AD域服务器配置向导，选择 “添加新林” ，输入域，点击下一步。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b10.png" alt=""></p>
<p>（4）填写密码，下一步 。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b11.png" alt=""></p>
<p>（5）提示DNS无法创建，不用管，继续下一步 。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b12.png" alt=""></p>
<p>（6）安装路径，默认，下一步 。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b13.png" alt=""></p>
<p>（7）查看选项，默认，下一步 。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b14.png" alt=""></p>
<p>（8）点击安装 。此过程可能会报错，提示登陆用户没有设置密码，去设置给当前登陆用户（Administrator）设置一个密码，然后回来点击 “重新运行先决条件检查” 即可！<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b15.png" alt=""></p>
<p>（9）安装完成，会提示注销重启 （此过程比较漫长，耐心等待）。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b16.png" alt=""></p>
<h2 id="四、将转换服务器加入到域控服务器"><a href="#四、将转换服务器加入到域控服务器" class="headerlink" title="四、将转换服务器加入到域控服务器"></a>四、将转换服务器加入到域控服务器</h2><p>（1）打开转换服务器，修改转换服务器的NDS，指向域控服务器（刚才的服务器ip地址）。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b17.png" alt=""></p>
<p>（2）在计算机属性中，修改计算机名称（随意），并添加到域控服务器（域为“添加新林”时设置的域名），确定后提示注销重启服务器。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b18.png" alt=""></p>
<p>（3）打开域控服务器，点击管理 &gt; 添加服务器，输入转换服务器修改后的计算机名称，立即查找。将搜索到的服务器双击添加到右边，点击确定。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b19.png" alt=""><br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b20.png" alt=""></p>
<p>（4）在域控服务器中，所有服务器显示两台服务器，并都是联机状态则表示成功 。<br><img src="/2020/04/18/Windows Server 2012 R2安装域控制器/b21.png" alt=""></p>
<p>至此，域控服务器安装完成。</p>
]]></content>
      <tags>
        <tag>windows server</tag>
      </tags>
  </entry>
  <entry>
    <title>gradle build时出现的乱码问题</title>
    <url>/2019/11/09/build%E6%97%B6%E5%87%BA%E7%8E%B0%E7%9A%84%E4%B9%B1%E7%A0%81%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="gradle-build时出现的乱码问题"><a href="#gradle-build时出现的乱码问题" class="headerlink" title="gradle build时出现的乱码问题"></a>gradle build时出现的乱码问题</h1><blockquote>
<p>日常的bug记录。新增一条swagger-ui页面乱码问题</p>
</blockquote>
<a id="more"></a>
<h2 id="问题一"><a href="#问题一" class="headerlink" title="问题一"></a>问题一</h2><p>新开的项目在用idea内自带的gradle去build打包的时候，出现了一系列的乱码，最后FAILED。<br>如下图<br><img src="/2019/11/09/build时出现的乱码问题/b2.png" alt=""></p>
<blockquote>
<p>因为我已经修改好了代码中的错误，右边展示的是成功的信息。所以就展示了一下空白</p>
</blockquote>
<p>将问题分享给同时开发这个项目的同事后，他找到了处理的方法。<br>通过点击idea最上层的help-&gt;Edit custom VM options可以打开个性化配置虚拟机文件，这里是配置通用项目的jvm虚拟机的启动参数。</p>
<p><img src="/2019/11/09/build时出现的乱码问题/b1.png" alt=""></p>
<p>在文件中加入<strong>-Dfile.encoding=UTF-8</strong>，重启idea即可看到控制台的乱码已经恢复了正常的文字。</p>
<p><img src="/2019/11/09/build时出现的乱码问题/b3.png" alt=""></p>
<h2 id="问题二"><a href="#问题二" class="headerlink" title="问题二"></a>问题二</h2><p>在我像上面那样配置完后，又进行了一个被我遗忘了的操作。然后我再打开接口html的时候，发现所有的文字都变成了乱码。百度尝试了一下，修改<strong>file –&gt; Settings –&gt; file encodings</strong>这种方法不能解决我这个问题。</p>
<p>呼叫了一波支援，大佬看了一下网页的编码和idea编辑器中的文件编码是一致的。都是UTF-8。只能把项目目录中的.idea和out和build目录删除，重新导入文件，解决了这个问题。</p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>hbase在windows上安装踩坑记录</title>
    <url>/2019/06/08/hbase%E5%9C%A8windows%E4%B8%8A%E5%AE%89%E8%A3%85%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="hbase在windows上安装踩坑记录"><a href="#hbase在windows上安装踩坑记录" class="headerlink" title="hbase在windows上安装踩坑记录"></a>hbase在windows上安装踩坑记录</h1><blockquote>
<p>记录一下在windows7机器上安装hbase时遇到的问题，以便于以后使用。</p>
</blockquote>
<a id="more"></a>
<h2 id="前置条件："><a href="#前置条件：" class="headerlink" title="前置条件："></a>前置条件：</h2><p>已经安装好了hadoop-3.1.1，并且从<a href="https://www.apache.org/dyn/closer.lua/hbase/2.1.5/hbase-2.1.5-bin.tar.gz" target="_blank" rel="noopener">apache hbase-2.1.5下载地址</a>中下载了压缩包。</p>
<h2 id="修改配置文件-conf-hbase-site-xml："><a href="#修改配置文件-conf-hbase-site-xml：" class="headerlink" title="修改配置文件/conf/hbase-site.xml："></a>修改配置文件/conf/hbase-site.xml：</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;</span><br><span class="line">	&lt;property&gt;  </span><br><span class="line">		&lt;name&gt;hbase.rootdir&lt;/name&gt;  </span><br><span class="line">		&lt;value&gt;file:///D:/hadoop/hbase-2.1.5/tmp/hbase/root&lt;/value&gt;  </span><br><span class="line">	&lt;/property&gt;  </span><br><span class="line">	&lt;property&gt;  </span><br><span class="line">		&lt;name&gt;hbase.tmp.dir&lt;/name&gt;  </span><br><span class="line">		&lt;value&gt;D:/hadoop/hbase-2.1.5/tmp/hbase/tmp&lt;/value&gt;  </span><br><span class="line">	&lt;/property&gt;  </span><br><span class="line">	&lt;property&gt;  </span><br><span class="line">		&lt;name&gt;hbase.zookeeper.quorum&lt;/name&gt;  </span><br><span class="line">		&lt;value&gt;127.0.0.1&lt;/value&gt;  </span><br><span class="line">	&lt;/property&gt;  </span><br><span class="line">	&lt;property&gt;  </span><br><span class="line">		&lt;name&gt;hbase.zookeeper.property.dataDir&lt;/name&gt;  </span><br><span class="line">		&lt;value&gt;D:/hadoop/hbase-2.1.5/tmp/hbase/zoo&lt;/value&gt;  </span><br><span class="line">	&lt;/property&gt;  </span><br><span class="line">	&lt;property&gt;</span><br><span class="line">		&lt;name&gt;hbase.cluster.distributed&lt;/name&gt;</span><br><span class="line">		&lt;value&gt;false&lt;/value&gt;</span><br><span class="line">	&lt;/property&gt;</span><br><span class="line">	&lt;property&gt;    </span><br><span class="line">        &lt;name&gt;hbase.zookeeper.property.clientPort&lt;/name&gt;    </span><br><span class="line">        &lt;value&gt;2181&lt;/value&gt;    </span><br><span class="line">    &lt;/property&gt; </span><br><span class="line">&lt;/configuration&gt;</span><br></pre></td></tr></table></figure>
<p>其中，<strong>hbase.cluster.distributed</strong>为HBase以分布式模式进行，这个功能在win下不支持。如果在win中设置为true的话。在运行start-hbase.cmd启动时，会报错<strong>This is not implemented yet. Stay tuned</strong>。</p>
<p>但是设置为false的话，会启动hbase自带的zookeeper，需要把本地装的zookeeper关闭。</p>
<h2 id="修改hbase-env-cmd："><a href="#修改hbase-env-cmd：" class="headerlink" title="修改hbase-env.cmd："></a>修改hbase-env.cmd：</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set JAVA_HOME=G:\JDK8_64</span><br><span class="line">set HBASE_MANAGES_ZK=false</span><br></pre></td></tr></table></figure>
<h2 id="环境变量的设置："><a href="#环境变量的设置：" class="headerlink" title="环境变量的设置："></a>环境变量的设置：</h2><p>添加系统变量 HBASE_HOME : D:/hadoop/hbase-2.1.5</p>
<p>在Path中添加%HBASE_HOME%\bin</p>
<h2 id="启动HBase服务-（未完成）"><a href="#启动HBase服务-（未完成）" class="headerlink" title="启动HBase服务:（未完成）"></a>启动HBase服务:（未完成）</h2><p>在启动HBase之前，我们需要先启动Hadoop；</p>
<p>接着，在\hbase\bin内打开命令行。运行<strong>start-hbase.cmd</strong>，即可完成启动</p>
]]></content>
      <tags>
        <tag>hbase</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo+next添加live2d和搜索功能</title>
    <url>/2020/05/01/hexo-next%E6%B7%BB%E5%8A%A0live2d%E5%92%8C%E6%90%9C%E7%B4%A2%E5%8A%9F%E8%83%BD/</url>
    <content><![CDATA[<h1 id="hexo-next添加live2d和搜索功能"><a href="#hexo-next添加live2d和搜索功能" class="headerlink" title="hexo+next添加live2d和搜索功能"></a>hexo+next添加live2d和搜索功能</h1><blockquote>
<p>之前的网站看腻了，就来换个新的设计。再加上修改自己使用时候出现的一些不方便的地方。都是直接用原文中的步骤进行就可以了。<br>原文地址<a href="https://www.jianshu.com/p/202c9e789c8f" target="_blank" rel="noopener">Hexo Next主题添加搜索功能</a><br><a href="https://github.com/EYHN/hexo-helper-live2d/blob/master/README.zh-CN.md" target="_blank" rel="noopener">hexo-helper-live2d</a></p>
</blockquote>
<a id="more"></a>
<h2 id="Hexo-Next主题添加搜索功能"><a href="#Hexo-Next主题添加搜索功能" class="headerlink" title="Hexo Next主题添加搜索功能"></a>Hexo Next主题添加搜索功能</h2><ol>
<li><p>在hexo的根目录下执行命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>
</li>
<li><p>在根目录下的/theme/next/_config.yml文件中添加配置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">search:</span><br><span class="line">  path: search.xml</span><br><span class="line">  field: post</span><br><span class="line">  format: html</span><br><span class="line">  limit: 10000</span><br></pre></td></tr></table></figure>
</li>
<li><p>在根目录下的/theme/next/_config.yml文件中搜索<code>local_search</code>，将<code>enable</code>改为<code>true</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">local_search:</span><br><span class="line">  enable: true</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>完成。</p>
<h2 id="添加hexo-helper-live2d"><a href="#添加hexo-helper-live2d" class="headerlink" title="添加hexo-helper-live2d"></a>添加hexo-helper-live2d</h2><ol>
<li><p>安装模块</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install --save hexo-helper-live2d</span><br></pre></td></tr></table></figure>
</li>
<li><p>向Hexo的_config.yml 文件或主题的 _config.yml 文件中添加配置。（我是添加在了hexo的文件中）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># hexo-helper-live2d 看板娘</span><br><span class="line">live2d:</span><br><span class="line">  model:</span><br><span class="line">    use: live2d-widget-model-koharu</span><br><span class="line">    scale: 1</span><br><span class="line">    hHeadPos: 0.5</span><br><span class="line">    vHeadPos: 0.618</span><br><span class="line">  display:</span><br><span class="line">    superSample: 2</span><br><span class="line">    width: 150</span><br><span class="line">    height: 300</span><br><span class="line">    position: left</span><br><span class="line">    hOffset: 0</span><br><span class="line">    vOffset: -20</span><br><span class="line">  mobile:</span><br><span class="line">    show: true</span><br><span class="line">    scale: 0.5</span><br><span class="line">  react:</span><br><span class="line">    opacityDefault: 0.7</span><br><span class="line">    opacityOnHover: 0.2</span><br></pre></td></tr></table></figure>
</li>
<li><p>更换一个模型</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install live2d-widget-model-koharu</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加点击弹出对话功能</p>
</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>idea启动命令过长bug</title>
    <url>/2020/08/23/idea%E5%90%AF%E5%8A%A8%E5%91%BD%E4%BB%A4%E8%BF%87%E9%95%BFbug/</url>
    <content><![CDATA[<h1 id="idea启动命令过长bug"><a href="#idea启动命令过长bug" class="headerlink" title="idea启动命令过长bug"></a>idea启动命令过长bug</h1><blockquote>
<p>在引用 MeteoInfoLib jar 包后带来的问题，报错内容为：<br>Error running ‘Application’: Command line is too long. Shorten command line for Application or also for Spring Boot default configuration.</p>
</blockquote>
<a id="more"></a>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><p>调整运行项目的配置，将 Configuration 中的 Shorten Command Line 修改为 JAR 就可以了。<br>具体原因就是和 bug 所说的一样，启动的命令行过长，idea 通过临时的 classpath.jar 传递长的类路径。原始类路径在MANIFEST.MF中定义为classpath.jar中的类路径属性。就完事了。</p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>idea常用插件记录</title>
    <url>/2019/03/28/idea%E5%B8%B8%E7%94%A8%E6%8F%92%E4%BB%B6%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="idea常用插件记录"><a href="#idea常用插件记录" class="headerlink" title="idea常用插件记录"></a>idea常用插件记录</h1><blockquote>
<p> 引用网上各种图片，部分为自己的使用截图。</p>
</blockquote>
<a id="more"></a>
<h2 id="Alibaba-Java-Coding-Guidelines"><a href="#Alibaba-Java-Coding-Guidelines" class="headerlink" title="Alibaba Java Coding Guidelines"></a>Alibaba Java Coding Guidelines</h2><p>作为格式化自己代码的一种方式，增加代码的有效性和美观性。</p>
<p><img src="/2019/03/28/idea常用插件记录/b1.png" alt="1"></p>
<h2 id="Background-Image-Plus"><a href="#Background-Image-Plus" class="headerlink" title="Background Image Plus"></a>Background Image Plus</h2><p>添加背景图片的一种插件，拉开你于旁边程序员的审美距离，凸显出你那该死的、无处安放的魅力啊。（也很容易暴露出你的直男审美）</p>
<p><img src="/2019/03/28/idea常用插件记录/b2.png" alt="设置图片"></p>
<p><img src="/2019/03/28/idea常用插件记录/b3.png" alt="图片详细参数设置"></p>
<p><img src="/2019/03/28/idea常用插件记录/b4.png" alt="效果图"></p>
<h2 id="Lombok-plugin"><a href="#Lombok-plugin" class="headerlink" title="Lombok plugin"></a>Lombok plugin</h2><p>可以让你在使用实体类的时候更加方便快捷，不用添加get/set方法，使用时直接build参数。让代码更加整洁，只需要用注解即可。</p>
<p><img src="/2019/03/28/idea常用插件记录/b5.png" alt="效果图"></p>
<h2 id="Nyan-progress-bar"><a href="#Nyan-progress-bar" class="headerlink" title="Nyan progress bar"></a>Nyan progress bar</h2><p>让你读取进度条时的心情放松下来。看着这个彩虹猫，再也不用为等进度而心烦意乱了。</p>
<p><img src="/2019/03/28/idea常用插件记录/b6.png" alt="效果图"></p>
<h2 id="Rainbow-Brackets"><a href="#Rainbow-Brackets" class="headerlink" title="Rainbow Brackets"></a>Rainbow Brackets</h2><p>可以让你的括号更加明显的展示给你，支持Alt + 右键淡化括号外代码、Ctrl + 右键高亮括号内代码。并且给你的多层括号上色区分。</p>
<p><img src="/2019/03/28/idea常用插件记录/b7.png" alt="淡化代码"></p>
<p><img src="/2019/03/28/idea常用插件记录/b8.png" alt="高亮代码"></p>
<h2 id="Translation"><a href="#Translation" class="headerlink" title="Translation"></a>Translation</h2><p>添加右键选项，选中想要的内容点击右键后进行翻译。可以选择google翻译、雅虎翻译或百度翻译（需要一定的上网技巧）</p>
<p><img src="/2019/03/28/idea常用插件记录/b9.png" alt="效果图"></p>
<h2 id="activate-power-mode-OR-Power-mode-II"><a href="#activate-power-mode-OR-Power-mode-II" class="headerlink" title="activate-power-mode OR Power mode II"></a>activate-power-mode OR Power mode II</h2><p>这个东西适合装逼时使用，在正常工作中使用时估计会对自身的心灵造成一定程度的打击。当我写出一堆bug的时候是越看越晕。</p>
<p><img src="/2019/03/28/idea常用插件记录/b10.gif" alt="效果图"></p>
]]></content>
      <tags>
        <tag>idea</tag>
      </tags>
  </entry>
  <entry>
    <title>jacob的第二次研究</title>
    <url>/2020/03/07/jaocb%E7%9A%84%E7%AC%AC%E4%BA%8C%E6%AC%A1%E7%A0%94%E7%A9%B6/</url>
    <content><![CDATA[<h1 id="jacob的第二次研究"><a href="#jacob的第二次研究" class="headerlink" title="jacob的第二次研究"></a>jacob的第二次研究</h1><blockquote>
<p>这是第二次研究用java来将office文档转换成pdf图片展示。记录一下大佬提供的资料，<a href="https://blog.csdn.net/u014727260/article/details/53858329" target="_blank" rel="noopener">https://blog.csdn.net/u014727260/article/details/53858329</a> 。</p>
</blockquote>
<a id="more"></a>
<h2 id="第一次研究历程"><a href="#第一次研究历程" class="headerlink" title="第一次研究历程"></a>第一次研究历程</h2><p>很久很久以前（刚刚入行的时候），有一个需求是将office转换成图片展示。当时提供给我的思路是用虚拟打印机来将文档转换，但是图片质量不好。后来尝试了poi、aspose，但是生成图片效果都太模糊，连文字都看不清楚，就切换到了jacob。</p>
<p>jacob也有很多缺点，我安装的版本不兼容office，只能安装wps用来调用。需要将jacob.dll放在jdk中的\jre\bin中。</p>
<p>但是jacob转换成的pdf的确是清晰了很多，比之前的jpg，png的展示效果也好。后来大佬写了一个pdf转SVG的工具，效果就更加的棒了，感觉就像是从240P到了1080P。</p>
<h2 id="本次研究"><a href="#本次研究" class="headerlink" title="本次研究"></a>本次研究</h2><p>上次的jacob的使用时，直接找到了一篇博客，把里面的内容拿了过来使用，并未仔细研究方法。</p>
<p>这次是需要将excel转换成pdf。一开始翻阅了一下jacob的源码，发现里面是调用的office的公开api。就去阅读了一下微软官网的文档<a href="https://docs.microsoft.com/zh-cn/office/vba/api/word.document.exportasfixedformat" target="_blank" rel="noopener">https://docs.microsoft.com/zh-cn/office/vba/api/word.document.exportasfixedformat</a> 。</p>
<h3 id="word转pdf"><a href="#word转pdf" class="headerlink" title="word转pdf"></a>word转pdf</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private static boolean word2PDF(String inputFile, String pdfFile) &#123;</span><br><span class="line">        int wdFormatPDF = 17;// word2pdf</span><br><span class="line">        ComThread.InitSTA();</span><br><span class="line">        ActiveXComponent app = null;</span><br><span class="line">        Dispatch doc = null;</span><br><span class="line">        Timer timer = new Timer();</span><br><span class="line">        timer.schedule(new TimerTask() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                if (!isOpen) &#123;</span><br><span class="line">                    try &#123;</span><br><span class="line">                        Runtime.getRuntime().exec(&quot;taskkill /F /IM wpp.exe /T&quot;);</span><br><span class="line">                        Runtime.getRuntime().exec(&quot;taskkill /F /IM wps*.exe /T&quot;);</span><br><span class="line">                    &#125; catch (Exception e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125; finally &#123;</span><br><span class="line">                        // 释放资源</span><br><span class="line">                        ComThread.Release();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;, 10000);</span><br><span class="line">        try &#123;</span><br><span class="line">            app = new ActiveXComponent(&quot;Word.Application&quot;);// 创建一个word对象</span><br><span class="line">            app.setProperty(&quot;Visible&quot;, new Variant(false)); // 不可见打开word</span><br><span class="line">            app.setProperty(&quot;AutomationSecurity&quot;, new Variant(3)); // 禁用宏</span><br><span class="line">            Dispatch docs = app.getProperty(&quot;Documents&quot;).toDispatch();// 获取文挡属性</span><br><span class="line">            // 调用Documents对象中Open方法打开文档，并返回打开的文档对象Document</span><br><span class="line">            doc = Dispatch.call(docs, &quot;Open&quot;, inputFile, false, true).toDispatch();</span><br><span class="line">            isOpen = true;</span><br><span class="line">            // 调用Document对象的SaveAs方法，将文档保存为pdf格式</span><br><span class="line">            Dispatch.call(doc, &quot;SaveAs&quot;, pdfFile, wdFormatPDF);// word保存为pdf格式宏，值为17</span><br><span class="line">            // word保存为pdf格式宏，值为17</span><br><span class="line">            return true;</span><br><span class="line">        &#125; catch (Exception e) &#123;</span><br><span class="line">            log.error(&quot;========Error:文档转换失败：&quot; + e.getMessage(), e);</span><br><span class="line">        &#125; finally &#123;</span><br><span class="line">            Dispatch.call(doc, &quot;Close&quot;, false);</span><br><span class="line">            if (app != null) &#123;</span><br><span class="line">                app.invoke(&quot;Quit&quot;, new Variant[]&#123;&#125;);</span><br><span class="line">            &#125;</span><br><span class="line">            timer.cancel();</span><br><span class="line">            ComThread.Release();</span><br><span class="line">            ComThread.quitMainSTA();</span><br><span class="line">        &#125;</span><br><span class="line">        return false;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>这里是word转换pdf，其中的定时器<strong>Timer.schedule</strong>是为了解决一种bug。曾经出现过打开文件失败后卡住进程，导致后台开启了很多wps占用内存。暂时没有找到原因，直接用结束进程的方式来解决了。</p>
<h3 id="excel转pdf"><a href="#excel转pdf" class="headerlink" title="excel转pdf"></a>excel转pdf</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private static boolean excel2PDF(String inputFile, String pdfFile) &#123;</span><br><span class="line">        ComThread.InitSTA();</span><br><span class="line">        ActiveXComponent app = null;</span><br><span class="line">        Timer timer = new Timer();</span><br><span class="line">        timer.schedule(new TimerTask() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                if (!isOpen) &#123;</span><br><span class="line">                    try &#123;</span><br><span class="line">                        Runtime.getRuntime().exec(&quot;taskkill /F /IM wpp.exe /T&quot;);</span><br><span class="line">                        Runtime.getRuntime().exec(&quot;taskkill /F /IM wps*.exe /T&quot;);</span><br><span class="line">                    &#125; catch (Exception e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125; finally &#123;</span><br><span class="line">                        // 释放资源</span><br><span class="line">                        ComThread.Release();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;, 10000);</span><br><span class="line">        try &#123;</span><br><span class="line">            // 创建一个excel对象</span><br><span class="line">            app = new ActiveXComponent(&quot;Excel.Application&quot;);</span><br><span class="line">            // 不可见打开</span><br><span class="line">            app.setProperty(&quot;Visible&quot;, new Variant(false));</span><br><span class="line">            // 禁用宏</span><br><span class="line">            app.setProperty(&quot;AutomationSecurity&quot;, new Variant(3));</span><br><span class="line">            // 获得excel中所有打开的文档,返回documents对象</span><br><span class="line">            Dispatch docs = app.getProperty(&quot;Workbooks&quot;).toDispatch();</span><br><span class="line">//            调用Documents对象中Open方法打开文档，并返回打开的文档对象Document</span><br><span class="line">            Dispatch excel = Dispatch</span><br><span class="line">                .invoke(docs, &quot;Open&quot;, Dispatch.Method,</span><br><span class="line">                    new Object[]&#123;inputFile, new Variant(false), new Variant(false)&#125;, new int[9])</span><br><span class="line">                .toDispatch();</span><br><span class="line">            //获得一个sheets集合</span><br><span class="line">            Dispatch currentSheet = Dispatch.get(excel, &quot;Sheets&quot;).toDispatch();</span><br><span class="line">            //获得集合数量</span><br><span class="line">            int count = Integer.valueOf(Dispatch.get(currentSheet, &quot;Count&quot;).getInt());</span><br><span class="line">            for (int i = 1; i &lt;= count; i++) &#123;</span><br><span class="line">                //根据表格下表获得工作表</span><br><span class="line">                Dispatch sheetOne = Dispatch</span><br><span class="line">                    .invoke(currentSheet, &quot;Item&quot;, Dispatch.Get, new Object[]&#123;new Variant(i)&#125;,</span><br><span class="line">                        new int[1]).getDispatch();</span><br><span class="line">                //获得工作表页面属性设置</span><br><span class="line">                Dispatch PageSetup = Dispatch.get(sheetOne, &quot;PageSetup&quot;).toDispatch();</span><br><span class="line">                //更改页面设置为取消</span><br><span class="line">                Dispatch.put(PageSetup, &quot;Zoom&quot;, false);</span><br><span class="line">                //更改打印方向为横向（只有1.87类型是excel）</span><br><span class="line">                Dispatch.put(PageSetup, &quot;Orientation&quot;, 2);</span><br><span class="line">            &#125;</span><br><span class="line">            // 转换格式 0=标准 (生成的PDF图片不会变模糊) 1=最小文件(生成的PDF图片糊的一塌糊涂)</span><br><span class="line">            Dispatch</span><br><span class="line">                .invoke(excel, &quot;ExportAsFixedFormat&quot;, Dispatch.Method, new Object[]&#123;new Variant(0),</span><br><span class="line">                    // PDF格式=0</span><br><span class="line">                    pdfFile, new Variant(0)</span><br><span class="line">                &#125;, new int[1]);</span><br><span class="line">            isOpen = true;</span><br><span class="line">            //关闭</span><br><span class="line">            Dispatch.call(excel, &quot;Close&quot;, false);</span><br><span class="line">            return true;</span><br><span class="line">        &#125; catch (Exception e) &#123;</span><br><span class="line">            log.error(&quot;========Error:文档转换失败：&quot; + e.getMessage(), e);</span><br><span class="line">        &#125; finally &#123;</span><br><span class="line">            if (app != null) &#123;</span><br><span class="line">                app.invoke(&quot;Quit&quot;, new Variant[]&#123;&#125;);</span><br><span class="line">            &#125;</span><br><span class="line">            timer.cancel();</span><br><span class="line">            ComThread.Release();</span><br><span class="line">            ComThread.quitMainSTA();</span><br><span class="line">        &#125;</span><br><span class="line">        return false;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>这里是excel转pdf。打开文件的方式还是和word一致的，调用<strong>AutomationSecurity</strong>属性为编程方式打开文件安全模式。</p>
<p>再通过<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Dispatch excel = Dispatch</span><br><span class="line">                .invoke(docs, &quot;Open&quot;, Dispatch.Method,</span><br><span class="line">                    new Object[]&#123;inputFile, new Variant(false), new Variant(false)&#125;, new int[9])</span><br><span class="line">                .toDispatch();</span><br></pre></td></tr></table></figure></p>
<p>语句调用<strong>Open</strong>方法，填入参数<strong>new Object[]{inputFile, new Variant(false), new Variant(false)}</strong>，获得工作薄。</p>
<p>因为excel文件会存在多个sheet，所以需要遍历调整每个sheet。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//获得一个sheets集合</span><br><span class="line">Dispatch currentSheet = Dispatch.get(excel, &quot;Sheets&quot;).toDispatch();</span><br></pre></td></tr></table></figure></p>
<p><strong>sheets</strong>本身是存在<strong>Count</strong>数量属性，调用get方法即可。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Dispatch.get(currentSheet, &quot;Count&quot;).getInt()</span><br></pre></td></tr></table></figure></p>
<p>之后可以用<strong>Item</strong>从集合中通过下标检索到一个对象。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Dispatch sheetOne = Dispatch.invoke(currentSheet, &quot;Item&quot;, Dispatch.Get, new Object[]&#123;new Variant(i)&#125;, new int[1]).getDispatch();</span><br></pre></td></tr></table></figure></p>
<p>业务需要原因，将打印区域取消，纸张横向。但是首先要得到单个sheet的属性才能修改。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//获得工作表页面属性设置</span><br><span class="line">                Dispatch PageSetup = Dispatch.get(sheetOne, &quot;PageSetup&quot;).toDispatch();</span><br><span class="line">                //更改页面设置为取消</span><br><span class="line">                Dispatch.put(PageSetup, &quot;Zoom&quot;, false);</span><br><span class="line">                //更改打印方向为横向（只有1.87类型是excel）</span><br><span class="line">                Dispatch.put(PageSetup, &quot;Orientation&quot;, 2);</span><br></pre></td></tr></table></figure></p>
<p>完成属性调整后，就可以正常使用<strong>ExportAsFixedFormat</strong>方法转换了。此处的参数需要看一下文档，0是表示不压缩转换。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Dispatch.invoke(excel, &quot;ExportAsFixedFormat&quot;, Dispatch.Method, new Object[]&#123;new Variant(0),pdfFile, new Variant(0)&#125;, new int[1]);</span><br></pre></td></tr></table></figure></p>
<p>完成后正常关闭即可。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>程序员还是需要不断的学习，学习方法对了，可以快速成长。学习方法不对，很有可能原地踏步。</p>
]]></content>
      <tags>
        <tag>office</tag>
      </tags>
  </entry>
  <entry>
    <title>jar包冲突后的处理过程</title>
    <url>/2020/09/26/jar%E5%8C%85%E5%86%B2%E7%AA%81%E5%90%8E%E7%9A%84%E5%A4%84%E7%90%86%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="jar包冲突后的处理过程"><a href="#jar包冲突后的处理过程" class="headerlink" title="jar包冲突后的处理过程"></a>jar包冲突后的处理过程</h1><blockquote>
<p>记录一下发现 jar 包冲突后，怎么用 idea 来排查解决</p>
</blockquote>
<a id="more"></a>
<h2 id="错误"><a href="#错误" class="headerlink" title="错误"></a>错误</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">***************************</span><br><span class="line">APPLICATION FAILED TO START</span><br><span class="line">***************************</span><br><span class="line"></span><br><span class="line">Description:</span><br><span class="line"></span><br><span class="line">An attempt was made to call a method that does not exist. The attempt was made from the following location:</span><br><span class="line"></span><br><span class="line">    org.hibernate.snowflake.GenerateLongId.configure(GenerateLongId.java:40)</span><br><span class="line"></span><br><span class="line">The following method did not exist:</span><br><span class="line"></span><br><span class="line">    org.hibernate.snowflake.SnowFlakeIdWorker.getInstance()Lorg/hibernate/snowflake/SnowFlakeIdWorker;</span><br><span class="line"></span><br><span class="line">The method&apos;s class, org.hibernate.snowflake.SnowFlakeIdWorker, is available from the following locations:</span><br><span class="line"></span><br><span class="line">    file:/E:/idea-workspace/web-api/out/production/classes/org/hibernate/snowflake/SnowFlakeIdWorker.class</span><br><span class="line">    jar:file:/E:/idea-workspace/web-api/lib/hibernate-comment-annotation-0.0.2-SNAPSHOT.jar!/org/hibernate/snowflake/SnowFlakeIdWorker.class</span><br><span class="line"></span><br><span class="line">It was loaded from the following location:</span><br><span class="line"></span><br><span class="line">    file:/E:/idea-workspace/web-api/out/production/classes/</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Action:</span><br><span class="line"></span><br><span class="line">Correct the classpath of your application so that it contains a single, compatible version of org.hibernate.snowflake.SnowFlakeIdWorker</span><br></pre></td></tr></table></figure>
<p>通过错误信息我们可以看出来，是引入的 jar 包和我自己本地配置的发生了同类冲突。需要解决的就是找到引入的地方，并且屏蔽。</p>
<h2 id="idea-jar包关联查询"><a href="#idea-jar包关联查询" class="headerlink" title="idea jar包关联查询"></a>idea jar包关联查询</h2><p>首先说明我用的是 gradle 管理 jar 包。在右侧的 gradle 工具中，可以查看整体的依赖关系图。<br>其中有 <code>Tasks-&gt;help-&gt;dependencies</code>这个工具。运行后，就会输出 jar 包依赖的树状图。根据树状图就可以找到是那两个 jar 包中的依赖冲突。</p>
<p><img src="/2020/09/26/jar包冲突后的处理过程/b1.png" alt="b1.png"></p>
<h2 id="屏蔽无关jar包"><a href="#屏蔽无关jar包" class="headerlink" title="屏蔽无关jar包"></a>屏蔽无关jar包</h2><p>据别的博客说，可用以下这种方式移除<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">compile(configuration: &apos;core&apos;, group: &apos;toolbox&apos;, name: &apos;web-tools&apos;, version: &apos;2.0.+&apos;) &#123;</span><br><span class="line">        exclude(module: &apos;spring&apos;)</span><br><span class="line">        exclude(module: &apos;commons-codec&apos;)</span><br><span class="line">        exclude(module: &apos;slf4j&apos;)</span><br><span class="line">        exclude(module: &apos;servlet-api&apos;)</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>但是我用这种方式发现无效，只好用这种全局屏蔽方式移除<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">configurations &#123;</span><br><span class="line">    compileOnly &#123;</span><br><span class="line">        extendsFrom annotationProcessor</span><br><span class="line">    &#125;</span><br><span class="line">    all*.exclude group:&apos;javax&apos;, module: &apos;javaee-api&apos;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><strong>注意：</strong>这个方法是因为我是本地导入了一个 jar 包和 gradle 管理的发生了冲突。<br>成功解决。</p>
]]></content>
      <tags>
        <tag>idea</tag>
      </tags>
  </entry>
  <entry>
    <title>java连接hbase摸索过程</title>
    <url>/2019/06/28/java%E8%BF%9E%E6%8E%A5hbase%E6%91%B8%E7%B4%A2%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="java连接hbase摸索过程"><a href="#java连接hbase摸索过程" class="headerlink" title="java连接hbase摸索过程"></a>java连接hbase摸索过程</h1><blockquote>
<p>记录自己摸索使用javaApi操作hbase的过程</p>
</blockquote>
<a id="more"></a>
<h2 id="连接hbase"><a href="#连接hbase" class="headerlink" title="连接hbase"></a>连接hbase</h2><p>在windows系统中的<strong>C:\Windows\System32\drivers\etc</strong>中找到<strong>hosts</strong>文件，添加<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">192.168.99.100 myhbase</span><br></pre></td></tr></table></figure></p>
<p><strong>注意：</strong> 192.168.99.100为我本地配置的虚拟机ip，myhbase是我的容器启动时的别名</p>
<p>在虚拟机系统中也同样更改hosts文件<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vim /etc/hosts</span><br></pre></td></tr></table></figure></p>
<p>添加 虚拟机ip 别名<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">192.168.99.100  myhbase</span><br></pre></td></tr></table></figure></p>
<h2 id="启动已经配置好的hbase容器"><a href="#启动已经配置好的hbase容器" class="headerlink" title="启动已经配置好的hbase容器"></a>启动已经配置好的hbase容器</h2><p>先用命令查看已经配置好的容器<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker ps -a</span><br></pre></td></tr></table></figure></p>
<p>然后在用命令启动容器<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker start b8956b1ba7ec</span><br></pre></td></tr></table></figure></p>
<p><strong>container ID</strong>这里是用上一条命令查出来的要启动的容器id字段，例如我的容器id为：b8956b1ba7ec<br>停止容器的命令为<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker stop b8956b1ba7ec</span><br></pre></td></tr></table></figure></p>
<h2 id="未完成"><a href="#未完成" class="headerlink" title="未完成"></a>未完成</h2><p>导入jar包：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">compile (group: &apos;org.apache.hbase&apos;, name: &apos;hbase-client&apos;, version: &apos;2.1.1&apos;)&#123;</span><br><span class="line">        exclude module: &apos;servlet-api&apos;</span><br><span class="line">    &#125;</span><br><span class="line">    compile (group: &apos;org.apache.hbase&apos;, name: &apos;hbase&apos;, version: &apos;2.1.1&apos;, ext: &apos;pom&apos;)</span><br></pre></td></tr></table></figure></p>
<p><strong>这里附加说明：</strong>本来我是导入了1.3.1版本的，后来进入容器查看了docker里面的hbase版本后（进入容器后命令为：<strong>hbase version</strong>）。就改为和镜像版本一致。</p>
<p>编写测试类：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">import java.io.IOException;</span><br><span class="line">import lombok.extern.slf4j.Slf4j;</span><br><span class="line">import org.apache.hadoop.conf.Configuration;</span><br><span class="line">import org.apache.hadoop.hbase.MasterNotRunningException;</span><br><span class="line">import org.apache.hadoop.hbase.TableName;</span><br><span class="line">import org.apache.hadoop.hbase.ZooKeeperConnectionException;</span><br><span class="line">import org.apache.hadoop.hbase.client.Admin;</span><br><span class="line">import org.apache.hadoop.hbase.client.ConnectionFactory;</span><br><span class="line">import org.apache.hadoop.hbase.client.TableDescriptor;</span><br><span class="line">import org.apache.hadoop.hbase.client.TableDescriptorBuilder;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * TODO</span><br><span class="line"> *</span><br><span class="line"> * @author zhangshuai</span><br><span class="line"> * @date 2019/6/24 15:15</span><br><span class="line"> */</span><br><span class="line"></span><br><span class="line">@Slf4j</span><br><span class="line">public class HbaseService &#123;</span><br><span class="line"></span><br><span class="line">    Admin admin;</span><br><span class="line">    Configuration conf;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 构造函数 加载配置</span><br><span class="line">     */</span><br><span class="line">    public HbaseService() &#123;</span><br><span class="line">        conf = new Configuration();</span><br><span class="line">        conf.set(&quot;hbase.zookeeper.quorum&quot;, &quot;192.168.99.100:2181&quot;);</span><br><span class="line">        </span><br><span class="line">        try &#123;</span><br><span class="line">            admin = ConnectionFactory.createConnection(conf).getAdmin();</span><br><span class="line">        &#125; catch (MasterNotRunningException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; catch (ZooKeeperConnectionException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; catch (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 创建一张表</span><br><span class="line">     *</span><br><span class="line">     * @param tableName</span><br><span class="line">     * @param column</span><br><span class="line">     * @throws Exception</span><br><span class="line">     */</span><br><span class="line">    public void createTable(String tableName, String column) throws Exception &#123;</span><br><span class="line">        if (admin.tableExists(TableName.valueOf(tableName))) &#123;</span><br><span class="line">            System.out.println(tableName + &quot;表已经存在！&quot;);</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            TableDescriptor tableDesc = TableDescriptorBuilder.newBuilder(TableName.valueOf(tableName))</span><br><span class="line">                .setColumnFamily(ColumnFamilyDescriptorBuilder.of(&quot;t1&quot;)).build();</span><br><span class="line">            admin.createTable(tableDesc);</span><br><span class="line">            System.out.println(tableName + &quot;表创建成功！&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) throws Exception &#123;</span><br><span class="line">        HbaseService hbase = new HbaseService();</span><br><span class="line">        hbase.createTable(&quot;t1&quot;,&quot;cf&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>此次注意：192.168.99.100是我的docker地址。</p>
<h3 id="错误内容一"><a href="#错误内容一" class="headerlink" title="错误内容一"></a>错误内容一</h3><p><strong>Connection refused: no further information:</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Caused by: org.apache.hbase.thirdparty.io.netty.channel.AbstractChannel$AnnotatedConnectException: Connection refused: no further information: myhbase/192.168.99.100:16020</span><br></pre></td></tr></table></figure></p>
<p><strong>Not trying to connect to</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">DEBUG org.apache.hadoop.hbase.ipc.AbstractRpcClient - Not trying to connect to myhbase/192.168.99.100:16020 this server is in the failed servers list</span><br></pre></td></tr></table></figure></p>
<h4 id="解决历程："><a href="#解决历程：" class="headerlink" title="解决历程："></a>解决历程：</h4><p>首先我检查了本机的hosts文件，文件路径在<strong>C:\Windows\System32\drivers\etc</strong>。发现已经配置好了192.168.99.100 myhbase。</p>
<p>再检查了docker的hosts文件，命令为：<strong>vim /etc/hosts</strong>。增加了192.168.99.100 myhbase。<br>发现依旧是这个错误。</p>
<p><strong>更新一下：</strong>docker里的hosts文件映射路径改成了容器ip。命令：docker inspect 容器名称或 id。<br>hosts文件内容修改为172.17.0.2 myhbase</p>
<p><img src="/2019/06/28/java连接hbase摸索过程/b3.png" alt=""></p>
<p>依旧报错，还是不行。</p>
<p>后来仔细研究发现，是容器端口映射问题。</p>
<p>本机可以访问<a href="http://192.168.99.100:16010/master-status" target="_blank" rel="noopener">http://192.168.99.100:16010/master-status</a> 。在页面上发现，镜像2.1.1版本的默认Region Servers端口为：16020</p>
<p><img src="/2019/06/28/java连接hbase摸索过程/b1.png" alt=""></p>
<p>可以在本页面往下拉发现hbase版本号</p>
<p><img src="/2019/06/28/java连接hbase摸索过程/b2.png" alt=""></p>
<p>将端口映射改为16020和16030就可以解决了上面的这个连接不上的错误。接下来，就是对hbase的api操作了。</p>
]]></content>
      <tags>
        <tag>hbase</tag>
      </tags>
  </entry>
  <entry>
    <title>java虚拟机的锁优化技术</title>
    <url>/2020/03/21/java%E8%99%9A%E6%8B%9F%E6%9C%BA%E7%9A%84%E9%94%81%E4%BC%98%E5%8C%96%E6%8A%80%E6%9C%AF/</url>
    <content><![CDATA[<h1 id="java虚拟机的锁优化技术"><a href="#java虚拟机的锁优化技术" class="headerlink" title="java虚拟机的锁优化技术"></a>java虚拟机的锁优化技术</h1><blockquote>
<p>这篇文章也是从hollis那里看到的，是一个系列文章之一。其中未转载的文章是因为看了以后不太明白，就没有记录。</p>
</blockquote>
<a id="more"></a>
<h2 id="原文前言"><a href="#原文前言" class="headerlink" title="原文前言"></a>原文前言</h2><h3 id="Synchronized的实现原理"><a href="#Synchronized的实现原理" class="headerlink" title="Synchronized的实现原理"></a>Synchronized的实现原理</h3><p>1、同步方法通过ACC_SYNCHRONIZED关键字隐式的对方法进行加锁。当线程要执行的方法被标注上ACC_SYNCHRONIZED时，需要先获得锁才能执行该方法。《深入理解多线程（一）——Synchronized的实现原理》</p>
<blockquote>
<p>上面这个文章，去除了源码和反编译部分，已经将作者的总结放在了上一篇《从StringBuffer和StringBuilder扩展开的问题》文章中。</p>
</blockquote>
<h3 id="Moniter的实现原理"><a href="#Moniter的实现原理" class="headerlink" title="Moniter的实现原理"></a>Moniter的实现原理</h3><p>2、同步代码块通过monitorenter和monitorexit执行来进行加锁。当线程执行到monitorenter的时候要先获得所锁，才能执行后面的方法。当线程执行到monitorexit的时候则要释放锁。《深入理解多线程（四）—— Moniter的实现原理》</p>
<blockquote>
<p>这篇文章，我将<code>moniter</code>的概念放在了我的那篇文章中。具体的源码以及流程图并没有拿过来。</p>
</blockquote>
<h4 id="原文摘抄"><a href="#原文摘抄" class="headerlink" title="原文摘抄"></a>原文摘抄</h4><p>这里再把原文中的流程复制过来记录一下：</p>
<p>ObjectMonitor中有几个关键属性：</p>
<blockquote>
<p>_owner：指向持有ObjectMonitor对象的线程</p>
<p>_WaitSet：存放处于wait状态的线程队列</p>
<p>_EntryList：存放处于等待锁block状态的线程队列</p>
<p>_recursions：锁的重入次数</p>
<p>_count：用来记录该线程获取锁的次数</p>
</blockquote>
<p>当多个线程同时访问一段同步代码时，首先会进入<code>_EntryList</code>队列中，当某个线程获取到对象的monitor后进入<code>_Owner</code>区域并把monitor中的<code>_owner</code>变量设置为当前线程，同时monitor中的计数器<code>_count</code>加1。即获得对象锁。</p>
<p>若持有monitor的线程调用wait()方法，将释放当前持有的monitor，<code>_owner</code>变量恢复为null，<code>_count</code>自减1，同时该线程进入<code>_WaitSet</code>集合中等待被唤醒。若当前线程执行完毕也将释放monitor(锁)并复位变量的值，以便其他线程进入获取monitor(锁)。如下图所示：<br><img src="/2020/03/21/java虚拟机的锁优化技术/b1.png" alt=""></p>
<p>ObjectMonitor类中提供了几个方法：</p>
<h5 id="获得锁："><a href="#获得锁：" class="headerlink" title="获得锁："></a>获得锁：</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ATTR ObjectMonitor::enter(TRAPS) &#123;</span><br><span class="line">  Thread * <span class="keyword">const</span> Self = THREAD ;</span><br><span class="line">  <span class="keyword">void</span> * cur ;</span><br><span class="line">  <span class="comment">//通过CAS尝试把monitor的`_owner`字段设置为当前线程</span></span><br><span class="line">  cur = Atomic::cmpxchg_ptr (Self, &amp;_owner, NULL) ;</span><br><span class="line">  <span class="comment">//获取锁失败</span></span><br><span class="line">  <span class="keyword">if</span> (cur == NULL) &#123;</span><br><span class="line">  	 <span class="keyword">assert</span> (_recursions == <span class="number">0</span>   , <span class="string">"invariant"</span>) ;</span><br><span class="line">     <span class="keyword">assert</span> (_owner      == Self, <span class="string">"invariant"</span>) ;</span><br><span class="line">     <span class="comment">// CONSIDER: set or assert OwnerIsThread == 1</span></span><br><span class="line">     <span class="keyword">return</span> ;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 如果旧值和当前线程一样，说明当前线程已经持有锁，此次为重入，_recursions自增，并获得锁。</span></span><br><span class="line">  <span class="keyword">if</span> (cur == Self) &#123; </span><br><span class="line">     <span class="comment">// TODO-<span class="doctag">FIXME:</span> check for integer overflow!  BUGID 6557169.</span></span><br><span class="line">     _recursions ++ ;</span><br><span class="line">     <span class="keyword">return</span> ;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 如果当前线程是第一次进入该monitor，设置_recursions为1，_owner为当前线程</span></span><br><span class="line">  <span class="keyword">if</span> (Self-&gt;is_lock_owned ((address)cur)) &#123; </span><br><span class="line">    <span class="keyword">assert</span> (_recursions == <span class="number">0</span>, <span class="string">"internal state error"</span>);</span><br><span class="line">    _recursions = <span class="number">1</span> ;</span><br><span class="line">    <span class="comment">// Commute owner from a thread-specific on-stack BasicLockObject address to</span></span><br><span class="line">    <span class="comment">// a full-fledged "Thread *".</span></span><br><span class="line">    _owner = Self ;</span><br><span class="line">    OwnerIsThread = <span class="number">1</span> ;</span><br><span class="line">    <span class="keyword">return</span> ;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 省略部分代码。</span></span><br><span class="line">  <span class="comment">// 通过自旋执行ObjectMonitor::EnterI方法等待锁的释放</span></span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">      jt-&gt;set_suspend_equivalent();</span><br><span class="line">      <span class="comment">// cleared by handle_special_suspend_equivalent_condition()</span></span><br><span class="line">      <span class="comment">// or java_suspend_self()</span></span><br><span class="line"></span><br><span class="line">      EnterI (THREAD) ;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (!ExitSuspendEquivalent(jt)) <span class="keyword">break</span> ;</span><br><span class="line"></span><br><span class="line">      <span class="comment">//</span></span><br><span class="line">      <span class="comment">// We have acquired the contended monitor, but while we were</span></span><br><span class="line">      <span class="comment">// waiting another thread suspended us. We don't want to enter</span></span><br><span class="line">      <span class="comment">// the monitor while suspended because that would surprise the</span></span><br><span class="line">      <span class="comment">// thread that suspended us.</span></span><br><span class="line">      <span class="comment">//</span></span><br><span class="line">          _recursions = <span class="number">0</span> ;</span><br><span class="line">      _succ = NULL ;</span><br><span class="line">      exit (Self) ;</span><br><span class="line"></span><br><span class="line">      jt-&gt;java_suspend_self();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/2020/03/21/java虚拟机的锁优化技术/b2.png" alt="获得锁"></p>
<h5 id="释放锁："><a href="#释放锁：" class="headerlink" title="释放锁："></a>释放锁：</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ATTR ObjectMonitor::exit(TRAPS) &#123;</span><br><span class="line">   Thread * Self = THREAD ;</span><br><span class="line">   <span class="comment">//如果当前线程不是Monitor的所有者</span></span><br><span class="line">   <span class="keyword">if</span> (THREAD != _owner) &#123; </span><br><span class="line">     <span class="keyword">if</span> (THREAD-&gt;is_lock_owned((address) _owner)) &#123; <span class="comment">// </span></span><br><span class="line">       <span class="comment">// Transmute _owner from a BasicLock pointer to a Thread address.</span></span><br><span class="line">       <span class="comment">// We don't need to hold _mutex for this transition.</span></span><br><span class="line">       <span class="comment">// Non-null to Non-null is safe as long as all readers can</span></span><br><span class="line">       <span class="comment">// tolerate either flavor.</span></span><br><span class="line">       <span class="keyword">assert</span> (_recursions == <span class="number">0</span>, <span class="string">"invariant"</span>) ;</span><br><span class="line">       _owner = THREAD ;</span><br><span class="line">       _recursions = <span class="number">0</span> ;</span><br><span class="line">       OwnerIsThread = <span class="number">1</span> ;</span><br><span class="line">     &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">       <span class="comment">// <span class="doctag">NOTE:</span> we need to handle unbalanced monitor enter/exit</span></span><br><span class="line">       <span class="comment">// in native code by throwing an exception.</span></span><br><span class="line">       <span class="comment">// <span class="doctag">TODO:</span> Throw an IllegalMonitorStateException ?</span></span><br><span class="line">       TEVENT (Exit - Throw IMSX) ;</span><br><span class="line">       <span class="keyword">assert</span>(<span class="keyword">false</span>, <span class="string">"Non-balanced monitor enter/exit!"</span>);</span><br><span class="line">       <span class="keyword">if</span> (<span class="keyword">false</span>) &#123;</span><br><span class="line">          THROW(vmSymbols::java_lang_IllegalMonitorStateException());</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span>;</span><br><span class="line">     &#125;</span><br><span class="line">   &#125;</span><br><span class="line">    <span class="comment">// 如果_recursions次数不为0.自减</span></span><br><span class="line">   <span class="keyword">if</span> (_recursions != <span class="number">0</span>) &#123;</span><br><span class="line">     _recursions--;        <span class="comment">// this is simple recursive enter</span></span><br><span class="line">     TEVENT (Inflated exit - recursive) ;</span><br><span class="line">     <span class="keyword">return</span> ;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">//省略部分代码，根据不同的策略（由QMode指定），从cxq或EntryList中获取头节点，通过ObjectMonitor::ExitEpilog方法唤醒该节点封装的线程，唤醒操作最终由unpark完成。</span></span><br></pre></td></tr></table></figure>
<p><img src="/2020/03/21/java虚拟机的锁优化技术/b3.png" alt=""></p>
<h4 id="HotSpot虚拟机中Moniter的的加锁以及解锁的原理总结"><a href="#HotSpot虚拟机中Moniter的的加锁以及解锁的原理总结" class="headerlink" title="HotSpot虚拟机中Moniter的的加锁以及解锁的原理总结"></a>HotSpot虚拟机中Moniter的的加锁以及解锁的原理总结</h4><p>通过这篇文章我们知道了sychronized加锁的时候，会调用objectMonitor的enter方法，解锁的时候会调用exit方法。事实上，只有在JDK1.6之前，synchronized的实现才会直接调用ObjectMonitor的enter和exit，这种锁被称之为重量级锁。为什么说这种方式操作锁很重呢？</p>
<ul>
<li>Java的线程是映射到操作系统原生线程之上的，如果要阻塞或唤醒一个线程就需要操作系统的帮忙，这就要从用户态转换到核心态，因此状态转换需要花费很多的处理器时间，对于代码简单的同步块（如被synchronized修饰的get 或set方法）状态转换消耗的时间有可能比用户代码执行的时间还要长，所以说synchronized是java语言中一个重量级的操纵。</li>
</ul>
<p>所以，在JDK1.6中出现对锁进行了很多的优化，进而出现轻量级锁，偏向锁，锁消除，适应性自旋锁，锁粗化(自旋锁在1.4就有 只不过默认的是关闭的，jdk1.6是默认开启的)，这些操作都是为了在线程之间更高效的共享数据 ，解决竞争问题。后面的文章会继续介绍这几种锁以及他们之间的关系。</p>
<h3 id="Java的对象模型"><a href="#Java的对象模型" class="headerlink" title="Java的对象模型"></a>Java的对象模型</h3><p>3、在HotSpot虚拟机中，使用oop-klass模型来表示对象。每一个Java类，在被JVM加载的时候，JVM会给这个类创建一个instanceKlass，保存在方法区，用来在JVM层表示该Java类。当我们在Java代码中，使用new创建一个对象的时候，JVM会创建一个instanceOopDesc对象，这个对象中包含了对象头以及实例数据。《深入理解多线程（二）—— Java的对象模型》</p>
<blockquote>
<p>这里先略过原文复制，有点没看懂原文作者写的东西，之后找别的资料互相理解后加上。</p>
</blockquote>
<h3 id="java的对象头"><a href="#java的对象头" class="headerlink" title="java的对象头"></a>java的对象头</h3><p>4、对象头中主要包含了GC分代年龄、锁状态标记、哈希码、epoch等信息。对象的状态一共有五种，分别是无锁态、轻量级锁、重量级锁、GC标记和偏向锁。《深入理解多线程（三）—— Java的对象头》</p>
<blockquote>
<p>这里是和上面关联的，我需要阅读一下书。再来补充</p>
</blockquote>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><p>事实上，只有在JDK1.6之前，synchronized的实现才会直接调用ObjectMonitor的enter和exit，这种锁被称之为重量级锁。</p>
<p>高效并发是从JDK 1.5 到 JDK 1.6的一个重要改进，HotSpot虚拟机开发团队在这个版本中花费了很大的精力去对Java中的锁进行优化，如适应性自旋、锁消除、锁粗化、轻量级锁和偏向锁等。这些技术都是为了在线程之间更高效的共享数据，以及解决竞争问题。</p>
<p>本文，主要先来介绍一下自旋、锁消除以及锁粗化等技术。</p>
<p>这里简单说明一下，本文要介绍的这几个概念，以及后面要介绍的轻量级锁和偏向锁，其实对于使用他的开发者来说是屏蔽掉了的，也就是说，作为一个Java开发，你只需要知道你想在加锁的时候使用synchronized就可以了，具体的锁的优化是虚拟机根据竞争情况自行决定的。</p>
<p>也就是说，在JDK 1.5 以后，我们即将介绍的这些概念，都被封装在synchronized中了。</p>
<h3 id="线程状态"><a href="#线程状态" class="headerlink" title="线程状态"></a>线程状态</h3><p>要想把锁说清楚，一个重要的概念不得不提，那就是线程和线程的状态。锁和线程的关系是怎样的呢，举个简单的例子你就明白了。</p>
<p>比如，你今天要去银行办业务，你到了银行之后，要先取一个号，然后你坐在休息区等待叫号，过段时间，广播叫到你的号码之后，会告诉你去哪个柜台办理业务，这时，你拿着你手里的号码，去到对应的柜台，找相应的柜员开始办理业务。当你办理业务的时候，这个柜台和柜台后面的柜员只能为你自己服务。当你办完业务离开之后，广播再喊其他的顾客前来办理业务。<br><img src="/2020/03/21/java虚拟机的锁优化技术/b4.png" alt=""></p>
<blockquote>
<p>这个例子中，每个顾客是一个线程。 柜台前面的那把椅子，就是锁。 柜台后面的柜员，就是共享资源。 你发现无法直接办理业务，要取号等待的过程叫做阻塞。 当你听到叫你的号码的时候，你起身去办业务，这就是唤醒。 当你坐在椅子上开始办理业务的时候，你就获得锁。 当你办完业务离开的时候，你就释放锁。</p>
</blockquote>
<p>对于线程来说，一共有五种状态，分别为：初始状态(New) 、就绪状态(Runnable) 、运行状态(Running) 、阻塞状态(Blocked) 和死亡状态(Dead) 。</p>
<p><img src="/2020/03/21/java虚拟机的锁优化技术/b5.png" alt=""></p>
<blockquote>
<p>想想第一次听说这个概念还是在学校的时候，讲到计算机组成原理</p>
</blockquote>
<h3 id="自旋锁"><a href="#自旋锁" class="headerlink" title="自旋锁"></a>自旋锁</h3><p>在<a href="http://www.hollischuang.com/archives/2030" target="_blank" rel="noopener">深入理解多线程（四）—— Moniter的实现原理</a>文章中，我们介绍的synchronized的实现方式中使用Monitor进行加锁，这是一种互斥锁，为了表示他对性能的影响我们称之为重量级锁。</p>
<p>这种互斥锁在互斥同步上对性能的影响很大，Java的线程是映射到操作系统原生线程之上的，如果要阻塞或唤醒一个线程就需要操作系统的帮忙，这就要从用户态转换到内核态，因此状态转换需要花费很多的处理器时间。</p>
<p>就像去银行办业务的例子，当你来到银行，发现柜台前面都有人的时候，你需要取一个号，然后再去等待区等待，一直等待被叫号。这个过程是比较浪费时间的，那么有没有什么办法改进呢？</p>
<p>有一种比较好的设计，那就是银行提供自动取款机，当你去银行取款的时候，你不需要取号，不需要去休息区等待叫号，你只需要找到一台取款机，排在其他人后面等待取款就行了。<br><img src="/2020/03/21/java虚拟机的锁优化技术/b6.png" alt="形象的图"></p>
<p>之所以能这样做，是因为取款的这个过程相比较之下是比较节省时间的。如果所有人去银行都只取款，或者办理业务的时间都很短的话，那也就可以不需要取号，不需要去单独的休息区，不需要听叫号，也不需要再跑到对应的柜台了。</p>
<p>而在程序中，Java虚拟机的开发工程师们在分析过大量数据后发现：共享数据的锁定状态一般只会持续很短的一段时间，为了这段时间去挂起和恢复线程其实并不值得。</p>
<p>如果物理机上有多个处理器，可以让多个线程同时执行的话。我们就可以让后面来的线程“稍微等一下”，但是并不放弃处理器的执行时间，看看持有锁的线程会不会很快释放锁。这个“稍微等一下”的过程就是自旋。</p>
<p>自旋锁在JDK 1.4中已经引入，在JDK 1.6中默认开启。</p>
<p>很多人在对于自旋锁的概念不清楚的时候可能会有以下疑问：这么听上去，自旋锁好像和阻塞锁没啥区别，反正都是等着嘛。</p>
<ul>
<li><p>对于去银行取钱的你来说，站在取款机面前等待和去休息区等待叫号有一个很大的区别：</p>
<ul>
<li><p>那就是如果你在休息区等待，这段时间你什么都不需要管，随意做自己的事情，等着被唤醒就行了。</p>
</li>
<li><p>如果你在取款机面前等待，那么你需要时刻关注自己前面还有没有人，因为没人会唤醒你。</p>
</li>
<li><p>很明显，这种直接去取款机前面排队取款的效率是比较高。</p>
</li>
</ul>
</li>
</ul>
<p>所以呢，自旋锁和阻塞锁最大的区别就是，到底要不要放弃处理器的执行时间。对于阻塞锁和自旋锁来说，都是要等待获得共享资源。但是阻塞锁是放弃了CPU时间，进入了等待区，等待被唤醒。而自旋锁是一直“自旋”在那里，时刻的检查共享资源是否可以被访问。</p>
<p>由于自旋锁只是将当前线程不停地执行循环体，不进行线程状态的改变，所以响应速度更快。但当线程数不停增加时，性能下降明显，因为每个线程都需要执行，占用CPU时间。如果线程竞争不激烈，并且保持锁的时间段。适合使用自旋锁。</p>
<h3 id="锁消除"><a href="#锁消除" class="headerlink" title="锁消除"></a>锁消除</h3><p>除了自旋锁之后，JDK中还有一种锁的优化被称之为锁消除。还拿去银行取钱的例子说。</p>
<p>你去银行取钱，所有情况下都需要取号，并且等待吗？其实是不用的，当银行办理业务的人不多的时候，可能根本不需要取号，直接走到柜台前面办理业务就好了。<br><img src="/2020/03/21/java虚拟机的锁优化技术/b7.png" alt="形象的图"><br>能这么做的前提是，没有人和你抢着办业务。</p>
<p>上面的这种例子，在锁优化中被称作“锁消除”，是JIT编译器对内部锁的具体实现所做的一种优化。</p>
<p>在动态编译同步块的时候，JIT编译器可以借助一种被称为逃逸分析（Escape Analysis）的技术来判断同步块所使用的锁对象是否只能够被一个线程访问而没有被发布到其他线程。</p>
<p>如果同步块所使用的锁对象通过这种分析被证实只能够被一个线程访问，那么JIT编译器在编译这个同步块的时候就会取消对这部分代码的同步。</p>
<p>如以下代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Object hollis = <span class="keyword">new</span> Object();</span><br><span class="line">    <span class="keyword">synchronized</span>(hollis) &#123;</span><br><span class="line">        System.out.println(hollis);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>代码中对hollis这个对象进行加锁，但是hollis对象的生命周期只在f()方法中，并不会被其他线程所访问到，所以在JIT编译阶段就会被优化掉。优化成：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Object hollis = <span class="keyword">new</span> Object();</span><br><span class="line">    System.out.println(hollis);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>这里，可能有读者会质疑了，代码是程序员自己写的，程序员难道没有能力判断要不要加锁吗？就像以上代码，完全没必要加锁，有经验的开发者一眼就能看的出来的。其实道理是这样，但是还是有可能有疏忽，比如我们经常在代码中使用StringBuffer作为局部变量，而StringBuffer中的append是线程安全的，有synchronized修饰的，这种情况开发者可能会忽略。这时候，JIT就可以帮忙优化，进行锁消除。</p>
</blockquote>
<p>总之，读者只需要知道，在使用synchronized的时候，如果JIT经过逃逸分析之后发现并无线程安全问题的话，就会做锁消除。</p>
<h3 id="锁粗化"><a href="#锁粗化" class="headerlink" title="锁粗化"></a>锁粗化</h3><p>很多人都知道，在代码中，需要加锁的时候，我们提倡尽量减小锁的粒度，这样可以避免不必要的阻塞。</p>
<p>这也是很多人原因是用同步代码块来代替同步方法的原因，因为往往他的粒度会更小一些，这其实是很有道理的。</p>
<p>还是我们去银行柜台办业务，最高效的方式是你坐在柜台前面的时候，只办和银行相关的事情。如果这个时候，你拿出手机，接打几个电话，问朋友要往哪个账户里面打钱，这就很浪费时间了。最好的做法肯定是提前准备好相关资料，在办理业务时直接办理就好了。</p>
<p>加锁也一样，把无关的准备工作放到锁外面，锁内部只处理和并发相关的内容。这样有助于提高效率。</p>
<p>那么，这和锁粗化有什么关系呢？可以说，大部分情况下，减小锁的粒度是很正确的做法，只有一种特殊的情况下，会发生一种叫做锁粗化的优化。</p>
<p>就像你去银行办业务，你为了减少每次办理业务的时间，你把要办的五个业务分成五次去办理，这反而适得其反了。因为这平白的增加了很多你重新取号、排队、被唤醒的时间。</p>
<p>如果在一段代码中连续的对同一个对象反复加锁解锁，其实是相对耗费资源的，这种情况可以适当放宽加锁的范围，减少性能消耗。</p>
<p>当JIT发现一系列连续的操作都对同一个对象反复加锁和解锁，甚至加锁操作出现在循环体中的时候，会将加锁同步的范围扩散（粗化）到整个操作序列的外部。</p>
<p>如以下代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">100000</span>;i++)&#123;  </span><br><span class="line">    <span class="keyword">synchronized</span>(<span class="keyword">this</span>)&#123;  </span><br><span class="line">        <span class="keyword">do</span>();  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>会被粗化成：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span>(<span class="keyword">this</span>)&#123;  </span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">100000</span>;i++)&#123;  </span><br><span class="line">        <span class="keyword">do</span>();  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这其实和我们要求的减小锁粒度并不冲突。减小锁粒度强调的是不要在银行柜台前做准备工作以及和办理业务无关的事情。而锁粗化建议的是，同一个人，要办理多个业务的时候，可以在同一个窗口一次性办完，而不是多次取号多次办理。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>自Java 6/Java 7开始，Java虚拟机对内部锁的实现进行了一些优化。这些优化主要包括锁消除（Lock Elision）、锁粗化（Lock Coarsening）、偏向锁（Biased Locking）以及适应性自旋锁（Adaptive Locking）。这些优化仅在Java虚拟机server模式下起作用（即运行Java程序时我们可能需要在命令行中指定Java虚拟机参数“-server”以开启这些优化）。</p>
<p>本文主要介绍了自旋锁、锁粗化和锁消除的概念。在JIT编译过程中，虚拟机会根据情况使用这三种技术对锁进行优化，目的是减少锁的竞争，提升性能。</p>
<p>其实原本不想写这么多，因为很容易让我自己在看的时候找不到重点。但是现在我还是需要这些来想明白整个过程。大概之后理解的深了可以精简一下吧</p>
<blockquote>
<p>题外话</p>
<p>记录一下面试题</p>
<p> 小米二面（1h30min）<br>1.怼项目，各种细节，扩展各个功能怎么做？<br>2.redis有哪几种数据类型，各有什么特点，各有哪些应用场景<br>3.redis的持久化哪几种？分别讲一下RDB和AOF的原理和AOF重写的过程<br>4.redis分布式锁哪几种？分别讲一下setnx和redlock原理？setnx怎么释放锁？释放锁的LUA脚本会写吗？<br>5.redis的主从复制了解吗？<br>6.mysql的隔离级别哪几种？分别讲一下原理<br>7.讲一下mysql的索引？B+树和B树有什么区别？为什么要用B+树而不用B树？B+树的查询时间复杂度多少<br>8.并发和并行的区别<br>9.volatile原理？他可以同步操作吗？它用在什么地方？<br>10.了解什么消息队列？讲一下rabbitmq的原理？怎么保证它消息不丢失和不重复消费？<br>11.讲一下分布式事务<br>\12. zookeeper了解吗？讲一下它的原理？哪些应用场景？它怎么实现分布式锁<br>13.讲一下2pc、3pc、CAP理论、BASE理论、paxos算法、raft算法<br>14.写代码：单链表交换相邻的两个节点 </p>
</blockquote>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>jpa的一次自定义find方法的使用过程</title>
    <url>/2019/05/17/jpa%E7%9A%84%E4%B8%80%E6%AC%A1%E8%87%AA%E5%AE%9A%E4%B9%89find%E6%96%B9%E6%B3%95%E7%9A%84%E4%BD%BF%E7%94%A8%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="一次jpa自定义查询方法的使用尝试过程"><a href="#一次jpa自定义查询方法的使用尝试过程" class="headerlink" title="一次jpa自定义查询方法的使用尝试过程"></a>一次jpa自定义查询方法的使用尝试过程</h1><h2 id="项目需求"><a href="#项目需求" class="headerlink" title="项目需求"></a>项目需求</h2><p>目前客户有一个需求：每一个用户想要看到的帖子顺序都不一样，用户可以按照自己的喜好排列帖子顺序，并且可以手动把某个帖子置顶显示。<br>现在项目数据交互使用的框架是spring-boot-starter-data-jpa。之前因为项目的工期很赶，所写的代码为直接使用jpa的findAll方法即可满足查询。现在查询条件的变化后，我想要在原本的基础上改动的内容越小越好。</p>
<a id="more"></a>
<h2 id="尝试过程"><a href="#尝试过程" class="headerlink" title="尝试过程"></a>尝试过程</h2><h3 id="第一次尝试"><a href="#第一次尝试" class="headerlink" title="第一次尝试"></a>第一次尝试</h3><p>我尝试使用下面的命名方式去直接自定义查询方法，来根据userId属性查询所关联的权重表，再根据权重表来查询到帖子表进行排序。<br>然而这种方法只能查询到这个用户已经排序过的帖子，并不可以看得到没有和该用户关联的帖子。放弃</p>
<h4 id="自定义方法名"><a href="#自定义方法名" class="headerlink" title="自定义方法名"></a>自定义方法名</h4><p>jpa框架在进行方法名解析时，会先把方法名多余的前缀截取掉，比如 find、findBy、read、readBy、get、getBy，然后对剩下部分进行解析。并且如果方法的最后一个参数是 Sort 或者 Pageable 类型，也会提取相关的信息，以便按规则进行排序或者分页查询。</p>
<p>在创建查询时，我们通过在方法名中使用属性名称来表达，比如 findByUserAddressZip ()。框架在解析该方法时，首先剔除 findBy，然后对剩下的属性进行解析，详细规则如下（此处假设该方法针对的域对象为 AccountInfo 类型）：</p>
<ul>
<li>先判断 userAddressZip （根据 POJO 规范，首字母变为小写，下同）是否为 AccountInfo 的一个属性，如果是，则表示根据该属性进行查询；如果没有该属性，继续第二步；</li>
<li>从右往左截取第一个大写字母开头的字符串（此处为 Zip），然后检查剩下的字符串是否为 AccountInfo 的一个属性，如果是，则表示根据该属性进行查询；如果没有该属性，则重复第二步，继续从右往左截取；最后假设 user 为 AccountInfo 的一个属性；</li>
<li>接着处理剩下部分（ AddressZip ），先判断 user 所对应的类型是否有 addressZip 属性，如果有，则表示该方法最终是根据 “AccountInfo.user.addressZip” 的取值进行查询；否则继续按照步骤 2 的规则从右往左截取，最终表示根据 “AccountInfo.user.address.zip” 的值进行查询。</li>
</ul>
<p>可能会存在一种特殊情况，比如 AccountInfo 包含一个 user 的属性，也有一个 userAddress 属性，此时会存在混淆。读者可以明确在属性之间加上下划线以显式表达意图，比如 “findByUser_AddressZip()” 或者 “findByUserAddress_Zip()”。</p>
<p>在查询时，通常需要同时根据多个属性进行查询，且查询的条件也格式各样（大于某个值、在某个范围等等），Spring Data JPA 为此提供了一些表达条件查询的关键字，大致如下：</p>
<ul>
<li>And — 等价于 SQL 中的 and 关键字，比如 findByUsernameAndPassword(String user, Striang pwd)；</li>
<li>Or — 等价于 SQL 中的 or 关键字，比如 findByUsernameOrAddress(String user, String addr)；</li>
<li>Between — 等价于 SQL 中的 between 关键字，比如 findBySalaryBetween(int max, int min)；</li>
<li>LessThan — 等价于 SQL 中的 “&lt;”，比如 findBySalaryLessThan(int max)；</li>
<li>GreaterThan — 等价于 SQL 中的”&gt;”，比如 findBySalaryGreaterThan(int min)；</li>
<li>IsNull — 等价于 SQL 中的 “is null”，比如 findByUsernameIsNull()；</li>
<li>IsNotNull — 等价于 SQL 中的 “is not null”，比如 findByUsernameIsNotNull()；</li>
<li>NotNull — 与 IsNotNull 等价；</li>
<li>Like — 等价于 SQL 中的 “like”，比如 findByUsernameLike(String user)；</li>
<li>NotLike — 等价于 SQL 中的 “not like”，比如 findByUsernameNotLike(String user)；</li>
<li>OrderBy — 等价于 SQL 中的 “order by”，比如 findByUsernameOrderBySalaryAsc(String user)；</li>
<li>Not — 等价于 SQL 中的 “！ =”，比如 findByUsernameNot(String user)；</li>
<li>In — 等价于 SQL 中的 “in”，比如 findByUsernameIn(Collection<string> userList) ，方法的参数可以是 Collection 类型，也可以是数组或者不定长参数；</string></li>
<li>NotIn — 等价于 SQL 中的 “not in”，比如 findByUsernameNotIn(Collection<string> userList) ，方法的参数可以是 Collection 类型，也可以是数组或者不定长参数；</string></li>
</ul>
<h3 id="第二次尝试"><a href="#第二次尝试" class="headerlink" title="第二次尝试"></a>第二次尝试</h3><p>在网上搜索相关文章时突然发现了这个Api查询条件的限制类，仔细查找研究了一下。发现还是没有找到可以解决这个的方案。</p>
<h4 id="Example-matchers"><a href="#Example-matchers" class="headerlink" title="Example matchers"></a>Example matchers</h4><p>首先，仓库接口需要继承<code>QueryByExampleExecutor</code>接口（继承这个 <code>JpaSpecificationExecutor</code> 也行），这样会引入一组以Example作参数的方法。然后创建一个<code>ExampleMatcher</code>对象，最后再用<code>Example</code>的of方法构造相应的Example对象并传递给相关查询方法。</p>
<p>Example不仅仅局限于默认的设置。你可以给strings定义自己的默认值然后去匹配。使用<code>ExampleMatcher</code>绑定null和特定属性的设置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Person person = <span class="keyword">new</span> Person();                          </span><br><span class="line">person.setFirstname(<span class="string">"Dave"</span>);                           </span><br><span class="line"></span><br><span class="line">ExampleMatcher matcher = ExampleMatcher.matching()    </span><br><span class="line">  .withMatcher(<span class="string">"username"</span>, ExampleMatcher.GenericPropertyMatchers.startsWith())  <span class="comment">//模糊查询匹配开头，即&#123;username&#125;%</span></span><br><span class="line">  .withMatcher(<span class="string">"address"</span> ,ExampleMatcher.GenericPropertyMatchers.contains())  <span class="comment">//全部模糊查询，即%&#123;address&#125;%</span></span><br><span class="line">  .withIgnorePaths(<span class="string">"lastname"</span>)  <span class="comment">//忽略字段，即不管lastname是什么值都不加入查询条件                 </span></span><br><span class="line">  .withIncludeNullValues()       </span><br><span class="line">  .withStringMatcherEnding();                          </span><br><span class="line"></span><br><span class="line">Example&lt;Person&gt; example = Example.of(person, matcher);</span><br></pre></td></tr></table></figure>
<p>其中：</p>
<ul>
<li><code>Person person = new Person();</code> 创建一个domain对象实例。</li>
<li>设置属性值去查询。</li>
<li><code>ExampleMatcher matcher = ExampleMatcher.matching()</code> 创建一个 <code>ExampleMatcher</code> 让其可以使用，但没有多余的配置项。</li>
<li><code>.withIgnorePaths(&quot;lastname&quot;)</code> Construct a new ExampleMatcher to ignore the property path lastname。用来排除某个属性的查询。</li>
<li><code>.withIncludeNullValues()</code> Construct a new ExampleMatcher to ignore the property path lastname and to include null values。让空值也参与查询。</li>
<li><code>.withStringMatcherEnding();</code>  Construct a new ExampleMatcher to ignore the property path lastname, to include null values, and use perform suffix string matching。匹配后缀字符串</li>
<li><code>Example&lt;Person&gt; example = Example.of(person, matcher);</code> 根据domain对象和配置的<code>ExampleMatcher</code>对象来创建一个<code>Example</code></li>
</ul>
<p>还可以给个别的属性指定行为.(比如.<code>firstname</code>和<code>lastname</code>以及domain对象的嵌套属性<code>address.city</code>) </p>
<ul>
<li><code>.ignoreCase()</code> 可以调整他让他匹配大小写敏感的选项。</li>
<li><code>endsWith</code> 以<code>firstname</code> 结束的前模糊查询。</li>
<li><code>startWith</code> 以<code>lastname</code>开始的后模糊查询。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ExampleMatcher matcher = ExampleMatcher.matching()</span><br><span class="line">  .withMatcher(<span class="string">"firstname"</span>, endsWith())</span><br><span class="line">  .withMatcher(<span class="string">"lastname"</span>, startsWith().ignoreCase());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="第四次尝试"><a href="#第四次尝试" class="headerlink" title="第四次尝试"></a>第四次尝试</h3><p>没办法，要改动的步骤越来越大。以上的方法都不行的前提下，我只好试了试 <code>Specification</code> 作为 <code>findAll</code> 的参数这种方法。可是虽然用起来要改动的代码很少，但是还是不能查询到我想要的查询结果。查询条件只能加在where上面，而我想要的是用户排过序的加入条件查询，没有排过序的也要排列在后面。</p>
<h4 id="JpaSpecificationExecutor"><a href="#JpaSpecificationExecutor" class="headerlink" title="JpaSpecificationExecutor"></a>JpaSpecificationExecutor</h4><p>首先，仓库接口要继承 <code>JpaSpecificationExecutor&lt;T&gt;</code> 这个类，之后就可以使用 <code>findAll(Specification&lt;T&gt; spec)</code>等方法了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Specification specification = <span class="keyword">new</span> Specification() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Predicate <span class="title">toPredicate</span><span class="params">(Root root, CriteriaQuery query,</span></span></span><br><span class="line"><span class="function"><span class="params">        CriteriaBuilder cb)</span> </span>&#123;</span><br><span class="line">        List&lt;Predicate&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">if</span> (Objects.nonNull(<span class="string">"1"</span>)) &#123;</span><br><span class="line">            Join&lt;WeightSort, ProjectInfo&gt; join = root.join(<span class="string">"weightSort"</span>, JoinType.LEFT);</span><br><span class="line">            list.add(cb.equal(join.get(<span class="string">"user"</span>).get(<span class="string">"username"</span>), <span class="string">"zs"</span>));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (Objects.nonNull(param.getYear())) &#123;</span><br><span class="line">            list.add(cb.equal(root.get(<span class="string">"year"</span>), param.getYear()));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (Objects.nonNull(param.getTag())) &#123;</span><br><span class="line">            list.add(cb.like(root.get(<span class="string">"tag"</span>), param.getTag() + <span class="string">"%"</span>));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> query.where(list.toArray(<span class="keyword">new</span> Predicate[<span class="number">0</span>])).getRestriction();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">Page&lt;ProjectInfo&gt; projectInfoPageTest = projectInfoRepository.findAll(specification, page);</span><br><span class="line">projectInfoPageTest.stream().forEach(projectInfo -&gt; &#123;</span><br><span class="line">    System.out.println(projectInfo.getId());</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>其中：</p>
<ul>
<li><code>join</code> 为外键关联查询，通过 <code>project</code> 类中的 <code>WeightSort weightSort；</code>中的 <code>User user</code> 中的 <code>String username</code>属性来作为条件查询。该条件加在where后面。</li>
<li><code>cb.equal</code> 为匹配查询，相当于where后面的=号属性查询。</li>
<li><code>cb.like</code> 为模糊匹配查询，相当于where后面的like属性查询。</li>
</ul>
<p>上面的代码产生的sql语句为：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	p.* </span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	bs_project_info p</span><br><span class="line">	<span class="keyword">LEFT</span> <span class="keyword">JOIN</span> bs_project_info_weight_sorts pw <span class="keyword">ON</span> p.id = pw.project_info_id</span><br><span class="line">	<span class="keyword">LEFT</span> <span class="keyword">JOIN</span> bs_weight_sort w <span class="keyword">ON</span> w.id = pw.weight_sorts_id</span><br><span class="line">	<span class="keyword">LEFT</span> <span class="keyword">JOIN</span> <span class="keyword">system_user</span> u <span class="keyword">ON</span> u.id = w.user_id</span><br><span class="line">	<span class="keyword">WHERE</span></span><br><span class="line">	 u.username = <span class="string">'zs'</span></span><br><span class="line">	 <span class="keyword">and</span> p.year = <span class="number">2019</span></span><br><span class="line">	 <span class="keyword">and</span> p.tag <span class="keyword">like</span> <span class="string">'sql%'</span></span><br></pre></td></tr></table></figure></p>
<p>但是这种方法还是要写很多行代码，不如把之前的原本的 <code>findAll(Example example)</code> 利用起来。<br>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Example example = Example.of(ProjectInfo.builder().weightSort(<span class="keyword">null</span>).build());</span><br><span class="line">Page&lt;ProjectInfo&gt; pages = projectInfoRepository.findAll((root, query, cb) -&gt; &#123;</span><br><span class="line">   List&lt;Predicate&gt; predicates = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    predicates.add(QueryByExamplePredicateBuilder.getPredicate(root, cb, example));</span><br><span class="line"></span><br><span class="line">    Join&lt;WeightSort, ProjectInfo&gt; join = root.join(<span class="string">"weightSort"</span>, JoinType.LEFT);</span><br><span class="line">    predicates.add(cb.equal(join.get(<span class="string">"user"</span>).get(<span class="string">"username"</span>), <span class="string">"zs"</span>));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> query.where(predicates.toArray(<span class="keyword">new</span> Predicate[<span class="number">0</span>])).getRestriction();</span><br><span class="line">&#125;, page);</span><br><span class="line">pages.stream().forEach(projectInfo -&gt; &#123;</span><br><span class="line">    System.out.println(projectInfo.getId());</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>这样就可以把一些属性相等的条件放进 <code>Example</code> 类里，而且该类本就支持不加入null的条件查询。不用再去判断传入参数为null时不做条件查询。利用上jpa的动态条件查询，节省了很多行代码。</p>
<h3 id="最终的结局"><a href="#最终的结局" class="headerlink" title="最终的结局"></a>最终的结局</h3><p>没办法，实在是没有找到可以解决这个问题的方法。只好直接使用原生sql语句来满足需求。</p>
<h4 id="原生sql，Query注释"><a href="#原生sql，Query注释" class="headerlink" title="原生sql，Query注释"></a>原生sql，Query注释</h4><p>@Query 注解的使用非常简单，只需在声明的方法上面标注该注解，同时提供一个 JP QL 查询语句即可，如下所示：</p>
<p>使用 @Query 提供自定义查询语句示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Query</span>(value = <span class="string">"SELECT "</span></span><br><span class="line">        + <span class="string">"p.* "</span></span><br><span class="line">        + <span class="string">" FROM "</span></span><br><span class="line">        + <span class="string">" bs_project_info p "</span></span><br><span class="line">        + <span class="string">" LEFT JOIN ( bs_project_info_weight_sorts pw JOIN bs_weight_sort w ON w.id = pw.weight_sorts_id AND w.user_id = :#&#123;#param.userId&#125; )</span></span><br><span class="line"><span class="string">        +  ON pw.project_info_id = p.id "</span></span><br><span class="line">        + <span class="string">"where  "</span></span><br><span class="line">        + <span class="string">" IF( :#&#123;#param.name&#125; IS NOT null, p.name= :#&#123;#param.name&#125;,  1=1) and "</span></span><br><span class="line">        + <span class="string">" IF( :#&#123;#param.year&#125; IS NOT null, p.year = :#&#123;#param.year&#125;,  1=1) and "</span></span><br><span class="line">        + <span class="string">" IF( :#&#123;#param.review&#125; IS NOT null, p.review = :#&#123;#param.review&#125;,  1=1) "</span></span><br><span class="line">        + <span class="string">"ORDER BY "</span></span><br><span class="line">        + <span class="string">"w.weight desc, p.create_time desc limit :#&#123;#param.pageStart&#125;, :#&#123;#param.sizeStart&#125;"</span>, nativeQuery=<span class="keyword">true</span>)</span><br><span class="line">    <span class="function">List&lt;ProjectInfo&gt; <span class="title">findAllPageProjectByParam</span><span class="params">(@Param(<span class="string">"param"</span>)</span> FindProjectParam param)</span>;</span><br></pre></td></tr></table></figure>
<p>输入参数 <code>FindProjectParam</code> 类：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ApiModel</span></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Builder</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@FieldDefaults</span>(level = AccessLevel.PRIVATE)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FindProjectParam</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"名称(不填为全部)"</span>, example = Mock.SPECIALNAMEID)</span><br><span class="line">    Long name;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"年度(不填为全部)"</span>, example = <span class="string">"2019"</span>)</span><br><span class="line">    Integer year;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"项目状态(3-待提交，1-已提交)(不填为全部)"</span>, example = Mock.REVIEW)</span><br><span class="line">    Integer review;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"用户id"</span>, example = Mock.USERNAME)</span><br><span class="line">    Long userId;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"页数"</span>)</span><br><span class="line">    Integer pageStart;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"条数"</span>)</span><br><span class="line">    Integer sizeStart;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>查询 <code>ProjectInfo</code> 类（数据库表名为：bs_project_info）：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.yiring.finance.domain.projectinfo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.constructioncontent.ConstructionContent;</span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.dictionary.DictionaryType;</span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.guide.Guide;</span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.uploadfile.FileOperation;</span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.weightsort.WeightSort;</span><br><span class="line"><span class="keyword">import</span> io.swagger.annotations.ApiModelProperty;</span><br><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"><span class="keyword">import</span> java.time.LocalDateTime;</span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.CascadeType;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Column;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Entity;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.FetchType;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.GeneratedValue;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.GenerationType;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Id;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.ManyToOne;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.OneToMany;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Table;</span><br><span class="line"><span class="keyword">import</span> lombok.AccessLevel;</span><br><span class="line"><span class="keyword">import</span> lombok.AllArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.Builder;</span><br><span class="line"><span class="keyword">import</span> lombok.Data;</span><br><span class="line"><span class="keyword">import</span> lombok.NoArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.experimental.FieldDefaults;</span><br><span class="line"><span class="keyword">import</span> org.hibernate.annotaions.Comment;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 项目</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> zhangshuai</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/4/16 17:31</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Builder</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@FieldDefaults</span>(level = AccessLevel.PRIVATE)</span><br><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Comment</span>(<span class="string">"项目"</span>)</span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"bs_project_info"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProjectInfo</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = -<span class="number">2079824786038520972L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"主键"</span>)</span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span>(strategy = GenerationType.IDENTITY)</span><br><span class="line">    Long id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"项目名称"</span>)</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"区域项目名称"</span>, example = <span class="string">"2"</span>, required = <span class="keyword">true</span>)</span><br><span class="line">    String projectName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"申报年份"</span>)</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"申报年份"</span>, example = <span class="string">"2"</span>, required = <span class="keyword">true</span>)</span><br><span class="line">    Integer year;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"创建时间"</span>)</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"创建时间"</span>, example = <span class="string">"2"</span>, required = <span class="keyword">true</span>)</span><br><span class="line">    LocalDateTime createTime;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@Builder</span>.Default</span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"权重"</span>)</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"权重"</span>, required = <span class="keyword">true</span>)</span><br><span class="line">    <span class="meta">@OneToMany</span>(fetch = FetchType.EAGER, cascade = CascadeType.ALL)</span><br><span class="line">    Set&lt;WeightSort&gt; weightSorts = <span class="keyword">new</span> HashSet&lt;&gt;();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>关联权重<code>WeightSort</code>类（数据库名：bs_weight_sort）：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.yiring.finance.domain.weightsort;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.user.User;</span><br><span class="line"><span class="keyword">import</span> io.swagger.annotations.ApiModelProperty;</span><br><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Entity;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.GeneratedValue;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.GenerationType;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Id;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.ManyToOne;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Table;</span><br><span class="line"><span class="keyword">import</span> lombok.AccessLevel;</span><br><span class="line"><span class="keyword">import</span> lombok.AllArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.Builder;</span><br><span class="line"><span class="keyword">import</span> lombok.Data;</span><br><span class="line"><span class="keyword">import</span> lombok.NoArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.experimental.FieldDefaults;</span><br><span class="line"><span class="keyword">import</span> org.hibernate.annotaions.Comment;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 用户关联项目权重表</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> zhangshuai</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/5/9 11:09</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Builder</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@FieldDefaults</span>(level = AccessLevel.PRIVATE)</span><br><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Comment</span>(<span class="string">"权重表"</span>)</span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"bs_weight_sort"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WeightSort</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1009267440536346667L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"主键"</span>)</span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span>(strategy = GenerationType.IDENTITY)</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"主键"</span>, example = <span class="string">"2"</span>)</span><br><span class="line">    Long id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"绑定用户"</span>)</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"绑定用户"</span>)</span><br><span class="line">    <span class="meta">@ManyToOne</span></span><br><span class="line">    User user;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"权重"</span>)</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(value =<span class="string">"权重"</span>, example = <span class="string">"2"</span>)</span><br><span class="line">    Long weight;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>关联用户<code>User</code>表（数据库表名：SYSTEM_USER）：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.yiring.finance.domain.user;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.dictionary.DictionaryType;</span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.permission.Permission;</span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.domain.role.Role;</span><br><span class="line"><span class="keyword">import</span> com.yiring.finance.secruity.JwtUser;</span><br><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"><span class="keyword">import</span> java.time.LocalDateTime;</span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"><span class="keyword">import</span> java.util.stream.Collectors;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.CascadeType;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Column;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Entity;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.FetchType;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.GeneratedValue;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.GenerationType;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Id;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Index;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.ManyToMany;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.ManyToOne;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.OneToOne;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Table;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Transient;</span><br><span class="line"><span class="keyword">import</span> lombok.AccessLevel;</span><br><span class="line"><span class="keyword">import</span> lombok.AllArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.Builder;</span><br><span class="line"><span class="keyword">import</span> lombok.Data;</span><br><span class="line"><span class="keyword">import</span> lombok.NoArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.experimental.FieldDefaults;</span><br><span class="line"><span class="keyword">import</span> org.hibernate.annotaions.Comment;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 用户</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> fangzhimin</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018/9/3 15:27</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Builder</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@FieldDefaults</span>(level = AccessLevel.PRIVATE)</span><br><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Comment</span>(<span class="string">"用户"</span>)</span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"SYSTEM_USER"</span>, indexes = &#123;</span><br><span class="line">        <span class="meta">@Index</span>(name = <span class="string">"IDX_USERNAME"</span>, columnList = <span class="string">"username"</span>, unique = <span class="keyword">true</span>),</span><br><span class="line">        <span class="meta">@Index</span>(name = <span class="string">"IDX_MOBILE"</span>, columnList = <span class="string">"mobile"</span>, unique = <span class="keyword">true</span>),</span><br><span class="line">        <span class="meta">@Index</span>(name = <span class="string">"IDX_EMAIL"</span>, columnList = <span class="string">"email"</span>, unique = <span class="keyword">true</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = -<span class="number">5787847701210907511L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"主键"</span>)</span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span>(strategy = GenerationType.IDENTITY)</span><br><span class="line">    Long id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"真实姓名"</span>)</span><br><span class="line">    String realName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"用户名"</span>)</span><br><span class="line">    <span class="meta">@Column</span>(unique = <span class="keyword">true</span>)</span><br><span class="line">    String username;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"密码"</span>)</span><br><span class="line">    String password;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"手机号"</span>)</span><br><span class="line">    <span class="meta">@Column</span>(unique = <span class="keyword">true</span>)</span><br><span class="line">    String mobile;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"最后登录IP地址"</span>)</span><br><span class="line">    String lastLoginIp;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"激活时间"</span>)</span><br><span class="line">    LocalDateTime activationTime;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"权限更新时间"</span>)</span><br><span class="line">    LocalDateTime authorityUpdateTime;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"最后重置密码时间"</span>)</span><br><span class="line">    LocalDateTime lastPasswordResetTime;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"最后登录时间"</span>)</span><br><span class="line">    LocalDateTime lastLoginTime;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"最后更新信息时间"</span>)</span><br><span class="line">    LocalDateTime lastUpdateTime;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Comment</span>(<span class="string">"创建时间"</span>)</span><br><span class="line">    LocalDateTime createTime;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 验证码（非持久化）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Transient</span></span><br><span class="line">    String code;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * token（非持久化）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Transient</span></span><br><span class="line">    String token;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="后记："><a href="#后记：" class="headerlink" title="后记："></a>后记：</h2><p>主要还是卡在了不能创建临时表之后查询。大佬自己查询操作了一下构建CriteriaQuery这个类。但是还是不能解决这个问题。只能先记录一下，等待以后的解决。</p>
]]></content>
      <tags>
        <tag>jpa</tag>
      </tags>
  </entry>
  <entry>
    <title>linux通过docker安装mongoDB</title>
    <url>/2020/03/28/linux%E5%AE%89%E8%A3%85mongoDB/</url>
    <content><![CDATA[<h1 id="linux通过docker安装mongoDB"><a href="#linux通过docker安装mongoDB" class="headerlink" title="linux通过docker安装mongoDB"></a>linux通过docker安装mongoDB</h1><blockquote>
<p>记录一下安装过程，之后再次安装时用。</p>
</blockquote>
<a id="more"></a>
<h2 id="前期准备"><a href="#前期准备" class="headerlink" title="前期准备"></a>前期准备</h2><p>拿到服务器后先检查一下内核版本<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">uname -r</span><br><span class="line"></span><br><span class="line">cat /etc/redhat-release</span><br></pre></td></tr></table></figure></p>
<p>再更新一下yum数据源<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 备份系统自带yum源配置文件</span><br><span class="line">mv /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.backup</span><br><span class="line"># 进入yum源配置文件所在的文件夹</span><br><span class="line">cd /etc/yum.repos.d/</span><br><span class="line"></span><br><span class="line">#163的yum源配置文件 </span><br><span class="line"># CentOS7</span><br><span class="line">wget http://mirrors.163.com/.help/CentOS7-Base-163.repo</span><br><span class="line"># CentOS6</span><br><span class="line">wget http://mirrors.163.com/.help/CentOS6-Base-163.repo</span><br><span class="line"># CentOS5</span><br><span class="line">wget http://mirrors.163.com/.help/CentOS5-Base-163.repo</span><br><span class="line"></span><br><span class="line">#ailiyun的yum源配置文件</span><br><span class="line"># CentOS7</span><br><span class="line">wget -O /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo</span><br><span class="line"># CentOS6</span><br><span class="line">wget -O /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-6.repo</span><br><span class="line"># CentOS5</span><br><span class="line"></span><br><span class="line"># 选择上面合适的数据源后，生成缓存</span><br><span class="line">yum makecache</span><br></pre></td></tr></table></figure></p>
<h2 id="安装docker（包含重新安装docker，更换版本）"><a href="#安装docker（包含重新安装docker，更换版本）" class="headerlink" title="安装docker（包含重新安装docker，更换版本）"></a>安装docker（包含重新安装docker，更换版本）</h2><p>使用命令<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install -y docker</span><br></pre></td></tr></table></figure></p>
<p>查看docker是否安装成功<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum list installed |grep docker</span><br></pre></td></tr></table></figure></p>
<p>启动docker服务并设置docker开机自启<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">systemctl start docker.service</span><br><span class="line">systemctl enable docker.service</span><br></pre></td></tr></table></figure></p>
<h3 id="错误开始"><a href="#错误开始" class="headerlink" title="错误开始"></a>错误开始</h3><p>在启动时报了一个错误，让我去看一下日志<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">systemctl status docker</span><br></pre></td></tr></table></figure></p>
<p>看到了这样的错误<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Error starting daemon: SELinux is not supported with the overlay2 graph driver...false)</span><br></pre></td></tr></table></figure></p>
<p>尝试卸载重装docker，依旧不能解决问题<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum remove docker \</span><br><span class="line">                  docker-client \</span><br><span class="line">                  docker-client-latest \</span><br><span class="line">                  docker-common \</span><br><span class="line">                  docker-latest \</span><br><span class="line">                  docker-latest-logrotate \</span><br><span class="line">                  docker-logrotate \</span><br><span class="line">                  docker-selinux \</span><br><span class="line">                  docker-engine-selinux \</span><br><span class="line">                  docker-engine</span><br></pre></td></tr></table></figure></p>
<p>查找了一下，需要修改docker配置文件<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vi /etc/sysconfig/docker</span><br></pre></td></tr></table></figure></p>
<p>打开后修改里面的内容，本来只有一个<code>--selinux-enabled</code>，在后面加上<code>=false</code>，就可以解决<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">--selinux-enabled=false</span><br></pre></td></tr></table></figure></p>
<h3 id="错误解决后"><a href="#错误解决后" class="headerlink" title="错误解决后"></a>错误解决后</h3><p>查看docker版本<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker version</span><br></pre></td></tr></table></figure></p>
<p>配置docker镜像加速器（阿里），打开<a href="https://cr.console.aliyun.com/cn-hangzhou/instances/mirrors?accounttraceid=fd4b2b1718054019ba8f49c3dcc68f62byrh" target="_blank" rel="noopener">阿里云</a> 登录，按照教程配置<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo mkdir -p /etc/docker</span><br><span class="line">sudo tee /etc/docker/daemon.json &lt;&lt;-&apos;EOF&apos;</span><br><span class="line">&#123;</span><br><span class="line">  &quot;registry-mirrors&quot;: [&quot;&quot;]</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br><span class="line">sudo systemctl daemon-reload</span><br><span class="line">sudo systemctl restart docker</span><br></pre></td></tr></table></figure></p>
<h3 id="重新安装docker"><a href="#重新安装docker" class="headerlink" title="重新安装docker"></a>重新安装docker</h3><p>依旧是用之前卸载方式卸载docker，然后恢复yum数据源。重新配置为centos7的阿里云源。<br>再增加阿里docker镜像<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum-config-manager --add-repo http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo</span><br></pre></td></tr></table></figure></p>
<p>安装依赖包<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install -y yum-utils device-mapper-persistent-data lvm2</span><br></pre></td></tr></table></figure></p>
<p>刷新yum缓存，重新安装<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install docker-ce -y</span><br></pre></td></tr></table></figure></p>
<p>用命令<code></code>出现错误<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">failed to start daemon: error initializing graphdriver: driver not supported</span><br></pre></td></tr></table></figure></p>
<p>按照百度后的方法<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rm -rf /var/lib/docker/*</span><br></pre></td></tr></table></figure></p>
<p>可以成功启动<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">systemctl status docker</span><br><span class="line">docker.service - Docker Application Container Engine</span><br><span class="line">   Loaded: loaded (/usr/lib/systemd/system/docker.service; disabled; vendor preset: disabled)</span><br><span class="line">   Active: active (running) since 五 2020-04-04 15:25:46 CST; 37s ago</span><br><span class="line">     Docs: https://docs.docker.com</span><br><span class="line"> Main PID: 32067 (dockerd)</span><br><span class="line">   Memory: 46.5M</span><br><span class="line">   CGroup: /system.slice/docker.service</span><br><span class="line">           └─32067 /usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.soc</span><br></pre></td></tr></table></figure></p>
<p>查看docker版本，配置阿里云镜像，继续安装mongoDB<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line"></span><br><span class="line">Client: Docker Engine - Community</span><br><span class="line"> Version:           19.03.8</span><br><span class="line"> API version:       1.40</span><br><span class="line"> Go version:        go1.12.17</span><br><span class="line"> Git commit:        afacb8b</span><br><span class="line"> Built:             Wed Mar 11 01:27:04 2020</span><br><span class="line"> OS/Arch:           linux/amd64</span><br><span class="line"> Experimental:      false</span><br><span class="line"></span><br><span class="line">Server: Docker Engine - Community</span><br><span class="line"> Engine:</span><br><span class="line">  Version:          19.03.8</span><br><span class="line">  API version:      1.40 (minimum version 1.12)</span><br><span class="line">  Go version:       go1.12.17</span><br><span class="line">  Git commit:       afacb8b</span><br><span class="line">  Built:            Wed Mar 11 01:25:42 2020</span><br><span class="line">  OS/Arch:          linux/amd64</span><br><span class="line">  Experimental:     false</span><br><span class="line"> containerd:</span><br><span class="line">  Version:          1.2.13</span><br><span class="line">  GitCommit:        7ad184331fa3e55e52b890ea95e65ba581ae3429</span><br><span class="line"> runc:</span><br><span class="line">  Version:          1.0.0-rc10</span><br><span class="line">  GitCommit:        dc9208a3303feef5b3839f4323d9beb36df0a9dd</span><br><span class="line"> docker-init:</span><br><span class="line">  Version:          0.18.0</span><br><span class="line">  GitCommit:        fec3683</span><br></pre></td></tr></table></figure></p>
<h2 id="安装mongoDB"><a href="#安装mongoDB" class="headerlink" title="安装mongoDB"></a>安装mongoDB</h2><p>查询一下镜像<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker search mongo</span><br></pre></td></tr></table></figure></p>
<p>安装镜像<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull mongo</span><br></pre></td></tr></table></figure></p>
<p>但是再次出现了bug<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Error response from daemon: error creating overlay mount to xxx merged: invalid argument</span><br></pre></td></tr></table></figure></p>
<p>百度一下发现都是删除镜像后将overlay2文件换成overlay文件系统。仔细思考后决定还是先重装docker版本。</p>
<p>重装后可以正常安装镜像，再次启动容器，成功（注意：没有解决服务器映射端口问题，直接用host模式）<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -d -it --net=host -v /home/mongoDB:/data/db --name docker_mongodb  mongo</span><br></pre></td></tr></table></figure></p>
<p>紧接着就是端口无法访问的问题了。防火墙已经打开端口，给linux安装好telnet，测试一下。<br>可以访问127.0.0.1:27107.但是不能访问服务器ip。<br>用<code>docker exec</code>命令进入容器执行<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#更新源</span><br><span class="line">apt-get update</span><br><span class="line"># 安装 vim</span><br><span class="line">apt-get install vim</span><br><span class="line"># 修改 mongo 配置文件</span><br><span class="line">vim /etc/mongod.conf.orig</span><br><span class="line"></span><br><span class="line">#配置用户密码</span><br><span class="line">mongo</span><br><span class="line">use &lt;admin&gt;</span><br><span class="line"></span><br><span class="line">db.createUser(&#123;</span><br><span class="line">    user: &apos;&lt;root&gt;&apos;,</span><br><span class="line">    pwd: &apos;&lt;root&gt;&apos;,</span><br><span class="line">    roles: [&#123; role: &apos;readWrite&apos;, db:&apos;&lt;admin&gt;&apos;&#125;]</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure></p>
<p>发现还是不能访问，直接关掉防火墙却可以。发现问题是在于防火墙。<br>之前使用的命令是<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --add-port=27107/tcp --permanent</span><br></pre></td></tr></table></figure></p>
<p>更换命令后即可在局域网访问<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">firewall-cmd --add-port=27017/tcp --permanent</span><br><span class="line">success</span><br><span class="line">firewall-cmd --query-port=27017/tcp --permanent</span><br><span class="line">yes</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux后台启动程序操作</title>
    <url>/2020/10/31/linux%E5%90%8E%E5%8F%B0%E5%90%AF%E5%8A%A8%E7%A8%8B%E5%BA%8F%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<h1 id="Linux后台启动程序操作"><a href="#Linux后台启动程序操作" class="headerlink" title="Linux后台启动程序操作"></a>Linux后台启动程序操作</h1><blockquote>
<p>感觉越来越难写了。有点累，好想放个寒假。<br>本文主要用于记录我操作 Linux 的过程。包含后台启动程序、找到占用的端口、关闭占用端口程序。</p>
</blockquote>
<a id="more"></a>
<h2 id="后台启动程序"><a href="#后台启动程序" class="headerlink" title="后台启动程序"></a>后台启动程序</h2><p>来自 <a href="https://www.runoob.com/linux/linux-comm-nohup.html" target="_blank" rel="noopener">runnoob.com</a><br><strong>语法格式：</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nohup Command [ Arg … ] [　&amp; ]</span><br></pre></td></tr></table></figure></p>
<p><strong>参数说明：</strong><br>Command：要执行的命令。</p>
<p>Arg：一些参数，可以指定输出文件。</p>
<p>&amp;：让命令在后台执行，终端退出后命令仍旧执行。</p>
<p><strong>实例</strong><br>以下命令在后台执行 root 目录下的 runoob.sh 脚本：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nohup /root/runoob.sh &amp;</span><br></pre></td></tr></table></figure></p>
<p>以下命令在后台执行 root 目录下的 runoob.sh 脚本，并重定向输入到 runoob.log 文件：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nohup /root/runoob.sh &gt; runoob.log 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure></p>
<p>2&gt;&amp;1 解释：</p>
<p>将标准错误 2 重定向到标准输出 &amp;1 ，标准输出 &amp;1 再被重定向输入到 runoob.log 文件中。</p>
<ul>
<li>0 – stdin (standard input，标准输入)</li>
<li>1 – stdout (standard output，标准输出)</li>
<li>2 – stderr (standard error，标准错误输出)</li>
</ul>
<p><strong>本人使用：</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nohup java -jar v.jar &gt; v.log 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure></p>
<h2 id="找到占用的端口"><a href="#找到占用的端口" class="headerlink" title="找到占用的端口"></a>找到占用的端口</h2><p>来自<a href="https://www.cnblogs.com/zjfjava/p/10513399.html" target="_blank" rel="noopener">https://www.cnblogs.com/zjfjava/p/10513399.html</a><br>用于查看指定端口号的进程情况<br><strong>语法格式：</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">netstat -tunlp | grep 端口号</span><br></pre></td></tr></table></figure></p>
<p><strong>参数说明：</strong><br>几个参数含义</p>
<ul>
<li>-t (tcp) 仅显示tcp相关选项</li>
<li>-u (udp)仅显示udp相关选项</li>
<li>-n 拒绝显示别名，能显示数字的全部转化为数字</li>
<li>-l 仅列出在Listen(监听)的服务状态</li>
<li>-p 显示建立相关链接的程序名</li>
</ul>
<p><strong>本人使用</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">netstat -tunlp | grep 8080</span><br></pre></td></tr></table></figure></p>
<h2 id="关闭占用端口程序"><a href="#关闭占用端口程序" class="headerlink" title="关闭占用端口程序"></a>关闭占用端口程序</h2><p><strong>本人使用</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">kill -9  进程号PID</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>linux搭建ftp</title>
    <url>/2020/02/15/linux%E6%90%AD%E5%BB%BAftp/</url>
    <content><![CDATA[<h1 id="linux搭建ftp"><a href="#linux搭建ftp" class="headerlink" title="linux搭建ftp"></a>linux搭建ftp</h1><blockquote>
<p>记录安装ftp过程以及遇到的问题。<br><a id="more"></a></p>
</blockquote>
<h2 id="安装流程"><a href="#安装流程" class="headerlink" title="安装流程"></a>安装流程</h2><ol>
<li><p>首先要确认服务器的端口开放情况，ftp被动模式使用21端口。</p>
</li>
<li><p>安装vsftp软件包</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install -y vsftpd</span><br></pre></td></tr></table></figure>
</li>
<li><p>修改ftp配置文件，默认位置在/etc/vsftpd/。最好备份一下原始文件以便后期调整。</p>
</li>
<li><p>ftp配置文件vsftpd.conf详细内容</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">anonymous_enable=NO                    #不允许匿名用户登陆 </span><br><span class="line"></span><br><span class="line">local_enable=YES                      #vsftpd所在系统的用户可以登录vsftpd </span><br><span class="line"></span><br><span class="line">write_enable=YES                      #允许使用任何可以修改文件系统的FTP的指令 </span><br><span class="line"></span><br><span class="line">local_umask=000                        #匿名用户新增文件的umask数值 </span><br><span class="line"></span><br><span class="line">anon_upload_enable=NO                  #匿名用户不可以上传文件 </span><br><span class="line"></span><br><span class="line">anon_mkdir_write_enable=NO            #匿名用户不可以修改文件 </span><br><span class="line"></span><br><span class="line">xferlog_enable=YES                    #启用一个日志文件，用于详细记录上传和下载。                </span><br><span class="line">use_localtime=YES                      #使用本地时间而不是GMT </span><br><span class="line"></span><br><span class="line">vsftpd_log_file=/var/log/vsftpd.log    #vsftpd日志存放位置 </span><br><span class="line"></span><br><span class="line">dual_log_enable=YES                    #用户登陆日志 </span><br><span class="line"></span><br><span class="line">connect_from_port_20=YES              #开启20端口     </span><br><span class="line"></span><br><span class="line">xferlog_file=/var/log/xferlog          #记录上传下载文件的日志 </span><br><span class="line"></span><br><span class="line">xferlog_std_format=YES                #记录日志使用标准格式 </span><br><span class="line"></span><br><span class="line">idle_session_timeout=600              #登陆之后超时时间60秒，登陆之后，一分钟不操作，就会断开连接。 </span><br><span class="line"></span><br><span class="line">chroot_local_user=YES                  #用于指定用户列表文件中的用户,是否允许切换到上级目录      </span><br><span class="line"></span><br><span class="line">listen=YES                            #开启监听 </span><br><span class="line"></span><br><span class="line">pam_service_name=vsftpd            #验证文件的名字 </span><br><span class="line"></span><br><span class="line">tcp_wrappers=YES                      #支持tcp_wrappers,限制访问</span><br><span class="line"></span><br><span class="line">(/etc/hosts.allow,/etc/hosts.deny) </span><br><span class="line"></span><br><span class="line">pasv_min_port=35000  </span><br><span class="line">pasv_max_port=45000 </span><br><span class="line">pasv_enable=YES </span><br><span class="line">pasv_promiscuous=YES </span><br><span class="line"></span><br><span class="line">anon_other_write_enable=YES</span><br><span class="line"></span><br><span class="line">userlist_enable=YES                    </span><br><span class="line">userlist_deny=NO                      #允许由userlist_file指定文件中的用户登录FTP服务器</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>这里说明一下用户权限问题，和配置文件user_list与ftpusers有关。<br>1:userlist_enable和userlist_deny两个选项联合起来针对的是：本地全体用户（除去ftpusers中的用户）和出现在user_list文件中的用户以及不在在user_list文件中的用户这三类用户集合进行的设置。<br>2:当且仅当userlist_enable=YES时：userlist_deny项的配置才有效，user_list文件才会被使用；当其为NO时，无论userlist_deny项为何值都是无效的，本地全体用户（ftpusers中的用户不能登入FTP）都可以登入FTP。<br>3:当userlist_enable=YES时，userlist_deny=YES时：user_list是一个黑名单，即：所有出现在名单中的用户都会被拒绝登入。<br>4:当userlist_enable=YES时，userlist_deny=NO时：user_list是一个白名单，即：只有出现在名单中的用户才会被准许登入(user_list之外的用户都被拒绝登入)。另外需要特别提醒的是：使用白名单后，匿名用户将无法登入！除非显式在user_list中加入一行：anonymous。</p>
<ol start="5">
<li><p>重启vsftpd</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 重启</span><br><span class="line">service vsftpd restart</span><br><span class="line"># 开启</span><br><span class="line">service vsftpd start</span><br><span class="line"># 停止</span><br><span class="line">service vsftpd stop</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加ftp用户<br>useradd 用户名 -s /sbin/nologin //限定用户test不能telnet，只能ftp。</p>
</li>
</ol>
<p>usermod -s /sbin/bash 用户名 //用户恢复正常 。该账户路径默认指向/home/ftpadmin目录。</p>
<p>设置ftpadmin用户密码，运行命令：”passwd ftpadmin” 。 输入两次密码，匹配成功后，就设置好了ftpadmin用户的密码了。</p>
<p>测试连接，您可以在“我的电脑”地址栏中输入 <a href="ftp://IP" target="_blank" rel="noopener">ftp://IP</a> 来连接FTP服务器，根据提示输入账户密码。</p>
<p>到这里正常的ftp配置已经完成了。</p>
<ol start="7">
<li>删除用户账号<br>使用 userdel -r xiaoluo命令删除。这个命令会删除用户的文件夹。<h2 id="遇到的问题（已解决）"><a href="#遇到的问题（已解决）" class="headerlink" title="遇到的问题（已解决）"></a>遇到的问题（已解决）</h2>我想要修改一下ftp用户的默认文件夹。当用户登录ftp时，本身的默认文件夹位置是在/home/用户名。<br>查询后使用下面的命令更换<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">usermod -d /tmp test (test为你的用户名)</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>更换后发现并不能从浏览器登录了。<br>在linux中使用命令登录：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ftp 127.0.0.1</span><br></pre></td></tr></table></figure></p>
<p>出现如下的错误提示：-bash: ftp: command not found。<br>使用命令，安装ftp客户端。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum -y install ftp</span><br></pre></td></tr></table></figure></p>
<p>再次登录，输入用户名和密码后。报500 OOPS: cannot change directory。<br>查询后尝试使用下面的命令关闭了Selinux<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 查询Selinux状态</span><br><span class="line">sestatus -v</span><br></pre></td></tr></table></figure></p>
<p>修改/etc/selinux/config 文件。将SELINUX=enforcing改为SELINUX=disabled。依旧不行。<br>再次尝试修改文件夹权限<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">chown 用户组:用户名 -R /tmp </span><br><span class="line">chmod -R 777 /tmp</span><br></pre></td></tr></table></figure></p>
<p>还是不行。查询后大多数都是用这几种有关的命令解决。目前没有找到原因，暂时使用默认文件夹</p>
<h3 id="解决方式"><a href="#解决方式" class="headerlink" title="解决方式"></a>解决方式</h3><blockquote>
<p>2020年03月13号更新</p>
</blockquote>
<p>呼叫大佬后，大佬也没明白我都做了什么操作。发给了我一个博客，照着一步一步操作后很顺利的成功了。</p>
<p>把博客贴在这里<a href="https://blog.csdn.net/wsyh12345678/article/details/83244940" target="_blank" rel="noopener">https://blog.csdn.net/wsyh12345678/article/details/83244940</a></p>
<p>和我之前的配置文件有所不同的地方<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dirmessage_enable=YES</span><br><span class="line"></span><br><span class="line">xferlog_enable=YES</span><br><span class="line"></span><br><span class="line">ascii_upload_enable=YES</span><br><span class="line"></span><br><span class="line">ascii_download_enable=YES</span><br></pre></td></tr></table></figure></p>
<p>同样将<strong>chroot_local_user=YES</strong>设置为yes。</p>
<p>被列入此文件的用户，在登录后将不能切换到自己目录以外的其他目录</p>
<p>从而有利于FTP服务器的安全管理和隐私保护。此文件需自己建立 ,里面存入用户名，一个用户占一行chroot_list_file=/etc/vsftpd/chroot_list</p>
<p>如果设置为<br>chroot_local_user＝YES#所有用户都只能访问自己的主目录</p>
<p>chroot_list_enable=YES(这行可以没有, 也可以有)</p>
<p>chroot_list_file=/etc/vsftpd.chroot_list</p>
<p>那么, 凡是加在文件vsftpd.chroot_list中的用户都是不受限止的用户，即, 可以浏览其主目录的上级目录.</p>
<p>如果不希望某用户能够浏览其主目录上级目录中的内容,可以如上设置,然后在文件vsftpd.chroot_list中去掉或不添加该用户即可。</p>
<p>也可以如下配置</p>
<p>chroot_local_user＝NO</p>
<p>chroot_list_enable=YES(这行必须要有, 否则文件vsftpd.chroot_list不会起作用)</p>
<p>chroot_list_file=/etc/vsftpd.chroot_list</p>
<p>然后把所有不希望有这种浏览其主目录之上的各目录权限的用户添加到文件vsftpd.chroot_list中即可(一行一个用户名，此时, 在该文件中的用户都是不可以浏览其主目录之外的目录的)<br><strong>关键步骤：</strong></p>
<ul>
<li><p>Mkdir /data/wwwroot #这里是根据自己需求来建立（就是FTP用户可以访问那个目录）</p>
</li>
<li><p>进入用户配置目录，没有的话自己建立，注意和vsftpd.conf文件中的对应 user_config_dir=/etc/vsftpd/userconf<br>cd /etc/vsftpd/userconf</p>
</li>
<li><p>建立一个同登录用户名称一样的文件(在配置userconf文件夹里创建)<br>vim ftpuser</p>
</li>
<li><p>写入用户登录FTP后能访问的目录位置：</p>
</li>
</ul>
<p>local_root=/www/test</p>
<ul>
<li>重启VSftp：</li>
</ul>
<p>/etc/init.d/vsftpd restart</p>
<p>之后即可用新建的用户访问自定义的文件夹了。</p>
]]></content>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>linux服务器搭配win7客户端的frp</title>
    <url>/2019/12/07/linux%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%90%AD%E9%85%8Dwin7%E5%AE%A2%E6%88%B7%E7%AB%AF%E7%9A%84frp/</url>
    <content><![CDATA[<h1 id="linux服务端搭配win7客户端的frp"><a href="#linux服务端搭配win7客户端的frp" class="headerlink" title="linux服务端搭配win7客户端的frp"></a>linux服务端搭配win7客户端的frp</h1><blockquote>
<p>teamviewer安装是方便一些，但是频繁的提示商业用途和不停的掉线太影响使用了。<br>我还是重新找了一个方法来远程电脑。</p>
</blockquote>
<a id="more"></a>
<h2 id="下载frp"><a href="#下载frp" class="headerlink" title="下载frp"></a>下载frp</h2><p>从<a href="https://github.com/fatedier/frp/releases" target="_blank" rel="noopener">https://github.com/fatedier/frp/releases</a> 下载最新版本的frp压缩包。因为是两种系统，所以把linux和windows的版本都下载了一份。</p>
<h2 id="linux服务端"><a href="#linux服务端" class="headerlink" title="linux服务端"></a>linux服务端</h2><p>使用WinSCP工具将压缩包传到了具有公网ip的云服务器上。解压后修改里面的frps.ini。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[common]</span><br><span class="line">bind_port = 7000           #与客户端绑定的进行通信的端口</span><br><span class="line">privilege_token = xxxx</span><br></pre></td></tr></table></figure></p>
<p>保存后启动服务./frps -c ./frps.ini。</p>
<h3 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h3><p>查看一下防火墙，记得打开7000端口。</p>
<h4 id="使用systemctl来控制自启动"><a href="#使用systemctl来控制自启动" class="headerlink" title="使用systemctl来控制自启动"></a>使用systemctl来控制自启动</h4><p>参考<a href="https://blog.csdn.net/x7418520/article/details/81077652" target="_blank" rel="noopener">Frp后台自动启动的几个方法</a>这篇博客配置<br>这个方法比较好用，很方便<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo vim /lib/systemd/system/frps.service</span><br></pre></td></tr></table></figure></p>
<p>在frps.service里写入以下内容<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[Unit]</span><br><span class="line">Description=fraps service</span><br><span class="line">After=network.target syslog.target</span><br><span class="line">Wants=network.target</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line">Type=simple</span><br><span class="line">#启动服务的命令（此处写你的frps的实际安装目录）</span><br><span class="line">ExecStart=/your/path/frps -c /your/path/frps.ini</span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure></p>
<p>然后就启动frps<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo systemctl start frps</span><br></pre></td></tr></table></figure></p>
<p>再打开自启动<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo systemctl enable frps</span><br></pre></td></tr></table></figure></p>
<p>如果要重启应用，可以这样，<strong>sudo systemctl restart frps</strong><br>如果要停止应用，可以输入，<strong>sudo systemctl stop frps</strong><br>如果要查看应用的日志，可以输入，<strong>sudo systemctl status frps</strong></p>
<h2 id="windows客户端"><a href="#windows客户端" class="headerlink" title="windows客户端"></a>windows客户端</h2><p>解压后修改其中的frpc.ini<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[common]</span><br><span class="line">server_addr = 服务器ip</span><br><span class="line">server_port = 7000</span><br><span class="line"></span><br><span class="line">[ssh]</span><br><span class="line">type = tcp</span><br><span class="line">local_ip = 127.0.0.1</span><br><span class="line">local_port = 3389 远程桌面端口</span><br><span class="line">remote_port = 7001 访问时的端口</span><br><span class="line">auth_token = xxxx 与服务器对应的token</span><br></pre></td></tr></table></figure></p>
<p>保存后启动服务<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">frpc.exe -c frpc.ini</span><br></pre></td></tr></table></figure></p>
<h3 id="注意-1"><a href="#注意-1" class="headerlink" title="注意"></a>注意</h3><p>这里可以配置成开机自动启动。<br>做一个.bat文件，文件内容：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\frp_0.30.0_windows_amd64\frpc.exe -c C:\frp_0.30.0_windows_amd64\frpc.ini</span><br></pre></td></tr></table></figure></p>
<p>再从任务计划程序中设置启动时运行文件即可。</p>
]]></content>
      <tags>
        <tag>frp</tag>
      </tags>
  </entry>
  <entry>
    <title>linux通过docker安装oracle的过程</title>
    <url>/2020/04/04/linux%E9%80%9A%E8%BF%87docker%E5%AE%89%E8%A3%85oracle%E7%9A%84%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="linux通过docker安装oracle的过程"><a href="#linux通过docker安装oracle的过程" class="headerlink" title="linux通过docker安装oracle的过程"></a>linux通过docker安装oracle的过程</h1><blockquote>
<p>业务需要，要在linux服务器上装一个oracle。作为一个没有安装经验的我来说，首先想到的就是整一个docker镜像，run一下岂不是很快乐。但是oracle公司早就防了一手，也才有的这个记录过程。<br>如果仅仅是个人使用，第一次的尝试就可以安装好oracle数据库。但是里面的数据太杂乱，就有了后面的尝试。</p>
</blockquote>
<a id="more"></a>
<h2 id="第一次尝试"><a href="#第一次尝试" class="headerlink" title="第一次尝试"></a>第一次尝试</h2><p>首先熟练的打开百度，打算去找一个镜像文档。排位最靠前的是<code>sath89/oracle</code>这个镜像的使用博客。但是可惜的是直接用<code>docker search oracle</code>命令已经搜不到这个镜像了。</p>
<p>改用第二个镜像<code>registry.cn-hangzhou.aliyuncs.com/helowin/oracle_11g</code></p>
<p>参考<a href="https://blog.csdn.net/qq_27050005/article/details/81479171" target="_blank" rel="noopener">https://blog.csdn.net/qq_27050005/article/details/81479171</a> 这篇博客来进行设置。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#pull镜像</span><br><span class="line">docker pull registry.cn-hangzhou.aliyuncs.com/helowin/oracle_11g</span><br><span class="line">#数据持久化：</span><br><span class="line">docker run -d -p 49160:22 -p 49161:1521 -v /jie:/u01/app/oracle/ --name xe  registry.cn-hangzhou.aliyuncs.com/helowin/oracle_11g</span><br><span class="line"> </span><br><span class="line">#====数据库默认信息</span><br><span class="line">#hostname: localhost</span><br><span class="line">#port: 1521</span><br><span class="line">#sid: helowin</span><br><span class="line">#username: system</span><br><span class="line">#password: helowin</span><br><span class="line"> </span><br><span class="line">docker exec -it xe bash</span><br><span class="line">su root # 密码：  helowin</span><br><span class="line">#====vi /etc/profile 并在文件最后添加如下命令</span><br><span class="line">#begin</span><br><span class="line">export ORACLE_HOME=/home/oracle/app/oracle/product/11.2.0/dbhome_2</span><br><span class="line">export ORACLE_SID=helowin</span><br><span class="line">export PATH=$ORACLE_HOME/bin:$PATH</span><br><span class="line"># esc :wq</span><br><span class="line"></span><br><span class="line">#这条保存配置命令如果不配合，在~/.bashrc里面加一句source /etc/profile的话。再次进入后会失效</span><br><span class="line">source /etc/profile</span><br><span class="line"></span><br><span class="line"># 加个软连接</span><br><span class="line">ln -s $ORACLE_HOME/bin/sqlplus /usr/bin</span><br><span class="line"> </span><br><span class="line">#====登录oracle数据库 ，修改密码</span><br><span class="line">su oracle ;</span><br><span class="line">sqlplus  </span><br><span class="line"># username: system</span><br><span class="line"># password: helowin</span><br><span class="line">alter user system identified by 123456;</span><br><span class="line">ALTER PROFILE DEFAULT LIMIT PASSWORD_LIFE_TIME UNLIMITED;</span><br><span class="line">#退出</span><br><span class="line">#===使用数据库工具 登录oracle</span><br><span class="line">hostname: localhost</span><br><span class="line">#记得开放你云服务的tcp 46161端口</span><br><span class="line">port: 46161</span><br><span class="line">sid: helowin</span><br><span class="line">username: system</span><br><span class="line">password: 123456</span><br></pre></td></tr></table></figure></p>
<p>但是碰到了容器打开后无法通过服务器ip+49161访问数据库，linux本机用<code>wget</code>命令也无法联通。</p>
<p>查看防火墙端口<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">开启端口</span><br><span class="line">[root@centos7 ~]# firewall-cmd --zone=public --add-port=1521/tcp --permanent</span><br><span class="line"></span><br><span class="line">重启防火墙：</span><br><span class="line"></span><br><span class="line">[root@centos7 ~]# firewall-cmd --reload</span><br><span class="line"></span><br><span class="line">查询端口号1521 是否开启：</span><br><span class="line">[root@centos7 ~]# firewall-cmd --query-port=1521/tcp</span><br><span class="line"></span><br><span class="line">查询有哪些端口是开启的:</span><br><span class="line"></span><br><span class="line">[root@centos7 ~]# firewall-cmd --list-port</span><br><span class="line"></span><br><span class="line">--zone #作用域</span><br><span class="line">--add-port=1521/tcp #添加端口，格式为：端口/通讯协议</span><br><span class="line">--permanent #永久生效，没有此参数重启后失效</span><br></pre></td></tr></table></figure></p>
<p>而用腾讯云的机器同样配置却可以访问。</p>
<p>尝试更改网络映射失败后，更换了参数，使用–net=host<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -d  --net=host -v /root/oracle:/u01/app/oracle/ --name oracle11g  registry.cn-hangzhou.aliyuncs.com/helowin/oracle_11g</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>此处命令如果没有删除端口映射的话，会报一个warning，提示端口映射无效。</p>
</blockquote>
<p>这样配置了之后可以成功的在其他局域网机器上访问。</p>
<p>但是问题来了，这个库里自己存在了很多表。大佬说找找有没有纯净一点的。</p>
<h2 id="第二次"><a href="#第二次" class="headerlink" title="第二次"></a>第二次</h2><p>又找到一个博客<a href="https://mileslin.github.io/2019/06/%E4%BD%BF%E7%94%A8-Docker-%E5%BB%BA%E7%AB%8B-Oracle-%E6%9C%8D%E5%8B%99/" target="_blank" rel="noopener">https://mileslin.github.io/2019/06/%E4%BD%BF%E7%94%A8-Docker-%E5%BB%BA%E7%AB%8B-Oracle-%E6%9C%8D%E5%8B%99/</a> ，这次用的是dockerhub上的oracle提供的镜像。（夸赞一下某个不存在的搜索引擎）</p>
<p>打开<a href="https://hub.docker.com/_/oracle-database-enterprise-edition" target="_blank" rel="noopener">Oracle Database Enterprise Edition</a>，需要点击<code>Proceed to Checkout</code> 。因为Oracle将镜像放在了<a href="https://success.docker.com/article/store" target="_blank" rel="noopener">Docker Store</a> （Docker提供给厂商进行商业行为的Repository）<br><img src="/2020/04/04/linux通过docker安装oracle的过程/b1.png" alt="Proceed to Checkout"></p>
<p>这样的话就需要我们登录dockerhub，点击之后需要填写一下资料<br><img src="/2020/04/04/linux通过docker安装oracle的过程/b2.png" alt="Agreement"></p>
<p>接下来到Docker Profile的My Content就可以找到Oracle的内容<br><img src="/2020/04/04/linux通过docker安装oracle的过程/b3.png" alt="Added_to_my_content"></p>
<p>点击<code>Setup</code>，就可以看到Oracle镜像的pull地址。</p>
<p>问题在于有点卡，下载了8个多小时。这中间还中断了几次，我就去尝试了另外的镜像。</p>
<p>下载完后，按照官方文档去启动容器.因为之前的端口局域网其他机器不能访问的问题没有解决，所以这里还是使用host模式。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -d -it --name oracle -v /home/OracleDBData:/ORCL --net=host store/oracle/database-enterprise:12.2.0.1</span><br></pre></td></tr></table></figure></p>
<p>成功启动后，进入容器内部<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker exec -it 4d7f(容器id简写) bash</span><br></pre></td></tr></table></figure></p>
<p>测试用户名密码，提示要用sysdba角色登录。于是边用下面的命令登录成功。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sqlplus / as sysdba</span><br><span class="line"></span><br><span class="line">username:system</span><br><span class="line">password:Oradoc_db1</span><br><span class="line"></span><br><span class="line">#sqlplus system/Oradoc_db1 也行</span><br></pre></td></tr></table></figure></p>
<p>但是用navicat去连的时候，却报<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ORA-12505, TNS:listener does not currently know of SID given in connect descriptor</span><br></pre></td></tr></table></figure></p>
<p>第一个反应是博客记录有点问题，去找了找官方的评论区。有推荐用<code>ORCLPDB1.localdomain</code>做服务名，也是不行。再看看官方文档，和博客的说法一样。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">DB_SID</span><br><span class="line">This parameter changes the ORACLE_SID of the database. The default value is set to ORCLCDB</span><br></pre></td></tr></table></figure></p>
<p>找了<a href="https://stackoverflow.com/questions/18192521/ora-12505-tnslistener-does-not-currently-know-of-sid-given-in-connect-descript/46848944" target="_blank" rel="noopener">stackoverflow.com</a> 上的一篇博客。测试了一下进入sqlplus后执行<code>ALTER SYSTEM REGISTER</code>命令。还是不行</p>
<p>大佬提供了一篇博客<a href="https://blog.csdn.net/shen_xiao_wei/article/details/5729463" target="_blank" rel="noopener">https://blog.csdn.net/shen_xiao_wei/article/details/5729463</a><br>通过博客里的<code>find -name listener.ora</code>命令，找到文件<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vi ./u01/app/oracle/product/12.2.0/dbhome_1/network/admin/samples/listener.ora</span><br></pre></td></tr></table></figure></p>
<p>发现里面全都是被注释的语句，自己加上以下的语句并没有什么变化，依旧无法连接。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">SID_LIST_LISTENER =</span><br><span class="line">        (SID_LIST =</span><br><span class="line">             (SID_DESC =</span><br><span class="line">                   (SID_NAME = ORCLCDB)</span><br><span class="line">                   (ORACLE_HOME = /private/app/oracle/product/8.0.3)</span><br><span class="line">                   (PROGRAM = ORCLCDB)</span><br><span class="line">             )</span><br><span class="line">      </span><br><span class="line">        )</span><br></pre></td></tr></table></figure></p>
<p>再看一看另外一个同名文件<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vi /u01/app/oracle/product/12.2.0/dbhome_1/admin/ORCLCDB/listener.ora</span><br></pre></td></tr></table></figure></p>
<p>里面只有以下的语句，把上面的语句加入后反而<code>lsnrctl</code>无法启动了。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">LISTENER =   (DESCRIPTION_LIST =     (DESCRIPTION =       (ADDRESS = (PROTOCOL = TCP)(HOST = 0.0.0.0)(PORT = 1521))       (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))     )   )</span><br><span class="line"></span><br><span class="line">#报错</span><br><span class="line">TNS-01150: The address of the specified listener name is incorrect</span><br></pre></td></tr></table></figure></p>
<p>再次仔细阅读了<strong>stackoverflow.com</strong>的博客，发现评论里有一条说法<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Now you can see the service</span><br><span class="line">Even if don&apos;t see try this one out</span><br><span class="line"></span><br><span class="line">sqlplus /nolog  </span><br><span class="line">conn system  </span><br><span class="line">alter system set local_listener = &apos;(ADDRESS=(PROTOCOL=TCP)(HOST=localhost)(PORT=1521))&apos; scope = both;  </span><br><span class="line">alter system register;  </span><br><span class="line">exit  </span><br><span class="line">lsnrctl status</span><br></pre></td></tr></table></figure></p>
<p>尝试了一下，效果十分明显。错误已经变成了<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ORA-01017： invalid username/password;logon denied</span><br></pre></td></tr></table></figure></p>
<p>在navicat里面重新输入了一遍SID、账号、密码和角色。点击测试连接，显示成功。点击确认，显示错误。<br><img src="/2020/04/04/linux通过docker安装oracle的过程/b4.png" alt=""><br>忽略中间大起大落的心态问题，去百度了一下。简单的在navicat中删除了之前第一次成功时建立的服务器连接解决问题。</p>
<h2 id="第三次"><a href="#第三次" class="headerlink" title="第三次"></a>第三次</h2><p>大佬提供了另外的一个博客<a href="https://www.35youth.cn/685.html" target="_blank" rel="noopener">使用Docker安装oracle 11g</a> 。<br>这篇使用的镜像是jaspeen/oracle-11g。之前曾经直接下载过，但是run后直接闪退，就放弃了。这次按照博客的步骤来<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull jaspeen/oracle-11g</span><br></pre></td></tr></table></figure></p>
<p>准备oracle压缩包，在<a href="https://www.oracle.com/database/technologies/oracle-database-software-downloads.html" target="_blank" rel="noopener">Oracle官网</a> 分别下载 linux.x64_11gR2_database_1of2.zip 和 linux.x64_11gR2_database_2of2.zip两个压缩包，下载完成后解压到home目录(如下目录结构)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">home</span><br><span class="line">  └─database</span><br><span class="line">      ├─doc</span><br><span class="line">      ├─install</span><br><span class="line">      ├─response</span><br><span class="line">      ├─rpm</span><br><span class="line">      ├─sshsetup</span><br><span class="line">      ├─stage</span><br><span class="line">      ├─runInstaller</span><br><span class="line">      └─welcome.html</span><br></pre></td></tr></table></figure></p>
<p>略过为什么要解压在这个包下，我直接启动容器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --privileged -d -v /home/oracle:/install/database --name jaspeen --net=host jaspeen/oracle-11g</span><br></pre></td></tr></table></figure>
<p>这次的命令是我输入错了。我的压缩包解压在home目录，我指定/home/oracle后导致启动报错<br><strong>/install/database/runInstaller: No such file or directory</strong>。</p>
<p>再次启动还是不行<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --privileged --name jaspeen --net=host -v /home:/install jaspeen/oracle-11g</span><br></pre></td></tr></table></figure></p>
<p>报错<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Database is not installed. Installing...</span><br><span class="line">Installing Oracle Database 11g</span><br><span class="line">bash: /install/database/runInstaller: Permission denied</span><br></pre></td></tr></table></figure></p>
<p>不知道是<code>--privileged</code>没有生效，还是权限不足。尝试Selinux临时关闭，<code>setenforce 0</code>也不能解决问题。</p>
<h2 id="第四次"><a href="#第四次" class="headerlink" title="第四次"></a>第四次</h2><p>又换了一个镜像，这次我看到镜像描述里说是sath89/oracle-12c的复制。就尝试一下。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull truevoly/oracle-12c</span><br><span class="line">docker run -d --privileged --net=host -v /home/oracle:/u01/app/oracle/ --name sath89  truevoly/oracle-12c</span><br></pre></td></tr></table></figure></p>
<p>结果启动容器失败<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Database not initialized. Initializing database.</span><br><span class="line">Starting tnslsnr</span><br><span class="line">Cannot create directory &quot;/u01/app/oracle/cfgtoollogs/dbca&quot;.</span><br><span class="line"></span><br><span class="line">Unique database identifier check passed.</span><br><span class="line">Error writing into silent log -- /u01/app/oracle/cfgtoollogs/dbca/silent.log_2020-04-01_07-33-18-AM (No such file or directory)</span><br><span class="line"></span><br><span class="line">/u01/app/oracle/ has enough space. Required space is 6140 MB , available space is 40222 MB.</span><br><span class="line">File Validations Successful.</span><br><span class="line">Error writing into silent log -- /u01/app/oracle/cfgtoollogs/dbca/silent.log_2020-04-01_07-33-18-AM (No such file or directory)</span><br><span class="line">Cannot create directory &quot;/u01/app/oracle/cfgtoollogs/dbca/xe&quot;.</span><br><span class="line">Error writing into silent log -- /u01/app/oracle/cfgtoollogs/dbca/silent.log_2020-04-01_07-33-18-AM (No such file or directory)</span><br><span class="line">Look at the log file &quot;/u01/app/oracle/cfgtoollogs/dbca/silent.log_2020-04-01_07-33-18-AM&quot; for further details.</span><br><span class="line">Error in file copy from &lt;/u01/app/oracle/cfgtoollogs/dbca/silent.log_2020-04-01_07-33-18-AM&gt; to &lt;/u01/app/oracle/cfgtoollogs/dbca/xe.log&gt;</span><br></pre></td></tr></table></figure></p>
<p>再次选择放弃。返回第二个官方镜像</p>
]]></content>
      <tags>
        <tag>oracle</tag>
      </tags>
  </entry>
  <entry>
    <title>mycat全局表错误</title>
    <url>/2020/08/30/mycat%E5%85%A8%E5%B1%80%E8%A1%A8%E9%94%99%E8%AF%AF/</url>
    <content><![CDATA[<h1 id="mycat全局表错误"><a href="#mycat全局表错误" class="headerlink" title="mycat全局表错误"></a>mycat全局表错误</h1><blockquote>
<p>具体报错内容为：can’t find table define in schema DIC_HIDDEN_DANGER_ADM schema:risk</p>
</blockquote>
<a id="more"></a>
<h2 id="缘由"><a href="#缘由" class="headerlink" title="缘由"></a>缘由</h2><p>部署时新加入了字典表，在mycat连接的两个库里都写入了一次。但是在查询时，和第一个库的普通表 join 时可以查询。和在另一个库的分表后的表 join 时会显示无法查询出字典表。</p>
<h2 id="处理"><a href="#处理" class="headerlink" title="处理"></a>处理</h2><p>在 schemal.xml 中将新写入的字典表加入 node 节点，添加  type=”global” 标记为全局表</p>
<h2 id="二次报错"><a href="#二次报错" class="headerlink" title="二次报错"></a>二次报错</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	r.dictionary_id <span class="keyword">AS</span> rid,</span><br><span class="line">	r.date_time <span class="keyword">AS</span> dateTime,</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	bs_mountain r</span><br><span class="line">	<span class="keyword">INNER</span> <span class="keyword">JOIN</span> dic_mountain ra <span class="keyword">ON</span> ra.id = r.dictionary_id</span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">	r.date_time <span class="keyword">BETWEEN</span> <span class="string">'2020-10-14 00:00:00'</span> </span><br><span class="line">	<span class="keyword">AND</span> <span class="string">'2020-10-15 00:00:00'</span> ;</span><br></pre></td></tr></table></figure>
<p>可以确定的是 bs_mountain 表中是肯定有 dictionary_id 字段和 date_time ，但还是报出了 <code>Unknown column &#39;r.dictionary_id&#39; in &#39;field list&#39;</code> 错误。</p>
<h2 id="二次处理"><a href="#二次处理" class="headerlink" title="二次处理"></a>二次处理</h2><p>通过 explain 关键字分析在 mycat 中执行的语句，得出结果<br>实际执行语句为<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	r.dictionary_id <span class="keyword">AS</span> rid,</span><br><span class="line">	r.date_time <span class="keyword">AS</span> dateTime,</span><br><span class="line">	</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	bs_mountain_32 </span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">	r.date_time <span class="keyword">BETWEEN</span> <span class="string">'2020-10-14 00:00:00'</span> </span><br><span class="line">	<span class="keyword">AND</span> <span class="string">'2020-10-15 00:00:00'</span></span><br></pre></td></tr></table></figure></p>
<p>mycat 在改写 sql 语句到分表的时候漏下了别名</p>
<p>而且当我的语句是：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	r.dictionary_id <span class="keyword">AS</span> rid,</span><br><span class="line">	r.date_time <span class="keyword">AS</span> dateTime,</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	bs_mountain r</span><br><span class="line">	<span class="keyword">INNER</span> <span class="keyword">JOIN</span> dic_mountain ra <span class="keyword">ON</span> ra.id = r.dictionary_id</span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">	r.date_time <span class="keyword">BETWEEN</span> <span class="string">'2020-10-14 00:00:00'</span> </span><br><span class="line">	<span class="keyword">AND</span> <span class="string">'2020-10-15 00:00:00'</span> </span><br><span class="line">	<span class="keyword">AND</span> ra.id = <span class="string">'1'</span>;</span><br></pre></td></tr></table></figure></p>
<p>会去执行一个全分表查询，不再单单只是定位到32分表的查询。</p>
<p>开始百度关键字 <code>mycat 别名 全局表 join</code>，发现问题是出在 mycat 在单库分表和未分表关联查询时会出现的别名丢失问题。</p>
<p>并没有去下载源码修改后重新打包，而是在业务中去过滤了。</p>
<p>直接使用sql语句将要关联的表手动查询出来后，再代码中拼接关联查询。</p>
]]></content>
      <tags>
        <tag>mycat</tag>
      </tags>
  </entry>
  <entry>
    <title>mycat服务器部署</title>
    <url>/2019/09/14/mycat%E6%9C%8D%E5%8A%A1%E5%99%A8%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h1 id="mycat服务器部署"><a href="#mycat服务器部署" class="headerlink" title="mycat服务器部署"></a>mycat服务器部署</h1><blockquote>
<p>记录一下自己将mycat部署到服务器上的步骤和遇到的bug。</p>
</blockquote>
<a id="more"></a>
<h2 id="前置条件"><a href="#前置条件" class="headerlink" title="前置条件"></a>前置条件</h2><p>已经完成在服务器部署多个mysql服务的任务，mysql版本选择为8.0以上。</p>
<h2 id="下载mycat"><a href="#下载mycat" class="headerlink" title="下载mycat"></a>下载mycat</h2><p>直接从github上面下载最新版本的mycat，(<a href="https://github.com/MyCATApache/Mycat-Server)[https://github.com/MyCATApache/Mycat-Server]" target="_blank" rel="noopener">https://github.com/MyCATApache/Mycat-Server)[https://github.com/MyCATApache/Mycat-Server]</a> 。</p>
<h2 id="解压后配置文件"><a href="#解压后配置文件" class="headerlink" title="解压后配置文件"></a>解压后配置文件</h2><blockquote>
<p>目前版本为mycat1.6.7，之后的版本更新后配置方式以官方文档为准。</p>
</blockquote>
<p>重要的有三个文件需要配置</p>
<h3 id="rule-xml"><a href="#rule-xml" class="headerlink" title="rule.xml"></a>rule.xml</h3><p>这个配置文件定义分片规则，目前只是用到了这些参数：时间格式，分片开始时间，结束时间。</p>
<p><strong>注意：</strong>这里的这个时间段是要对应之后的schema.xml中的table标签的subTables属性的分表个数。例如下面的按月分表从2019年1月一直分到2024年12月的规则，那么相对应的subTables属性就需要固定好从2019年1月分到2024年12月的表名<br><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">function</span> <span class="attr">name</span>=<span class="string">"partbymonthofair"</span></span></span><br><span class="line"><span class="tag">	<span class="attr">class</span>=<span class="string">"io.mycat.route.function.PartitionByMonth"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dateFormat"</span>&gt;</span>yyyy-MM<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"sBeginDate"</span>&gt;</span>2019-01<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"sEndDate"</span>&gt;</span>2024-12<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">function</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>规定表分片规则<br><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">tableRule</span> <span class="attr">name</span>=<span class="string">"env-air-sharding-by-date"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">rule</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">columns</span>&gt;</span>date_time<span class="tag">&lt;/<span class="name">columns</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">algorithm</span>&gt;</span>partbymonthofair<span class="tag">&lt;/<span class="name">algorithm</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">rule</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tableRule</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<h3 id="schema-xml"><a href="#schema-xml" class="headerlink" title="schema.xml"></a>schema.xml</h3><p>在这里定义表的规则。<br><strong>schema</strong>标签是用于定义具体的库，之后的server.xml中配置的访问用户需要有对这个库的操作权限。<br>其中的属性：</p>
<ul>
<li><strong>checkSQLschema</strong>是检查sql语句中是否带有env_v2这个名字，true为自动删除sql语句中的这个名字。</li>
<li><strong> sqlMaxLimit</strong>是指自动在查询语句中限制返回的条数，sql语句中有的话会自动替换。</li>
<li><strong>dataNode</strong>是用于定义具体的库，之后的server.xml中配置的访问用户需要有对这个库的操作权限。</li>
</ul>
<p><strong>table</strong>标签定义MyCat 中的逻辑表。<br>其中的属性：</p>
<ul>
<li><strong>name</strong>定义逻辑表的表名，这个名字就如同我在数据库中执行 create table 命令指定的名字一样，同schema 标签中定义的名字必须唯一。</li>
<li><strong>dataNode</strong>定义这个逻辑表所属的 dataNode, 该属性的值需要和 dataNode 标签中 name 属性的值相互对应。</li>
<li>如果需要定义的 dn 过多 可以使用如下的方法减少配置。（此次出自mysql权威指南）</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"travelrecord"</span> <span class="attr">dataNode</span>=<span class="string">"multipleDn$0-99,multipleDn2$100-199"</span><span class="attr">rule</span>=<span class="string">"auto-sharding-long"</span> &gt;</span><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line"></span><br><span class="line">&lt;dataNode name="multipleDn$0-99" dataHost="localhost1" database="db$0-99"&lt;/dataNode&gt;</span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dataNode</span> <span class="attr">name</span>=<span class="string">"multipleDn2$100-199"</span> <span class="attr">dataHost</span>=<span class="string">"localhost1"</span> <span class="attr">database</span>=<span class="string">" db$100-199"</span>&gt;</span><span class="tag">&lt;/<span class="name">dataNode</span>&gt;</span></span><br></pre></td></tr></table></figure>
<ul>
<li><strong>rule</strong>该属性用于指定逻辑表要使用的规则名字，规则名字在 rule.xml 中定义，必须和tableRule 标签中 name 属性属性值一一对应。</li>
<li><strong>needAddLimit</strong>是否需要自动的在每个语句后面加上 limit 限制，默认为true。</li>
</ul>
<h4 id="实例："><a href="#实例：" class="headerlink" title="实例："></a>实例：</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">schema</span> <span class="attr">name</span>=<span class="string">"db"</span> <span class="attr">checkSQLschema</span>=<span class="string">"false"</span> <span class="attr">sqlMaxLimit</span>=<span class="string">"300"</span> <span class="attr">dataNode</span>=<span class="string">"dn2"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"gather_air_data"</span> <span class="attr">subTables</span>=<span class="string">"gather_air_data_$201901-201912,gather_air_data_$202001-202012,gather_air_data_$202101-202112,gather_air_data_$202201-202212,gather_air_data_$202301-202312,gather_air_data_$202401-202412"</span> <span class="attr">dataNode</span>=<span class="string">"dn3"</span> <span class="attr">rule</span>=<span class="string">"env-air-sharding-by-date"</span>  <span class="attr">needAddLimit</span> =<span class="string">"false"</span>/&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"gather_city_aqi_data"</span> <span class="attr">subTables</span>=<span class="string">"gather_city_aqi_data_$201901-201912,gather_city_aqi_data_$202001-202012,gather_city_aqi_data_$202101-202112,gather_city_aqi_data_$202201-202212,gather_city_aqi_data_$202301-202312,gather_city_aqi_data_$202401-202412"</span> <span class="attr">dataNode</span>=<span class="string">"dn3"</span> <span class="attr">rule</span>=<span class="string">"env-air-sharding-by-date"</span>  <span class="attr">needAddLimit</span> =<span class="string">"false"</span>/&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">table</span> <span class="attr">name</span>=<span class="string">"gather_observation_factor"</span> <span class="attr">subTables</span>=<span class="string">"gather_observation_factor_$201901-201912,gather_observation_factor_$202001-202012,gather_observation_factor_$202101-202112,gather_observation_factor_$202201-202212,gather_observation_factor_$202301-202312,gather_observation_factor_$202401-202412"</span> <span class="attr">dataNode</span>=<span class="string">"dn3"</span> <span class="attr">rule</span>=<span class="string">"env-air-sharding-by-date"</span>  <span class="attr">needAddLimit</span> =<span class="string">"false"</span>/&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">schema</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>dataNode</strong> 标签定义了 MyCat 中的数据节点<br>其中的属性：</p>
<ul>
<li><strong>name</strong>定义数据节点的名字</li>
<li><strong>dataHost</strong>该属性用于定义该分片属于哪个数据库实例的，属性值是引用dataHost标签上定义的 name属性</li>
<li><strong>database</strong>该属性用于定义该分片属性哪个具体数据库实例上的具体库<h4 id="实例：-1"><a href="#实例：-1" class="headerlink" title="实例："></a>实例：</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dataNode</span> <span class="attr">name</span>=<span class="string">"dn2"</span> <span class="attr">dataHost</span>=<span class="string">"localhost"</span> <span class="attr">database</span>=<span class="string">"db"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dataNode</span> <span class="attr">name</span>=<span class="string">"dn3"</span> <span class="attr">dataHost</span>=<span class="string">"localhost"</span> <span class="attr">database</span>=<span class="string">"db_v2"</span> /&gt;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p><strong>dataHost</strong>标签定义了具体的数据库实例、读写分离配置和心跳语句<br>其中的属性：</p>
<ul>
<li><strong>maxCon</strong>与<strong>minCon</strong>指定每个读写实例连接池的最大与最小连接，以及初始化连接池的大小。</li>
<li><strong>balance</strong>是负载均衡类型，有三种取值<ol>
<li>balance=”0”, 不开启读写分离机制，所有读操作都发送到当前可用的 writeHost 上</li>
<li>balance=”1”，全部的 readHost 与 stand by writeHost 参与 select 语句的负载均衡，简单的说，当双主双从模式(M1-&gt;S1，M2-&gt;S2，并且 M1 与 M2 互为主备)，正常情况下M2,S1,S2 都参与 select 语句的负载均衡。</li>
<li>balance=”2”，所有读操作都随机的在 writeHost、readhost 上分发。</li>
<li>balance=”3”，所有读请求随机的分发到 wiriterHost 对应的 readhost 执行，writerHost 不负担读压力。</li>
</ol>
</li>
<li><strong>writeType</strong>负载均衡类型（具体还是不太明白，套的官方示例）</li>
<li><strong>dbType</strong>指定后端连接的数据库类型</li>
<li><strong>dbDriver</strong>指定连接后端数据库使用的 Driver，目前可选的值有 native 和 JDBC。</li>
</ul>
<p><strong>heartbeat</strong>标签内指明用于和后端数据库进行心跳检查的语句。例如,MYSQL 可以使用 select user()。</p>
<p><strong>writeHost 标签</strong>、<strong>readHost 标签</strong>两个标签都指定后端数据库的相关配置给 mycat，用于实例化后端连接池。<br>其中的属性：</p>
<ul>
<li><strong>url</strong>后端实例连接地址</li>
<li><strong>user</strong>后端存储实例需要的用户名字。</li>
<li><strong>password</strong>后端存储实例需要的密码。</li>
</ul>
<h4 id="实例：-2"><a href="#实例：-2" class="headerlink" title="实例："></a>实例：</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dataHost</span> <span class="attr">name</span>=<span class="string">"localhost"</span> <span class="attr">maxCon</span>=<span class="string">"1000"</span> <span class="attr">minCon</span>=<span class="string">"10"</span> <span class="attr">balance</span>=<span class="string">"1"</span> <span class="attr">writeType</span>=<span class="string">"0"</span> <span class="attr">dbType</span>=<span class="string">"mysql"</span> <span class="attr">dbDriver</span>=<span class="string">"native"</span> <span class="attr">switchType</span>=<span class="string">"-1"</span>  <span class="attr">slaveThreshold</span>=<span class="string">"100"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">heartbeat</span>&gt;</span>select user()<span class="tag">&lt;/<span class="name">heartbeat</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">writeHost</span> <span class="attr">host</span>=<span class="string">"hostMaster"</span> <span class="attr">url</span>=<span class="string">"localhost:3306"</span> <span class="attr">user</span>=<span class="string">"root"</span> <span class="attr">password</span>=<span class="string">"root"</span> /&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">writeHost</span> <span class="attr">host</span>=<span class="string">"hostSalve"</span> <span class="attr">url</span>=<span class="string">"localhost:3308"</span> <span class="attr">user</span>=<span class="string">"root"</span> <span class="attr">password</span>=<span class="string">"root"</span> /&gt;</span></span><br><span class="line">		</span><br><span class="line"><span class="tag">&lt;/<span class="name">dataHost</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="server-xml"><a href="#server-xml" class="headerlink" title="server.xml"></a>server.xml</h3><p>server.xml 几乎保存了所有 mycat 需要的系统配置信息。<br>以下内容出自mycat权威指南:</p>
<ul>
<li><p>server.xml 中的标签本就不多，这个标签主要用于定义登录 mycat 的用户和权限。例如上面的例子中，我定义了一个用户，用户名为 test、密码也为 test，可访问的 schema 也只有 TESTDB 一个。</p>
</li>
<li><p>如果我在 schema.xml 中定义了多个 schema，那么这个用户是无法访问其他的 schema。在 mysql 客户端看来则是无法使用 use 切换到这个其他的数据库。</p>
</li>
</ul>
<p>实例：<br><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">user</span> <span class="attr">name</span>=<span class="string">"root"</span> <span class="attr">defaultAccount</span>=<span class="string">"true"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span>&gt;</span>root<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"schemas"</span>&gt;</span>db<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">user</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<h2 id="配置成服务并启动"><a href="#配置成服务并启动" class="headerlink" title="配置成服务并启动"></a>配置成服务并启动</h2><p>从./bin目录下打开命令行启动mycat</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mycat install #配置成服务</span><br><span class="line"></span><br><span class="line">mycat start #启动服务</span><br><span class="line"></span><br><span class="line">mycat stop #停止服务</span><br></pre></td></tr></table></figure>
<h2 id="后记："><a href="#后记：" class="headerlink" title="后记："></a>后记：</h2><p>可以使用在server.xml中配置好的用户去远程访问，记得开启服务器的防火墙8066端口。</p>
<h3 id="遇到的bug"><a href="#遇到的bug" class="headerlink" title="遇到的bug"></a>遇到的bug</h3><p>在将程序部署到服务器上时遇到了一些问题。</p>
<h4 id="1-jpa的save方法在传入id修改数据时，是全部数据字段修改。而mycat不能修改分片的时间根据字段。"><a href="#1-jpa的save方法在传入id修改数据时，是全部数据字段修改。而mycat不能修改分片的时间根据字段。" class="headerlink" title="1. jpa的save方法在传入id修改数据时，是全部数据字段修改。而mycat不能修改分片的时间根据字段。"></a>1. jpa的save方法在传入id修改数据时，是全部数据字段修改。而mycat不能修改分片的时间根据字段。</h4><p>报错：<strong>Sharding column can’t be updated GATHER_OBSERVATION_FACTOR-&gt;DATE_TIME</strong><br>处理方式：将datetime字段加上@Column(updatable=false)注释</p>
<h4 id="2-jpa对一条数据查出来后，直接把原数据的id改为null会报错"><a href="#2-jpa对一条数据查出来后，直接把原数据的id改为null会报错" class="headerlink" title="2.jpa对一条数据查出来后，直接把原数据的id改为null会报错"></a>2.jpa对一条数据查出来后，直接把原数据的id改为null会报错</h4><p>报错：<strong>identifier of an instance of .. altered to null</strong><br>处理方式：不能直接更新已经查到的数据，要new一个对象实例，进行属性复制（BeanUtils.copyProperties(source, dist)）</p>
<h4 id="3-在rule-xml的tableRule标签中的分片根据表字段columns不能为空，为空无法分片"><a href="#3-在rule-xml的tableRule标签中的分片根据表字段columns不能为空，为空无法分片" class="headerlink" title="3.在rule.xml的tableRule标签中的分片根据表字段columns不能为空，为空无法分片"></a>3.在rule.xml的tableRule标签中的分片根据表字段columns不能为空，为空无法分片</h4><p>报错：<strong>columnValue:NULL Please check if the format satisfied.</strong><br>处理方式：删除掉为null的数据</p>
<h4 id="4-jpa删除时注意实际执行语句，有时执行语句是按id删除的。mycat会报错"><a href="#4-jpa删除时注意实际执行语句，有时执行语句是按id删除的。mycat会报错" class="headerlink" title="4.jpa删除时注意实际执行语句，有时执行语句是按id删除的。mycat会报错"></a>4.jpa删除时注意实际执行语句，有时执行语句是按id删除的。mycat会报错</h4>]]></content>
      <tags>
        <tag>mycat</tag>
      </tags>
  </entry>
  <entry>
    <title>mysql简洁配置只读用户</title>
    <url>/2020/05/15/mysql%E7%AE%80%E6%B4%81%E9%85%8D%E7%BD%AE%E5%8F%AA%E8%AF%BB%E7%94%A8%E6%88%B7/</url>
    <content><![CDATA[<h1 id="mysql简洁配置只读用户"><a href="#mysql简洁配置只读用户" class="headerlink" title="mysql简洁配置只读用户"></a>mysql简洁配置只读用户</h1><blockquote>
<p>这里只是记录一下自己对数据库的操作。之后可以直接重新使用。</p>
</blockquote>
<a id="more"></a>
<h2 id="过程"><a href="#过程" class="headerlink" title="过程"></a>过程</h2><p>删除已有用户，之前配置错误。需要重新来。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">drop user &apos;qw&apos;@&apos;%&apos;;</span><br></pre></td></tr></table></figure></p>
<p>配置用户<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">CREATE USER &apos;qw&apos;@&apos;%&apos; IDENTIFIED BY &apos;qw123456&apos;;</span><br></pre></td></tr></table></figure></p>
<p>分配只读权限，前面的*是库名。后面的*是表名<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">GRANT select ON *.* TO &apos;qw&apos;@&apos;%&apos;;</span><br></pre></td></tr></table></figure></p>
<p>刷新权限<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">flush privileges;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>mysql配置主从复制</title>
    <url>/2019/09/20/mysql%E9%85%8D%E7%BD%AE%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6/</url>
    <content><![CDATA[<h1 id="mysql配置主从复制"><a href="#mysql配置主从复制" class="headerlink" title="mysql配置主从复制"></a>mysql配置主从复制</h1><blockquote>
<p>本文来自<a href="https://www.cnblogs.com/gl-developer/p/6170423.html" target="_blank" rel="noopener">https://www.cnblogs.com/gl-developer/p/6170423.html</a>, 仅仅为了下次使用做一个备份。</p>
</blockquote>
<a id="more"></a>
<h2 id="配置过程"><a href="#配置过程" class="headerlink" title="配置过程"></a>配置过程</h2><ul>
<li>主服务器：<ul>
<li>开启二进制日志</li>
<li>配置唯一的server-id</li>
<li>获得master二进制日志文件名及位置</li>
<li>创建一个用于slave和master通信的用户账号</li>
</ul>
</li>
<li>从服务器：<ul>
<li>配置唯一的server-id</li>
<li>使用master分配的用户账号读取master二进制日志</li>
<li>启用slave服务<h3 id="一丶主数据库master修改："><a href="#一丶主数据库master修改：" class="headerlink" title="一丶主数据库master修改："></a>一丶主数据库master修改：</h3><strong>1.</strong>修改mysql配置<br>找到主数据库的配置文件my.cnf(或者my.ini)，在[mysqld]部分插入如下两行：<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">log-bin=mysql-bin #开启二进制日志</span><br><span class="line">server-id=1 #设置server-id</span><br><span class="line"></span><br><span class="line"># 不同步哪些数据库  </span><br><span class="line">binlog-ignore-db = mysql  </span><br><span class="line">binlog-ignore-db = test  </span><br><span class="line">binlog-ignore-db = information_schema  </span><br><span class="line">  </span><br><span class="line"># 只同步哪些数据库，除此之外，其他不同步  </span><br><span class="line">binlog-do-db = game</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<p><strong>2.</strong>重启mysql，创建用于同步的用户账号<br>打开mysql命令行</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"> mysql -u root -p <span class="comment">--protocol=tcp --host=localhost --port=3308（选择端口）</span></span><br><span class="line"> <span class="keyword">use</span> mysql;</span><br><span class="line"> CREATE USER 'repl'@'123.57.44.85' IDENTIFIED BY 'slavepass';#创建用户</span><br><span class="line"> GRANT REPLICATION SLAVE ON *.* TO 'repl'@'%';#分配权限</span><br><span class="line"> flush privileges;   #刷新权限</span><br></pre></td></tr></table></figure>
<p><strong>3.</strong>查看master状态，记录二进制文件名(mysql-bin.000003)和位置(73)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SHOW MASTER STATUS;</span><br><span class="line">+------------------+----------+--------------+------------------+</span><br><span class="line">| File             | Position | Binlog_Do_DB | Binlog_Ignore_DB |</span><br><span class="line">+------------------+----------+--------------+------------------+</span><br><span class="line">| mysql-bin.000003 | 73       | test         | manual,mysql     |</span><br><span class="line">+------------------+----------+--------------+------------------+</span><br></pre></td></tr></table></figure>
<h3 id="二丶从服务器slave修改："><a href="#二丶从服务器slave修改：" class="headerlink" title="二丶从服务器slave修改："></a>二丶从服务器slave修改：</h3><p><strong>1.</strong>修改mysql配置<br>同样找到my.cnf配置文件，添加server-id</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">server-id=2 #设置server-id，必须唯一</span><br></pre></td></tr></table></figure>
<p><strong>2.</strong>重启mysql，打开mysql会话，执行同步SQL语句(需要主服务器主机名，登陆凭据，二进制文件的名称和位置。其中的端口号是主库)：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">mysql&gt; CHANGE MASTER TO</span><br><span class="line">    -&gt;     MASTER_HOST='182.92.172.80',</span><br><span class="line">    -&gt;     MASTER_USER='repl',</span><br><span class="line">    -&gt;	   MASTER_PORT=3306,</span><br><span class="line">    -&gt;     MASTER_PASSWORD='slavepass',</span><br><span class="line">    -&gt;     MASTER_LOG_FILE='mysql-bin.000003',</span><br><span class="line">    -&gt;     MASTER_LOG_POS=73;</span><br></pre></td></tr></table></figure>
<p><strong>3.</strong>启动slave同步进程：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">start</span> <span class="keyword">slave</span>;</span><br></pre></td></tr></table></figure>
<p><strong>4.</strong>查看slave状态：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; show slave status\G;</span><br><span class="line">*************************** 1. row ***************************</span><br><span class="line">               Slave_IO_State: Waiting for master to send event</span><br><span class="line">                  Master_Host: 182.92.172.80</span><br><span class="line">                  Master_User: rep1</span><br><span class="line">                  Master_Port: 3306</span><br><span class="line">                Connect_Retry: 60</span><br><span class="line">              Master_Log_File: mysql-bin.000013</span><br><span class="line">          Read_Master_Log_Pos: 11662</span><br><span class="line">               Relay_Log_File: mysqld-relay-bin.000022</span><br><span class="line">                Relay_Log_Pos: 11765</span><br><span class="line">        Relay_Master_Log_File: mysql-bin.000013</span><br><span class="line">             Slave_IO_Running: Yes</span><br><span class="line">            Slave_SQL_Running: Yes</span><br><span class="line">              Replicate_Do_DB: </span><br><span class="line">          Replicate_Ignore_DB:</span><br></pre></td></tr></table></figure></p>
<p>当Slave_IO_Running和Slave_SQL_Running都为YES的时候就表示主从同步设置成功了。</p>
]]></content>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>mysql错误：Incorrect key file for table</title>
    <url>/2019/11/30/mysql%E9%94%99%E8%AF%AF%EF%BC%9AIncorrect%20key%20file%20for%20table/</url>
    <content><![CDATA[<h1 id="mysql错误：Incorrect-key-file-for-table"><a href="#mysql错误：Incorrect-key-file-for-table" class="headerlink" title="mysql错误：Incorrect key file for table"></a>mysql错误：Incorrect key file for table</h1><blockquote>
<p>此次错误有多种，首先是我本机的<strong>Got fatal error 1236 from master when reading data from binary log: ‘Could not find first log file name in binary log index file’</strong>。<br>然后是服务器的<strong>Incorrect key file for table ‘.\env_v2_air\gather_air_data_201911.MYI’; try to repair it</strong></p>
</blockquote>
<a id="more"></a>
<h2 id="本机错误"><a href="#本机错误" class="headerlink" title="本机错误"></a>本机错误</h2><h3 id="发生场景："><a href="#发生场景：" class="headerlink" title="发生场景："></a>发生场景：</h3><p>本机的mysql一开始使用5.7版本作为主库，8.1版本作为从库。这次主从同步崩溃后，尝试使用复制data文件夹的方式已经不能够把表复制到从库，而且C盘的空间要不足了。我便尝试将主从库搬迁到剩余空间比较多的F盘。<br>经过了二天的sql数据导出和导入，我将原主库的表成功写入了新的主库和从库里。磁盘的读写速度是限制了这次sql数据迁移效率的最大问题，平均1秒几十条的速度就导致磁盘读写占用90%。日常软件的使用都收到了影响。<br>当我配置主从同步的时候，从库的状态出现了错误：<strong>Got fatal error 1236 from master when reading data from binary log: ‘Could not find first log file name in binary log index file</strong></p>
<h3 id="处理方法："><a href="#处理方法：" class="headerlink" title="处理方法："></a>处理方法：</h3><h4 id="第一次尝试："><a href="#第一次尝试：" class="headerlink" title="第一次尝试："></a>第一次尝试：</h4><p>发现没有配置主库的端口，配置端口后start slave可以成功显示两个yes。但是却发现表不同步，于是重新复制主库的文件。</p>
<h4 id="第二次尝试："><a href="#第二次尝试：" class="headerlink" title="第二次尝试："></a>第二次尝试：</h4><p>复制主库的data文件后，start slave报错。<strong>Slave failed to initialize relay log info structure from the repository</strong>。使用网上一篇文章的操作<a href="https://blog.csdn.net/lwei_998/article/details/41210945" target="_blank" rel="noopener">Slave failed to initialize relay log info structure from the repository, Error_code: 1872</a>。重新reset slave,并没有在my.ini中添加relay_log配置。即可重新开启主从复制</p>
<h2 id="服务器错误"><a href="#服务器错误" class="headerlink" title="服务器错误"></a>服务器错误</h2><h3 id="发生场景：-1"><a href="#发生场景：-1" class="headerlink" title="发生场景："></a>发生场景：</h3><p>服务器异常关机后，重新启动程序时报错：<strong>Incorrect key file for table ‘.\v_v_a\g_a_data_201911.MYI’; try to repair it</strong>。<br>主从同步也报错：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Relay log read failure: Could not parse relay log event entry. The possible reasons are: the master&apos;s binary log is corrupted (you can check this by running &apos;mysqlbinlog&apos; on the binary log), the slave&apos;s relay log is corrupted (you can check this by running &apos;mysqlbinlog&apos; on the relay log), a network problem, or a bug in the master&apos;s or slave&apos;s MySQL code. If you want to check the master&apos;s binary log or slave&apos;s relay log, you will be able to know their names by issuing &apos;SHOW SLAVE STATUS&apos; on this slave.</span><br></pre></td></tr></table></figure></p>
<h3 id="处理方法：-1"><a href="#处理方法：-1" class="headerlink" title="处理方法："></a>处理方法：</h3><h4 id="第一次尝试：-1"><a href="#第一次尝试：-1" class="headerlink" title="第一次尝试："></a>第一次尝试：</h4><p>首先运行<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">check table g_a_data_201911;</span><br></pre></td></tr></table></figure></p>
<p>然后<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">repair table g_a_data_201911;</span><br></pre></td></tr></table></figure></p>
<p>再my.ini加入<strong>relay_log_recovery=1</strong>重新启动mysql，主从同步报错更换<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Could not execute Write_rows event on table env_v2_air.gather_air_data_201911; Incorrect key file for table &apos;.\env_v2_air\gather_air_data_201911.MYI&apos;; try to repair it, Error_code: 126; Incorrect key file for table &apos;.\env_v2_air\gather_air_data_201911.MYI&apos;; try to repair it, Error_code: 126; Incorrect key file for table &apos;gather_air_data_201911&apos;; try to repair it, Error_code: 1034; handler error HA_ERR_CRASHED; the event&apos;s master log mysql-bin.000211, end_log_pos 547782372</span><br></pre></td></tr></table></figure></p>
<h4 id="第二次尝试：-1"><a href="#第二次尝试：-1" class="headerlink" title="第二次尝试："></a>第二次尝试：</h4><p>在从库里重复运行上面的命令，检查数据是否一致后。重新开启主从复制，成功。</p>
<h2 id="后记："><a href="#后记：" class="headerlink" title="后记："></a>后记：</h2><p>因为时间关系，并没有详细的错误原理说明。</p>
]]></content>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>office online + wopiHost实现在线编辑office文档</title>
    <url>/2020/04/11/office%20online%20+%20wopiHost%E5%AE%9E%E7%8E%B0%E5%9C%A8%E7%BA%BF%E7%BC%96%E8%BE%91office%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[<h1 id="office-online-wopiHost实现在线编辑office文档"><a href="#office-online-wopiHost实现在线编辑office文档" class="headerlink" title="office online + wopiHost实现在线编辑office文档"></a>office online + wopiHost实现在线编辑office文档</h1><blockquote>
<p>跟着博客一步一步走完成的office online server的安装，在此记录一下碰到的问题与解决思路。<br><a href="https://www.netnr.com/home/list/114" target="_blank" rel="noopener">https://www.netnr.com/home/list/114</a> wopi参考文档。<br><a href="https://www.netnr.com/doc/code/4964095842855914510/4688957725858114675" target="_blank" rel="noopener">https://www.netnr.com/doc/code/4964095842855914510/4688957725858114675</a> 配合上面的项目的文档<br><a href="https://blog.csdn.net/jiaqu2177/article/details/81945692" target="_blank" rel="noopener">https://blog.csdn.net/jiaqu2177/article/details/81945692</a> 安装office online server参考文档</p>
</blockquote>
<a id="more"></a>
<h2 id="问题汇总"><a href="#问题汇总" class="headerlink" title="问题汇总"></a>问题汇总</h2><p>按照步骤成功的安装完毕，但是还是会遇到一些文档里没有说的问题。</p>
<h3 id="问题一："><a href="#问题一：" class="headerlink" title="问题一："></a>问题一：</h3><p>文档中的第九步一开始看没有明白是什么意思。请教大佬后，通过服务器管理器 -&gt; 工具 -&gt; （iis）管理器 -&gt; 添加了网站。成功获得文件信息。<br><img src="/2020/04/11/office online + wopiHost实现在线编辑office文档/b1.png" alt="添加网站信息-配合netnr/WopiHost使用"></p>
<h3 id="问题二："><a href="#问题二：" class="headerlink" title="问题二："></a>问题二：</h3><p>无法编辑word文件，重启一下服务器后可以进入编辑url</p>
<p><img src="/2020/04/11/office online + wopiHost实现在线编辑office文档/b2.png" alt="报错信息"></p>
<p><img src="/2020/04/11/office online + wopiHost实现在线编辑office文档/b3.png" alt="powershell命令信息"></p>
<h3 id="问题三："><a href="#问题三：" class="headerlink" title="问题三："></a>问题三：</h3><p>无法编辑excel文件，是因为文件有宏命令，或者是文件中包含一些不能显示的类型。看来这个线上的office还是很有局限性。</p>
<p><img src="/2020/04/11/office online + wopiHost实现在线编辑office文档/b4.png" alt="有宏命令"></p>
<p><img src="/2020/04/11/office online + wopiHost实现在线编辑office文档/b5.png" alt="发票格式excel"></p>
<h3 id="问题四："><a href="#问题四：" class="headerlink" title="问题四："></a>问题四：</h3><p>用office online打开从doc格式转成docx格式的文件会开启兼容模式。提示是 <a href="https://support.office.com/zh-CN/client/results?Shownav=true&amp;lcid=2052&amp;ns=WDWAEndUser&amp;version=15&amp;omkt=zh-CN&amp;ver=15&amp;apps=WDWAENDUSER%2CXLWAENDUSER%2CPPWAENDUSER%2CONWAENDUSER&amp;HelpID=CompatMode" target="_blank" rel="noopener">兼容模式，功能收到限制</a></p>
<h3 id="问题五："><a href="#问题五：" class="headerlink" title="问题五："></a>问题五：</h3><p>有文字框的doc文档格式错乱，不能正常的显示。添加otf字体后依旧是格式错乱。参考微云后发现文字框被转换成了图片，无法修改其中的内容。并未有解决的方法。</p>
]]></content>
      <tags>
        <tag>office</tag>
      </tags>
  </entry>
  <entry>
    <title>rabbitMQ远程连接失败问题</title>
    <url>/2019/11/16/rabbitMQ%E8%BF%9C%E7%A8%8B%E8%BF%9E%E6%8E%A5%E5%A4%B1%E8%B4%A5%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="rabbitMQ远程连接失败问题"><a href="#rabbitMQ远程连接失败问题" class="headerlink" title="rabbitMQ远程连接失败问题"></a>rabbitMQ远程连接失败问题</h1><blockquote>
<p>将数据部署到服务器，程序开在本地时出现的远程连接问题。</p>
</blockquote>
<a id="more"></a>
<h2 id="问题一"><a href="#问题一" class="headerlink" title="问题一"></a>问题一</h2><p>将rabbitMQ部署在服务器后，同事想要从本地连接web网页查看一下队列情况，发现根本打不开网页。程序连接报错：<strong>Login was refused using authentication mechanism PLAIN. For details see the broker logfile.</strong></p>
<p>首先检查了一下服务器的端口打开情况，防火墙已经通过了所有的端口。<br>百度后查看了一下guest用户的权限，确认是最高权限的试用账号以及可以全host登录。<br>再次百度查找资料后（不是我找的），找到一个文章说guest用户只能在localhost本地登录。尝试新建用户root并开启所有权限。可以用这个新的账号远程登录了。</p>
<h2 id="问题二"><a href="#问题二" class="headerlink" title="问题二"></a>问题二</h2><p>但是在程序中连接仍旧报错：<strong>Received ERROR {message=[Bad CONNECT], content-type=[text/plain], version=[1.0,1.1,1.2],<br>content-length=[26]} session=<em>system</em> text/plain payload=non-loopback access denied</strong></p>
<p>查询到需要添加参数<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">@EnableWebSocketMessageBroker</span><br><span class="line">public class WebSocketConfig extends AbstractWebSocketMessageBrokerConfigurer &#123;</span><br><span class="line"></span><br><span class="line">@Override</span><br><span class="line">public void configureMessageBroker(MessageBrokerRegistry config) &#123;</span><br><span class="line">    config.enableStompBrokerRelay(&quot;/topic&quot;)</span><br><span class="line">            .setRelayHost(&quot;192.168.1.8&quot;)</span><br><span class="line">            .setRelayPort(61613)</span><br><span class="line">            .setClientLogin(&quot;user&quot;)</span><br><span class="line">            .setClientPasscode(&quot;user&quot;)</span><br><span class="line">            .setSystemLogin(&quot;user&quot;)</span><br><span class="line">            .setSystemPasscode(&quot;user&quot;);</span><br><span class="line"></span><br><span class="line">    //config.enableSimpleBroker(&quot;/topic&quot;);</span><br><span class="line">    config.setApplicationDestinationPrefixes(&quot;/app&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@Override</span><br><span class="line">public void registerStompEndpoints(StompEndpointRegistry registry) &#123;</span><br><span class="line">    registry.addEndpoint(&quot;/gs-guide-websocket&quot;).setAllowedOrigins(&quot;*&quot;).withSockJS();   </span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>其中的setClientLogin到setSystemPasscode就是新增加的参数，设置为新建的root用户即可正常连接了。</p>
]]></content>
      <tags>
        <tag>rabbitmq</tag>
      </tags>
  </entry>
  <entry>
    <title>spring boot参考指南摘抄</title>
    <url>/2020/01/18/spring-boot%E5%8F%82%E8%80%83%E6%8C%87%E5%8D%97%E6%91%98%E6%8A%84%E4%B8%80/</url>
    <content><![CDATA[<h1 id="spring-boot参考指南摘抄一"><a href="#spring-boot参考指南摘抄一" class="headerlink" title="spring boot参考指南摘抄一"></a>spring boot参考指南摘抄一</h1><blockquote>
<p>记录摘抄自己阅读文档的部分内容，方便日后记忆。<br>内容有 @RestController和@RequestMapping注解、@EnableAutoConfiguration注解、Starters依赖、放置应用的main类、 自动配置、  Spring Beans和依赖注入、使用@SpringBootApplication注解、Application事件和监听器、使用ApplicationRunner或CommandLineRunner、外部化配置、访问命令行属性、Application属性文件、 Profiles。</p>
</blockquote>
<a id="more"></a>
<h2 id="RestController和-RequestMapping注解（来自11-3-1）"><a href="#RestController和-RequestMapping注解（来自11-3-1）" class="headerlink" title="@RestController和@RequestMapping注解（来自11.3.1）"></a>@RestController和@RequestMapping注解（来自11.3.1）</h2><p>Example类上使用的第一个注解是 <strong>@RestController </strong> ，这被称为构造型（stereotype）注解。它为阅读代码的人提供暗示（这是一个支持REST的控制器），对于Spring，该类扮演了一个特角色。在本示例中，我们的类是一个web@Controller  ，所以当web请求进来时，Spring会考虑是否使用它来处理。</p>
<p><strong>@RequestMapping </strong> 注解提供路由信息，它告诉Spring任何来自”/“路径的HTTP请求都应该被映射到 home  方法。 @RestController  注解告诉Spring以字符串的形式渲染结果，并直接返回给调用者。</p>
<p>注： @RestController  和 @RequestMapping  是Spring MVC中的注解（它们不是Spring Boot的特定部分）</p>
<h2 id="EnableAutoConfiguration注解（来自11-3-2）"><a href="#EnableAutoConfiguration注解（来自11-3-2）" class="headerlink" title="@EnableAutoConfiguration注解（来自11.3.2）"></a>@EnableAutoConfiguration注解（来自11.3.2）</h2><p>第二个类级别的注解是 <strong>@EnableAutoConfiguration</strong>  ，这个注解告诉Spring Boot根据添加的jar依赖猜测你想如何配置Spring。由于 spring-boot-starter-web  添加了Tomcat和Spring MVC，所以auto-configuration将假定你正在开发一个web应用，并对Spring进行相应地设置。</p>
<h2 id="Starters（来自13-5）"><a href="#Starters（来自13-5）" class="headerlink" title="Starters（来自13.5）"></a>Starters（来自13.5）</h2><p><strong>Starters</strong>是一个依赖描述符的集合，你可以将它包含进项目中，这样添加依赖就非常方便。你可以获取所有Spring及相关技术的一站式服务，而不需要翻阅示例代码，拷贝粘贴大量的依赖描述符。例如，如果你想使用Spring和JPA进行数据库访问，只需要在项目中包含 <strong>spring-boot-starter-data-jpa </strong>依赖，然后你就可以开始了。</p>
<p>该starters包含很多搭建，快速运行项目所需的依赖，并提供一致的，可管理传递性<br>的依赖集。</p>
<h2 id="放置应用的main类（来自14-2）"><a href="#放置应用的main类（来自14-2）" class="headerlink" title="放置应用的main类（来自14.2）"></a>放置应用的main类（来自14.2）</h2><p>通常建议将应用的main类放到其他类所在包的顶层<strong>(root package)</strong>，并将 @EnableAutoConfiguration  注解到你的main类上，这样就隐式地定义了一个基础的包搜索路径（search package），以搜索某些特定的注解实体（比如@Service，@Component等） 。例如，如果你正在编写一个JPA应用，Spring将搜索 @EnableAutoConfiguration  注解的类所在包下的 @Entity  实体。</p>
<p>采用root package方式，你就可以使用 @ComponentScan  注解而不需要指定 basePackage  属性，也可以使用 @SpringBootApplication  注解，只要将main类放到root package中。</p>
<h2 id="自动配置（来自16）"><a href="#自动配置（来自16）" class="headerlink" title="自动配置（来自16）"></a>自动配置（来自16）</h2><p>Spring Boot自动配置（auto-configuration）尝试根据添加的jar依赖自动配置你的<br>Spring应用。</p>
<p>例如，如果classpath下存在 HSQLDB  ，并且你没有手动配置任何数据库连接的beans，那么Spring Boot将自动配置一个内存型（in-memory）数据库。</p>
<p>实现自动配置有两种可选方式，分别是将 <strong>@EnableAutoConfiguration</strong>  或 @SpringBootApplication 注解到 @Configuration  类上。</p>
<p>注：你应该只添加一个 @EnableAutoConfiguration  注解，通常建议将它添加到<br>主配置类（primary  @Configuration  ）上。</p>
<h2 id="Spring-Beans和依赖注入（来自17）"><a href="#Spring-Beans和依赖注入（来自17）" class="headerlink" title="Spring Beans和依赖注入（来自17）"></a>Spring Beans和依赖注入（来自17）</h2><p>你可以自由地使用任何标准的Spring框架技术去定义beans和它们注入的依赖。简单起见，我们经常使用 <strong>@ComponentScan </strong> 注解搜索beans，并结合 @Autowired  构造器注入。</p>
<p>如果遵循以上的建议组织代码结构（将应用的main类放到包的最上层，即rootpackage），那么你就可以添加 @ComponentScan  注解而不需要任何参数，所有应用组件（ @Component  ,  @Service  ,  @Repository  ,  @Controller  等）都会自动注册成Spring Beans。</p>
<h2 id="使用-SpringBootApplication注解"><a href="#使用-SpringBootApplication注解" class="headerlink" title="使用@SpringBootApplication注解"></a>使用@SpringBootApplication注解</h2><p>很多Spring Boot开发者经常使用 @Configuration  ， @EnableAutoConfiguration  ， @ComponentScan  注解他们的main类，由于这些注解如此频繁地一块使用（特别是遵循以上最佳实践的时候），Spring Boot就提供了一个方便的<strong> @SpringBootApplication  </strong>注解作为代替。</p>
<p>@SpringBootApplication  注解等价于以默认属性使用 @Configuration  ， @EnableAutoConfiguration  和 @ComponentScan 。</p>
<p>注：  @SpringBootApplication  注解也提供了用于自定义 @EnableAutoConfiguration  和 @ComponentScan  属性的别名（aliases）。</p>
<h2 id="Application事件和监听器（来自23-5）"><a href="#Application事件和监听器（来自23-5）" class="headerlink" title="Application事件和监听器（来自23.5）"></a>Application事件和监听器（来自23.5）</h2><p>除了常见的Spring框架事件，比如ContextRefreshedEvent， SpringApplication  也会发送其他的application事件。</p>
<p>注： 有些事件实际上是在 ApplicationContext  创建前触发的，所以你不能在那些事件（处理类）中通过 @Bean  注册监听器，只能通过 SpringApplication.addListeners(…)  或 SpringApplicationBuilder.listeners(…)  方法注册。如果想让监听器自动注册，而不关心应用的创建方式，你可以在工程中添加一个 META-INF/spring.factories  文件，并使用 org.springframework.context.ApplicationListener  作为key指向那些监听器。</p>
<p>应用运行时，事件会以下面的次序发送：</p>
<ol>
<li>在运行开始，但除了监听器注册和初始化以外的任何处理之前，会发送一<br>个 ApplicationStartedEvent  。</li>
<li>在Environment将被用于已知的上下文，但在上下文被创建前，会发送一<br>个 ApplicationEnvironmentPreparedEvent  。</li>
<li>在refresh开始前，但在bean定义已被加载后，会发送一<br>个 ApplicationPreparedEvent  。</li>
<li>在refresh之后，相关的回调处理完，会发送一个 ApplicationReadyEvent  ，<br>表示应用准备好接收请求了。</li>
<li>启动过程中如果出现异常，会发送一个 ApplicationFailedEvent  。</li>
</ol>
<p>注： 通常不需要使用application事件，但知道它们的存在是有用的（在某些场合可能<br>会使用到），比如，在Spring Boot内部会使用事件处理各种任务。</p>
<blockquote>
<p>这里需要单独写一篇博客来记录一下，现在还不是太明白这个监听事件的过程。监听的启动顺序，事件的启动顺序，以及这里的一些类的用处。</p>
</blockquote>
<h2 id="使用ApplicationRunner或CommandLineRunner（来自23-8）"><a href="#使用ApplicationRunner或CommandLineRunner（来自23-8）" class="headerlink" title="使用ApplicationRunner或CommandLineRunner（来自23.8）"></a>使用ApplicationRunner或CommandLineRunner（来自23.8）</h2><p>如果需要在 SpringApplication  启动后执行一些特殊的代码，你可以实现 ApplicationRunner  或 CommandLineRunner  接口，这两个接口工作方式相同，都只提供单一的 run  方法，该方法仅在 SpringApplication.run(…)  完成之前调用。</p>
<p>CommandLineRunner  接口能够访问string数组类型的应用参数，而 ApplicationRunner  使用的是上面描述过的 ApplicationArguments  接口：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import org.springframework.boot.*</span><br><span class="line">import org.springframework.stereotype.*</span><br><span class="line"></span><br><span class="line">@Component</span><br><span class="line">public class MyBean implements CommandLineRunner &#123;</span><br><span class="line">    public void run(String... args) &#123;</span><br><span class="line">    // Do something...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="外部化配置（来自24）"><a href="#外部化配置（来自24）" class="headerlink" title="外部化配置（来自24）"></a>外部化配置（来自24）</h2><p>Spring Boot设计了一个非常特别的 PropertySource  顺序，以允许对属性值进行<br>合理的覆盖，属性会以如下的优先级顺序进行设值：</p>
<ol>
<li>home目录下的devtools全局设置属性（ ~/.spring-boot-<br>devtools.properties  ，如果devtools激活）。</li>
<li>测试用例上的@TestPropertySource注解。</li>
<li>测试用例上的@SpringBootTest#properties注解。</li>
<li>命令行参数</li>
<li>来自 SPRING_APPLICATION_JSON  的属性（环境变量或系统属性中内嵌的内联<br>JSON）。</li>
<li>ServletConfig  初始化参数。</li>
<li>ServletContext  初始化参数。</li>
<li>来自于 java:comp/env  的JNDI属性。</li>
<li>Java系统属性（System.getProperties()）。</li>
<li>操作系统环境变量。</li>
<li>RandomValuePropertySource，只包含 random.*  中的属性。</li>
<li>没有打进jar包的Profile-specific应用属性（ application-{profile}.properties  和YAML变量）。</li>
<li>打进jar包中的Profile-specific应用属性（ application-{profile}.properties  和YAML变量）。</li>
<li>没有打进jar包的应用配置（ application.properties  和YAML变量）。</li>
<li>打进jar包中的应用配置（ application.properties  和YAML变量）。</li>
<li>@Configuration  类上的 @PropertySource  注解。</li>
<li>默认属性（使用 SpringApplication.setDefaultProperties  指定）。</li>
</ol>
<h2 id="访问命令行属性（来自24-2）"><a href="#访问命令行属性（来自24-2）" class="headerlink" title="访问命令行属性（来自24.2）"></a>访问命令行属性（来自24.2）</h2><p>默认情况下， SpringApplication  会将所有命令行配置参数（以’–’开头，比如 –server.port=9000  ）转化成一个 property  ，并将其添加到SpringEnvironment  中。正如以上章节提过的，命令行属性总是优先于其他属性源。</p>
<h2 id="Application属性文件（来自24-3）"><a href="#Application属性文件（来自24-3）" class="headerlink" title="Application属性文件（来自24.3）"></a>Application属性文件（来自24.3）</h2><p>SpringApplication  将从以下位置加载 application.properties  文件，并把<br>它们添加到Spring  Environment  中：</p>
<ol>
<li>当前目录下的 /config  子目录。</li>
<li>当前目录。</li>
<li>classpath下的 /config  包。</li>
<li>classpath根路径（root）。<br>该列表是按优先级排序的（列表中位置高的路径下定义的属性将覆盖位置低的）。</li>
</ol>
<p>注： 你可以使用YAML（’.yml’）文件替代’.properties’。</p>
<p>如果不喜欢将 application.properties  作为配置文件名，你可以通过指<br>定 spring.config.name  环境属性来切换其他的名称，也可以使<br>用 spring.config.location  环境属性引用一个明确的路径（目录位置或文件路<br>径列表以逗号分割）。</p>
<h2 id="Profiles（来自25）"><a href="#Profiles（来自25）" class="headerlink" title="Profiles（来自25）"></a>Profiles（来自25）</h2><p>Spring Profiles提供了一种隔离应用程序配置的方式，并让这些配置只在特定的环<br>境下生效。任何 @Component  或 @Configuration  都能注解 @Profile  ，从而限<br>制加载它的时机：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">@Profile(&quot;production&quot;)</span><br><span class="line">public class ProductionConfiguration &#123;</span><br><span class="line">	// ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>Spring boot</tag>
      </tags>
  </entry>
  <entry>
    <title>rebbitmq安装与使用场景</title>
    <url>/2019/08/14/rebbitmq%E5%AE%89%E8%A3%85%E4%B8%8E%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF/</url>
    <content><![CDATA[<h1 id="RebbitMQ安装与使用场景"><a href="#RebbitMQ安装与使用场景" class="headerlink" title="RebbitMQ安装与使用场景"></a>RebbitMQ安装与使用场景</h1><blockquote>
<p>随着项目经验的增多，所遇到的业务场景也各种各样。什么业务需求使用什么样的技术，是我现在需要学习的一个方向。</p>
</blockquote>
<a id="more"></a>
<h2 id="为什么要使用MQ"><a href="#为什么要使用MQ" class="headerlink" title="为什么要使用MQ"></a>为什么要使用MQ</h2><p>我这周所碰到的业务场景是类似与知网的一个查重功能。需要将新建立的内容和之前数据库中存储的所有内容做出一个匹配，查询是否是类似的内容并给出相似度。</p>
<p>这其中碰到的问题在于每次匹配的时间过长，而且使用者并不需要及时的知道这个相似度。</p>
<p>通过<a href="https://mp.weixin.qq.com/s?__biz=MjM5ODYxMDA5OQ==&amp;mid=2651960012&amp;idx=1&amp;sn=c6af5c79ecead98daa4d742e5ad20ce5&amp;chksm=bd2d07108a5a8e0624ae6ad95001c4efe09d7ba695f2ddb672064805d771f3f84bee8123b8a6&amp;mpshare=1&amp;scene=1&amp;srcid=04054h4e90lz5Qc2YKnLNuvY" target="_blank" rel="noopener">到底什么时候该使用MQ</a>这个文章的学习，感觉还是可以运用一下消息队列这个技术。</p>
<pre><code>**以下内容出自那篇文章**
</code></pre><h3 id="什么是MQ"><a href="#什么是MQ" class="headerlink" title="什么是MQ"></a>什么是MQ</h3><p>消息总线（Message Queue），后文称MQ，是一种跨进程的通信机制，用于上下游传递消息。</p>
<p>在互联网架构中，MQ是一种非常常见的上下游“逻辑解耦+物理解耦”的消息通信服务。</p>
<p>使用了MQ之后，消息发送上游只需要依赖MQ，逻辑上和物理上都不用依赖其他服务。</p>
<h3 id="什么时候不使用消息总线"><a href="#什么时候不使用消息总线" class="headerlink" title="什么时候不使用消息总线"></a>什么时候不使用消息总线</h3><p>既然MQ是互联网分层架构中的解耦利器，那所有通讯都使用MQ岂不是很好？这是一个严重的误区，调用与被调用的关系，是无法被MQ取代的。</p>
<p>MQ的<strong>不足</strong>是：</p>
<ul>
<li>系统更复杂，多了一个MQ组件</li>
<li>消息传递路径更长，延时会增加</li>
<li>消息可靠性和重复性互为矛盾，消息不丢不重难以同时保证</li>
<li>上游无法知道下游的执行结果，这一点是很致命的</li>
</ul>
<p>举个<strong>栗子</strong>：用户登录场景，登录页面调用passport服务，passport服务的执行结果直接影响登录结果，此处的“登录页面”与“passport服务”就必须使用调用关系，而不能使用MQ通信。</p>
<p>无论如何，记住这个<strong>结论</strong>：调用方实时依赖执行结果的业务场景，请使用调用，而不是MQ。</p>
<h3 id="什么时候使用MQ"><a href="#什么时候使用MQ" class="headerlink" title="什么时候使用MQ"></a>什么时候使用MQ</h3><h4 id="【典型场景一：数据驱动的任务依赖】"><a href="#【典型场景一：数据驱动的任务依赖】" class="headerlink" title="【典型场景一：数据驱动的任务依赖】"></a>【典型场景一：数据驱动的任务依赖】</h4><p>什么是任务依赖，举个栗子，互联网公司经常在凌晨进行一些数据统计任务，这些任务之间有一定的依赖关系，比如：</p>
<ul>
<li>task3需要使用task2的输出作为输入</li>
<li>task2需要使用task1的输出作为输入<br>这样的话，tast1, task2, task3之间就有任务依赖关系，必须task1先执行，再task2执行，载task3执行。</li>
</ul>
<p><img src="/2019/08/14/rebbitmq安装与使用场景/b1.png" alt="1"></p>
<p>对于这类需求，常见的实现<strong>方式</strong>是，使用cron人工排执行时间表：</p>
<ol>
<li>task1，0:00执行，经验执行时间为50分钟</li>
<li>task2，1:00执行（为task1预留10分钟buffer），经验执行时间也是50分钟</li>
<li>task3，2:00执行（为task2预留10分钟buffer）</li>
</ol>
<p>这种方法的<strong>坏处</strong>是：</p>
<ol>
<li>如果有一个任务执行时间超过了预留buffer的时间，将会得到错误的结果，因为后置任务不清楚前置任务是否执行成功，此时要手动重跑任务，还有可能要调整排班表</li>
<li>总任务的执行时间很长，总是要预留很多buffer，如果前置任务提前完成，后置任务不会提前开始</li>
<li>如果一个任务被多个任务依赖，这个任务将会称为关键路径，排班表很难体现依赖关系，容易出错</li>
<li>如果有一个任务的执行时间要调整，将会有多个任务的执行时间要调整</li>
</ol>
<p>无论如何，采用“cron排班表”的方法，各任务耦合，<strong>谁用过谁痛谁知道</strong></p>
<p><img src="/2019/08/14/rebbitmq安装与使用场景/b2.png" alt="2"></p>
<p>优化方案是，采用MQ解耦：</p>
<ol>
<li>不需要预留buffer，上游任务执行完，下游任务总会在第一时间被执行</li>
<li>依赖多个任务，被多个任务依赖都很好处理，只需要订阅相关消息即可</li>
<li>有任务执行时间变化，下游任务都不需要调整执行时间</li>
</ol>
<p>需要<strong>特别说明</strong>的是，MQ只用来传递上游任务执行完成的消息，并不用于传递真正的输入输出数据。</p>
<h4 id="【典型场景二：上游不关心执行结果】"><a href="#【典型场景二：上游不关心执行结果】" class="headerlink" title="【典型场景二：上游不关心执行结果】"></a>【典型场景二：上游不关心执行结果】</h4><p>上游需要关注执行结果时要用“调用”，上游不关注执行结果时，就可以使用MQ了。</p>
<p>举个栗子，58同城的很多下游需要关注“用户发布帖子”这个事件，比如招聘用户发布帖子后，招聘业务要奖励58豆，房产用户发布帖子后，房产业务要送2个置顶，二手用户发布帖子后，二手业务要修改用户统计数据。</p>
<p><img src="/2019/08/14/rebbitmq安装与使用场景/b3.png" alt="3"><br>对于这类需求，常见的实现方式是，使用调用关系：<br>帖子发布服务执行完成之后，调用下游招聘业务、房产业务、二手业务，来完成消息的通知，但事实上，这个通知是否正常正确的执行，帖子发布服务根本不关注。</p>
<p>这种方法的<strong>坏处</strong>是：</p>
<ol>
<li>帖子发布流程的执行时间增加了</li>
<li>下游服务当机，可能导致帖子发布服务受影响，上下游逻辑+物理依赖严重</li>
<li>每当增加一个需要知道“帖子发布成功”信息的下游，修改代码的是帖子发布服务，这一点是最恶心的，属于架构设计中典型的依赖倒转。</li>
</ol>
<p><img src="/2019/08/14/rebbitmq安装与使用场景/b4.png" alt="4"><br>优化方案是，采用MQ解耦：</p>
<ol>
<li>帖子发布成功后，向MQ发一个消息</li>
<li>哪个下游关注“帖子发布成功”的消息，主动去MQ订阅</li>
</ol>
<p>采用MQ的<strong>优点</strong>是：</p>
<ol>
<li>上游执行时间短</li>
<li>上下游逻辑+物理解耦，除了与MQ有物理连接，模块之间都不相互依赖</li>
<li>新增一个下游消息关注方，上游不需要修改任何代码</li>
</ol>
<h4 id="【典型场景三：上游关注执行结果，但执行时间很长】"><a href="#【典型场景三：上游关注执行结果，但执行时间很长】" class="headerlink" title="【典型场景三：上游关注执行结果，但执行时间很长】"></a>【典型场景三：上游关注执行结果，但执行时间很长】</h4><p>有时候上游需要关注执行结果，但执行结果时间很长（典型的是调用离线处理，或者跨公网调用），也经常使用回调网关+MQ来解耦。</p>
<p>举个栗子，微信支付，跨公网调用微信的接口，执行时间会比较长，但调用方又非常关注执行结果，此时一般怎么玩呢？<br><img src="/2019/08/14/rebbitmq安装与使用场景/b5.png" alt="5"><br>一般采用“回调网关+MQ”方案来解耦：</p>
<ol>
<li>调用方直接跨公网调用微信接口</li>
<li>微信返回调用成功，此时并不代表返回成功</li>
<li>微信执行完成后，回调统一网关</li>
<li>网关将返回结果通知MQ</li>
<li>请求方收到结果通知</li>
</ol>
<p>这里<strong>需要注意</strong>的是，不应该由回调网关来调用上游来通知结果，如果是这样的话，每次新增调用方，回调网关都需要修改代码，仍然会反向依赖，使用回调网关+MQ的方案，新增任何对微信支付的调用，都不需要修改代码啦。</p>
<p><strong>文章的总结：</strong><br>什么时候不使用MQ？</p>
<ol>
<li>上游实时关注执行结果</li>
</ol>
<p>什么时候使用MQ？</p>
<ol>
<li>数据驱动的任务依赖</li>
<li>上游不关心多下游执行结果</li>
<li>异步返回执行时间长</li>
</ol>
<h2 id="具体RebbitMQ安装"><a href="#具体RebbitMQ安装" class="headerlink" title="具体RebbitMQ安装"></a>具体RebbitMQ安装</h2><p>docker在进行测试和模拟操作的时候还是可以为我们节省不少的时间的，本次就是使用docker下载rebbitMQ镜像来进行使用。</p>
<p><a href="https://hub.docker.com/_/rabbitmq/" target="_blank" rel="noopener">https://hub.docker.com/_/rabbitmq/</a>  从官网查看到当前版本为3.7.17</p>
<p>docker下载镜像<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull rabbitmq:3.7.17-management</span><br></pre></td></tr></table></figure></p>
<p>启动容器<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -d --hostname my-rabbit -p 5672:5672 -p 15672:15672 -p 15674:15674 -p 61613:61613 rabbitmq:3.7.17-management</span><br></pre></td></tr></table></figure></p>
<p>浏览器访问docker ip ： 15672可以进入管理界面</p>
<p><img src="/2019/08/14/rebbitmq安装与使用场景/b6.png" alt="6"></p>
<p>用户名guest，密码guest</p>
]]></content>
      <tags>
        <tag>RabbitMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>spring boot参考指南摘抄二</title>
    <url>/2020/02/01/spring-boot%E5%8F%82%E8%80%83%E6%8C%87%E5%8D%97%E6%91%98%E6%8A%84%E4%BA%8C/</url>
    <content><![CDATA[<h1 id="spring-boot参考指南摘抄二"><a href="#spring-boot参考指南摘抄二" class="headerlink" title="spring boot参考指南摘抄二"></a>spring boot参考指南摘抄二</h1><blockquote>
<p>把要素分开存放。<br>静态内容、</p>
</blockquote>
<a id="more"></a>
<h2 id="静态内容（来自27-1-5）"><a href="#静态内容（来自27-1-5）" class="headerlink" title="静态内容（来自27.1.5）"></a>静态内容（来自27.1.5）</h2><p>默认情况下，Spring Boot从classpath下的 /static  （ /public  ， /resources  或 /META-INF/resources  ）文件夹，或从 ServletContext  根目录提供静态内容。这是通过Spring MVC的 ResourceHttpRequestHandler  实现的，你可以自定义 WebMvcConfigurerAdapter  并覆写 addResourceHandlers  方法来改变该行为（加载静态文件）。</p>
<p>注： 如果你的应用将被打包成jar，那就不要使用 src/main/webapp  文件夹。尽管<br>该文件夹是通常的标准格式，但它仅在打包成war的情况下起作用，在打包成jar<br>时，多数构建工具都会默认忽略它。</p>
<h2 id="CORS支持（来自27-1-10）"><a href="#CORS支持（来自27-1-10）" class="headerlink" title="CORS支持（来自27.1.10）"></a>CORS支持（来自27.1.10）</h2><p>从4.2版本开始，Spring MVC对CORS提供开箱即用的支持。不用添加任何特殊配置，只需要在Spring Boot应用的controller方法上注解 @CrossOrigin  ，并添加CORS配置。通过注册一个自定义 addCorsMappings(CorsRegistry)  方法的 WebMvcConfigurer  bean可以指定全局CORS配置：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">public class MyConfiguration &#123;</span><br><span class="line">	@Bean</span><br><span class="line">	public WebMvcConfigurer corsConfigurer() &#123;</span><br><span class="line">		return new WebMvcConfigurerAdapter() &#123;</span><br><span class="line">			@Override</span><br><span class="line">			public void addCorsMappings(CorsRegistry registry) &#123;</span><br><span class="line">				registry.addMapping(&quot;/api/**&quot;);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="安全（来自28）"><a href="#安全（来自28）" class="headerlink" title="安全（来自28）"></a>安全（来自28）</h2><p>如果添加了Spring Security的依赖，那么web应用默认对所有的HTTP路径（也称为终点，端点，表示API的具体网址）使用’basic’认证。为了给web应用添加方法级别（method-level）的保护，你可以添加 @EnableGlobalMethodSecurity  并使用想要的设置。</p>
<p>默认的安全配置是通过 SecurityAutoConfiguration  ， SpringBootWebSecurityConfiguration（用于web安全）， AuthenticationManagerConfiguration  （可用于非web应用的认证配置）进行管理的。你可添加一个 @EnableWebSecurity  bean来彻底关掉Spring Boot的默认配置。为了对它进行自定义，你需要使用外部的属性配置和 WebSecurityConfigurerAdapter  类型的beans（比如，添加基于表单的登陆）。 想要关闭认证管理的配置，你可以添加一个 AuthenticationManager  类型的bean，或在 @Configuration  类的某个方法里注入 AuthenticationManagerBuilder  来配置全局的 AuthenticationManager  。</p>
<p>可以看看<a href="https://github.com/spring-projects/spring-boot/tree/v1.4.1.RELEASE/spring-boot-samples/" target="_blank" rel="noopener">Spring Boot应用示例</a></p>
<h2 id="实体类（来自29-3-1）"><a href="#实体类（来自29-3-1）" class="headerlink" title="实体类（来自29.3.1）"></a>实体类（来自29.3.1）</h2>]]></content>
      <tags>
        <tag>Spring boot</tag>
      </tags>
  </entry>
  <entry>
    <title>springboot配置redis多数据源</title>
    <url>/2020/12/12/springboot%E9%85%8D%E7%BD%AEredis%E5%A4%9A%E6%95%B0%E6%8D%AE%E6%BA%90/</url>
    <content><![CDATA[<h1 id="springboot配置redis多数据源"><a href="#springboot配置redis多数据源" class="headerlink" title="springboot配置redis多数据源"></a>springboot配置redis多数据源</h1><blockquote>
<p>本文来自<a href="https://iogogogo.github.io/2020/01/10/spring-boot-redis-multi-instance/" target="_blank" rel="noopener">https://iogogogo.github.io/2020/01/10/spring-boot-redis-multi-instance/</a> ，我用了后记录一下。</p>
</blockquote>
<a id="more"></a>
<h2 id="序章"><a href="#序章" class="headerlink" title="序章"></a>序章</h2><p>Spring Boot 2.x版本升级以后，Redis连接库由原来的<code>Jedis</code>换成了<code>Lettuce</code>，但是提供给上层使用的api没有变化，在日常使用过程中难免会有需要使用多个库的情况，或者使用多个Redis实例，那么这个时候就需要维护两个Redis连接池或者说两个<code>RedisTemplate</code> 。</p>
<h2 id="实现思路"><a href="#实现思路" class="headerlink" title="实现思路"></a>实现思路</h2><ol>
<li>多数据源最终表现其实就是 <code>RedisConnectionFactory</code> 不同</li>
<li>Spring Boot 通过<code>RedisStandaloneConfiguration</code>维护了一套默认的<code>RedisConnectionFactory</code>，要实现多个实例或者使用多个db，理论上只需要自己在维护一套<code>RedisConnectionFactory</code>即可。</li>
<li>不管是Jedis或是Lettuce，都是通用的，所以我们可以将两个的实现方式都整理出来，区别如下：<ul>
<li><code>Lettuce</code>使用的是<code>LettuceConnectionFactory</code></li>
<li><code>Jedis</code>使用的是<code>JedisConnectionFactory</code></li>
</ul>
</li>
</ol>
<h2 id="创建Redis服务"><a href="#创建Redis服务" class="headerlink" title="创建Redis服务"></a>创建Redis服务</h2><p>为了方便测试，直接使用<code>docker</code>创建一个Redis服务，以下是一个完整的启动脚本</p>
<ol>
<li><code>--restart=always</code>设置了Redis服务随着docker进程的启动而启动</li>
<li><code>-p 6379:6379</code>将docker启动的服务端口映射到宿主机，这里是必须的，否则宿主机和redis的端口是不能通信的</li>
<li><code>--requirepass &quot;redis&quot;</code> 设置密码为<code>redis</code></li>
<li><code>--appendonly yes</code>保存aof持久化文件</li>
<li><code>-v ~/share/docker/data/redis:/data \</code> 将docker启动的redis的数据映射到宿主机目录</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkdir -p ~/share/docker/data/redis</span><br><span class="line">chmod 755 -R ~/share/docker/data/redis</span><br><span class="line"></span><br><span class="line">docker run -dit --restart=always --name redis \</span><br><span class="line">    -v ~/share/docker/data/redis:/data \</span><br><span class="line">    -p 6379:6379 \</span><br><span class="line">    redis:latest redis-server --requirepass &quot;redis&quot; --appendonly yes</span><br></pre></td></tr></table></figure>
<p>如果觉得不需要设置这么多，可以使用以下最简单的脚本，仅仅将端口做了映射，其他的都是最简单的配置<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -dit --name redis -p 6379:6379 redis:latest</span><br></pre></td></tr></table></figure></p>
<h2 id="项目pom配置"><a href="#项目pom配置" class="headerlink" title="项目pom配置"></a>项目pom配置</h2><p>使用Spring Boot集成Redis，只需要将<code>spring-boot-starter-data-redis</code>和<code>commons-pool2</code>加到依赖即可<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">  &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">  &lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">  &lt;groupId&gt;org.apache.commons&lt;/groupId&gt;</span><br><span class="line">  &lt;artifactId&gt;commons-pool2&lt;/artifactId&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure></p>
<h2 id="自定义RedisConfigure"><a href="#自定义RedisConfigure" class="headerlink" title="自定义RedisConfigure"></a>自定义RedisConfigure</h2><p>这里这一步是最重要的，因为Spring Boot默认帮我们维护了一个RedisConnectionFactory，前面说了要使用不同的Redis实例就需要自己在维护一个RedisConnectionFactory，这里就以使用两个redis的database为例</p>
<h3 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h3><p><code>spring.redis</code>开头的都是Spring Boot自动注入需要加载的配置，我们为了在使用一个db2，这里加了一个<code>spring.redis-db-2</code>开头的配置<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">spring:</span><br><span class="line">  redis:</span><br><span class="line">    database: 0</span><br><span class="line">    host: localhost</span><br><span class="line">    port: 6379</span><br><span class="line">    password:</span><br><span class="line">    timeout: 60000</span><br><span class="line">    lettuce:</span><br><span class="line">      pool:</span><br><span class="line">        max-active: 8 # 连接池最大连接数（使用负值表示没有限制） 默认 8</span><br><span class="line">        max-wait: -1 # 连接池最大阻塞等待时间（使用负值表示没有限制） 默认 -1</span><br><span class="line">        max-idle: 8 # 连接池中的最大空闲连接 默认 8</span><br><span class="line">        min-idle: 0 # 连接池中的最小空闲连接 默认 0</span><br><span class="line">  redis-db-2:</span><br><span class="line">    database: 2</span><br><span class="line">    host: 127.0.0.1</span><br><span class="line">    port: 6379</span><br><span class="line">    password:</span><br><span class="line">    timeout: 60000</span><br><span class="line">    lettuce:</span><br><span class="line">      pool:</span><br><span class="line">        max-active: 8</span><br><span class="line">        max-wait: 8</span><br><span class="line">        max-idle: 8</span><br><span class="line">        min-idle: 0</span><br></pre></td></tr></table></figure></p>
<h3 id="RedisConfigure"><a href="#RedisConfigure" class="headerlink" title="RedisConfigure"></a>RedisConfigure</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.iogogogo.redis.configure;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.fasterxml.jackson.annotation.JsonAutoDetect;</span><br><span class="line"><span class="keyword">import</span> com.fasterxml.jackson.annotation.PropertyAccessor;</span><br><span class="line"><span class="keyword">import</span> com.fasterxml.jackson.databind.ObjectMapper;</span><br><span class="line"><span class="keyword">import</span> io.vavr.Tuple;</span><br><span class="line"><span class="keyword">import</span> io.vavr.Tuple6;</span><br><span class="line"><span class="keyword">import</span> org.apache.commons.lang3.StringUtils;</span><br><span class="line"><span class="keyword">import</span> org.apache.commons.pool2.impl.GenericObjectPoolConfig;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Value;</span><br><span class="line"><span class="keyword">import</span> org.springframework.cache.annotation.EnableCaching;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.connection.RedisConnectionFactory;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.connection.RedisPassword;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.connection.RedisStandaloneConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.connection.jedis.JedisClientConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.connection.jedis.JedisConnectionFactory;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.connection.lettuce.LettuceConnectionFactory;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.connection.lettuce.LettucePoolingClientConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.core.RedisTemplate;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.serializer.Jackson2JsonRedisSerializer;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.serializer.StringRedisSerializer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.time.Duration;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by tao.zeng on 2020-01-10.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.database&#125;") int database,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.host&#125;") String host,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.port&#125;") int port,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.password&#125;") String password,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.timeout&#125;") long timeout,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.lettuce.pool.max-active&#125;") int maxActive,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.lettuce.pool.max-wait&#125;") int maxWait,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.lettuce.pool.max-idle&#125;") int maxIdle,</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Value</span>("$&#123;spring.redis-db-2.lettuce.pool.min-idle&#125;") int minIdle</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@EnableCaching</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisConfigure</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RedisTemplate <span class="title">redisTemplateDB_2</span><span class="params">(Tuple6&lt;RedisStandaloneConfiguration, Long, Integer, Integer, Integer, Integer&gt; redisConfigurationDB_2)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        Long timeout = redisConfigurationDB_2._2();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> maxActive = redisConfigurationDB_2._3(),</span><br><span class="line">                maxWait = redisConfigurationDB_2._4(),</span><br><span class="line">                maxIdle = redisConfigurationDB_2._5(),</span><br><span class="line">                minIdle = redisConfigurationDB_2._6();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* ========= 基本配置 ========= */</span></span><br><span class="line">        RedisStandaloneConfiguration standaloneConfiguration = redisConfigurationDB_2._1();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* ========= 连接池通用配置 ========= */</span></span><br><span class="line">        GenericObjectPoolConfig genericObjectPoolConfig = <span class="keyword">new</span> GenericObjectPoolConfig();</span><br><span class="line">        genericObjectPoolConfig.setMaxTotal(maxActive);</span><br><span class="line">        genericObjectPoolConfig.setMaxWaitMillis(maxWait);</span><br><span class="line">        genericObjectPoolConfig.setMaxIdle(maxIdle);</span><br><span class="line">        genericObjectPoolConfig.setMinIdle(minIdle);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* ========= jedis pool ========= */</span></span><br><span class="line">        <span class="comment">// jedisConnectionFactory(standaloneConfiguration, genericObjectPoolConfig, timeout);</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* ========= lettuce pool ========= */</span></span><br><span class="line">        LettuceConnectionFactory connectionFactory = lettuceConnectionFactory(standaloneConfiguration, genericObjectPoolConfig, timeout);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 连接池初始化</span></span><br><span class="line">        connectionFactory.afterPropertiesSet();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建 RedisTemplate</span></span><br><span class="line">        <span class="keyword">return</span> createRedisTemplate(connectionFactory);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * lettuceConnectionFactory</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> standaloneConfiguration Redis标准配置</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> genericObjectPoolConfig Redis通用配置</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> timeout                 超时时间</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> LettuceConnectionFactory <span class="title">lettuceConnectionFactory</span><span class="params">(RedisStandaloneConfiguration standaloneConfiguration, GenericObjectPoolConfig genericObjectPoolConfig, <span class="keyword">long</span> timeout)</span> </span>&#123;</span><br><span class="line">        LettucePoolingClientConfiguration.LettucePoolingClientConfigurationBuilder builder = LettucePoolingClientConfiguration.builder();</span><br><span class="line">        builder.poolConfig(genericObjectPoolConfig);</span><br><span class="line">        builder.commandTimeout(Duration.ofSeconds(timeout));</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> LettuceConnectionFactory(standaloneConfiguration, builder.build());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * jedisConnectionFactory</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> standaloneConfiguration Redis标准配置</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> genericObjectPoolConfig Redis通用配置</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> timeout                 超时时间</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> JedisConnectionFactory <span class="title">jedisConnectionFactory</span><span class="params">(RedisStandaloneConfiguration standaloneConfiguration, GenericObjectPoolConfig genericObjectPoolConfig, <span class="keyword">long</span> timeout)</span> </span>&#123;</span><br><span class="line">        JedisClientConfiguration.DefaultJedisClientConfigurationBuilder builder = (JedisClientConfiguration.DefaultJedisClientConfigurationBuilder) JedisClientConfiguration</span><br><span class="line">                .builder();</span><br><span class="line">        builder.connectTimeout(Duration.ofSeconds(timeout));</span><br><span class="line">        builder.usePooling();</span><br><span class="line">        builder.poolConfig(genericObjectPoolConfig);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> JedisConnectionFactory(standaloneConfiguration, builder.build());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> redisConnectionFactory</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RedisTemplate <span class="title">redisTemplate</span><span class="params">(RedisConnectionFactory redisConnectionFactory)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> createRedisTemplate(redisConnectionFactory);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * json 实现 redisTemplate</span></span><br><span class="line"><span class="comment">     * &lt;p&gt;</span></span><br><span class="line"><span class="comment">     * 该方法不能加 <span class="doctag">@Bean</span> 否则不管如何调用，RedisConnectionFactory 都会是默认配置</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> redisConnectionFactory</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> RedisTemplate <span class="title">createRedisTemplate</span><span class="params">(RedisConnectionFactory redisConnectionFactory)</span> </span>&#123;</span><br><span class="line">        RedisTemplate&lt;String, Object&gt; redisTemplate = <span class="keyword">new</span> RedisTemplate&lt;&gt;();</span><br><span class="line">        redisTemplate.setConnectionFactory(redisConnectionFactory);</span><br><span class="line"></span><br><span class="line">        Jackson2JsonRedisSerializer&lt;Object&gt; jackson2JsonRedisSerializer = <span class="keyword">new</span> Jackson2JsonRedisSerializer&lt;&gt;(Object.class);</span><br><span class="line">        ObjectMapper objectMapper = <span class="keyword">new</span> ObjectMapper();</span><br><span class="line">        objectMapper.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);</span><br><span class="line">        objectMapper.enableDefaultTyping(ObjectMapper.DefaultTyping.NON_FINAL);</span><br><span class="line">        jackson2JsonRedisSerializer.setObjectMapper(objectMapper);</span><br><span class="line"></span><br><span class="line">        StringRedisSerializer stringRedisSerializer = <span class="keyword">new</span> StringRedisSerializer();</span><br><span class="line"></span><br><span class="line">        redisTemplate.setValueSerializer(jackson2JsonRedisSerializer);</span><br><span class="line">        redisTemplate.setKeySerializer(stringRedisSerializer);</span><br><span class="line">        redisTemplate.afterPropertiesSet();</span><br><span class="line">        <span class="keyword">return</span> redisTemplate;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 自定义Redis的配置加载</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> database</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> password</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> host</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> port</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> timeout</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> maxActive</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> maxWait</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> maxIdle</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> minIdle</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> Tuple6&lt;RedisStandaloneConfiguration, Long, Integer, Integer, Integer, Integer&gt;</span><br><span class="line">    redisConfigurationDB_2(<span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.database&#125;"</span>) <span class="keyword">int</span> database,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.password&#125;"</span>) String password,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.host&#125;"</span>) String host,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.port&#125;"</span>) <span class="keyword">int</span> port,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.timeout&#125;"</span>) <span class="keyword">long</span> timeout,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.lettuce.pool.max-active&#125;"</span>) <span class="keyword">int</span> maxActive,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.lettuce.pool.max-wait&#125;"</span>) <span class="keyword">int</span> maxWait,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.lettuce.pool.max-idle&#125;"</span>) <span class="keyword">int</span> maxIdle,</span><br><span class="line">                           <span class="meta">@Value</span>(<span class="string">"$&#123;spring.redis-db-2.lettuce.pool.min-idle&#125;"</span>) <span class="keyword">int</span> minIdle) &#123;</span><br><span class="line"></span><br><span class="line">        RedisStandaloneConfiguration standaloneConfiguration = <span class="keyword">new</span> RedisStandaloneConfiguration();</span><br><span class="line">        standaloneConfiguration.setDatabase(database);</span><br><span class="line">        standaloneConfiguration.setHostName(host);</span><br><span class="line">        standaloneConfiguration.setPort(port);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (StringUtils.isNotEmpty(password)) &#123;</span><br><span class="line">            RedisPassword redisPassword = RedisPassword.of(password);</span><br><span class="line">            standaloneConfiguration.setPassword(redisPassword);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> Tuple.of(standaloneConfiguration, timeout, maxActive, maxWait, maxIdle, minIdle);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个配置有点长，但是都有注释，而且这里也用到了上篇文章中讲到的<code>tuple</code>（在方法有多个返回值时，元组真香）,其实就已经可以使用两个不同的<code>RedisTemplate</code>了，这时候我们在启动项目时分别注入<code>redisTemplate</code>和<code>redisTemplateDB_2</code>，别问我为啥名字是这两货。</p>
<h2 id="通过debug查看配置是否成功"><a href="#通过debug查看配置是否成功" class="headerlink" title="通过debug查看配置是否成功"></a>通过debug查看配置是否成功</h2><p>启动类我们实现<code>CommandLineRunner</code>，然后通过debug查看<code>redisTemplate</code>和<code>redisTemplateDB2</code>的<code>RedisConnectionFactory</code><br>并且我们在database=0存储了一个string数据，在database=2存储了一个hash数据<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.iogogogo.redis;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lombok.extern.slf4j.Slf4j;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.CommandLineRunner;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.SpringBootApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.core.RedisTemplate;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by tao.zeng on 2020-01-10.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisApplication</span> <span class="keyword">implements</span> <span class="title">CommandLineRunner</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RedisTemplate redisTemplateDB_2;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(RedisApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">(String... args)</span> </span>&#123;</span><br><span class="line">        log.info(<span class="string">"redisTemplate:&#123;&#125;"</span>, redisTemplate);</span><br><span class="line">        redisTemplate.opsForValue().set(<span class="string">"iogogogo"</span>, <span class="string">"redisTemplate save value"</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        log.info(<span class="string">"redisTemplateDB_2:&#123;&#125;"</span>, redisTemplateDB2);</span><br><span class="line">        redisTemplateDB2.opsForHash().put(<span class="string">"iogogogo"</span>, <span class="string">"iogogogo-hash"</span>, <span class="string">"redisTemplateDB_2 save value"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>redisTemplate<br><img src="/2020/12/12/springboot配置redis多数据源/b1.png" alt="redisTemplate"></li>
<li>redisTemplateDB_2<br><img src="/2020/12/12/springboot配置redis多数据源/b2.png" alt="redisTemplateDB_2"></li>
</ul>
<p>略过原文的查看存储数据。。。</p>
<p>通过以上日志我们可以看到分别在不同的数据库已经存储了不同的数据，到这里我们redis配置多实例就已经完成。但是我们每次注入的时候都要记得自己创建的Bean的名字，这样对其他人不太友好，那么我们可以进一步封装一下，自定义一个handler进行获取RedisTemplate，这样会不会更便捷呢？</p>
<h2 id="自定义RedisHandler和RedisOperations"><a href="#自定义RedisHandler和RedisOperations" class="headerlink" title="自定义RedisHandler和RedisOperations"></a>自定义RedisHandler和RedisOperations</h2><p>做这一步主要是为了将下层的RedisTemplate进行统一的封装，对外只是一个<code>RedisHandler</code>和<code>RedisOperations</code>，<code>RedisHandler</code>提供获取<code>RedisOperations</code>的方法，<code>RedisOperations</code>里面可以封装一些常用的redis操作，这样就只需要和<code>RedisOperations</code>进行操作，从而避免同时操作多个<code>RedisTemplate</code></p>
<h3 id="项目结构"><a href="#项目结构" class="headerlink" title="项目结构"></a>项目结构</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">➜  example-redis git:(master) ✗ tree ./</span><br><span class="line">  ./</span><br><span class="line">  ├── example-redis.iml</span><br><span class="line">  ├── pom.xml</span><br><span class="line">  └── src</span><br><span class="line">      ├── main</span><br><span class="line">      │   ├── java</span><br><span class="line">      │   │   └── com</span><br><span class="line">      │   │       └── iogogogo</span><br><span class="line">      │   │           └── redis</span><br><span class="line">      │   │               ├── RedisApplication.java</span><br><span class="line">      │   │               └── configure</span><br><span class="line">      │   │                   ├── RedisConfigure.java</span><br><span class="line">      │   │                   ├── handler</span><br><span class="line">      │   │                   │   └── RedisHandler.java</span><br><span class="line">      │   │                   └── util</span><br><span class="line">      │   │                       └── RedisOperations.java</span><br><span class="line">      │   └── resources</span><br><span class="line">      │       └── application.yml</span><br><span class="line">      └── test</span><br><span class="line">          └── java</span><br></pre></td></tr></table></figure>
<h3 id="RedisOperations"><a href="#RedisOperations" class="headerlink" title="RedisOperations"></a>RedisOperations</h3><p>该类可以作为redis的工具类使用，自己添加一些常用的方法<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.iogogogo.redis.configure.util;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">import</span> lombok.extern.slf4j.Slf4j;</span><br><span class="line">  <span class="keyword">import</span> org.springframework.data.redis.core.RedisTemplate;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Created by tao.zeng on 2020-01-10.</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="meta">@Slf</span>4j</span><br><span class="line">  <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisOperations</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">private</span> RedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">      <span class="function"><span class="keyword">private</span> <span class="title">RedisOperations</span><span class="params">()</span> </span>&#123;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="function"><span class="keyword">public</span> <span class="title">RedisOperations</span><span class="params">(RedisTemplate redisTemplate)</span> </span>&#123;</span><br><span class="line">          <span class="keyword">this</span>.redisTemplate = redisTemplate;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="function"><span class="keyword">public</span> RedisTemplate <span class="title">redisTemplate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">          <span class="keyword">return</span> redisTemplate;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="RedisHandler"><a href="#RedisHandler" class="headerlink" title="RedisHandler"></a>RedisHandler</h3><p><code>RedisHandler</code>提供了统一操作<code>RedisTemplate</code>的入口，对用户来说下层的<code>RedisTemplate</code>是谁就不需要关心了<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.iogogogo.redis.configure.handler;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">import</span> com.iogogogo.redis.configure.util.RedisOperations;</span><br><span class="line">  <span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line">  <span class="keyword">import</span> org.springframework.data.redis.core.RedisTemplate;</span><br><span class="line">  <span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Created by tao.zeng on 2020-01-10.</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="meta">@Component</span></span><br><span class="line">  <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">      <span class="meta">@Autowired</span></span><br><span class="line">      <span class="keyword">private</span> RedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">      <span class="meta">@Autowired</span></span><br><span class="line">      <span class="keyword">private</span> RedisTemplate redisTemplateDB_2;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">private</span> RedisOperations redisOperations, redisDB2Operations;</span><br><span class="line"></span><br><span class="line">      <span class="function"><span class="keyword">public</span> RedisOperations <span class="title">redisOperations</span><span class="params">()</span> </span>&#123;</span><br><span class="line">          <span class="keyword">if</span> (redisOperations == <span class="keyword">null</span>) &#123;</span><br><span class="line">              redisOperations = <span class="keyword">new</span> RedisOperations(redisTemplate);</span><br><span class="line">          &#125;</span><br><span class="line">          <span class="keyword">return</span> redisOperations;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="function"><span class="keyword">public</span> RedisOperations <span class="title">redisDB2Operations</span><span class="params">()</span> </span>&#123;</span><br><span class="line">          <span class="keyword">if</span> (redisDB2Operations == <span class="keyword">null</span>) &#123;</span><br><span class="line">              redisDB2Operations = <span class="keyword">new</span> RedisOperations(redisTemplateDB_2);</span><br><span class="line">          &#125;</span><br><span class="line">          <span class="keyword">return</span> redisDB2Operations;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="最终结果"><a href="#最终结果" class="headerlink" title="最终结果"></a>最终结果</h2><p>我们在启动时看一下经过RedisHandler保证的RedisTemplate是否和我们想的一致，修改以后的启动类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.iogogogo.redis;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.iogogogo.redis.configure.handler.RedisHandler;</span><br><span class="line"><span class="keyword">import</span> com.iogogogo.redis.configure.util.RedisOperations;</span><br><span class="line"><span class="keyword">import</span> lombok.extern.slf4j.Slf4j;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.CommandLineRunner;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.SpringBootApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.redis.core.RedisTemplate;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by tao.zeng on 2020-01-10.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisApplication</span> <span class="keyword">implements</span> <span class="title">CommandLineRunner</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RedisTemplate redisTemplateDB_2;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RedisHandler redisHandler;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(RedisApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">(String... args)</span> </span>&#123;</span><br><span class="line">        log.info(<span class="string">"redisTemplate:&#123;&#125;"</span>, redisTemplate);</span><br><span class="line">        redisTemplate.opsForValue().set(<span class="string">"iogogogo"</span>, <span class="string">"redisTemplate save value"</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        log.info(<span class="string">"redisTemplateDB_2:&#123;&#125;"</span>, redisTemplateDB_2);</span><br><span class="line">        redisTemplateDB_2.opsForHash().put(<span class="string">"iogogogo"</span>, <span class="string">"iogogogo-hash"</span>, <span class="string">"redisTemplateDB_2 save value"</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 通过统一的RedisOperations对Redis进行操作</span></span><br><span class="line">        <span class="comment">// 这里可以自己封装一些常用的方法，这样就能把下层的RedisTemplate进行封装，对外仅仅只是一个RedisOperations而已</span></span><br><span class="line">        <span class="comment">// 当然这里也提供对应的方法获取不同RedisTemplate对象最后的封装实例</span></span><br><span class="line">        RedisOperations redisOperations = redisHandler.redisOperations();</span><br><span class="line">        RedisOperations redisDB2Operations = redisHandler.redisDB2Operations();</span><br><span class="line"></span><br><span class="line">        log.info(<span class="string">"redisOperations redisTemplate:&#123;&#125;"</span>, redisOperations.redisTemplate());</span><br><span class="line">        log.info(<span class="string">"redisDB2Operations redisTemplate:&#123;&#125;"</span>, redisDB2Operations.redisTemplate());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>启动日志<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2020-01-10 23:54:39.579  INFO 66673 --- [           main] com.iogogogo.redis.RedisApplication      : Started RedisApplication in 8.672 seconds (JVM running for 10.951)</span><br><span class="line">2020-01-10 23:54:39.580  INFO 66673 --- [           main] com.iogogogo.redis.RedisApplication      : redisTemplate:org.springframework.data.redis.core.RedisTemplate@44924587</span><br><span class="line">2020-01-10 23:54:39.679  INFO 66673 --- [           main] io.lettuce.core.EpollProvider            : Starting without optional epoll library</span><br><span class="line">2020-01-10 23:54:39.682  INFO 66673 --- [           main] io.lettuce.core.KqueueProvider           : Starting without optional kqueue library</span><br><span class="line">2020-01-10 23:54:39.910  INFO 66673 --- [           main] com.iogogogo.redis.RedisApplication      : redisTemplateDB2:org.springframework.data.redis.core.RedisTemplate@7fb66650</span><br><span class="line">2020-01-10 23:54:39.927  INFO 66673 --- [           main] com.iogogogo.redis.RedisApplication      : redisOperations redisTemplate:org.springframework.data.redis.core.RedisTemplate@44924587</span><br><span class="line">2020-01-10 23:54:39.929  INFO 66673 --- [           main] com.iogogogo.redis.RedisApplication      : redisDB2Operations redisTemplate:org.springframework.data.redis.core.RedisTemplate@7fb66650</span><br></pre></td></tr></table></figure></p>
<p>通过日志我们可以看到</p>
<ol>
<li><p><code>redisTemplate</code>和<code>redisOperations`</code> redisTemplate`是同一个对象</p>
</li>
<li><p><code>redisTemplateDB2</code> 和<code>redisDB2Operations`</code> redisTemplate`也是同一个对象</p>
</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>通过本文，我们可以看到一个项目如果需要使用使用多个redis连接获取使用不同的数据库，完全可以使用自定义<code>RedisConnectionFactory</code>来完成，第二点就是如果定义的<code>RedisTemplate</code>过多，我们可以在上面定义一个<code>RedisHandler</code>来进行封装下层的API操作，暴露一个统一的入口进行简化处理。</p>
<p>示例代码：<a href="https://github.com/iogogogo/life-example/tree/master/example-redis" target="_blank" rel="noopener">https://github.com/iogogogo/life-example/tree/master/example-redis</a></p>
]]></content>
      <tags>
        <tag>redis</tag>
      </tags>
  </entry>
  <entry>
    <title>sql代码规范</title>
    <url>/2019/02/22/sql%E4%BB%A3%E7%A0%81%E8%A7%84%E8%8C%83/</url>
    <content><![CDATA[<p>摘要：当前研发工作中经常出现因数据库表、数据库表字段格式不规则而影响开发进度的问题，在后续开发使用原来数据库表时，也会因为数据库表的可读性不够高，表字段规则不统一，造成数据查询，数据使用效率低的问题，所以有必要整理出一套合适的数据库表字段命名规范来解决优化这些问题。<br><a id="more"></a><br>本文是一篇包含了数据库命名、数据库表命名、数据库表字段命名及SQL语言编码的规范文档，针对研发中易产生的问题和常见错误做了一个整理和修改，为日后涉及到数据库相关的研发工作做好准备。<a href="https://www.mysql.com" target="_blank" rel="noopener">mysql</a>!</p>
<h1 id="一、数据库命名规范"><a href="#一、数据库命名规范" class="headerlink" title="一、数据库命名规范"></a>一、数据库命名规范</h1><p>采用26个英文字母(区分大小写)和0-9的自然数(经常不需要)加上下划线’_‘组成，命名简洁明确，多个单词用下划线’_‘分隔，一个项目一个数据库，多个项目慎用同一个数据库</p>
<h1 id="二、数据库表命名规范"><a href="#二、数据库表命名规范" class="headerlink" title="二、数据库表命名规范"></a>二、数据库表命名规范</h1><h2 id="2-1数据表命名规范"><a href="#2-1数据表命名规范" class="headerlink" title="2.1数据表命名规范"></a>2.1数据表命名规范</h2><h3 id="（1）采用26个英文字母-区分大小写-和0-9的自然数-经常不需要-加上下划线’‘组成，命名简洁明确，多个单词用下划线’‘分隔"><a href="#（1）采用26个英文字母-区分大小写-和0-9的自然数-经常不需要-加上下划线’‘组成，命名简洁明确，多个单词用下划线’‘分隔" class="headerlink" title="（1）采用26个英文字母(区分大小写)和0-9的自然数(经常不需要)加上下划线’‘组成，命名简洁明确，多个单词用下划线’‘分隔"></a>（1）采用26个英文字母(区分大小写)和0-9的自然数(经常不需要)加上下划线’<em>‘组成，命名简洁明确，多个单词用下划线’</em>‘分隔</h3><h3 id="（2）全部小写命名，禁止出现大写"><a href="#（2）全部小写命名，禁止出现大写" class="headerlink" title="（2）全部小写命名，禁止出现大写"></a>（2）全部小写命名，禁止出现大写</h3><h3 id="（3）禁止使用数据库关键字，如：name，time-，datetime，password等"><a href="#（3）禁止使用数据库关键字，如：name，time-，datetime，password等" class="headerlink" title="（3）禁止使用数据库关键字，如：name，time ，datetime，password等"></a>（3）禁止使用数据库关键字，如：name，time ，datetime，password等</h3><h3 id="（4）表名称不应该取得太长（一般不超过三个英文单词）"><a href="#（4）表名称不应该取得太长（一般不超过三个英文单词）" class="headerlink" title="（4）表名称不应该取得太长（一般不超过三个英文单词）"></a>（4）表名称不应该取得太长（一般不超过三个英文单词）</h3><h3 id="（5）表的名称一般使用名词或者动宾短语"><a href="#（5）表的名称一般使用名词或者动宾短语" class="headerlink" title="（5）表的名称一般使用名词或者动宾短语"></a>（5）表的名称一般使用名词或者动宾短语</h3><h3 id="（6）用单数形式表示名称，例如，使用-employee，而不是-employees"><a href="#（6）用单数形式表示名称，例如，使用-employee，而不是-employees" class="headerlink" title="（6）用单数形式表示名称，例如，使用 employee，而不是 employees"></a>（6）用单数形式表示名称，例如，使用 employee，而不是 employees</h3><p>明细表的名称为：主表的名称+字符dtl（detail缩写）</p>
<p>例如：采购定单的名称为：po_order，则采购定单的明细表为：po_orderdtl </p>
<h3 id="（7）表必须填写描述信息（使用SQL语句建表时）"><a href="#（7）表必须填写描述信息（使用SQL语句建表时）" class="headerlink" title="（7）表必须填写描述信息（使用SQL语句建表时）"></a>（7）表必须填写描述信息（使用SQL语句建表时）</h3><h2 id="2-2命名规范"><a href="#2-2命名规范" class="headerlink" title="2.2命名规范"></a>2.2命名规范</h2><h3 id="①模块-功能点-示例：alllive-log-alllive-category"><a href="#①模块-功能点-示例：alllive-log-alllive-category" class="headerlink" title="①模块_+功能点  示例：alllive_log   alllive_category"></a>①模块_+功能点  示例：alllive_log   alllive_category</h3><h3 id="②功能点-示例：live-message"><a href="#②功能点-示例：live-message" class="headerlink" title="②功能点  示例：live   message"></a>②功能点  示例：live   message</h3><h3 id="③通用表-示例：all-user"><a href="#③通用表-示例：all-user" class="headerlink" title="③通用表  示例：all_user"></a>③通用表  示例：all_user</h3><h2 id="2-3待优化命名示例"><a href="#2-3待优化命名示例" class="headerlink" title="2.3待优化命名示例"></a>2.3待优化命名示例</h2><h3 id="①冗余："><a href="#①冗余：" class="headerlink" title="①冗余："></a>①冗余：</h3><p>错误示例：yy_alllive_video_recomment    yy_alllive_open_close_log</p>
<p>说明：去除项目名，简化表名长度，去除”yy_”</p>
<h3 id="②相同类别表命名存在差异，管理性差"><a href="#②相同类别表命名存在差异，管理性差" class="headerlink" title="②相同类别表命名存在差异，管理性差"></a>②相同类别表命名存在差异，管理性差</h3><p>错误示例：yy_all_live_category    yy_alllive_comment_user</p>
<p>说明：去除项目名，统一命名规则，均为”yy_alllive_”开头即可</p>
<h3 id="③命名格式存在差异"><a href="#③命名格式存在差异" class="headerlink" title="③命名格式存在差异"></a>③命名格式存在差异</h3><p>错误示例：yy_showfriend    yy_user_getpoints    yy_live_program_get</p>
<p>说明：去除项目名，统一命名规则，动宾短语分离且动宾逻辑顺序统一</p>
<h1 id="三、数据库字段命名规范"><a href="#三、数据库字段命名规范" class="headerlink" title="三、数据库字段命名规范"></a>三、数据库字段命名规范</h1><h2 id="3-1字段命名规范"><a href="#3-1字段命名规范" class="headerlink" title="3.1字段命名规范"></a>3.1字段命名规范</h2><h3 id="（1）采用26个英文字母-区分大小写-和0-9的自然数-经常不需要-加上下划线’‘组成，命名简洁明确，多个单词用下划线’‘分隔-1"><a href="#（1）采用26个英文字母-区分大小写-和0-9的自然数-经常不需要-加上下划线’‘组成，命名简洁明确，多个单词用下划线’‘分隔-1" class="headerlink" title="（1）采用26个英文字母(区分大小写)和0-9的自然数(经常不需要)加上下划线’‘组成，命名简洁明确，多个单词用下划线’‘分隔"></a>（1）采用26个英文字母(区分大小写)和0-9的自然数(经常不需要)加上下划线’<em>‘组成，命名简洁明确，多个单词用下划线’</em>‘分隔</h3><h3 id="（2）全部小写命名，禁止出现大写-1"><a href="#（2）全部小写命名，禁止出现大写-1" class="headerlink" title="（2）全部小写命名，禁止出现大写"></a>（2）全部小写命名，禁止出现大写</h3><h3 id="（3）字段必须填写描述信息"><a href="#（3）字段必须填写描述信息" class="headerlink" title="（3）字段必须填写描述信息"></a>（3）字段必须填写描述信息</h3><h3 id="（4）禁止使用数据库关键字，如：name，time-，datetime-password-等"><a href="#（4）禁止使用数据库关键字，如：name，time-，datetime-password-等" class="headerlink" title="（4）禁止使用数据库关键字，如：name，time ，datetime password 等"></a>（4）禁止使用数据库关键字，如：name，time ，datetime password 等</h3><h3 id="（5）字段名称一般采用名词或动宾短语"><a href="#（5）字段名称一般采用名词或动宾短语" class="headerlink" title="（5）字段名称一般采用名词或动宾短语"></a>（5）字段名称一般采用名词或动宾短语</h3><h3 id="（6）采用字段的名称必须是易于理解，一般不超过三个英文单词"><a href="#（6）采用字段的名称必须是易于理解，一般不超过三个英文单词" class="headerlink" title="（6）采用字段的名称必须是易于理解，一般不超过三个英文单词"></a>（6）采用字段的名称必须是易于理解，一般不超过三个英文单词</h3><h3 id="（7）在命名表的列时，不要重复表的名称"><a href="#（7）在命名表的列时，不要重复表的名称" class="headerlink" title="（7）在命名表的列时，不要重复表的名称"></a>（7）在命名表的列时，不要重复表的名称</h3><p>例如，在名employe的表中避免使用名为employee_lastname的字段</p>
<h3 id="（8）不要在列的名称中包含数据类型"><a href="#（8）不要在列的名称中包含数据类型" class="headerlink" title="（8）不要在列的名称中包含数据类型"></a>（8）不要在列的名称中包含数据类型</h3><h3 id="（9）字段命名使用完整名称，禁止缩写"><a href="#（9）字段命名使用完整名称，禁止缩写" class="headerlink" title="（9）字段命名使用完整名称，禁止缩写"></a>（9）字段命名使用完整名称，禁止缩写</h3><h2 id="3-2命名规范"><a href="#3-2命名规范" class="headerlink" title="3.2命名规范"></a>3.2命名规范</h2><h3 id="①名词-示例：user-id-user-name-sex"><a href="#①名词-示例：user-id-user-name-sex" class="headerlink" title="①名词  示例：user_id    user_name    sex"></a>①名词  示例：user_id    user_name    sex</h3><h3 id="②动宾短语-示例：is-friend-is-good"><a href="#②动宾短语-示例：is-friend-is-good" class="headerlink" title="②动宾短语  示例：is_friend   is_good"></a>②动宾短语  示例：is_friend   is_good</h3><h2 id="3-3待优化命名示例"><a href="#3-3待优化命名示例" class="headerlink" title="3.3待优化命名示例"></a>3.3待优化命名示例</h2><h3 id="①大小写规则不统一"><a href="#①大小写规则不统一" class="headerlink" title="①大小写规则不统一"></a>①大小写规则不统一</h3><p>错误示例：user_id    houseID</p>
<p>说明：使用统一规则，修改为”user_id”，”house_id”</p>
<h3 id="②加下划线规则不统一"><a href="#②加下划线规则不统一" class="headerlink" title="②加下划线规则不统一"></a>②加下划线规则不统一</h3><p>错误示例：username    userid    isfriend    isgood</p>
<p>说明：使用下划线进行分类，提升可性，方便管理，修改为”user_name”，”user_id”，”is_friend”，”is_good”</p>
<h3 id="③字段表示不明确"><a href="#③字段表示不明确" class="headerlink" title="③字段表示不明确"></a>③字段表示不明确</h3><p>错误示例：uid    pid</p>
<p>说明：使用完整名称，提高可读性，修改为”user_id”，”person_id”</p>
<h2 id="3-4字段类型规范"><a href="#3-4字段类型规范" class="headerlink" title="3.4字段类型规范"></a>3.4字段类型规范</h2><h3 id="（1）所有字段在设计时，除以下数据类型timestamp、image、datetime、smalldatetime、uniqueidentifier、binary、sql-variant、binary-、varbinary外，必须有默认值，字符型的默认值为一个空字符值串’’，数值型的默认值为数值0，逻辑型的默认值为数值0"><a href="#（1）所有字段在设计时，除以下数据类型timestamp、image、datetime、smalldatetime、uniqueidentifier、binary、sql-variant、binary-、varbinary外，必须有默认值，字符型的默认值为一个空字符值串’’，数值型的默认值为数值0，逻辑型的默认值为数值0" class="headerlink" title="（1）所有字段在设计时，除以下数据类型timestamp、image、datetime、smalldatetime、uniqueidentifier、binary、sql_variant、binary 、varbinary外，必须有默认值，字符型的默认值为一个空字符值串’’，数值型的默认值为数值0，逻辑型的默认值为数值0"></a>（1）所有字段在设计时，除以下数据类型timestamp、image、datetime、smalldatetime、uniqueidentifier、binary、sql_variant、binary 、varbinary外，必须有默认值，字符型的默认值为一个空字符值串’’，数值型的默认值为数值0，逻辑型的默认值为数值0</h3><h3 id="（2）系统中所有逻辑型中数值0表示为“假”，数值1表示为“真”，datetime、smalldatetime类型的字段没有默认值，必须为NULL"><a href="#（2）系统中所有逻辑型中数值0表示为“假”，数值1表示为“真”，datetime、smalldatetime类型的字段没有默认值，必须为NULL" class="headerlink" title="（2）系统中所有逻辑型中数值0表示为“假”，数值1表示为“真”，datetime、smalldatetime类型的字段没有默认值，必须为NULL"></a>（2）系统中所有逻辑型中数值0表示为“假”，数值1表示为“真”，datetime、smalldatetime类型的字段没有默认值，必须为NULL</h3><h3 id="（3）用尽量少的存储空间来存储一个字段的数据"><a href="#（3）用尽量少的存储空间来存储一个字段的数据" class="headerlink" title="（3）用尽量少的存储空间来存储一个字段的数据"></a>（3）用尽量少的存储空间来存储一个字段的数据</h3><p>使用int就不要使用varchar、char，</p>
<p>用varchar(16)就不要使varchar(256)</p>
<p>IP地址使用int类型</p>
<p>固定长度的类型最好使用char，例如：邮编(postcode)</p>
<p>能使用tinyint就不要使用smallint，int</p>
<p>最好给每个字段一个默认值，最好不能为null</p>
<h3 id="（4）用合适的字段类型节约空间"><a href="#（4）用合适的字段类型节约空间" class="headerlink" title="（4）用合适的字段类型节约空间"></a>（4）用合适的字段类型节约空间</h3><p>字符转化为数字(能转化的最好转化，同样节约空间、提高查询性能)</p>
<p>避免使用NULL字段(NULL字段很难查询优化、NULL字段的索引需要额外空间、NULL字段的复合索引无效)</p>
<p>少用text类型(尽量使用varchar代替text字段)</p>
<h2 id="3-5数据库中每个字段的规范描述"><a href="#3-5数据库中每个字段的规范描述" class="headerlink" title="3.5数据库中每个字段的规范描述"></a>3.5数据库中每个字段的规范描述</h2><h3 id="（1）尽量遵守第三范式的标准（3NF）"><a href="#（1）尽量遵守第三范式的标准（3NF）" class="headerlink" title="（1）尽量遵守第三范式的标准（3NF）"></a>（1）尽量遵守第三范式的标准（3NF）</h3><pre><code>表内的每一个值只能被表达一次 

表内的每一行都应当被唯一的标示 

表内不应该存储依赖于其他键的非键信息
</code></pre><h3 id="2-如果字段事实上是与其它表的关键字相关联而未设计为外键引用，需建索引"><a href="#2-如果字段事实上是与其它表的关键字相关联而未设计为外键引用，需建索引" class="headerlink" title="(2)如果字段事实上是与其它表的关键字相关联而未设计为外键引用，需建索引"></a>(2)如果字段事实上是与其它表的关键字相关联而未设计为外键引用，需建索引</h3><h3 id="3-如果字段与其它表的字段相关联，需建索引"><a href="#3-如果字段与其它表的字段相关联，需建索引" class="headerlink" title="(3)如果字段与其它表的字段相关联，需建索引"></a>(3)如果字段与其它表的字段相关联，需建索引</h3><h3 id="4-如果字段需做模糊查询之外的条件查询，需建索引"><a href="#4-如果字段需做模糊查询之外的条件查询，需建索引" class="headerlink" title="(4)如果字段需做模糊查询之外的条件查询，需建索引"></a>(4)如果字段需做模糊查询之外的条件查询，需建索引</h3><h3 id="5-除了主关键字允许建立簇索引外，其它字段所建索引必须为非簇索引"><a href="#5-除了主关键字允许建立簇索引外，其它字段所建索引必须为非簇索引" class="headerlink" title="(5)除了主关键字允许建立簇索引外，其它字段所建索引必须为非簇索引"></a>(5)除了主关键字允许建立簇索引外，其它字段所建索引必须为非簇索引</h3><h1 id="四、SQL语言编码规范"><a href="#四、SQL语言编码规范" class="headerlink" title="四、SQL语言编码规范"></a>四、SQL语言编码规范</h1><h2 id="4-1大小写规范"><a href="#4-1大小写规范" class="headerlink" title="4.1大小写规范"></a>4.1大小写规范</h2><h3 id="（1）所有关键字必须大写，如：INSERT、UPDATE、DELETE、SELECT及其子句，IF……ELSE、CASE、DECLARE等"><a href="#（1）所有关键字必须大写，如：INSERT、UPDATE、DELETE、SELECT及其子句，IF……ELSE、CASE、DECLARE等" class="headerlink" title="（1）所有关键字必须大写，如：INSERT、UPDATE、DELETE、SELECT及其子句，IF……ELSE、CASE、DECLARE等"></a>（1）所有关键字必须大写，如：INSERT、UPDATE、DELETE、SELECT及其子句，IF……ELSE、CASE、DECLARE等</h3><h3 id="（2）所有函数及其参数中除用户变量以外的部分必须大写"><a href="#（2）所有函数及其参数中除用户变量以外的部分必须大写" class="headerlink" title="（2）所有函数及其参数中除用户变量以外的部分必须大写"></a>（2）所有函数及其参数中除用户变量以外的部分必须大写</h3><h3 id="（3）在定义变量时用到的数据类型必须小写"><a href="#（3）在定义变量时用到的数据类型必须小写" class="headerlink" title="（3）在定义变量时用到的数据类型必须小写"></a>（3）在定义变量时用到的数据类型必须小写</h3><h2 id="4-2注释"><a href="#4-2注释" class="headerlink" title="4.2注释"></a>4.2注释</h2><p>注释可以包含在批处理中，在触发器、存储过程中包含描述性注释将大大增加文本的可读性和可维护性，本规范建议： </p>
<h3 id="（1）注释以英文为主，实际应用中，发现以中文注释的SQL语句版本在英文环境中不可用，为避免后续版本执行过程中发生某些异常错误，建议使用英文注释"><a href="#（1）注释以英文为主，实际应用中，发现以中文注释的SQL语句版本在英文环境中不可用，为避免后续版本执行过程中发生某些异常错误，建议使用英文注释" class="headerlink" title="（1）注释以英文为主，实际应用中，发现以中文注释的SQL语句版本在英文环境中不可用，为避免后续版本执行过程中发生某些异常错误，建议使用英文注释"></a>（1）注释以英文为主，实际应用中，发现以中文注释的SQL语句版本在英文环境中不可用，为避免后续版本执行过程中发生某些异常错误，建议使用英文注释</h3><h3 id="（2）注释尽可能详细、全面创建每一数据对象前，应具体描述该对象的功能和用途，传入参数的含义应该有所说明，如果取值范围确定，也应该一并说明，取值有特定含义的变量（如boolean类型变量），应给出每个值的含义"><a href="#（2）注释尽可能详细、全面创建每一数据对象前，应具体描述该对象的功能和用途，传入参数的含义应该有所说明，如果取值范围确定，也应该一并说明，取值有特定含义的变量（如boolean类型变量），应给出每个值的含义" class="headerlink" title="（2）注释尽可能详细、全面创建每一数据对象前，应具体描述该对象的功能和用途，传入参数的含义应该有所说明，如果取值范围确定，也应该一并说明，取值有特定含义的变量（如boolean类型变量），应给出每个值的含义"></a>（2）注释尽可能详细、全面创建每一数据对象前，应具体描述该对象的功能和用途，传入参数的含义应该有所说明，如果取值范围确定，也应该一并说明，取值有特定含义的变量（如boolean类型变量），应给出每个值的含义</h3><h3 id="（3）注释语法：单行注释、多行注释"><a href="#（3）注释语法：单行注释、多行注释" class="headerlink" title="（3）注释语法：单行注释、多行注释"></a>（3）注释语法：单行注释、多行注释</h3><p>单行注释：注释前有两个连字符（–）对变量、条件子句可以采用该类注释</p>
<p>多行注释：符号之间的内容为注释内容，对某项完整的操作建议使用该类注释</p>
<h3 id="（4）注释简洁，同时应描述清晰"><a href="#（4）注释简洁，同时应描述清晰" class="headerlink" title="（4）注释简洁，同时应描述清晰"></a>（4）注释简洁，同时应描述清晰</h3><h3 id="（5）函数注释："><a href="#（5）函数注释：" class="headerlink" title="（5）函数注释："></a>（5）函数注释：</h3><p>编写函数文本–如触发器、存储过程以及其他数据对象–时，必须为每个函数增加适当注释，该注释以多行注释为主，主要结构如下： </p>
<p>CREATE PROCEDURE sp_xxx </p>
]]></content>
      <tags>
        <tag>sql</tag>
      </tags>
  </entry>
  <entry>
    <title>win10使用docker和virtualBox冲突的过程</title>
    <url>/2020/07/31/win10%E4%BD%BF%E7%94%A8docker%E5%92%8CvirtualBox%E5%86%B2%E7%AA%81%E7%9A%84%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="win10使用docker和virtualBox冲突的过程"><a href="#win10使用docker和virtualBox冲突的过程" class="headerlink" title="win10使用docker和virtualBox冲突的过程"></a>win10使用docker和virtualBox冲突的过程</h1><blockquote>
<p>本文并没有解决 win10 同时使用 docker 和 virtualBox 时起冲突的问题。只是记录一下解决的过程。<br>系统版本：Windows 10 专业版 2004<br>操作系统版本：19041.329<br>CPU：i5-4590。内存：16GB</p>
</blockquote>
<a id="more"></a>
<h2 id="过程"><a href="#过程" class="headerlink" title="过程"></a>过程</h2><p>首先，我本机中装有 Docker for Windows ，Hyper-v已经开启。尝试用VMware时，可以成功安装并启动 Centos7 系统。但是无法和 win10 主机共享文件夹。</p>
<p>同事的电脑安装 virtualBox 可以正常使用，我就用他的版本来尝试了一下。发现因为本机的  Hyper-v 已经开启的缘故，virtualBox 无法成功安装开机。关闭了 Hyper-v 后发现还是显示 VT-X 没有启动，同事的电脑没有安装过 docker，可以成功的启动。</p>
<p>检查后关闭了 Windows 功能中的<code>适用于 Linux 的 Windows 子系统</code>，然后还是依旧不能成功启动 virtualBox 安装的虚拟机。</p>
<p>检查网络适配器，已经只剩下 virtualBox 的和机器本身的网卡。目前进度卡在这里，决定放弃在我本机上用 virtualBox 安装 Centos7 使用。</p>
]]></content>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>windows同时安装多个mysql8.0服务</title>
    <url>/2019/03/12/windows%E5%90%8C%E6%97%B6%E5%AE%89%E8%A3%85%E5%A4%9A%E4%B8%AAmysql8-0%E6%9C%8D%E5%8A%A1/</url>
    <content><![CDATA[<h1 id="windows同时安装多个mysql8-0服务"><a href="#windows同时安装多个mysql8-0服务" class="headerlink" title="windows同时安装多个mysql8.0服务"></a>windows同时安装多个mysql8.0服务</h1><blockquote>
<p>原作者地址为<a href="https://blog.csdn.net/m0_37890289/article/details/80003994。" target="_blank" rel="noopener">https://blog.csdn.net/m0_37890289/article/details/80003994。</a> 我在mysql主从复制时使用的这篇文章，这里做一个备份。</p>
</blockquote>
<a id="more"></a>
<h2 id="官网下载-mysql文件。官网下载链接：https-dev-mysql-com-downloads-mysql"><a href="#官网下载-mysql文件。官网下载链接：https-dev-mysql-com-downloads-mysql" class="headerlink" title="官网下载 mysql文件。官网下载链接：https://dev.mysql.com/downloads/mysql/"></a>官网下载 mysql文件。官网下载链接：<a href="https://dev.mysql.com/downloads/mysql/" target="_blank" rel="noopener">https://dev.mysql.com/downloads/mysql/</a></h2><p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b1.png" alt="1"><br>　　1. 选择mysql下载的系统版本。<br>　　2. 此处可以下载MSI安装包，图简单的朋友可以下载，然后“下一步”安装即可。<br>　　3. 此处下载ZIP压缩包版（这次记录ZIP压缩包安装方法）</p>
<h2 id="解压下载好的ZIP文件，到自己喜欢的位置。"><a href="#解压下载好的ZIP文件，到自己喜欢的位置。" class="headerlink" title="解压下载好的ZIP文件，到自己喜欢的位置。"></a>解压下载好的ZIP文件，到自己喜欢的位置。</h2><p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b2.png" alt="2"><br>　　1. 因为我这次要安装多个mysql服务，为mysql的主从复制做准备，所以我的文件结构如上。<br>　　2. 官网下载的ZIP压缩包。<br>　　3. 解压3次副本。</p>
<h2 id="在解压后的文件夹下新建my-ini文件。内容参考如下："><a href="#在解压后的文件夹下新建my-ini文件。内容参考如下：" class="headerlink" title="在解压后的文件夹下新建my.ini文件。内容参考如下："></a>在解压后的文件夹下新建my.ini文件。内容参考如下：</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[mysql]</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"># 设置mysql客户端默认字符集</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">default-character-set=utf8 </span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">[mysqld]</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">#设置3306端口</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">port = 3306 </span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"># 设置mysql的安装目录</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">basedir=E:\mysql\mysql3\mysql-8.0.11-winx64</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"># 设置mysql数据库的数据的存放目录</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">datadir=E:\mysql\mysql3\mysql-8.0.11-winx64\data</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">sql_mode=NO_ENGINE_SUBSTITUTION,STRICT_TRANS_TABLES </span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">character-set-server = utf8mb4</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">performance_schema_max_table_instances = 600</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">table_definition_cache = 400</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">table_open_cache = 256</span><br></pre></td></tr></table></figure>
<p>处理完目录结构如下。<br><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b3.png" alt="3"></p>
<h2 id="管理员身份运行cmd-并将切换到你解压过后的文件的bin目录下。"><a href="#管理员身份运行cmd-并将切换到你解压过后的文件的bin目录下。" class="headerlink" title="管理员身份运行cmd,并将切换到你解压过后的文件的bin目录下。"></a>管理员身份运行cmd,并将切换到你解压过后的文件的bin目录下。</h2><p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b4.png" alt="4"></p>
<h2 id="初始化mysql"><a href="#初始化mysql" class="headerlink" title="初始化mysql"></a>初始化mysql</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    mysqld --defaults-file=D:\mysql\mysql3\mysql-8.0.11-winx64\my.ini --initialize --console</span><br></pre></td></tr></table></figure>
<p>注意路径的填写，要和自己的解压安装包路径一致。<br>执行完毕后，文件结构多了一个data目录。并记录初始化密码（后面第一次进入mysql修改密码需要）。</p>
<h2 id="初始化mysql服务。"><a href="#初始化mysql服务。" class="headerlink" title="初始化mysql服务。"></a>初始化mysql服务。</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    mysqld install MySQL3 --defaults-file=&quot;D:\mysql\mysql3\mysql-8.0.11-winx64\my.ini&quot;</span><br></pre></td></tr></table></figure>
<p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b5.png" alt="5"></p>
<h2 id="启动mysql服务。"><a href="#启动mysql服务。" class="headerlink" title="启动mysql服务。"></a>启动mysql服务。</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    net start MySQL3</span><br></pre></td></tr></table></figure>
<p>这里的服务名字和上面的命令中起的服务名一致</p>
<p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b6.png" alt="6"></p>
<h3 id="删除服务"><a href="#删除服务" class="headerlink" title="删除服务"></a>删除服务</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sc delete MySQL3（服务名称）</span><br></pre></td></tr></table></figure>
<p>为了防止出现一不小心把命令行关闭了，而且还没记录密码，需要重新开始的情况。在此记录一下删除命令</p>
<h2 id="以root身份进入mysql。"><a href="#以root身份进入mysql。" class="headerlink" title="以root身份进入mysql。"></a>以root身份进入mysql。</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    mysql -u root -p --protocol=tcp --host=localhost --port=3308（选择端口）</span><br></pre></td></tr></table></figure>
<p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b7.png" alt="7"><br>此处就需要上面初始化mysql时候系统生成的密码了。<br><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b8.png" alt="8"></p>
<h2 id="修改root密码。"><a href="#修改root密码。" class="headerlink" title="修改root密码。"></a>修改root密码。</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    ALTER USER &apos;root&apos;@&apos;localhost&apos; IDENTIFIED WITH mysql_native_password BY &apos;newpassword&apos;;</span><br></pre></td></tr></table></figure>
<p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b9.png" alt="9"></p>
<h2 id="密码修改后刷新权限，就可以使用mysql了。"><a href="#密码修改后刷新权限，就可以使用mysql了。" class="headerlink" title="密码修改后刷新权限，就可以使用mysql了。"></a>密码修改后刷新权限，就可以使用mysql了。</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    flush privileges;</span><br></pre></td></tr></table></figure>
<p><img src="/2019/03/12/windows同时安装多个mysql8-0服务/b10.png" alt="10"></p>
<p><strong>这个方法可以在一台电脑上安装N个mysql服务，注意修改不同的端口号即可。</strong></p>
<h2 id="后记："><a href="#后记：" class="headerlink" title="后记："></a>后记：</h2><p>在一次安装时，新建完my.ini文件执行时。出现了一下的这个错误：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">error: Found option without preceding group in config file: D:\mysql-5.6.24-win32\my.ini at line: 1  </span><br><span class="line">Fatal error in defaults handling. Program aborted</span><br></pre></td></tr></table></figure>
<p>查询后发现是文件的编码问题，从utf-8改为ANSI后就可以了。</p>
<h2 id="9月更新"><a href="#9月更新" class="headerlink" title="9月更新"></a>9月更新</h2><p>发现这个博客应该配合<strong>配置mysql远程访问</strong>一起使用会比较好。加入后续步骤</p>
<h3 id="更改root用户访问ip限制"><a href="#更改root用户访问ip限制" class="headerlink" title="更改root用户访问ip限制"></a>更改root用户访问ip限制</h3><p>首先要进入你想要更改的mysql：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">mysql -u root -p <span class="comment">--protocol=tcp --host=localhost --port=3308（选择端口）</span></span><br></pre></td></tr></table></figure></p>
<p>然后查询和修改<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> mysql;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> host, <span class="keyword">user</span>, authentication_string, <span class="keyword">plugin</span> <span class="keyword">from</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>
<p>可以看到现在的root用户的host是localhost<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">update</span> <span class="keyword">user</span> <span class="keyword">set</span> host = <span class="string">'%'</span> <span class="keyword">where</span> <span class="keyword">user</span> = <span class="string">'root'</span>;</span><br></pre></td></tr></table></figure></p>
<p>修改后需要刷新一下权限<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">flush</span> <span class="keyword">privileges</span>;</span><br></pre></td></tr></table></figure></p>
<p>然后开启全部访问权限<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">GRANT</span> ALL <span class="keyword">PRIVILEGES</span> <span class="keyword">ON</span> *.* <span class="keyword">TO</span> <span class="string">'root'</span>@<span class="string">'%'</span> <span class="keyword">WITH</span> <span class="keyword">GRANT</span> <span class="keyword">OPTION</span>;</span><br></pre></td></tr></table></figure></p>
<p>再次刷新<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">flush</span> <span class="keyword">privileges</span>;</span><br></pre></td></tr></table></figure></p>
<p>完成以上步骤后就可以从本机中测试连接服务器的mysql了。</p>
<p>如果还是连接失败的话，记得开启服务器防火墙上的入站规则中的端口3306和3308（我自己配置的mysql端口就是这两个）</p>
]]></content>
      <tags>
        <tag>sql</tag>
      </tags>
  </entry>
  <entry>
    <title>【定时任务|开机启动】Windows Server 2008/2012 计划任务配置（任务计划程序）每分钟执行BAT</title>
    <url>/2019/02/22/%E3%80%90%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1-%E5%BC%80%E6%9C%BA%E5%90%AF%E5%8A%A8%E3%80%91Windows-Server-2008-2012-%E8%AE%A1%E5%88%92%E4%BB%BB%E5%8A%A1%E9%85%8D%E7%BD%AE%EF%BC%88%E4%BB%BB%E5%8A%A1%E8%AE%A1%E5%88%92%E7%A8%8B%E5%BA%8F%EF%BC%89%E6%AF%8F%E5%88%86%E9%92%9F%E6%89%A7%E8%A1%8CBAT/</url>
    <content><![CDATA[<blockquote>
<p>转载自<a href="https://blog.csdn.net/happymagic/article/details/51833361" target="_blank" rel="noopener">https://blog.csdn.net/happymagic/article/details/51833361</a> 这里仅仅是作为以后使用的备份</p>
</blockquote>
<a id="more"></a>
<p>　　首先Windows Server 2008不同于其他服务器操作系统和Windows Server 2003有着很大的区别，计划任务的名称是“任务计划程序”不在控制面板里，而是在“管理工具”里。由于服务器需要做些任务，定时执行，自己写程序吧，麻烦，所以采用BAT进行代替操作，网络上很多人都在说每分钟执行怎么配置，今天我们就配置一下。</p>
<ol>
<li>打开“<strong>任务计划程序</strong>”–&gt; 点击“<strong>任务计划程序库</strong>”，在右侧会看到操作项里有“创建基本任务”和“创建任务”如图所示：<img src="https://i.imgur.com/VWv9VJw.jpg" alt=""></li>
<li>点击“<strong>创建任务</strong>”后如图所示：填写好相应的名称和勾选好必要的条件<img src="https://i.imgur.com/Emir4yA.jpg" alt=""></li>
<li><p>选择“<strong>触发器</strong>”选项，点击“<strong>新建</strong>，创建任务执行时间，<strong>重复任务间隔</strong>”这个选择后，后面有时间选择，是每小时，还是每分，可自己选择后再修改时间，再确定。<img src="https://i.imgur.com/MmIiOFq.jpg" alt=""><br><strong>特殊说明：</strong>Windows计划任务是精确到分钟级别的,其实，它除了下拉选择外，还可手动修改时间值，比如上面的“1分钟”!!!<img src="https://i.imgur.com/BH1MqDj.jpg" alt=""></p>
</li>
<li><p>再来配置需要执行的“操作”，就是选择所写的程序或是BAT文件，这里很重要的配置是选择BAT文件后，在“起始于（可选）”这里一定要填写相应执行程序或是BAT文件的所在目录，要不然是执行不成功的。<img src="https://i.imgur.com/VCKAC4z.jpg" alt=""></p>
</li>
<li><p>添加后，列表栏中会出现添加的计划任务。<img src="https://i.imgur.com/4J8raf0.jpg" alt=""></p>
</li>
</ol>
]]></content>
      <tags>
        <tag>window server</tag>
      </tags>
  </entry>
  <entry>
    <title>一个普通的日常bug</title>
    <url>/2019/07/19/%E4%B8%80%E4%B8%AA%E6%99%AE%E9%80%9A%E7%9A%84%E6%97%A5%E5%B8%B8bug/</url>
    <content><![CDATA[<h1 id="一个普通的日常bug（-Failed-to-configure-a-DataSource-‘url’-attribute-is-not-specified-）"><a href="#一个普通的日常bug（-Failed-to-configure-a-DataSource-‘url’-attribute-is-not-specified-）" class="headerlink" title="一个普通的日常bug（ Failed to configure a DataSource: ‘url’ attribute is not specified ）"></a>一个普通的日常bug（ Failed to configure a DataSource: ‘url’ attribute is not specified ）</h1><blockquote>
<p>SpringBoot 2.0 报错: Failed to configure a DataSource: ‘url’ attribute is not specified and no embedded datasource could be configured.</p>
</blockquote>
<a id="more"></a>
<h2 id="bug日志"><a href="#bug日志" class="headerlink" title="bug日志"></a>bug日志</h2><p><strong>关键词：</strong> Failed to configure a DataSource: ‘url’ attribute is not specified and no embedded datasource could be configured.</p>
<p><strong>关键词：</strong> Reason: Failed to determine a suitable driver class.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">2019-07-15 11:11:27.857  INFO 7604 --- [           main] com.yiring.zjtq.Application              : Starting Application on myhbase with PID 7604 (E:\idea-workspace\zjtq-web\out\production\classes started by Administrator in E:\idea-workspace\zjtq-web)</span><br><span class="line">2019-07-15 11:11:27.867  INFO 7604 --- [           main] com.yiring.zjtq.Application              : The following profiles are active: prod</span><br><span class="line">2019-07-15 11:11:27.922  INFO 7604 --- [           main] ConfigServletWebServerApplicationContext : Refreshing org.springframework.boot.web.servlet.context.AnnotationConfigServletWebServerApplicationContext@68567e20: startup date [Mon Jul 15 11:11:27 CST 2019]; root of context hierarchy</span><br><span class="line">2019-07-15 11:11:28.667  INFO 7604 --- [           main] .s.d.r.c.RepositoryConfigurationDelegate : Multiple Spring Data modules found, entering strict repository configuration mode!</span><br><span class="line">2019-07-15 11:11:28.689  INFO 7604 --- [           main] .s.d.r.c.RepositoryConfigurationDelegate : Multiple Spring Data modules found, entering strict repository configuration mode!</span><br><span class="line">2019-07-15 11:11:29.081  INFO 7604 --- [           main] trationDelegate$BeanPostProcessorChecker : Bean &apos;org.springframework.transaction.annotation.ProxyTransactionManagementConfiguration&apos; of type [org.springframework.transaction.annotation.ProxyTransactionManagementConfiguration$$EnhancerBySpringCGLIB$$5d98e995] is not eligible for getting processed by all BeanPostProcessors (for example: not eligible for auto-proxying)</span><br><span class="line">2019-07-15 11:11:29.543  INFO 7604 --- [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat initialized with port(s): 8080 (http)</span><br><span class="line">2019-07-15 11:11:29.578  INFO 7604 --- [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]</span><br><span class="line">2019-07-15 11:11:29.578  INFO 7604 --- [           main] org.apache.catalina.core.StandardEngine  : Starting Servlet Engine: Apache Tomcat/8.5.32</span><br><span class="line">2019-07-15 11:11:29.583  INFO 7604 --- [ost-startStop-1] o.a.catalina.core.AprLifecycleListener   : The APR based Apache Tomcat Native library which allows optimal performance in production environments was not found on the java.library.path: [C:\Program Files\Java\jdk1.8.0_161\bin;C:\Windows\Sun\Java\bin;C:\Windows\system32;C:\Windows;C:\ProgramData\Oracle\Java\javapath;C:\Windows\system32;C:\Windows;C:\Windows\System32\Wbem;C:\Windows\System32\WindowsPowerShell\v1.0\;C:\Program Files\TortoiseSVN\bin;E:\apache-maven-3.5.2\bin;C:\Program Files (x86)\OpenGrADS\Contents\Cygwin\Versions\2.0.a9.oga.1\i686;C:\Program Files\Git\cmd;C:\Users\Administrator\.gradle\wrapper\dists\gradle-5.2.1-all\bviwmvmbexq6idcscbicws5me\gradle-5.2.1\bin;D:\nodejs\;C:\Program Files\TortoiseGit\bin;C:\Program Files\Microsoft VS Code\bin;D:\hadoop\hadoop-3.1.1\bin;C:\Program Files\Java\jdk1.8.0_161\bin;C:\Program Files\Java\jdk1.8.0_161\jre\bin;D:\hadoop\hbase-2.1.5\bin;C:\Program Files\Microsoft VS Code\bin;C:\Users\Administrator\AppData\Roaming\npm;C:\Users\Administrator\AppData\Local\Programs\Fiddler;C:\Users\Administrator\AppData\Local\Yarn\bin;F:\docker\Docker Toolbox;.]</span><br><span class="line">2019-07-15 11:11:29.749  INFO 7604 --- [ost-startStop-1] o.a.c.c.C.[.[localhost].[/zjtq/v3]       : Initializing Spring embedded WebApplicationContext</span><br><span class="line">2019-07-15 11:11:29.750  INFO 7604 --- [ost-startStop-1] o.s.web.context.ContextLoader            : Root WebApplicationContext: initialization completed in 1828 ms</span><br><span class="line">2019-07-15 11:11:29.862  INFO 7604 --- [ost-startStop-1] o.s.b.w.servlet.FilterRegistrationBean   : Mapping filter: &apos;characterEncodingFilter&apos; to: [/*]</span><br><span class="line">2019-07-15 11:11:29.863  INFO 7604 --- [ost-startStop-1] o.s.b.w.servlet.FilterRegistrationBean   : Mapping filter: &apos;hiddenHttpMethodFilter&apos; to: [/*]</span><br><span class="line">2019-07-15 11:11:29.863  INFO 7604 --- [ost-startStop-1] o.s.b.w.servlet.FilterRegistrationBean   : Mapping filter: &apos;httpPutFormContentFilter&apos; to: [/*]</span><br><span class="line">2019-07-15 11:11:29.863  INFO 7604 --- [ost-startStop-1] o.s.b.w.servlet.FilterRegistrationBean   : Mapping filter: &apos;requestContextFilter&apos; to: [/*]</span><br><span class="line">2019-07-15 11:11:29.864  INFO 7604 --- [ost-startStop-1] .s.DelegatingFilterProxyRegistrationBean : Mapping filter: &apos;springSecurityFilterChain&apos; to: [/*]</span><br><span class="line">2019-07-15 11:11:29.864  INFO 7604 --- [ost-startStop-1] o.s.b.w.servlet.FilterRegistrationBean   : Mapping filter: &apos;webStatFilter&apos; to urls: [/*]</span><br><span class="line">2019-07-15 11:11:29.864  INFO 7604 --- [ost-startStop-1] o.s.b.w.servlet.ServletRegistrationBean  : Servlet dispatcherServlet mapped to [/]</span><br><span class="line">2019-07-15 11:11:29.865  INFO 7604 --- [ost-startStop-1] o.s.b.w.servlet.ServletRegistrationBean  : Servlet statViewServlet mapped to [/druid/*]</span><br><span class="line">2019-07-15 11:11:29.900  INFO 7604 --- [           main] c.a.d.s.b.a.DruidDataSourceAutoConfigure : Init DruidDataSource</span><br><span class="line">2019-07-15 11:11:30.037  WARN 7604 --- [           main] ConfigServletWebServerApplicationContext : Exception encountered during context initialization - cancelling refresh attempt: org.springframework.beans.factory.UnsatisfiedDependencyException: Error creating bean with name &apos;org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaConfiguration&apos;: Unsatisfied dependency expressed through constructor parameter 0; nested exception is org.springframework.beans.factory.BeanCreationException: Error creating bean with name &apos;dataSource&apos; defined in class path resource [com/alibaba/druid/spring/boot/autoconfigure/DruidDataSourceAutoConfigure.class]: Invocation of init method failed; nested exception is org.springframework.boot.autoconfigure.jdbc.DataSourceProperties$DataSourceBeanCreationException: Failed to determine a suitable driver class</span><br><span class="line">2019-07-15 11:11:30.039  INFO 7604 --- [           main] o.apache.catalina.core.StandardService   : Stopping service [Tomcat]</span><br><span class="line">2019-07-15 11:11:30.074  INFO 7604 --- [           main] ConditionEvaluationReportLoggingListener : </span><br><span class="line"></span><br><span class="line">Error starting ApplicationContext. To display the conditions report re-run your application with &apos;debug&apos; enabled.</span><br><span class="line">2019-07-15 11:11:30.080 ERROR 7604 --- [           main] o.s.b.d.LoggingFailureAnalysisReporter   : </span><br><span class="line"></span><br><span class="line">***************************</span><br><span class="line">APPLICATION FAILED TO START</span><br><span class="line">***************************</span><br><span class="line"></span><br><span class="line">Description:</span><br><span class="line"></span><br><span class="line">Failed to configure a DataSource: &apos;url&apos; attribute is not specified and no embedded datasource could be configured.</span><br><span class="line"></span><br><span class="line">Reason: Failed to determine a suitable driver class</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Action:</span><br><span class="line"></span><br><span class="line">Consider the following:</span><br><span class="line">	If you want an embedded database (H2, HSQL or Derby), please put it on the classpath.</span><br><span class="line">	If you have database settings to be loaded from a particular profile you may need to activate it (no profiles are currently active).</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Process finished with exit code 1</span><br></pre></td></tr></table></figure>
<h2 id="尝试解决"><a href="#尝试解决" class="headerlink" title="尝试解决"></a>尝试解决</h2><p>首先使出一招百度一下，搜到了很多的同类报错解决方法。以下四种方法出自<a href="https://www.jianshu.com/p/836d455663da" target="_blank" rel="noopener">https://www.jianshu.com/p/836d455663da</a></p>
<h3 id="方案一："><a href="#方案一：" class="headerlink" title="方案一："></a>方案一：</h3><p>排除此类的autoconfig。启动以后就可以正常运行。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@SpringBootApplication(exclude= &#123;DataSourceAutoConfiguration.class&#125;)</span><br></pre></td></tr></table></figure></p>
<h3 id="方案二："><a href="#方案二：" class="headerlink" title="方案二："></a>方案二：</h3><p>在application.properties/或者application.yml文件中没有添加数据库配置信息.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">spring:</span><br><span class="line">  datasource:</span><br><span class="line">    url: jdbc:mysql://localhost:3306/read_data?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false</span><br><span class="line">    username: root</span><br><span class="line">    password: 123456</span><br><span class="line">    driver-class-name: com.mysql.jdbc.Driver</span><br></pre></td></tr></table></figure>
<h3 id="方案三："><a href="#方案三：" class="headerlink" title="方案三："></a>方案三：</h3><p>在spring xml配置文件中引用了数据库地址 所以需要对:等进行转义处理.但是在application.properties/或者application.yml文件并不需要转义,错误和正确方法写在下面了.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//错误示例</span><br><span class="line">spring.datasource.url = jdbc:mysql\://192.168.0.20\:1504/f_me?setUnicode=true&amp;characterEncoding=utf8</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//正确示例</span><br><span class="line">spring.datasource.url = jdbc:mysql://192.168.0.20:1504/f_me?setUnicode=true&amp;characterEncoding=utf8</span><br></pre></td></tr></table></figure>
<h3 id="方案四："><a href="#方案四：" class="headerlink" title="方案四："></a>方案四：</h3><p>yml或者properties文件没有被扫描到,需要在pom文件中<build></build>添加如下.来保证文件都能正常被扫描到并且加载成功.<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!-- 如果不添加此节点mybatis的mapper.xml文件都会被漏掉。 --&gt;</span><br><span class="line">&lt;resources&gt;</span><br><span class="line">    &lt;resource&gt;</span><br><span class="line">        &lt;directory&gt;src/main/java&lt;/directory&gt;</span><br><span class="line">        &lt;includes&gt;</span><br><span class="line">            &lt;include&gt;**/*.yml&lt;/include&gt;</span><br><span class="line">            &lt;include&gt;**/*.properties&lt;/include&gt;</span><br><span class="line">            &lt;include&gt;**/*.xml&lt;/include&gt;</span><br><span class="line">        &lt;/includes&gt;</span><br><span class="line">        &lt;filtering&gt;false&lt;/filtering&gt;</span><br><span class="line">    &lt;/resource&gt;</span><br><span class="line">    &lt;resource&gt;</span><br><span class="line">        &lt;directory&gt;src/main/resources&lt;/directory&gt;</span><br><span class="line">        &lt;includes&gt;</span><br><span class="line">            &lt;include&gt;**/*.yml&lt;/include&gt;</span><br><span class="line">            &lt;include&gt;**/*.properties&lt;/include&gt;</span><br><span class="line">            &lt;include&gt;**/*.xml&lt;/include&gt;</span><br><span class="line">        &lt;/includes&gt;</span><br><span class="line">        &lt;filtering&gt;false&lt;/filtering&gt;</span><br><span class="line">    &lt;/resource&gt;</span><br><span class="line">&lt;/resources&gt;</span><br></pre></td></tr></table></figure></p>
<h3 id="方案五（最终解决版）："><a href="#方案五（最终解决版）：" class="headerlink" title="方案五（最终解决版）："></a>方案五（最终解决版）：</h3><p>问题是出在了新导入jar包时，出现报错。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">repositories &#123;</span><br><span class="line">    maven &#123; url &apos;http://download.osgeo.org/webdav/geotools/&apos; &#125;</span><br><span class="line">    maven &#123; url &apos;http://maven.aliyun.com/nexus/content/groups/public/&apos; &#125;</span><br><span class="line">    maven &#123; url &apos;https://repo.spring.io/libs-snapshot&apos; &#125;</span><br><span class="line">    maven &#123; url &apos;https://repo.spring.io/milestone&apos; &#125;</span><br><span class="line">    mavenLocal()</span><br><span class="line">    mavenCentral()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/07/19/一个普通的日常bug/b2.png" alt=""></p>
<p>此时的setting界面为：<br><img src="/2019/07/19/一个普通的日常bug/b3.png" alt=""></p>
<p>经过查询，找到下图的这个选项javac。将其修改为eclipse后，便解决了这个错误。<br><img src="/2019/07/19/一个普通的日常bug/b1.png" alt=""></p>
<h3 id="方案六（问题再次诞生过程）："><a href="#方案六（问题再次诞生过程）：" class="headerlink" title="方案六（问题再次诞生过程）："></a>方案六（问题再次诞生过程）：</h3><p>项目在导入jar包前可以正常运行。导入jar包为：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">repositories &#123;</span><br><span class="line">    maven &#123; url &apos;http://download.osgeo.org/webdav/geotools/&apos; &#125;</span><br><span class="line">    maven &#123; url &apos;http://maven.aliyun.com/nexus/content/groups/public/&apos; &#125;</span><br><span class="line">    maven &#123; url &apos;https://repo.spring.io/libs-snapshot&apos; &#125;</span><br><span class="line">    maven &#123; url &apos;https://repo.spring.io/milestone&apos; &#125;</span><br><span class="line">    mavenLocal()</span><br><span class="line">    mavenCentral()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-cache&apos;)</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-data-jpa&apos;)</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-jdbc&apos;)</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-data-redis&apos;)</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-security&apos;)</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-validation&apos;)</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-web&apos;)</span><br><span class="line">    compile(&apos;org.springframework.boot:spring-boot-starter-websocket&apos;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    runtime(&apos;mysql:mysql-connector-java&apos;)</span><br><span class="line">    compileOnly(&apos;org.projectlombok:lombok&apos;)</span><br><span class="line">    compileOnly(&apos;org.springframework.boot:spring-boot-configuration-processor&apos;)</span><br><span class="line">    asciidoctor(&apos;org.springframework.restdocs:spring-restdocs-asciidoctor&apos;)</span><br><span class="line">    testCompile(&apos;org.springframework.restdocs:spring-restdocs-mockmvc&apos;)</span><br><span class="line">    testCompile(&apos;org.springframework.security:spring-security-test&apos;)</span><br><span class="line">    testCompile(&apos;org.springframework.boot:spring-boot-starter-test&apos;) &#123;</span><br><span class="line">        exclude group: &apos;junit&apos;, module: &apos;junit;&apos;</span><br><span class="line">    &#125;</span><br><span class="line">    testCompile(&apos;org.testng:testng:6.14.3&apos;)</span><br><span class="line"></span><br><span class="line">    compile(&apos;com.alibaba:druid-spring-boot-starter:1.1.10&apos;)</span><br><span class="line">    compile(&apos;org.apache.httpcomponents:httpclient:4.5.6&apos;)</span><br><span class="line">    compile(&apos;org.apache.httpcomponents:httpmime:4.5.6&apos;)</span><br><span class="line">    compile(&apos;com.google.guava:guava:26.0-jre&apos;)</span><br><span class="line">    compile(&apos;commons-net:commons-net:3.6&apos;)</span><br><span class="line">    compile(&apos;com.alibaba:fastjson:1.2.49&apos;)</span><br><span class="line">    compile(&apos;io.jsonwebtoken:jjwt:0.9.1&apos;)</span><br><span class="line">    compile(&apos;com.aliyun:aliyun-java-sdk-core:4.0.8&apos;)</span><br><span class="line">    compile(&apos;com.aliyun:aliyun-java-sdk-dysmsapi:1.1.0&apos;)</span><br><span class="line">    compile(&apos;jcifs:jcifs:1.3.17&apos;) &#123;</span><br><span class="line">        exclude module: &apos;servlet-api&apos;</span><br><span class="line">    &#125;</span><br><span class="line">    compile fileTree(dir: &apos;lib&apos;, includes: [&apos;*jar&apos;])</span><br><span class="line"></span><br><span class="line">    compile (&apos;commons-fileupload:commons-fileupload:1.3.1&apos;)</span><br><span class="line"></span><br><span class="line">    compile (&apos;org.apache.commons:commons-pool2:2.6.0&apos;)</span><br><span class="line"></span><br><span class="line">	//新导入jar包开始</span><br><span class="line">    implementation group: &apos;org.geotools&apos;, name: &apos;gt-shapefile&apos;, version: &apos;21.0&apos;</span><br><span class="line">    implementation group: &apos;org.geotools&apos;, name: &apos;gt-geojson&apos;, version: &apos;21.0&apos;</span><br><span class="line">    implementation group: &apos;org.meteothink&apos;, name: &apos;wContour&apos;, version: &apos;1.6.1&apos;</span><br><span class="line">    implementation group: &apos;com.vividsolutions&apos;, name: &apos;jts&apos;, version: &apos;1.13&apos;</span><br><span class="line">    implementation group: &apos;org.locationtech.jts&apos;, name: &apos;jts-core&apos;, version: &apos;1.16.1&apos;</span><br><span class="line">    //新导入jar包结束</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>初步怀疑是新jar包和之前的jar包冲突，或者是新导入jar包中有错误。方案五的解决办法在这种操作场景下并不能解决问题了。</p>
<p>最终删除后重新git导入了整个项目，删除了gradle全局配置。重新导入一遍这个jar包，选择了一下idea-&gt;setting-&gt;Build,Execution,Deployment-&gt;complier-&gt;java Compiler中的use compiler为javac（在导入时注意提示。）</p>
<h2 id="解决bug途中产生的新bug"><a href="#解决bug途中产生的新bug" class="headerlink" title="解决bug途中产生的新bug"></a>解决bug途中产生的新bug</h2><p><strong>关键字：</strong>错误: 找不到或无法加载主类 com.yiring.zjtq.Application<br>当edit configurations显示下面的错误时，可以重新rebuild一下项目。或者更改一下jar包管理文件，删除jar包再导入。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Run Configuration Error: Not a valid Spring Boot application class</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/07/19/一个普通的日常bug/b4.png" alt=""></p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>一次geotools jar包内报错的解决方式</title>
    <url>/2019/08/02/%E4%B8%80%E6%AC%A1geotools%20jar%E5%8C%85%E5%86%85%E6%8A%A5%E9%94%99%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="一次geotools-jar包内报错的解决方式"><a href="#一次geotools-jar包内报错的解决方式" class="headerlink" title="一次geotools jar包内报错的解决方式"></a>一次geotools jar包内报错的解决方式</h1><blockquote>
<p>在此记录一下geotools这个jar包在使用中碰到的问题。目前仍旧没有解决，只是解决了一部分。</p>
</blockquote>
<a id="more"></a>
<h2 id="最根本需求："><a href="#最根本需求：" class="headerlink" title="最根本需求："></a>最根本需求：</h2><p>目前碰到的需求是需要将矩形地图按照省级边界线将多余的地方裁剪。一切的问题都是出在解决这个需求的路上发生的。</p>
<h3 id="第一个bug"><a href="#第一个bug" class="headerlink" title="第一个bug"></a>第一个bug</h3><p><strong>关键字：</strong> Points of LinearRing do not form a closed linestring。</p>
<p>问题报错出现在了jar包内的org.geotools.geojson.geom.PolygonHandler这个类的第54行到65行<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">try &#123;</span><br><span class="line">                LinearRing outer = factory.createLinearRing(rings.get(0));</span><br><span class="line">                LinearRing[] inner = null;</span><br><span class="line">                if (rings.size() &gt; 1) &#123;</span><br><span class="line">                    inner = new LinearRing[rings.size() - 1];</span><br><span class="line">                    for (int i = 1; i &lt; rings.size(); i++) &#123;</span><br><span class="line">                        inner[i - 1] = factory.createLinearRing(rings.get(i));</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                value = factory.createPolygon(outer, inner);</span><br><span class="line">                rings = null;</span><br><span class="line">            &#125; catch (IllegalArgumentException e) &#123;</span><br><span class="line">                return true;</span><br><span class="line">            &#125;</span><br></pre></td></tr></table></figure></p>
<p><strong>处理方式：</strong>暂时只能捕获异常后返回正常的值。在java目录下新建一个和本类相同的类和jar包路径，重新rebuild项目。即可让jar包在调用时使用自己修改过的这个类<br><img src="/2019/08/02/一次geotools jar包内报错的解决方式/b1.png" alt=""></p>
<h3 id="第二个bug"><a href="#第二个bug" class="headerlink" title="第二个bug"></a>第二个bug</h3><p>使用格点文件直接进行裁剪和叠加色斑图时，会产生一些未裁剪成功的图，以及和未裁剪的色斑图的拼图面不一致的情况。目前的处理方式是将格点离散成站点格式，再重新插值为格点。希望这种方式可以处理裁剪后色斑拼图丢失的问题。</p>
<p><strong>2019年09月04日更新</strong><br>没能想到别的办法来处理这个裁剪的问题，而且上述的离散再插值的方式，经过长时间的采集数据发现，仍会有丢失色斑的情况出现。只能放弃从后端来处理地图边界问题，转而从前端用空白背景来掩盖未被裁剪掉的省外部分。</p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>从StringBuffer和StringBuilder扩展开的问题</title>
    <url>/2020/03/14/%E4%BB%8EStringBuffer%E5%92%8CStringBuilder%E6%89%A9%E5%B1%95%E5%BC%80%E7%9A%84%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="从StringBuffer和StringBuilder扩展开的问题"><a href="#从StringBuffer和StringBuilder扩展开的问题" class="headerlink" title="从StringBuffer和StringBuilder扩展开的问题"></a>从StringBuffer和StringBuilder扩展开的问题</h1><blockquote>
<p>原文来自<a href="https://www.hollischuang.com/archives/3912" target="_blank" rel="noopener">https://www.hollischuang.com/archives/3912</a> 。这里里面本来是有很多从一个问题开始详细扩展讲解的文章，但是当我转载的时候发现已经404了，很是可惜。</p>
</blockquote>
<a id="more"></a>
<blockquote>
<p>看了很多公众号的文章，发现有些喜欢起一些比较高大上的名字。比如这个原文的名字就是《国内Java面试总是问StringBuffer，StringBuilder区别是啥？档次为什么这么低？》，类似的名字还有《又一次过亿级架构事故发生了！》《高并发、微服务为什么一定要用 Docker 和 K8S ?》<br>这种名字有些类似于UC小编的震惊体，而且后面两个点进去之后是推销课程的广告。其实作为一个技术类型的文章，直接了当的把内容总结在标题上还是更加的易于阅读和寻找。</p>
</blockquote>
<h2 id="全部问题"><a href="#全部问题" class="headerlink" title="全部问题"></a>全部问题</h2><p>标题上的问题是一个开场基础，可扩展为：</p>
<ol>
<li><p>StringBuffer，StringBuilder区别是啥？ </p>
</li>
<li><p>什么是线程安全？</p>
</li>
<li><p>如何保证线程安全？</p>
</li>
<li><p>什么是锁？死锁？</p>
</li>
<li><p>synchronized的实现原理是什么？</p>
</li>
<li><p>有了synchronized，还要volatile干什么？</p>
</li>
<li><p>synchronized的锁优化是怎么回事？（锁粗化？锁消除？自旋锁？偏向锁？轻量级锁？）</p>
</li>
<li><p>知道JMM吗？（原子性？可见性？有序性？）</p>
</li>
<li><p>Java并发包了解吗？</p>
</li>
<li><p>那什么是fail-fast？什么是fail-safe？</p>
</li>
<li><p>什么是CopyOnWrite？</p>
</li>
<li><p>那AQS呢？那CAS呢？</p>
</li>
<li><p>CAS都知道，那乐观锁一定知道了？</p>
</li>
<li><p>乐观锁悲观锁区别是什么？</p>
</li>
<li><p>数据库如何实现悲观锁和乐观锁？</p>
</li>
<li><p>数据库锁有了解么？行级锁？表级锁？共享锁？排他锁？gap锁？next-key lock？</p>
</li>
<li><p>数据库锁和隔离级别有什么关系？</p>
</li>
<li><p>数据库锁和索引有什么关系？</p>
</li>
<li><p>什么是聚簇索引？非聚簇索引？最左前缀是什么？B+树索引？联合索引？回表？</p>
</li>
<li><p>分布式锁有了解吗？</p>
</li>
<li><p>Redis怎么实现分布式锁？</p>
</li>
<li><p>为什么要用Redis？</p>
</li>
<li><p>Redis和memcache区别是什么？</p>
</li>
<li><p>Zookeeper怎么实现分布式锁？</p>
</li>
<li><p>什么是Zookeeper？</p>
</li>
<li><p>什么是CAP？</p>
</li>
<li><p>什么是BASE？和CAP什么区别？</p>
</li>
<li><p>CAP怎么推导？如何取舍？</p>
</li>
<li><p>分布式系统怎么保证数据一致性？</p>
</li>
<li><p>啥是分布式事务？分布式事务方案？</p>
</li>
<li><p>那么，最后了，来手写一个线程安全的单例吧？</p>
</li>
<li><p>不用synchronized和lock能实现线程安全的单例吗？</p>
</li>
<li><p>这你都能答上？那好吧，你给我解释下什么是Paxos算法吧？</p>
</li>
</ol>
<h2 id="部分问题答案"><a href="#部分问题答案" class="headerlink" title="部分问题答案"></a>部分问题答案</h2><p>因为篇幅的缘故，原文的作者只是写了关键点，但是详细的文章大多数都404了。只好我以后再按照这个路线再找一找同类文章。</p>
<h4 id="StringBuffer，StringBuilder区别是啥？"><a href="#StringBuffer，StringBuilder区别是啥？" class="headerlink" title="StringBuffer，StringBuilder区别是啥？"></a>StringBuffer，StringBuilder区别是啥？</h4><p>StringBuffer是线程安全的，而StringBuilder是非线程安全的。</p>
<h4 id="什么是线程安全？"><a href="#什么是线程安全？" class="headerlink" title="什么是线程安全？"></a>什么是线程安全？</h4><p>线程安全是编程中的术语，指某个函数、函数库在并发环境中被调用时，能够正确地处理多个线程之间的共享变量，使程序功能正确完成。即在多线程场景下，不发生有序性、原子性以及可见性问题。</p>
<h4 id="如何保证线程安全？"><a href="#如何保证线程安全？" class="headerlink" title="如何保证线程安全？"></a>如何保证线程安全？</h4><p>Java中主要通过加锁来实现线程安全。通常使用synchronized和Lock</p>
<h4 id="什么是锁？死锁？"><a href="#什么是锁？死锁？" class="headerlink" title="什么是锁？死锁？"></a>什么是锁？死锁？</h4><p>死锁是指两个或两个以上的进程在执行过程中，由于竞争资源或者由于彼此通信而造成的一种阻塞的现象，若无外力作用，它们都将无法推进下去。此时称系统处于死锁状态或系统产生了死锁，这些永远在互相等待的进程称为死锁进程。</p>
<p>死锁的发生必须具备以下四个必要条件：互斥条件、请求和保持条件、不剥夺条件、环路等待条件</p>
<p>死锁的解决办法就是破坏以上四种必备条件中的一个或者多个。</p>
<h4 id="synchronized的实现原理是什么？"><a href="#synchronized的实现原理是什么？" class="headerlink" title="synchronized的实现原理是什么？"></a>synchronized的实现原理是什么？</h4><p><a href="http://www.hollischuang.com/archives/1883" target="_blank" rel="noopener">http://www.hollischuang.com/archives/1883</a> 这篇文章是原文作者的</p>
<p> 主要说的是： 方法级的同步是隐式的。同步方法的常量池中会有一个<code>ACC_SYNCHRONIZED</code>标志。当某个线程要访问某个方法的时候，会检查是否有<code>ACC_SYNCHRONIZED</code>，如果有设置，则需要先获得监视器锁，然后开始执行方法，方法执行之后再释放监视器锁。这时如果其他线程来请求执行方法，会因为无法获得监视器锁而被阻断住。值得注意的是，如果在方法执行过程中，发生了异常，并且方法内部并没有处理该异常，那么在异常被抛到方法外面之前监视器锁会被自动释放。 </p>
<p> 同步代码块使用<code>monitorenter</code>和<code>monitorexit</code>两个指令实现。 总结为： 可以把执行monitorenter指令理解为加锁，执行monitorexit理解为释放锁。 每个对象维护着一个记录着被锁次数的计数器。未被锁定的对象的该计数器为0，当一个线程获得锁（执行monitorenter）后，该计数器自增变为 1 ，当同一个线程再次获得该对象的锁的时候，计数器再次自增。当同一个线程释放锁（执行monitorexit指令）的时候，计数器再自减。当计数器为0的时候。锁将被释放，其他线程便可以获得锁。<br>同步方法通过ACC_SYNCHRONIZED关键字隐式的对方法进行加锁。当线程要执行的方法被标注上ACC_SYNCHRONIZED时，需要先获得锁才能执行该方法。</p>
<p>同步代码块通过monitorenter和monitorexit执行来进行加锁。当线程执行到monitorenter的时候要先获得所锁，才能执行后面的方法。当线程执行到monitorexit的时候则要释放锁。</p>
<p>每个对象自身维护这一个被加锁次数的计数器，当计数器数字为0时表示可以被任意线程获得锁。当计数器不为0时，只有获得锁的线程才能再次获得锁。即可重入锁。</p>
<h5 id="扩展出的Monitor介绍"><a href="#扩展出的Monitor介绍" class="headerlink" title="扩展出的Monitor介绍"></a>扩展出的Monitor介绍</h5><p><strong>管程</strong> (英语：Monitors，也称为监视器) 是一种程序结构，结构内的多个子程序（对象或模块）形成的多个工作线程互斥访问共享资源。这些共享资源一般是硬件设备或一群变量。管程实现了在一个时间点，最多只有一个线程在执行管程的某个子程序。与那些通过修改数据结构实现互斥访问的并发程序设计相比，管程实现很大程度上简化了程序设计。 管程提供了一种机制，线程可以临时放弃互斥访问，等待某些条件得到满足后，重新获得执行权恢复它的互斥访问。</p>
<p>在多线程访问共享资源的时候，经常会带来可见性和原子性的安全问题。为了解决这类线程安全的问题，Java提供了同步机制、互斥锁机制，这个机制保证了在同一时刻只有一个线程能访问共享资源。这个机制的保障来源于监视锁Monitor，每个对象都拥有自己的监视锁Monitor。</p>
<p>先来举个例子，然后我们在上源码。我们可以把监视器理解为包含一个特殊的房间的建筑物，这个特殊房间同一时刻只能有一个客人（线程）。这个房间中包含了一些数据和代码。<br><img src="/2020/03/14/从StringBuffer和StringBuilder扩展开的问题/1" alt="b1.png"></p>
<p>如果一个顾客想要进入这个特殊的房间，他首先需要在走廊（Entry Set）排队等待。调度器将基于某个标准（比如 FIFO）来选择排队的客户进入房间。如果，因为某些原因，该客户客户暂时因为其他事情无法脱身（线程被挂起），那么他将被送到另外一间专门用来等待的房间（Wait Set），这个房间的可以可以在稍后再次进入那件特殊的房间。如上面所说，这个建筑屋中一共有三个场所。<br><img src="/2020/03/14/从StringBuffer和StringBuilder扩展开的问题/2" alt="b2.png"></p>
<p>总之，监视器是一个用来监视这些线程进入特殊的房间的。他的义务是保证（同一时间）只有一个线程可以访问被保护的数据和代码。</p>
<p>Monitor其实是一种同步工具，也可以说是一种同步机制，它通常被描述为一个对象，主要特点是：</p>
<blockquote>
<p>对象的所有方法都被“互斥”的执行。好比一个Monitor只有一个运行“许可”，任一个线程进入任何一个方法都需要获得这个“许可”，离开时把许可归还。<br>通常提供singal机制：允许正持有“许可”的线程暂时放弃“许可”，等待某个谓词成真（条件变量），而条件成立后，当前进程可以“通知”正在等待这个条件变量的线程，让他可以重新去获得运行许可。</p>
</blockquote>
<h4 id="有了synchronized，还要volatile干什么？"><a href="#有了synchronized，还要volatile干什么？" class="headerlink" title="有了synchronized，还要volatile干什么？"></a>有了synchronized，还要volatile干什么？</h4><p>volatile通常被比喻成”轻量级的synchronized“，volatile可以保证可见性和有序性，实现原理是通过内存屏障实现的。</p>
<p>volatile有一个重要的作用，是synchronized不具备的，那就是禁止指令重排序。这一特点在双重校验锁实现单例的时候有用到，虽然使用了synchronized关键字，但是如果不用volatile修饰单例对象，就会存在问题。</p>
<h4 id="synchronized的锁优化是怎么回事？（锁粗化？锁消除？自旋锁？偏向锁？轻量级锁？）"><a href="#synchronized的锁优化是怎么回事？（锁粗化？锁消除？自旋锁？偏向锁？轻量级锁？）" class="headerlink" title="synchronized的锁优化是怎么回事？（锁粗化？锁消除？自旋锁？偏向锁？轻量级锁？）"></a>synchronized的锁优化是怎么回事？（锁粗化？锁消除？自旋锁？偏向锁？轻量级锁？）</h4><p>取自原文作者<a href="http://www.hollischuang.com/archives/2344" target="_blank" rel="noopener">http://www.hollischuang.com/archives/2344</a> 。<br>这个内容过多分成了第二个文章，原文总结：</p>
<p>自Java 6/Java 7开始，Java虚拟机对内部锁的实现进行了一些优化。这些优化主要包括锁消除（Lock Elision）、锁粗化（Lock Coarsening）、偏向锁（Biased Locking）以及适应性自旋锁（Adaptive Locking）。这些优化仅在Java虚拟机server模式下起作用（即运行Java程序时我们可能需要在命令行中指定Java虚拟机参数“-server”以开启这些优化）。</p>
<p>本文主要介绍了自旋锁、锁粗化和锁消除的概念。在JIT编译过程中，虚拟机会根据情况使用这三种技术对锁进行优化，目的是减少锁的竞争，提升性能。</p>
<h4 id="知道JMM吗？（原子性？可见性？有序性？）"><a href="#知道JMM吗？（原子性？可见性？有序性？）" class="headerlink" title="知道JMM吗？（原子性？可见性？有序性？）"></a>知道JMM吗？（原子性？可见性？有序性？）</h4><p>Java内存模型（Java Memory Model ,JMM）是一种符合内存模型规范的，屏蔽了各种硬件和操作系统的访问差异的，保证了Java程序在各种平台下对内存的访问都能保证效果一致的机制及规范。<br>原文作者还有一篇详细说明的文章，<a href="http://www.hollischuang.com/archives/2550" target="_blank" rel="noopener">http://www.hollischuang.com/archives/2550</a></p>
<h4 id="Java并发包了解吗？"><a href="#Java并发包了解吗？" class="headerlink" title="Java并发包了解吗？"></a>Java并发包了解吗？</h4><p>java.util.concurrent包(J.U.C)中包含的是java并发编程中有用的一些工具类，包括几个部分：</p>
<p>1、locks部分：包含在java.util.concurrent.locks包中，提供显式锁(互斥锁和速写锁)相关功能；</p>
<p>2、atomic部分：包含在java.util.concurrent.atomic包中，提供原子变量类相关的功能，是构建非阻塞算法的基础；</p>
<p>3、executor部分：散落在java.util.concurrent包中，提供线程池相关的功能；</p>
<p>4、collections部分：散落在java.util.concurrent包中，提供并发容器相关功能；</p>
<p>5、tools部分：散落在java.util.concurrent包中，提供同步工具类，如信号量、闭锁、栅栏等功能；</p>
<h4 id="那什么是fail-fast？什么是fail-safe？"><a href="#那什么是fail-fast？什么是fail-safe？" class="headerlink" title="那什么是fail-fast？什么是fail-safe？"></a>那什么是fail-fast？什么是fail-safe？</h4><p>我们通常说的Java中的fail-fast机制，默认指的是Java集合的一种错误检测机制。说白了就是在做系统设计的时候先考虑异常情况，一旦发生异常，直接停止并上报。当多个线程对部分集合进行结构上的改变的操作时，有可能会产生fail-fast机制，这个时候就会抛出ConcurrentModificationException。</p>
<p>ConcurrentModificationException，当方法检测到对象的并发修改，但不允许这种修改时就抛出该异常。</p>
<p>为了避免触发fail-fast机制，导致异常，我们可以使用Java中提供的一些采用了fail-safe机制的集合类。</p>
<p>这样的集合容器在遍历时不是直接在集合内容上访问的，而是先复制原有集合内容，在拷贝的集合上进行遍历。</p>
<p>java.util.concurrent包下的容器都是fail-safe的，可以在多线程下并发使用，并发修改。同时也可以在foreach中进行add/remove 。</p>
<h4 id="什么是CopyOnWrite？"><a href="#什么是CopyOnWrite？" class="headerlink" title="什么是CopyOnWrite？"></a>什么是CopyOnWrite？</h4><p>Copy-On-Write简称COW，是一种用于程序设计中的优化策略。其基本思路是，从一开始大家都在共享同一个内容，当某个人想要修改这个内容的时候，才会真正把内容Copy出去形成一个新的内容然后再改，这是一种延时懒惰策略。</p>
<p>CopyOnWrite容器即写时复制的容器。通俗的理解是当我们往一个容器添加元素的时候，不直接往当前容器添加，而是先将当前容器进行Copy，复制出一个新的容器，然后新的容器里添加元素，添加完元素之后，再将原容器的引用指向新的容器。</p>
<h4 id="那AQS呢？那CAS呢？"><a href="#那AQS呢？那CAS呢？" class="headerlink" title="那AQS呢？那CAS呢？"></a>那AQS呢？那CAS呢？</h4><p>AQS(AbstractQueuedSynchronizer)，即队列同步器。它是构建锁或者其他同步组件的基础框架（如ReentrantLock、ReentrantReadWriteLock、Semaphore等），JUC并发包的作者（Doug Lea）期望它能够成为实现大部分同步需求的基础。它是JUC并发包中的核心基础组件。</p>
<p>CAS是项乐观锁技术，当多个线程尝试使用CAS同时更新同一个变量时，只有其中一个线程能更新变量的值，而其它线程都失败，失败的线程并不会被挂起，而是被告知这次竞争中失败，并可以再次尝试。</p>
<p>CAS 操作包含三个操作数 —— 内存位置（V）、预期原值（A）和新值(B)。如果内存位置的值与预期原值相匹配，那么处理器会自动将该位置值更新为新值。否则，处理器不做任何操作。无论哪种情况，它都会在 CAS 指令之前返回该位置的值。（在 CAS 的一些特殊情况下将仅返回 CAS 是否成功，而不提取当前值。）CAS 有效地说明了“我认为位置 V 应该包含值 A；如果包含该值，则将 B 放到这个位置；否则，不要更改该位置，只告诉我这个位置现在的值即可。”这其实和乐观锁的冲突检查+数据更新的原理是一样的。</p>
<p>####　CAS都知道，那乐观锁一定知道了？<br>乐观锁（ Optimistic Locking ） 相对悲观锁而言，乐观锁假设认为数据一般情况下不会造成冲突，所以在数据进行提交更新的时候，才会正式对数据的冲突与否进行检测，如果发现冲突了，则让返回用户错误的信息，让用户决定如何去做。</p>
<p>相对于悲观锁，在对数据库进行处理的时候，乐观锁并不会使用数据库提供的锁机制。一般的实现乐观锁的方式就是记录数据版本。</p>
<p>实现数据版本有两种方式，第一种是使用版本号，第二种是使用时间戳。</p>
<h4 id="乐观锁悲观锁区别是什么？"><a href="#乐观锁悲观锁区别是什么？" class="headerlink" title="乐观锁悲观锁区别是什么？"></a>乐观锁悲观锁区别是什么？</h4><p>原文作者<a href="http://www.hollischuang.com/archives/934" target="_blank" rel="noopener">http://www.hollischuang.com/archives/934</a> 。 同下</p>
<p><strong>悲观锁：</strong><br>当我们要对一个数据库中的一条数据进行修改的时候，为了避免同时被其他人修改，最好的办法就是直接对该数据进行加锁以防止并发。</p>
<p>这种借助数据库锁机制在修改数据之前先锁定，再修改的方式被称之为悲观并发控制（又名“悲观锁”，Pessimistic Concurrency Control，缩写“PCC”）。</p>
<blockquote>
<p>在关系数据库管理系统里，悲观并发控制（又名“悲观锁”，Pessimistic Concurrency Control，缩写“PCC”）是一种并发控制的方法。它可以阻止一个事务以影响其他用户的方式来修改数据。如果一个事务执行的操作都某行数据应用了锁，那只有当这个事务把锁释放，其他事务才能够执行与该锁冲突的操作。<br>悲观并发控制主要用于数据争用激烈的环境，以及发生并发冲突时使用锁保护数据的成本要低于回滚事务的成本的环境中。</p>
</blockquote>
<p><strong>乐观锁：</strong></p>
<blockquote>
<p>在关系数据库管理系统里，乐观并发控制（又名“乐观锁”，Optimistic Concurrency Control，缩写“OCC”）是一种并发控制的方法。它假设多用户并发的事务在处理时不会彼此互相影响，各事务能够在不产生锁的情况下处理各自影响的那部分数据。在提交数据更新之前，每个事务会先检查在该事务读取数据后，有没有其他事务又修改了该数据。如果其他事务有更新的话，正在提交的事务会进行回滚。乐观事务控制最早是由孔祥重（H.T.Kung）教授提出。</p>
</blockquote>
<p>乐观锁（ Optimistic Locking ） 相对悲观锁而言，乐观锁假设认为数据一般情况下不会造成冲突，所以在数据进行提交更新的时候，才会正式对数据的冲突与否进行检测，如果发现冲突了，则让返回用户错误的信息，让用户决定如何去做。</p>
<p>相对于悲观锁，在对数据库进行处理的时候，乐观锁并不会使用数据库提供的锁机制。一般的实现乐观锁的方式就是记录数据版本。</p>
<p><strong>总结：</strong><br>实现数据版本有两种方式，第一种是使用版本号，第二种是使用时间戳。</p>
<p>在乐观锁与悲观锁的选择上面，主要看下两者的区别以及适用场景就可以了。</p>
<p>1、乐观锁并未真正加锁，效率高。一旦锁的粒度掌握不好，更新失败的概率就会比较高，容易发生业务失败。</p>
<p>2、悲观锁依赖数据库锁，效率低。更新失败的概率比较低。</p>
<p>随着互联网三高架构（高并发、高性能、高可用）的提出，悲观锁已经越来越少的被使用到生产环境中了，尤其是并发量比较大的业务场景。</p>
<h4 id="数据库如何实现悲观锁和乐观锁？"><a href="#数据库如何实现悲观锁和乐观锁？" class="headerlink" title="数据库如何实现悲观锁和乐观锁？"></a>数据库如何实现悲观锁和乐观锁？</h4><p>原文作者<a href="http://www.hollischuang.com/archives/934" target="_blank" rel="noopener">http://www.hollischuang.com/archives/934</a> 。</p>
<h4 id="数据库锁有了解么？行级锁？表级锁？共享锁？排他锁？gap锁？next-key-lock？"><a href="#数据库锁有了解么？行级锁？表级锁？共享锁？排他锁？gap锁？next-key-lock？" class="headerlink" title="数据库锁有了解么？行级锁？表级锁？共享锁？排他锁？gap锁？next-key lock？"></a>数据库锁有了解么？行级锁？表级锁？共享锁？排他锁？gap锁？next-key lock？</h4><p>原文作者：<br><a href="http://www.hollischuang.com/archives/914" target="_blank" rel="noopener">MySQL中的行级锁,表级锁,页级锁</a><br><a href="http://www.hollischuang.com/archives/923" target="_blank" rel="noopener">MySQL中的共享锁与排他锁</a></p>
<h4 id="数据库锁和隔离级别有什么关系？"><a href="#数据库锁和隔离级别有什么关系？" class="headerlink" title="数据库锁和隔离级别有什么关系？"></a>数据库锁和隔离级别有什么关系？</h4><p>很多DBMS定义了多个不同的“事务隔离等级”来控制锁的程度和并发能力。</p>
<p>ANSI/ISO SQL定义的标准隔离级别有四种，从高到底依次为：可序列化(Serializable)、可重复读(Repeatable reads)、提交读(Read committed)、未提交读(Read uncommitted)。</p>
<h4 id="数据库锁和索引有什么关系？"><a href="#数据库锁和索引有什么关系？" class="headerlink" title="数据库锁和索引有什么关系？"></a>数据库锁和索引有什么关系？</h4><p>在MySQL中，行级锁并不是直接锁记录，而是锁索引。索引分为主键索引和非主键索引两种，如果一条sql语句操作了主键索引，MySQL就会锁定这条主键索引；如果一条语句操作了非主键索引，MySQL会先锁定该非主键索引，再锁定相关的主键索引。</p>
<h4 id="什么是聚簇索引？非聚簇索引？最左前缀是什么？B-树索引？联合索引？回表？"><a href="#什么是聚簇索引？非聚簇索引？最左前缀是什么？B-树索引？联合索引？回表？" class="headerlink" title="什么是聚簇索引？非聚簇索引？最左前缀是什么？B+树索引？联合索引？回表？"></a>什么是聚簇索引？非聚簇索引？最左前缀是什么？B+树索引？联合索引？回表？</h4><p>主键索引的叶子节点存的是整行数据。在InnoDB中，主键索引也被称为聚簇索引（clustered index）</p>
<p>非主键索引的叶子节点的内容是主键的值，在InnoDB中，非主键索引也被称为非聚簇索引（secondary index）</p>
<p>当我们创建一个联合索引的时候，如(key1,key2,key3)，相当于创建了（key1）、(key1,key2)和(key1,key2,key3)三个索引，这就是最左匹配原则。</p>
<p>在 InnoDB 里，索引B+ Tree的叶子节点存储了整行数据的是主键索引。而索引B+ Tree的叶子节点存储了主键的值的是非主键索引。因为主键索引树的叶子节点直接就是我们要查询的整行数据了。而非主键索引的叶子节点是主键的值，查到主键的值以后，还需要再通过主键的值再进行一次查询，这个过程叫做回表。</p>
<h4 id="分布式锁有了解吗？"><a href="#分布式锁有了解吗？" class="headerlink" title="分布式锁有了解吗？"></a>分布式锁有了解吗？</h4><p>目前比较常用的有以下几种方案：</p>
<p>基于数据库实现分布式锁 基于缓存（redis，memcached，tair）实现分布式锁 基于Zookeeper实现分布式锁</p>
<h4 id="Redis怎么实现分布式锁？"><a href="#Redis怎么实现分布式锁？" class="headerlink" title="Redis怎么实现分布式锁？"></a>Redis怎么实现分布式锁？</h4><p>多个进程执行以下Redis命令：</p>
<p>SETNX lock.foo</p>
<p>如果 SETNX 返回1，说明该进程获得锁，SETNX将键 lock.foo 的值设置为锁的超时时间（当前时间 + 锁的有效时间）。 如果 SETNX 返回0，说明其他进程已经获得了锁，进程不能进入临界区。进程可以在一个循环中不断地尝试 SETNX 操作，以获得锁。</p>
<h4 id="为什么要用Redis？"><a href="#为什么要用Redis？" class="headerlink" title="为什么要用Redis？"></a>为什么要用Redis？</h4><p>分布式缓存，提升性能</p>
<h4 id="Redis和memcache区别是什么？"><a href="#Redis和memcache区别是什么？" class="headerlink" title="Redis和memcache区别是什么？"></a>Redis和memcache区别是什么？</h4><p>1、存储方式：Memcache把数据全部存在内存之中，断电后会挂掉，数据不能超 过内存大小。Redis支持数据的持久化，可以将内存中的数据保存在磁盘中，重启时可以再次加载进行使用。（RDB快照和AOF日志两 种持久化方式）。</p>
<p>2、Redis支持数据的备份，及master-slave模式的数据备份。</p>
<p>3、数据支持类型：Redis在数据支持上要比Memcache多得多。</p>
<p>4、使用底层模型不同：新版本的Redis直接自己构建了VM机制，因为一般的系统调用系统函数的话，会浪费一定的时间去移动和请求。</p>
<h4 id="Zookeeper怎么实现分布式锁？"><a href="#Zookeeper怎么实现分布式锁？" class="headerlink" title="Zookeeper怎么实现分布式锁？"></a>Zookeeper怎么实现分布式锁？</h4><p>基于zookeeper临时有序节点可以实现的分布式锁。</p>
<p>大致思想即为：每个客户端对某个方法加锁时，在zookeeper上的与该方法对应的指定节点的目录下，生成一个唯一的瞬时有序节点。 判断是否获取锁的方式很简单，只需要判断有序节点中序号最小的一个。 当释放锁的时候，只需将这个瞬时节点删除即可。同时，其可以避免服务宕机导致的锁无法释放，而产生的死锁问题。</p>
<h4 id="什么是Zookeeper？"><a href="#什么是Zookeeper？" class="headerlink" title="什么是Zookeeper？"></a>什么是Zookeeper？</h4><p>Zookeeper是一个开放源码的分布式服务协调组件，是Google Chubby的开源实现。是一个高性能的分布式数据一致性解决方案。他将那些复杂的、容易出错的分布式一致性服务封装起来，构成一个高效可靠的原语集，并提供一系列简单易用的接口给用户使用。</p>
<h4 id="什么是CAP？"><a href="#什么是CAP？" class="headerlink" title="什么是CAP？"></a>什么是CAP？</h4><p>CAP理论：一个分布式系统最多只能同时满足一致性（Consistency）、可用性（Availability）和分区容错性（Partition tolerance）这三项中的两项。</p>
<h4 id="什么是BASE？和CAP什么区别？"><a href="#什么是BASE？和CAP什么区别？" class="headerlink" title="什么是BASE？和CAP什么区别？"></a>什么是BASE？和CAP什么区别？</h4><p>BASE理论是对CAP理论的延伸，核心思想是即使无法做到强一致性（Strong Consistency，CAP的一致性就是强一致性），但应用可以采用适合的方式达到最终一致性（Eventual Consitency）。</p>
<p>BASE是指基本可用（Basically Available）、软状态（ Soft State）、最终一致性（ Eventual Consistency）。</p>
<h4 id="CAP怎么推导？如何取舍？"><a href="#CAP怎么推导？如何取舍？" class="headerlink" title="CAP怎么推导？如何取舍？"></a>CAP怎么推导？如何取舍？</h4><p>对于涉及到钱财这样不能有一丝让步的场景，C必须保证。网络发生故障宁可停止服务，这是保证CP，舍弃A。比如前几年支付宝光缆被挖断的事件，在网络出现故障的时候，支付宝就在可用性和数据一致性之间选择了数据一致性，用户感受到的是支付宝系统长时间宕机，但是其实背后是无数的工程师在恢复数据，保证数数据的一致性。</p>
<p>对于其他场景，比较普遍的做法是选择可用性和分区容错性，舍弃强一致性，退而求其次使用最终一致性来保证数据的安全。</p>
<h4 id="啥是分布式事务？分布式事务方案？"><a href="#啥是分布式事务？分布式事务方案？" class="headerlink" title="啥是分布式事务？分布式事务方案？"></a>啥是分布式事务？分布式事务方案？</h4><p>分布式事务是指会涉及到操作多个数据库的事务。其实就是将对同一库事务的概念扩大到了对多个库的事务。目的是为了保证分布式系统中的数据一致性。分布式事务处理的关键是必须有一种方法可以知道事务在任何地方所做的所有动作，提交或回滚事务的决定必须产生统一的结果（全部提交或全部回滚）</p>
<h4 id="手写一个线程安全的单例吧？"><a href="#手写一个线程安全的单例吧？" class="headerlink" title="手写一个线程安全的单例吧？"></a>手写一个线程安全的单例吧？</h4><p>原文地址<a href="http://www.hollischuang.com/archives/205" target="_blank" rel="noopener">http://www.hollischuang.com/archives/205</a></p>
<h4 id="不用synchronized和lock能实现线程安全的单例吗？"><a href="#不用synchronized和lock能实现线程安全的单例吗？" class="headerlink" title="不用synchronized和lock能实现线程安全的单例吗？"></a>不用synchronized和lock能实现线程安全的单例吗？</h4><p>借助CAS（AtomicReference）实现单例模式：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> AtomicReference&lt;Singleton&gt; INSTANCE = <span class="keyword">new</span> AtomicReference&lt;Singleton&gt;(); </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            Singleton singleton = INSTANCE.get();</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">null</span> != singleton) &#123;</span><br><span class="line">                <span class="keyword">return</span> singleton;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            singleton = <span class="keyword">new</span> Singleton();</span><br><span class="line">            <span class="keyword">if</span> (INSTANCE.compareAndSet(<span class="keyword">null</span>, singleton)) &#123;</span><br><span class="line">                <span class="keyword">return</span> singleton;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>用CAS的好处在于不需要使用传统的锁机制来保证线程安全,CAS是一种基于忙等待的算法,依赖底层硬件的实现,相对于锁它没有线程切换和阻塞的额外消耗,可以支持较大的并行度。 CAS的一个重要缺点在于如果忙等待一直执行不成功(一直在死循环中),会对CPU造成较大的执行开销。</p>
<h4 id="Paxos算法吧？"><a href="#Paxos算法吧？" class="headerlink" title="Paxos算法吧？"></a>Paxos算法吧？</h4><p>原文作者也没有详细说明。</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>其实这整个一个推导过程可以分出很多个知识点，每个知识点又可以延伸，就形成了原文作者说的体系。这篇文章后面有很多的知识点我还没有涉及到，需要认真的去查阅一下相关的知识。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>使用JPA和Mycat保存分表数据时出现主键冲突</title>
    <url>/2020/05/30/%E4%BD%BF%E7%94%A8JPA%E5%92%8CMycat%E4%BF%9D%E5%AD%98%E5%88%86%E8%A1%A8%E6%95%B0%E6%8D%AE%E6%97%B6%E5%87%BA%E7%8E%B0%E4%B8%BB%E9%94%AE%E5%86%B2%E7%AA%81/</url>
    <content><![CDATA[<h1 id="使用JPA和Mycat保存分表数据时出现主键冲突"><a href="#使用JPA和Mycat保存分表数据时出现主键冲突" class="headerlink" title="使用JPA和Mycat保存分表数据时出现主键冲突"></a>使用JPA和Mycat保存分表数据时出现主键冲突</h1><blockquote>
<p>将一个百万数据级别的表拆分为几十万数据的表后，保存数据时出现了问题</p>
</blockquote>
<a id="more"></a>
<h2 id="bug描述"><a href="#bug描述" class="headerlink" title="bug描述"></a>bug描述</h2><p><strong>A different object with the same identifier value was already associated with the session</strong> 一开始保存时出现了这个相同 id 的问题。之前调用 Jpa 的 save 方法时，有 id 为 update 没有 id 时为 insert 。尝试增加一下事务管理，错误变成了<strong>Sharding column can’t be updated 分表字段</strong>。</p>
<h2 id="处理方式"><a href="#处理方式" class="headerlink" title="处理方式"></a>处理方式</h2><ol>
<li><p>将主键的自增方式转换为snowflake。通过添加自增算法类，然后再添加下面的主键注解</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@GeneratedValue(generator = &quot;snowflake&quot;)</span><br><span class="line">    @GenericGenerator(name = &quot;snowflake&quot;, strategy = &quot;snowflake.GenerateId&quot;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用Jpa注解自己创建sql语句更新</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Modifying</span></span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"update ClimateEveryYearStationData set totalPreTwenty = ?1, avgTem = ?2, maxTem = ?3, minTem = ?4 where id = ?5"</span>)</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>使用grib2json解析风场grib文件</title>
    <url>/2020/10/17/%E4%BD%BF%E7%94%A8grib2json%E8%A7%A3%E6%9E%90%E9%A3%8E%E5%9C%BAgrib%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<h1 id="使用grib2json解析风场grib文件"><a href="#使用grib2json解析风场grib文件" class="headerlink" title="使用grib2json解析风场grib文件"></a>使用grib2json解析风场grib文件</h1><blockquote>
<p>本 jar 包由大佬在研究绘制风场动态图时提供。<br>参考博客 <a href="https://www.iteye.com/blog/umbrellall1-2509006" target="_blank" rel="noopener">https://www.iteye.com/blog/umbrellall1-2509006</a></p>
</blockquote>
<a id="more"></a>
<h2 id="尝试过程"><a href="#尝试过程" class="headerlink" title="尝试过程"></a>尝试过程</h2><p>大佬提供了这个 jar 包的 github 地址。我去研究了一下，把源码 clone 到本地。<br>到本地时出现了 <code>edu.ucar.grib</code> 依赖包无法下载问题，尝试更换 maven 源到阿里云，依旧不行。<br>在 <a href="https://mvnrepository.com/" target="_blank" rel="noopener">https://mvnrepository.com/</a> 中搜索了一下。主仓库Central中没能搜到4.3.19版本，其他版本并不能使用。但是在别的仓库 Boundless 中搜索到，但是数据仓库已经404。<br>向大佬求助，大佬查到了这个 jar 包的官网 <a href="https://docs.unidata.ucar.edu/netcdf-java/current/userguide/using_netcdf_java_artifacts.html" target="_blank" rel="noopener">https://docs.unidata.ucar.edu/netcdf-java/current/userguide/using_netcdf_java_artifacts.html</a> 。但是我更换这个官网的源后，依旧不能下载。没办法只能手动替换 maven 文件夹中的 jar 包，这才成功的开了起来。</p>
<h3 id="第二个问题"><a href="#第二个问题" class="headerlink" title="第二个问题"></a>第二个问题</h3><p>开启后，由于我本人开发经验太少的原因。没有接触过 <code>CliFactory.parseArguments(Options.class, args);</code> 相关。<br>按照步骤将源码 <code>maven package</code> 再试图使用命令行 <code>grib2json --names --data --fp 2 --fs 103 --fv 10.0 gfs.t18z.pgrbf00.2p5deg.grib2</code> 没能得到想要的结果。<br>通过关键字 <code>CliFactory.parseArguments</code> 找到了上面的那篇博客，才成功的将命令行输对。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">//            args = new String[]&#123;</span></span><br><span class="line"><span class="comment">//                "--data","--fp wind","--filter.surface 103","--filter.value 10.0","E:\\idea-workspace\\grib2json\\target\\grib2json-0.8.0-SNAPSHOT\\bin\\ Z_NAFP_C_BABJ_20201203000801_P_CLDAS_RT_CHN_0P05_HOR-WIN-2020120300.grib2"&#125;;</span></span><br><span class="line">            args = <span class="keyword">new</span> String[]&#123;</span><br><span class="line">                <span class="string">"--data"</span>,<span class="string">"--names"</span>,<span class="string">"--filter.parameter"</span>,<span class="string">"wind"</span>,<span class="string">"--filter.surface"</span>,<span class="string">"103"</span>,<span class="string">"--filter.value"</span>, <span class="string">"10.0"</span>,<span class="string">"--output"</span>, <span class="string">"D://a.txt"</span>, <span class="string">"D://text.GRB2"</span>&#125;;</span><br><span class="line"></span><br><span class="line">            Options options = CliFactory.parseArguments(Options.class, args);</span><br><span class="line">            <span class="keyword">if</span> (options.getShowHelp() || options.getFile() == <span class="keyword">null</span>) &#123;</span><br><span class="line">                printUsage();</span><br><span class="line">                System.exit(options.getShowHelp() ? <span class="number">0</span> : <span class="number">1</span>);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            LoggerContext lc = (LoggerContext) LoggerFactory.getILoggerFactory();</span><br><span class="line">            <span class="keyword">if</span> (!options.getEnableLogging()) &#123;</span><br><span class="line">                lc.stop();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            List&lt;Options&gt; optionGroups = options.getRecipe() != <span class="keyword">null</span> ?</span><br><span class="line">                readRecipeFile(args, options.getRecipe()) :</span><br><span class="line">                Collections.singletonList(options);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">new</span> Grib2Json(options.getFile(), optionGroups).write();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span> (JewelRuntimeException t) &#123;</span><br><span class="line">            printUsage();</span><br><span class="line">            System.out.println();</span><br><span class="line">            System.err.println(t.getMessage());</span><br><span class="line">            System.exit(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span> (IllegalArgumentException e) &#123;</span><br><span class="line">            System.err.println(e.getMessage());</span><br><span class="line">            System.exit(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">            t.printStackTrace(System.err);</span><br><span class="line">            System.exit(<span class="number">2</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>之后就将解析出来的文件返回给了前端使用。</p>
]]></content>
      <tags>
        <tag>grib2json</tag>
      </tags>
  </entry>
  <entry>
    <title>使用mycat分表后的A different object with the same identifier value错误</title>
    <url>/2019/10/26/%E4%BD%BF%E7%94%A8mycat%E5%88%86%E8%A1%A8%E5%90%8E%E7%9A%84A-different-object-with-the-same-identifier-value%E9%94%99%E8%AF%AF/</url>
    <content><![CDATA[<h1 id="使用mycat分表后的A-different-object-with-the-same-identifier-value错误"><a href="#使用mycat分表后的A-different-object-with-the-same-identifier-value错误" class="headerlink" title="使用mycat分表后的A different object with the same identifier value错误"></a>使用mycat分表后的A different object with the same identifier value错误</h1><blockquote>
<p>这个问题在之前使用mycat时并没有发生。这次的业务场景是我需要在同一事务中，先查询历史数据，然后再写入新的数据。该新的数据是按时间分表。</p>
</blockquote>
<a id="more"></a>
<h2 id="错误："><a href="#错误：" class="headerlink" title="错误："></a>错误：</h2><p><strong>A different object with the same identifier value was already associated with the session</strong> ：[com.yiring.risk.domain.hiddendangerpoint.HiddenDangerPointNationalScene#776]</p>
<h2 id="首次尝试-更换JPA查询"><a href="#首次尝试-更换JPA查询" class="headerlink" title="首次尝试-更换JPA查询"></a>首次尝试-更换JPA查询</h2><p>产生这个bug的时候，任务时间很紧张。我就没有百度，直接询问团队大佬。大佬表示有可能是因为同一事务中的查出的数据id和要保存的数据id为同一个。</p>
<p>就把JPA原本的findAllByDateTimeBetween改成了通过@Query(value=”原生sql”，nativeQuery = true)来直接查询出List&lt;Map&lt;String, Object&gt;&gt; </p>
<p>本次尝试失败</p>
<h2 id="第二次尝试-更换成JDBC查询"><a href="#第二次尝试-更换成JDBC查询" class="headerlink" title="第二次尝试-更换成JDBC查询"></a>第二次尝试-更换成JDBC查询</h2><p>上次的修改失败了之后，再次尝试将原生sql查询，更改为@JdbcTemplate注解后。通过jdbcTemplate.queryForList(sql);查询。</p>
<p>再次尝试失败</p>
<h2 id="第三次尝试-减少每次查询时的数据"><a href="#第三次尝试-减少每次查询时的数据" class="headerlink" title="第三次尝试-减少每次查询时的数据"></a>第三次尝试-减少每次查询时的数据</h2><p>大佬再次思考过后，认为是saveAll时的数据有不同的时间。在同一事务中的不同时间有可能分到不同的分表里，也就是可能在缓存区里有相同的id。</p>
<p>决定把数据分为按同一时间存储</p>
<p>目前解决了这个bug，但是具体的细节还是没有搞太明白。之后再进行补充</p>
]]></content>
      <tags>
        <tag>mycat</tag>
      </tags>
  </entry>
  <entry>
    <title>使用geotools时遇到的NoClassDefFoundError</title>
    <url>/2019/07/26/%E4%BD%BF%E7%94%A8geotools%E6%97%B6%E9%81%87%E5%88%B0%E7%9A%84NoClassDefFoundError/</url>
    <content><![CDATA[<h1 id="使用geotools时遇到的NoClassDefFoundError"><a href="#使用geotools时遇到的NoClassDefFoundError" class="headerlink" title="使用geotools时遇到的NoClassDefFoundError"></a>使用geotools时遇到的NoClassDefFoundError</h1><blockquote>
<p>这个问题很常见，但是并没有用常见的解决方式去解决。再次记录一下</p>
</blockquote>
<a id="more"></a>
<h2 id="错误信息"><a href="#错误信息" class="headerlink" title="错误信息"></a>错误信息</h2><p><strong>关键字：</strong>Handler dispatch failed; nested exception is java.lang.NoClassDefFoundError: org/apache/commons/lang3/JavaVersion</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">org.springframework.web.util.NestedServletException: Handler dispatch failed; nested exception is java.lang.NoClassDefFoundError: org/apache/commons/lang3/JavaVersion</span><br><span class="line">	at org.springframework.web.servlet.DispatcherServlet.doDispatch(DispatcherServlet.java:1006) ~[spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.DispatcherServlet.doService(DispatcherServlet.java:925) ~[spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.FrameworkServlet.processRequest(FrameworkServlet.java:974) [spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.FrameworkServlet.doGet(FrameworkServlet.java:866) [spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at javax.servlet.http.HttpServlet.service(HttpServlet.java:635) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.springframework.web.servlet.FrameworkServlet.service(FrameworkServlet.java:851) [spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at javax.servlet.http.HttpServlet.service(HttpServlet.java:742) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:231) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.tomcat.websocket.server.WsFilter.doFilter(WsFilter.java:52) [tomcat-embed-websocket-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at com.yiring.zjtq.secruity.SecurityFilter.invoke(SecurityFilter.java:58) [classes/:na]</span><br><span class="line">	at com.yiring.zjtq.secruity.SecurityFilter.doFilter(SecurityFilter.java:37) [classes/:na]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:101) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at com.alibaba.druid.support.http.WebStatFilter.doFilter(WebStatFilter.java:123) [druid-1.1.10.jar:1.1.10]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:320) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.access.intercept.FilterSecurityInterceptor.invoke(FilterSecurityInterceptor.java:127) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.access.intercept.FilterSecurityInterceptor.doFilter(FilterSecurityInterceptor.java:91) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at com.yiring.zjtq.secruity.SecurityFilter.invoke(SecurityFilter.java:58) [classes/:na]</span><br><span class="line">	at com.yiring.zjtq.secruity.SecurityFilter.doFilter(SecurityFilter.java:37) [classes/:na]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.access.ExceptionTranslationFilter.doFilter(ExceptionTranslationFilter.java:119) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.authentication.AnonymousAuthenticationFilter.doFilter(AnonymousAuthenticationFilter.java:111) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.servletapi.SecurityContextHolderAwareRequestFilter.doFilter(SecurityContextHolderAwareRequestFilter.java:170) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.savedrequest.RequestCacheAwareFilter.doFilter(RequestCacheAwareFilter.java:63) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.authentication.www.BasicAuthenticationFilter.doFilterInternal(BasicAuthenticationFilter.java:158) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at com.yiring.zjtq.secruity.JwtAuthenticationTokenFilter.doFilterInternal(JwtAuthenticationTokenFilter.java:62) [classes/:na]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.authentication.logout.LogoutFilter.doFilter(LogoutFilter.java:116) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.CorsFilter.doFilterInternal(CorsFilter.java:96) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.header.HeaderWriterFilter.doFilterInternal(HeaderWriterFilter.java:66) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.context.request.async.WebAsyncManagerIntegrationFilter.doFilterInternal(WebAsyncManagerIntegrationFilter.java:56) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy$VirtualFilterChain.doFilter(FilterChainProxy.java:334) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy.doFilterInternal(FilterChainProxy.java:215) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.security.web.FilterChainProxy.doFilter(FilterChainProxy.java:178) [spring-security-web-5.0.7.RELEASE.jar:5.0.7.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.DelegatingFilterProxy.invokeDelegate(DelegatingFilterProxy.java:357) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.DelegatingFilterProxy.doFilter(DelegatingFilterProxy.java:270) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.springframework.web.filter.RequestContextFilter.doFilterInternal(RequestContextFilter.java:99) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.springframework.web.filter.HttpPutFormContentFilter.doFilterInternal(HttpPutFormContentFilter.java:109) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.springframework.web.filter.HiddenHttpMethodFilter.doFilterInternal(HiddenHttpMethodFilter.java:93) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.springframework.web.filter.CharacterEncodingFilter.doFilterInternal(CharacterEncodingFilter.java:200) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.filter.OncePerRequestFilter.doFilter(OncePerRequestFilter.java:107) [spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:193) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:166) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.StandardWrapperValve.invoke(StandardWrapperValve.java:198) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.StandardContextValve.invoke(StandardContextValve.java:96) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.authenticator.AuthenticatorBase.invoke(AuthenticatorBase.java:493) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.StandardHostValve.invoke(StandardHostValve.java:140) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.valves.ErrorReportValve.invoke(ErrorReportValve.java:81) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.core.StandardEngineValve.invoke(StandardEngineValve.java:87) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.catalina.connector.CoyoteAdapter.service(CoyoteAdapter.java:342) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.coyote.http11.Http11Processor.service(Http11Processor.java:800) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.coyote.AbstractProcessorLight.process(AbstractProcessorLight.java:66) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.coyote.AbstractProtocol$ConnectionHandler.process(AbstractProtocol.java:800) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.tomcat.util.net.NioEndpoint$SocketProcessor.doRun(NioEndpoint.java:1471) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at org.apache.tomcat.util.net.SocketProcessorBase.run(SocketProcessorBase.java:49) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at java.util.concurrent.ThreadPoolExecutor.runWorker(ThreadPoolExecutor.java:1149) [na:1.8.0_161]</span><br><span class="line">	at java.util.concurrent.ThreadPoolExecutor$Worker.run(ThreadPoolExecutor.java:624) [na:1.8.0_161]</span><br><span class="line">	at org.apache.tomcat.util.threads.TaskThread$WrappingRunnable.run(TaskThread.java:61) [tomcat-embed-core-8.5.32.jar:8.5.32]</span><br><span class="line">	at java.lang.Thread.run(Thread.java:748) [na:1.8.0_161]</span><br><span class="line">Caused by: java.lang.NoClassDefFoundError: org/apache/commons/lang3/JavaVersion</span><br><span class="line">	at org.geotools.util.NIOUtilities.clean(NIOUtilities.java:207) ~[gt-metadata-21.0.jar:na]</span><br><span class="line">	at org.geotools.util.NIOUtilities.clean(NIOUtilities.java:185) ~[gt-metadata-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.shapefile.dbf.DbaseFileReader.close(DbaseFileReader.java:300) ~[gt-shapefile-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.shapefile.ShapefileFeatureSource.readAttributes(ShapefileFeatureSource.java:586) ~[gt-shapefile-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.shapefile.ShapefileFeatureSource.buildFeatureType(ShapefileFeatureSource.java:479) ~[gt-shapefile-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.shapefile.ShapefileFeatureStore.buildFeatureType(ShapefileFeatureStore.java:137) ~[gt-shapefile-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.store.ContentFeatureSource.getAbsoluteSchema(ContentFeatureSource.java:327) ~[gt-main-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.store.ContentFeatureSource.getSchema(ContentFeatureSource.java:296) ~[gt-main-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.store.ContentFeatureCollection.&lt;init&gt;(ContentFeatureCollection.java:69) ~[gt-main-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.store.ContentFeatureSource.getFeatures(ContentFeatureSource.java:545) ~[gt-main-21.0.jar:na]</span><br><span class="line">	at org.geotools.data.store.ContentFeatureSource.getFeatures(ContentFeatureSource.java:105) ~[gt-main-21.0.jar:na]</span><br><span class="line">	at com.yiring.zjtq.util.GeoUtils.isoLinesCrop(GeoUtils.java:217) ~[classes/:na]</span><br><span class="line">	at com.yiring.zjtq.service.currentmonitor.impl.RainAndTemLiveServiceImpl.getPreTotalStainMap(RainAndTemLiveServiceImpl.java:144) ~[classes/:na]</span><br><span class="line">	at com.yiring.zjtq.service.currentmonitor.impl.RainAndTemLiveServiceImpl$$FastClassBySpringCGLIB$$b4c107d6.invoke(&lt;generated&gt;) ~[classes/:na]</span><br><span class="line">	at org.springframework.cglib.proxy.MethodProxy.invoke(MethodProxy.java:204) ~[spring-core-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.aop.framework.CglibAopProxy$DynamicAdvisedInterceptor.intercept(CglibAopProxy.java:684) ~[spring-aop-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at com.yiring.zjtq.service.currentmonitor.impl.RainAndTemLiveServiceImpl$$EnhancerBySpringCGLIB$$598b587c.getPreTotalStainMap(&lt;generated&gt;) ~[classes/:na]</span><br><span class="line">	at com.yiring.zjtq.web.currentmonitor.RainAndTemLiveController.stainMap(RainAndTemLiveController.java:33) ~[classes/:na]</span><br><span class="line">	at sun.reflect.NativeMethodAccessorImpl.invoke0(Native Method) ~[na:1.8.0_161]</span><br><span class="line">	at sun.reflect.NativeMethodAccessorImpl.invoke(NativeMethodAccessorImpl.java:62) ~[na:1.8.0_161]</span><br><span class="line">	at sun.reflect.DelegatingMethodAccessorImpl.invoke(DelegatingMethodAccessorImpl.java:43) ~[na:1.8.0_161]</span><br><span class="line">	at java.lang.reflect.Method.invoke(Method.java:498) ~[na:1.8.0_161]</span><br><span class="line">	at org.springframework.web.method.support.InvocableHandlerMethod.doInvoke(InvocableHandlerMethod.java:209) ~[spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.method.support.InvocableHandlerMethod.invokeForRequest(InvocableHandlerMethod.java:136) ~[spring-web-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.mvc.method.annotation.ServletInvocableHandlerMethod.invokeAndHandle(ServletInvocableHandlerMethod.java:102) ~[spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter.invokeHandlerMethod(RequestMappingHandlerAdapter.java:877) ~[spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter.handleInternal(RequestMappingHandlerAdapter.java:783) ~[spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.mvc.method.AbstractHandlerMethodAdapter.handle(AbstractHandlerMethodAdapter.java:87) ~[spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	at org.springframework.web.servlet.DispatcherServlet.doDispatch(DispatcherServlet.java:991) ~[spring-webmvc-5.0.8.RELEASE.jar:5.0.8.RELEASE]</span><br><span class="line">	... 91 common frames omitted</span><br><span class="line"></span><br><span class="line">Disconnected from the target VM, address: &apos;127.0.0.1:58378&apos;, transport: &apos;socket&apos;</span><br><span class="line">2019-0</span><br></pre></td></tr></table></figure>
<h2 id="解决过程"><a href="#解决过程" class="headerlink" title="解决过程"></a>解决过程</h2><p>百度搜索时并没有找到javaVersion这个类报错的解决方法。秉着同类问题的心思去实验了一下第一页的解决方法，并没成功。接着就是找冲突jar包。</p>
<h3 id="冲突jar包寻找过程"><a href="#冲突jar包寻找过程" class="headerlink" title="冲突jar包寻找过程"></a>冲突jar包寻找过程</h3><p>这个项目运用的是gradle管理，使用dependencies导出jar包结构。确定只有gt-shapefile和gt-geojson引入了commons-lang3</p>
<p>尝试屏蔽commons-lang3并换成自己的版本<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">compile (group: &apos;org.geotools&apos;, name: &apos;gt-shapefile&apos;, version: &apos;21.0&apos;)&#123;</span><br><span class="line">        exclude module: &apos;commons-lang3&apos;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">compile (group: &apos;org.geotools&apos;, name: &apos;gt-geojson&apos;, version: &apos;21.0&apos;)&#123;</span><br><span class="line">        exclude module: &apos;commons-lang3&apos;</span><br><span class="line">&#125;</span><br><span class="line">compile group: &apos;org.apache.commons&apos;, name: &apos;commons-lang3&apos;, version: &apos;3.9&apos;</span><br></pre></td></tr></table></figure></p>
<p>不成功后，导入不同版本（3.9-&gt;3.8.1-&gt;3.7）的jar包，并且去api查看是否是新的jar包更新了这个枚举类。依旧失败。</p>
<p>呼叫大佬帮助后，大佬修改了jar包下载地址的顺序，重新导入的时候不勾选下图的那个选项，然后就解决了。应该是之前下载的时候下错了jar包<br><img src="/2019/07/26/使用geotools时遇到的NoClassDefFoundError/b2.png" alt=""></p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>使用rabbitmq时出现的Channel shutdown: channel error;</title>
    <url>/2019/10/26/%E4%BD%BF%E7%94%A8rabbitmq%E6%97%B6%E5%87%BA%E7%8E%B0%E7%9A%84Channel-shutdown-channel-error/</url>
    <content><![CDATA[<h1 id="使用rabbitmq时出现的Channel-shutdown-channel-error"><a href="#使用rabbitmq时出现的Channel-shutdown-channel-error" class="headerlink" title="使用rabbitmq时出现的Channel shutdown: channel error;"></a>使用rabbitmq时出现的Channel shutdown: channel error;</h1><blockquote>
<p>本次问题并不是需要很复杂的处理，但是还是要记录一下。</p>
</blockquote>
<a id="more"></a>
<h2 id="错误："><a href="#错误：" class="headerlink" title="错误："></a>错误：</h2><p><strong>Channel shutdown: channel error; protocol method: #method&lt;channel.close&gt;</strong>(reply-code=406, reply-text=PRECONDITION_FAILED - inequivalent arg ‘x-message-ttl’ for queue ‘river.water.warning’ in vhost ‘ /‘: received the value ‘60000’ of type ‘signedint’ but current is none, class-id=50, method-id=10)</p>
<h2 id="修复方法"><a href="#修复方法" class="headerlink" title="修复方法"></a>修复方法</h2><p>这次的问题出现在我配置了<strong>rabbitMQ</strong>的<strong>TTL</strong>后出现的。原因是因为消费段和生产者的设置不同，会造成启动后的消息写入不到队列中。只需要把两边的配置同步即可解决</p>
]]></content>
      <tags>
        <tag>RabbitMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>删除一个找不到该项目的文件</title>
    <url>/2020/10/24/%E5%88%A0%E9%99%A4%E4%B8%80%E4%B8%AA%E6%89%BE%E4%B8%8D%E5%88%B0%E8%AF%A5%E9%A1%B9%E7%9B%AE%E7%9A%84%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<h1 id="删除一个找不到该项目的文件"><a href="#删除一个找不到该项目的文件" class="headerlink" title="删除一个找不到该项目的文件"></a>删除一个找不到该项目的文件</h1><blockquote>
<p>原文来自知乎<br><a href="https://www.zhihu.com/question/350091243" target="_blank" rel="noopener">求助：如何删除win10系统，“找不到该项目”的文件？文件名最后是一个点的文件，没有扩展名？</a></p>
</blockquote>
<a id="more"></a>
<h2 id="起因"><a href="#起因" class="headerlink" title="起因"></a>起因</h2><p>在使用 smb 链接写入文件的时候，没能正确的给文件加入后缀名。于是就在远程目录里创建了一个最后是一个点的文件。<br>正常的删除和百度第一页中的 bat 命令行、360等工具都试过了。不行</p>
<h2 id="有效的方法"><a href="#有效的方法" class="headerlink" title="有效的方法"></a>有效的方法</h2><p>7-zip，修改重命名为“xxx. txt”，直接成功。<br>还是保留一下原文的成功方式<br>7-zip，以管理员身份闹着玩，重命名修改为“xxx. txt”，删除。成功！！！！！！！</p>
]]></content>
      <tags>
        <tag>日常</tag>
      </tags>
  </entry>
  <entry>
    <title>分布式的架构演进</title>
    <url>/2020/05/25/%E5%88%86%E5%B8%83%E5%BC%8F%E7%9A%84%E6%9E%B6%E6%9E%84%E6%BC%94%E8%BF%9B/</url>
    <content><![CDATA[<h1 id="分布式的架构演进"><a href="#分布式的架构演进" class="headerlink" title="分布式的架构演进"></a>分布式的架构演进</h1><blockquote>
<p>最近在浏览微信文章的时候发现了一篇很不错的项目优化路线，和我的项目演进经历很是相像。记录一下，再研究项目之后的优化方向。<br>来自架构师修炼（微信号：jiagouxiulian）的 <a href="https://mp.weixin.qq.com/s?__biz=MzI2MTk1NDY0Mw==&amp;mid=2247484223&amp;idx=1&amp;sn=d8e190e8c199ddf2934e938e4e111d7d&amp;chksm=ea53ce03dd2447150dc582d513e7766e4195569207885192e37e7d3e1a80b0b0f3f871e9907d&amp;mpshare=1&amp;scene=23&amp;srcid=&amp;sharer_sharetime=1590021834076&amp;sharer_shareid=42a3d887c59629d50ec3421c14c20068#rd" target="_blank" rel="noopener">谁能想到，我给技术总监“上了一课”</a><br><a id="more"></a></p>
</blockquote>
<h2 id="序言"><a href="#序言" class="headerlink" title="序言"></a>序言</h2><p>正当我沉迷在源码中不能自拔的时候，总监突然来到我旁边轻声的对我说道：最近这段时间大家的任务也不是特别多，空闲时间比较足，你这边能不能做点技术分享什么的，一来可以联络一下同事之间的感情，二来也可以增加同事之间的学习氛围。</p>
<p><img src="/2020/05/25/分布式的架构演进/b1.png" alt=""></p>
<p>宝宝心里难受，一直在想我要讲一个什么样的话题才能让大部分的人都能听得懂并且感兴趣呢？</p>
<p>这里面肯定不时出现太多的代码，既然这样，那我就讲一讲分布式的架构演进吧，这个话题既高端，也能让大部分的人听懂，我就是个天才。</p>
<p><img src="/2020/05/25/分布式的架构演进/b2.png" alt=""></p>
<h2 id="单体服务"><a href="#单体服务" class="headerlink" title="单体服务"></a>单体服务</h2><p>我记得在我实习的时候用的就是单体的服务，那个时候的架构很简单，前后端分离都还没有，直接 JSP+Java 实现一套项目，整个流程相当简单，就连 Nginx 都没有用到。</p>
<blockquote>
<p>这也是我第一次尝试做一个网站时所碰到的简单架构</p>
</blockquote>
<p>我们一起来看看当时的架构是什么样的：<br><img src="/2020/05/25/分布式的架构演进/b3.png" alt="jsp+java"></p>
<ol>
<li>引入 Nginx<br>没错，就是这么简单，浏览器通过接口访问服务器，服务器通过用户的请求操作数据库，然后再相应给浏览器，这就是一个最简单的单体服务的流程。</li>
</ol>
<p>后来领导觉得直接将端口暴露出去相当危险，我们需要做一层代理，让用户直接通过域名访问。</p>
<p>然后流程就被设计成了这样：<br><img src="/2020/05/25/分布式的架构演进/b4.png" alt="引入nginx"></p>
<p>这是引入 Nginx 之后的架构图，在这样运行了一段时间之后，突然有一天，领导找到我并说到：你写的代码是不是有问题，为什么一个普通的查询需要很长的时间？基本操作都卡的要死，给你一周时间，赶紧给我解决！</p>
<ol start="2">
<li>引入 Redis 单体<br>经过我的排查之后发现导致程序变卡的原因是数据库受到了瓶颈，压力太大，承受不住那么大的请求，既然问题找到了，那解决就很简单了。</li>
</ol>
<p>我在程序和数据库之间增加一个中间件：Redis，使用它来降低数据库的访问，这样性能自然会得到提升。<br><img src="/2020/05/25/分布式的架构演进/b5.png" alt="引入redis缓存"></p>
<p>舒服，引入 Redis，并且对代码做了一些优化之后，发现速度上来了，我有可以快乐的写 Bug 了。</p>
<p>就这样过去了大概一个月左右，这天我正在和同事讨论一些八卦，突然感觉背后一阵阴风 ，我知道，出事了。</p>
<p>没想到是领导又来找我麻烦了，他说由于我们的产品太火了，下载注册人数都几十万了，日活跃人数也是上万，我们现在的这套架构撑不住了，你有没有什么好的建议？我惊讶了一下，我们的产品这么受欢迎吗？</p>
<p>于是我和领导说，我们可以将数据做一下读写分离，这样也可以提升一下程序的性能，但是对于现在的情况，就算加了读写分离，作用应该也不大，我们应该将单体多部署几台，提升程序的吞吐量。</p>
<ol start="3">
<li>引入 MySQL 读写分离<br><img src="/2020/05/25/分布式的架构演进/b6.png" alt="引入 MySQL 读写分离"></li>
</ol>
<blockquote>
<p>这也是我在18年参与的项目重构时，整个项目的架构。也是使用的mycat做分库分表,读写分离</p>
</blockquote>
<p>这就是将数据库改造成读写分离之后的架构，读操作和写操作分别在不同的库中，这样，查询和写入就不会那么长的时间了，因为在读库中没有写操作，写库中没有读操作。</p>
<p>由于我们一般是读的操作比较多，所以这个时候我们我们可以将读库的配置设置的好一点，写库的设置的差一点，均衡分配。</p>
<p>但仅仅这样是也是不能支撑那么大流量的，所以这个时候我们还需要将服务器做集群。</p>
<ol start="4">
<li>引入服务器集群<br><img src="/2020/05/25/分布式的架构演进/b7.png" alt="引入服务器集群"></li>
</ol>
<p>这就是我们单体的最终架构，改造完成之后性能确实得到了很大的提升，因为服务做了集群之后，分散了很多的请求。</p>
<p>比如一个 Tomcat 能支持的最高并发是 200，那现在三个服务就能支持 600 的并发，性能提升了 3 倍，最终扩充了多少台服务器我也不清楚，因为这个是运维做的。</p>
<p>集群算是做完了，总算可以满足领导的要求了，为了搞定这一套一套的升级，不知道熬了多少夜，看到电脑旁边掉落的那几根头发，我满意的点了点头。</p>
<p><img src="/2020/05/25/分布式的架构演进/b8.png" alt=""></p>
<p>在后面的两个月的时间里，我们在不停的做迭代更新，几乎每周都会有版本上线，两个月过后版本终于稳定下来了，不怎么有更新了，所以又来到了程序猿的空闲时间。</p>
<p>同事们每天上班都做着自己的事情，有学习的，有逛淘宝的，有玩游戏的，更夸张的是居然有个同事闲着没事居然去撩产品小姐姐，握草，这是想要自掘坟墓吗？</p>
<p>大概空闲了一周的时间左右，我们的技术总监说话了，今天下午 3 点，所有后端开发人员会议室开会，听到这个消息，我就知道，有活干了，难道是接了新的项目？</p>
<p>到了下午 3 点，我们来到会议室，只见技术总监已经提前到了，并且屏幕上写着五个大字，看到这五个字，我心里想，该来的还是会来，躲不过去的，那就是：分布式架构。</p>
<h2 id="分布式架构"><a href="#分布式架构" class="headerlink" title="分布式架构"></a>分布式架构</h2><p>因为我知道，我们产品注册人数已经高达几十万，以我们现在的架构肯定会有撑不住的一天，这个产品肯定会被重构，并且是以分布式的架构进行重构，果然，今天技术总监就召开了这个会议。</p>
<p>技术总监：我们的产品现在比较火热，不管是注册人数还是日活跃人数都是比较高的，为了让程序能有更好的健壮性，我希望我们可以对这个项目进行重构，以分布式的架构，今天开会就是我们一起来做个技术选型，你们对分布式熟悉吗？</p>
<p>同事 A：分布式系统（distributed system）是建立在网络之上的软件系统。正是因为软件的特性，所以分布式系统具有高度的内聚性和透明性。</p>
<p>因此，网络和分布式系统之间的区别更多的在于高层软件（特别是操作系统），而不是硬件。</p>
<p>技术总监：嗯，不错，不过这个解释比较官方，还有没有比较通俗易懂的解释？</p>
<p>同事 B：分布式将一个大的系统拆分成无数个细微的子系统，让每一个系统都负责一定的职责，他们相互独立，但是又相互联系。</p>
<p>技术总监：哟，不错哦，这个同事可以，晚上加鸡腿，那有人可以举个例子吗？</p>
<p>Me：从前有一个有一个饭店，里面只有老板和一个员工，这个员工负责饭店所有的事情，包括但不限于：厨师、服务员、收银员、清洁工等，在炒菜的时候就不能去收钱，在打扫卫生的时候就不能炒菜，这个员工干了一个月之后，突然有一天，他病倒了，餐厅的生意就停滞了，这个时候老板就想到了一个人不行，那我多招几个人，一个人负责一个职位，这样即使某个人请假或者离职了，对我的生意影响不是很大，比如：清洁工离职了，虽然没有人打扫卫生，但是这并不影响我开门做生意，分布式就是这个道理。</p>
<p>技术总监：这个例子讲的不错，你晚上不用加班了，如果你要是能画一个分布式的基础图出来，我今晚请大家撸串。</p>
<p>Me：我专治各种不服，于是我就给技术总监”上了一课”，请看图：<br><img src="/2020/05/25/分布式的架构演进/b9.png" alt=""></p>
<h3 id="为什么要使用分布式架构"><a href="#为什么要使用分布式架构" class="headerlink" title="为什么要使用分布式架构"></a>为什么要使用分布式架构</h3><p><strong>技术总监</strong>：不错，画的很好，但是你们知道我们为什么要将单体的服务重构为分布式吗？答对有鸡腿。</p>
<p><strong>同事 C</strong>：</p>
<ul>
<li><p>Spring Cloud 专注于提供良好的开箱即用经验的典型用例和可扩展性机制覆盖</p>
</li>
<li><p>分布式/版本化配置</p>
</li>
<li><p>服务注册和发现</p>
</li>
<li><p>路由</p>
</li>
<li><p>Service-to-Service 调用</p>
</li>
<li><p>负载均衡</p>
</li>
<li><p>断路器</p>
</li>
<li><p>分布式消息传递</p>
</li>
</ul>
<p>这是分布式的优点，这样看起来可能比较抽象，举个例子来说，对于单体服务来说，如果我想更新订单中的某个功能，我是不是需要重启整个服务。</p>
<p>这个时候就会导致整个项目都处于不可用状态，或者在处理订单的时候由于程序代码写的有问题，导致死锁了，这个时候也会导致整个服务处于宕机专改，容错率很差。</p>
<p>但是分布式不同，如上图所示，订单服务、售后服务、用户服务都是独立的服务，如果需要更新订单服务或者订单服务发生死锁，受影响的只会是订单服务，售后服务与用户服务还是可以正常工作的，这就是分布式相对单体来说最大的优势之一。</p>
<p><strong>技术总监</strong>：想不到我们这个团队人才辈出啊，不错不错，看来我都没有讲下去的必要了啊，大家对分布式都相当熟悉了啊，那分布式架构存在缺点或者不足吗？</p>
<p>同事 D：<br><img src="/2020/05/25/分布式的架构演进/b10.png" alt="分布式架构与单体架构对比"><br>这是我平时刷博客的时候看到的，觉得总结的不错！</p>
<h3 id="分布式基础组件"><a href="#分布式基础组件" class="headerlink" title="分布式基础组件"></a>分布式基础组件</h3><p><strong>技术总监</strong>：既然大家对分布式都这么熟悉了，那我也就不在多说了，我们接下来直接来说说关于分布式组件的选型吧，大家有什么意见都可以提出来，首先谁来说一下分布式组件都有哪些？请开始你们的表演。<br><img src="/2020/05/25/分布式的架构演进/b11.png" alt=""></p>
<p><strong>同事 E</strong>：那我就不客气了，由于我们都是对 Spring Cloud 比较熟悉，现在他也是比较主流，那我介绍一下 Spring Cloud 的基础组件吧。</p>
<ul>
<li><p>Spring Cloud Config：配置管理工具包，让你可以把配置放到远程服务器，集中化管理集群配置，目前支持本地存储、Git 以及 Subversion。</p>
</li>
<li><p>Spring Cloud Bus：事件、消息总线，用于在集群（例如，配置变化事件）中传播状态变化，可与 Spring Cloud Config 联合实现热部署。</p>
</li>
<li><p>Eureka：云端服务发现，一个基于 REST 的服务，用于定位服务，以实现云端中间层服务发现和故障转移。</p>
</li>
<li><p>Hystrix：熔断器，容错管理工具，旨在通过熔断机制控制服务和第三方库的节点,从而对延迟和故障提供更强大的容错能力。</p>
</li>
<li><p>Zuul：Zuul 是在云平台上提供动态路由,监控,弹性,安全等边缘服务的框架。Zuul 相当于是设备和 Netflix 流应用的 Web 网站后端所有请求的前门。</p>
</li>
<li><p>Archaius：配置管理 API，包含一系列配置管理 API，提供动态类型化属性、线程安全配置操作、轮询框架、回调机制等功能。</p>
</li>
<li><p>Consul：封装了 Consul 操作，Consul 是一个服务发现与配置工具，与 Docker 容器可以无缝集成。</p>
</li>
<li><p>Spring Cloud for Cloud Foundry：通过 Oauth2 协议绑定服务到 CloudFoundry，CloudFoundry 是 VMware 推出的开源 PaaS 云平台。</p>
</li>
<li><p>Spring Cloud Sleuth：日志收集工具包，封装了 Dapper 和 log-based 追踪以及 Zipkin 和 HTrace 操作，为 Spring Cloud 应用实现了一种分布式追踪解决方案。</p>
</li>
<li><p>Spring Cloud Data Flow：大数据操作工具，作为 Spring XD 的替代产品，它是一个混合计算模型，结合了流数据与批量数据的处理方式。</p>
</li>
<li><p>Spring Cloud Security：基于 Spring Security 的安全工具包，为你的应用程序添加安全控制。</p>
</li>
<li><p>Spring Cloud Zookeeper：操作 Zookeeper 的工具包，用于使用 Zookeeper 方式的服务发现和配置管理。</p>
</li>
<li><p>Spring Cloud Stream：数据流操作开发包，封装了与 Redis、Rabbit、Kafka 等发送接收消息。</p>
</li>
<li><p>Spring Cloud CLI：基于 Spring Boot CLI，可以让你以命令行方式快速建立云组件。</p>
</li>
<li><p>Ribbon：提供云端负载均衡，有多种负载均衡策略可供选择，可配合服务发现和断路器使用。</p>
</li>
<li><p>Turbine：Turbine 是聚合服务器发送事件流数据的一个工具，用来监控集群下 Hystrix 的 Metrics 情况。</p>
</li>
<li><p>Feign：Feign 是一种声明式、模板化的 HTTP 客户端。</p>
</li>
<li><p>Spring Cloud Task：提供云端计划任务管理、任务调度。</p>
</li>
<li><p>Spring Cloud Connectors：便于云端应用程序在各种 PaaS 平台连接到后端，如：数据库和消息代理服务。</p>
</li>
<li><p>Spring Cloud Cluster：提供 Leadership 选举，如：Zookeeper，Redis，Hazelcast，Consul 等常见状态模式的抽象和实现。</p>
</li>
<li><p>Spring Cloud Starters：Spring Boot 式的启动项目，为 Spring Cloud 提供开箱即用的依赖管理。</p>
</li>
</ul>
<p>我们常用的组件：</p>
<ul>
<li><p>Spring Cloud Config</p>
</li>
<li><p>Spring Cloud Bus</p>
</li>
<li><p>Hystrix</p>
</li>
<li><p>Zuul</p>
</li>
<li><p>Ribbon</p>
</li>
<li><p>Feign</p>
</li>
</ul>
<p><strong>技术总监</strong>：不错，组件分析的不错，但是你的讲解比较官方，下面我们来一个一个的讲解一下我们经常使用的这些组件。</p>
<ol>
<li>Eureka</li>
</ol>
<p>Eureka 属于 Spring Cloud Netflix 下的组件之一，主要负责服务的注册与发现，何为注册与发现？</p>
<p>在刚刚我们分析的分布式中存在这一个问题，那就是订单服务与用户服务被独立了，那么他们怎么进行通信呢？比如在订单服务中获取用户的基础信息，这个时候我们需要怎么办？</p>
<p>如果按照上面的架构图，直接去数据库获取就可以了，因为服务虽然独立了，但是数据库还是共享的，所以直接查询数据库就能得到结果，如果我们将数据库也拆分了呢？这个时候我们该怎么办呢？</p>
<p>有人想到了，服务调用，服务调用是不是需要 IP 和端口才可以，那问题来了，对于订单服务来说，我怎么知道用户服务的 IP 和端口呢？在订单服务中写死吗？如果用户服务的端口发生改变了呢？</p>
<p>这个时候 Eureka 就出来了，他就是为了解决服务的通信问题，每个服务都可以将自己的信息注册到 Eureka 中，比如 IP、端口、服务名等信息，这个时候如果订单服务想要获取用户服务的信息，只需要去 Eureka 中获取即可。<br><img src="/2020/05/25/分布式的架构演进/b12.png" alt="Eureka"></p>
<p>这就是 Eureka 的主要功能，也是我们使用中的最值得注意的，他让服务之间的通信变得更加的简单灵活。</p>
<ol start="2">
<li>Spring Cloud Config</li>
</ol>
<p>Spring Cloud Config 为分布式系统中的外部配置提供服务器和客户端支持。使用 Config Server，您可以在所有环境中管理应用程序的外部属性。</p>
<p>客户端和服务器上的概念映射与 Spring Environment 和 PropertySource 抽象相同，因此它们与 Spring 应用程序非常契合，但可以与任何以任何语言运行的应用程序一起使用。</p>
<p>随着应用程序通过从开发人员到测试和生产的部署流程，您可以管理这些环境之间的配置，并确定应用程序具有迁移时需要运行的一切。</p>
<p>服务器存储后端的默认实现使用 Git，因此它轻松支持标签版本的配置环境，以及可以访问用于管理内容的各种工具。可以轻松添加替代实现，并使用 Spring 配置将其插入。</p>
<p>简单点来说集中来管理每个服务的配置文件，将配置文件与服务分离，这么多的目的是什么？</p>
<p>举个简单的栗子，我们配置文件中肯定会存在数据库的连接信息，Redis 的连接信息，我们的环境是多样的，有开发环境、测试环境、预发布环境、生产环境。</p>
<p>每个环境对应的连接信息肯定是不相同的，难道每次发布的时候都要去修改一下服务中的配置文件？</p>
<p>我能不能将这些变动较大的配置集中管理，不同环境的管理者分别对他们进行修改，就不需要再服务中做改动了，Config 就做到了。<br><img src="/2020/05/25/分布式的架构演进/b13.png" alt="Spring Cloud Config"></p>
<p>这就是 Config 的大致架构，所有的配置文件都集中交给 Config 管理，拿 Config 怎么管理这些配置文件呢？</p>
<p>你可以将每个环境的配置文件存放再一个位置，比如 Lgitlab、SVN、本地等等，Config 会根据根据你设置的位置读取配置文件进行管理，然后其他服务启动的时候直接到 Config 配置中心获取对应的配置文件即可。</p>
<p>这样开发人员只需要关注 -dev 的配置文件，测试人员只需要关注 -test 的配置文件，完全和服务解耦，你值得拥有。</p>
<ol start="3">
<li>Netflix Zuul（网关）</li>
</ol>
<p>这个时候技术总监突然提了一个问题，他说：既然我们将一个服务拆分成了很多微服务，那岂不是要暴漏很多接口给浏览器？这样会不会造成安全隐患呢？有谁可以来说说这个问题。</p>
<p><strong>同事 A</strong>：我们可以通过 Nginx 反向代理，开放二级域名，然后将域名映射到微服务中。</p>
<p><strong>技术总监</strong>：这个方案也可以，也是不需要使用的，但不是最完善的，还有没有更好的方案？</p>
<p>Nginx 虽然把端口隐藏了，如果我们的服务都是需要一些权限的校验，Nginx 是无法替我们完成的，这个时候我们难道要在每个服务中都添加一套权限校验的逻辑吗？</p>
<p><strong>同事 B</strong>：我觉得我们可以使用网关，它既可以做分流转发，也可以做权限控制，使用 Nginx+网关，我觉得是比较好的一种方案，以下是网关 Zuul 的介绍。</p>
<p>路由在微服务体系结构的一个组成部分。例如，/可以映射到您的 Web 应用程序，/api/users 映射到用户服务，并将 /api/shop 映射到商店服务。Zuul 是 Netflix 的基于 JVM 的路由器和服务器端负载均衡器。</p>
<p>Netflix 使用 Zuul 进行以下操作：</p>
<ul>
<li><p>认证 -洞察</p>
</li>
<li><p>压力测试</p>
</li>
<li><p>金丝雀测试</p>
</li>
<li><p>动态路由</p>
</li>
<li><p>服务迁移</p>
</li>
<li><p>负载脱落</p>
</li>
<li><p>安全</p>
</li>
<li><p>静态响应处理</p>
</li>
<li><p>主动/主动流量管理</p>
</li>
</ul>
<p>我们在日常开发过程中并不会使用那么多，基本上就是认证、动态路由、安全等等，我画了一张关于网关的架构图，请看：<br><img src="/2020/05/25/分布式的架构演进/b14.png" alt="Netflix"></p>
<p><strong>技术总监</strong>：你们真的太优秀了，没错，Nginx 只能为我们做反向代理，不能做到权限认证，网关不但可以做到代理，也能做到权限认证、甚至还能做限流，所以我们要做分布式项目，少了他可不行。</p>
<ol start="4">
<li>Spring Cloud Bus<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">application.yml</span><br><span class="line">spring:</span><br><span class="line">  datasource:</span><br><span class="line">    username: root</span><br><span class="line">    password: 123456</span><br><span class="line">    url: jdbc:mysql://localhost:3306/test</span><br><span class="line">    driver-class-name: com.mysql.cj.jdbc.Driver</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p><strong>技术总监</strong>：比如上面这行配置大家都应该很熟悉，这是数据库的连接信息，如果它发生改变了怎么办呢？</p>
<p>我们都知道，服务启动的时候会去 Config 配置中心拉取配置信息，但是启动完成之后修改了配置文件我们应该怎么办呢，重启服务器吗？</p>
<p><strong>同事 C</strong>：我们可以通过 Spring Cloud Bus 来解决这个问题，Spring Cloud Bus 将轻量级消息代理链接到分布式系统的节点。然后可以将其用于广播状态更改（例如，配置更改）或其他管理指令。</p>
<p>该项目包括 AMQP 和 Kafka 经纪人实施。另外，在类路径上找到的任何 Spring Cloud Stream 绑定程序都可以作为传输工具使用。</p>
<p>这个需要我们有一点的 MQ 基础，不管是 RabbitMQ 还是 Kafka，都可以。</p>
<p>Bus 的基本原理就是：配置文件发生改变时，Config 会发出一个 MQ，告诉服务，配置文件发生改变了，并且还发出了改变的哪些信息，这个时候服务只需要根据 MQ 的信息做实时修改即可。</p>
<p>这是一个很简单的原理，理解起来可能也不会怎么难，画个图来理解一下：<br><img src="/2020/05/25/分布式的架构演进/b15.png" alt="Spring Cloud Bus"><br>大致流程就是这样，核心就是通过 MQ 机制实现不重启服务也能做到配置文件的改动，这方便了运维工程师，不用每次修改配置文件的时候都要去重启一遍服务的烦恼。</p>
<ol start="5">
<li>Feign<br><strong>技术总监</strong>：漂亮，和你们讲技术就是省事，刚才我们说到了注册中心可以方便服务于服务之间的通信，但是他们具体是怎么通信的你们有谁知道吗？</li>
</ol>
<p><strong>同事 D</strong>：由于我们刚刚讲的分布式架构是 Spring Cloud，所以这里推荐使用：Feign。</p>
<p>Feign 是一个声明式的 Web 服务客户端。这使得 Web 服务客户端的写入更加方便 要使用 Feign 创建一个界面并对其进行注释。</p>
<p>它具有可插入注释支持，包括 Feign 注释和 JAX-RS 注释。Feign 还支持可插拔编码器和解码器。</p>
<p>Spring Cloud 增加了对 Spring MVC 注释的支持，并使用 Spring Web 中默认使用的 HttpMessageConverters。</p>
<p>Spring Cloud 集成 Ribbon 和 Eureka 以在使用 Feign 时提供负载均衡的 HTTP 客户端。<br><img src="/2020/05/25/分布式的架构演进/b16.png" alt="Feign"></p>
<p>Feign 基于 Rest 风格，简单易懂，他的底层是对 HttpClient 进行了一层封装，使用十分方便。</p>
<p><strong>技术总监</strong>：不错，那如果服务的调用出现问题怎么办？比如调用超时，这个时候后我们如何处理？</p>
<ol start="6">
<li>Netflix Hystrix（熔断）</li>
</ol>
<p><strong>同事 E</strong>：这个 Cloud 也给我们考虑到了，我们只需要引入熔断即可。<br>Hystrix 支持回退的概念：当电路断开或出现错误时执行的默认代码路径。要为给定的@FeignClient 启用回退，请将 Fallback 属性设置为实现回退的类名。</p>
<p>我们可以改造一下刚刚的调用架构：<br><img src="/2020/05/25/分布式的架构演进/b17.png" alt="Netflix Hystrix（熔断）"><br>在这里我部署了一台备用服务器，当用户服务宕机了之后，订单服务进行远程调用的时候可以进入备用服务，这样就不会导致系统崩溃。</p>
<p><strong>技术总监</strong>：分布式大致架构差不多了，还有一些组件这里也不做做介绍了，使用的时候可以自己了解一下，不是很难。</p>
<p>我们接着往下说，我现在这里有一个需求，修改密码，修改密码需要发送短信验证码，发送短信在短信服务中，修改密码在用户服务中，这个时候就会出现服务调用。</p>
<p>而且我们知道，发送短信一般都是调用第三方的接口，那比如阿里的，既然牵扯到调用，那么就会存在很多不确定因素，比如网络问题。</p>
<p>假如，用户再点击发送短信验证码到时候用户服务调用短信服务，但是在短信服务中执行调用阿里的接口花费了很长的时间。</p>
<p>这个时候就会导致用户服务调短信服务超时，会返回给用户失败，但是，短信最后又发出去了，这种问题怎么解决呢？</p>
<h3 id="MQ（消息中间件）"><a href="#MQ（消息中间件）" class="headerlink" title="MQ（消息中间件）"></a>MQ（消息中间件）</h3><p><strong>同事 B</strong>：我们可以通过消息中间件来实现，使用异步讲给用户的反馈和发送短信分离，只要用户点了发送短信，直接返回成功，然后再启动发送验证码，60 秒重发一下，就算发送失败，用户还可以选择重新发送。<br><img src="/2020/05/25/分布式的架构演进/b18.png" alt="MQ"></p>
<blockquote>
<p>这里我曾经用mq来解决采集数据时出现的问题。当数据更新时，发送消息到队列。队列接受到消息后启动更新服务。免去了更新服务需要一直查询对比数据问题。</p>
</blockquote>
<h3 id="分布式事务"><a href="#分布式事务" class="headerlink" title="分布式事务"></a>分布式事务</h3><p><strong>技术总监</strong>：漂亮，MQ 不但可以解耦服务，它还可以用来削峰，提高系统的性能，是一个不错的选择。</p>
<p>既然我们使用了分布式架构，那么有一点是我们必须要注意的，那就是事务问题。</p>
<p>如果一个服务的修改依赖另外一个服务的操作，这个时候如果操作不慎，就会导致可怕的后果。</p>
<p>举个例子，两个服务：钱包服务（用于充值提现）、交易钱包服务（用于交易），我现在想从钱包服务中转 1000 元到交易钱包服务中，我们应该如何保证他们数据的一致性呢？</p>
<p><strong>同事 C</strong>：我这里有两种方案，第一种是通过 MQ 来保证一致性，另外一种就是通过分布式事务来确保一致性。</p>
<ol>
<li>MQ 确保一致性</li>
</ol>
<p>钱包服务分为三步：</p>
<ul>
<li><p>生成一个订单表，记录着转入转出的状态。</p>
</li>
<li><p>向 MQ 发送一条确认消息。</p>
</li>
<li><p>开启本地事务，执行转出操操作，并且提交事务。</p>
</li>
</ul>
<p>交易钱包服务：接收 MQ 的消息，进行转入操作（此操作需要 Ack 确认机制的支持）。</p>
<p>系统中会一直定时扫描订单中状态，没有成功的就做补偿机制或者重试机制，这个不是唯一要求。<br><img src="/2020/05/25/分布式的架构演进/b19.png" alt="MQ 确保一致性"><br>以上就是 MQ 确保分布式事务的大致思路，不是唯一，仅供参考。</p>
<ol start="2">
<li>Seata（分布式事务）<br>Seata 有三个基本组成部分：</li>
</ol>
<ul>
<li><p>事务协调器（TC）：维护全局事务和分支事务的状态，驱动全局提交或回滚。</p>
</li>
<li><p>事务管理器 TM：定义全局事务的范围：开始全局事务，提交或回滚全局事务。</p>
</li>
<li><p>资源管理器（RM）：管理分支事务正在处理的资源，与 TC 进行对话以注册分支事务并报告分支事务的状态，并驱动分支事务的提交或回滚。</p>
</li>
</ul>
<p><img src="/2020/05/25/分布式的架构演进/b20.png" alt="Seata"></p>
<p>Seata 管理的分布式事务的典型生命周期：</p>
<ul>
<li><p>TM 要求 TC 开始一项新的全球交易。TC 生成代表全局事务的 XID。</p>
</li>
<li><p>XID 通过微服务的调用链传播。</p>
</li>
<li><p>RM 将本地事务注册为 XID 到 TC 的相应全局事务的分支。</p>
</li>
<li><p>TM 要求 TC 提交或回滚 XID 的相应全局事务。</p>
</li>
<li><p>TC 驱动 XID 对应的全局事务下的所有分支事务以完成分支提交或回滚。</p>
</li>
</ul>
<h3 id="完整的分布式架构"><a href="#完整的分布式架构" class="headerlink" title="完整的分布式架构"></a>完整的分布式架构</h3><p>完整的分布式架构如下图：</p>
<p><img src="/2020/05/25/分布式的架构演进/b21.png" alt=""></p>
<p>这就是一套分布式基本的架构，请求从浏览器发出，经过 Nginx 反向代理到 Zuul 网关。</p>
<p>网关经过权限校验、然后分别转发到对应的服务中，每个服务都有自己独立的数据库，如果需要跨库查询的时候就需要用到分布式的远程调用（Feign）。</p>
<p>虽然这里我将服务拆分了，但是有一点需要注意的是网关，网关承载着所有的请求，如果请求过大会发生什么呢？</p>
<p>服务宕机，所以一般情况下，网关都是集群部署，不止网关可以集群，其他的服务都可以做集群配置，比如：注册中心、Redis、MQ 等等都可以。</p>
<p>那我们将这个流程图再改良一下：<br><img src="/2020/05/25/分布式的架构演进/b22.png" alt=""></p>
<p>现在这套架构就是比较成熟的一套了，不管是性能还是稳定，都是杠杠的，技术选择性的会也开得差不多了，最后技术总监做了一个总结。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li><p>单体服务与分布式服务区别<br>单体服务与分布式服务对比如下图：<br><img src="/2020/05/25/分布式的架构演进/b10.png" alt="分布式架构与单体架构对比"></p>
</li>
<li><p>什么时候使用分布式/集群？<br>总结如下几点：</p>
</li>
</ol>
<ul>
<li>单机无法支持的时候。</li>
<li>想要更好的隔离性（功能与功能）。</li>
<li>想要更好用户体验的时候。</li>
<li>想要更好的扩展性。</li>
<li>想要更快的响应，更搞得吞吐量。</li>
</ul>
<ol start="3">
<li><p>使用分布式注意事项<br>虽然现在分布式技术已经十分成熟，但是里面的坑不是一点两点，比如：==如何保证分布式事务的一致性、如何保证服务调用的幂等性、如何保证消息的幂等性、如何设置熔断（服务的降级），如何保证服务的健壮性等等，==这些都是一直需要关注的问题，只有解决了这些问题，你的分布式架构才能真正的立于不败之地。</p>
</li>
<li><p>关于组件停更消息</p>
</li>
</ol>
<p>目前注册中心 Eureka、网关 Zuul，Feign 都相继停更了，停更不代表不能使用，只是除了 Bug 可能不会主动修复，所以这个时候我们可能就需要选择另外的组件了。</p>
<p>注册中心可以使用 Consul、Nacos，Zookeeper，网关则可以通过 Gateway 替换，OpenFeign 替换 Fiegn。</p>
<p>所以也没必要听到组件停更的消息就担心 Cloud 会不会凉，放心，它至少最近几年是不会凉的。</p>
<p><img src="/2020/05/25/分布式的架构演进/b23.png" alt=""></p>
]]></content>
      <tags>
        <tag>架构</tag>
      </tags>
  </entry>
  <entry>
    <title>单例模式的七种写法</title>
    <url>/2020/02/29/%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F%E7%9A%84%E4%B8%83%E7%A7%8D%E5%86%99%E6%B3%95/</url>
    <content><![CDATA[<h1 id="单例模式的七种写法"><a href="#单例模式的七种写法" class="headerlink" title="单例模式的七种写法"></a>单例模式的七种写法</h1><blockquote>
<p>在阅读公众号的时候看到了这篇文章，写的很好，网站的设计布局也很巧妙，记录一下<a href="http://www.hollischuang.com/archives/205" target="_blank" rel="noopener">http://www.hollischuang.com/archives/205</a> 。</p>
</blockquote>
<a id="more"></a>
<h2 id="七种写法"><a href="#七种写法" class="headerlink" title="七种写法"></a>七种写法</h2><p>很喜欢他的鼠标停留注释，但是还是写markdown快一些。</p>
<p><strong>懒汉：</strong>类初始化的时候并不创建，要用的时候才建。</p>
<p><strong>饿汉：</strong>在类加载的时候就创建好一个对象。</p>
<p><strong>lazy loading：</strong>只有在需要的时候才会去加载必要的数据，这样可以避免即时加载所带来的不必要的系统开销。</p>
<h3 id="第一种（懒汉，线程不安全）："><a href="#第一种（懒汉，线程不安全）：" class="headerlink" title="第一种（懒汉，线程不安全）："></a>第一种（懒汉，线程不安全）：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance;  </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span> <span class="params">()</span></span>&#123;&#125;  </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;  </span><br><span class="line">        instance = <span class="keyword">new</span> Singleton();  </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="keyword">return</span> instance;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="第二种（懒汉，线程安全）："><a href="#第二种（懒汉，线程安全）：" class="headerlink" title="第二种（懒汉，线程安全）："></a>第二种（懒汉，线程安全）：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ublic <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance;  </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span> <span class="params">()</span></span>&#123;&#125;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">synchronized</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;  </span><br><span class="line">        instance = <span class="keyword">new</span> Singleton();  </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="keyword">return</span> instance;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这种写法能够在多线程中很好的工作，而且看起来它也具备很好的lazy loading，但是，遗憾的是，效率很低，99%情况下不需要同步。</p>
<h3 id="第三种（饿汉）："><a href="#第三种（饿汉）：" class="headerlink" title="第三种（饿汉）："></a>第三种（饿汉）：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">new</span> Singleton();  </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span> <span class="params">()</span></span>&#123;&#125;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">return</span> instance;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这种方式基于classloder机制，在<a href="http://www.hollischuang.com/archives/197" target="_blank" rel="noopener">深度分析Java的ClassLoader机制（源码级别）</a>和<a href="http://www.hollischuang.com/archives/201" target="_blank" rel="noopener">Java类的加载、链接和初始化</a>两个文章中有关于CLassload而机制的线程安全问题的介绍，避免了多线程的同步问题，不过，instance在类装载时就实例化，虽然导致类装载的原因有很多种，在单例模式中大多数都是调用getInstance方法， 但是也不能确定有其他的方式（或者其他的静态方法）导致类装载，这时候初始化instance显然没有达到lazy loading的效果。</p>
<h3 id="第四种（饿汉，变种）："><a href="#第四种（饿汉，变种）：" class="headerlink" title="第四种（饿汉，变种）："></a>第四种（饿汉，变种）：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> Singleton instance = <span class="keyword">null</span>;  </span><br><span class="line">    <span class="keyword">static</span> &#123;  </span><br><span class="line">    instance = <span class="keyword">new</span> Singleton();  </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span> <span class="params">()</span></span>&#123;&#125;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.instance;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>表面上看起来差别挺大，其实更第三种方式差不多，都是在类初始化即实例化instance。</p>
<h3 id="第五种（静态内部类）："><a href="#第五种（静态内部类）：" class="headerlink" title="第五种（静态内部类）："></a>第五种（静态内部类）：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonHolder</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Singleton INSTANCE = <span class="keyword">new</span> Singleton();  </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span> <span class="params">()</span></span>&#123;&#125;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">return</span> SingletonHolder.INSTANCE;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这种方式同样利用了classloder的机制来保证初始化instance时只有一个线程，它跟第三种和第四种方式不同的是（很细微的差别）：第三种和第四种方式是只要Singleton类被装载了，那么instance就会被实例化（没有达到lazy loading效果），而这种方式是Singleton类被装载了，instance不一定被初始化。因为SingletonHolder类没有被主动使用，只有显示通过调用getInstance方法时，才会显示装载SingletonHolder类，从而实例化instance。想象一下，如果实例化instance很消耗资源，我想让他延迟加载，另外一方面，我不希望在Singleton类加载时就实例化，因为我不能确保Singleton类还可能在其他的地方被主动使用从而被加载，那么这个时候实例化instance显然是不合适的。这个时候，这种方式相比第三和第四种方式就显得很合理。</p>
<h3 id="第六种（枚举）："><a href="#第六种（枚举）：" class="headerlink" title="第六种（枚举）："></a>第六种（枚举）：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Singleton &#123;  </span><br><span class="line">    INSTANCE;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">whateverMethod</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这种方式是Effective Java作者Josh Bloch 提倡的方式，它不仅能避免多线程同步问题，而且还能防止反序列化重新创建新的对象，可谓是很坚强的壁垒啊，在《深度分析Java的枚举类型—-枚举的线程安全性及序列化问题》（此文章在我转载的时候就已经404了）中有详细介绍枚举的线程安全问题和序列化问题。不过，个人认为由于1.5中才加入enum特性，用这种方式写不免让人感觉生疏，在实际工作中，我也很少看见有人这么写过。</p>
<h3 id="第七种（双重校验锁）："><a href="#第七种（双重校验锁）：" class="headerlink" title="第七种（双重校验锁）："></a>第七种（双重校验锁）：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">static</span> Singleton singleton;  </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span> <span class="params">()</span></span>&#123;&#125;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">if</span> (singleton == <span class="keyword">null</span>) &#123;  </span><br><span class="line">        <span class="keyword">synchronized</span> (Singleton.class) &#123;  </span><br><span class="line">        <span class="keyword">if</span> (singleton == <span class="keyword">null</span>) &#123;  </span><br><span class="line">            singleton = <span class="keyword">new</span> Singleton();  </span><br><span class="line">        &#125;  </span><br><span class="line">        &#125;  </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="keyword">return</span> singleton;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>有两个问题需要注意：</p>
<p>1.如果单例由不同的类装载器装入，那便有可能存在多个单例类的实例。假定不是远端存取，例如一些servlet容器对每个servlet使用完全不同的类装载器，这样的话如果有两个servlet访问一个单例类，它们就都会有各自的实例。</p>
<p>2.如果Singleton实现了java.io.Serializable接口，那么这个类的实例就可能被序列化和复原。不管怎样，如果你序列化一个单例类的对象，接下来复原多个那个对象，那你就会有多个单例类的实例。</p>
<p>对第一个问题修复的办法是：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> Class <span class="title">getClass</span><span class="params">(String classname)</span>  </span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ClassNotFoundException </span>&#123;  </span><br><span class="line">    ClassLoader classLoader = Thread.currentThread().getContextClassLoader();</span><br><span class="line">    <span class="keyword">if</span>(classLoader == <span class="keyword">null</span>)     </span><br><span class="line">          classLoader = Singleton.class.getClassLoader();     </span><br><span class="line">          <span class="keyword">return</span> (classLoader.loadClass(classname));     </span><br><span class="line">       &#125;     </span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>对第二个问题修复的办法是：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> <span class="keyword">implements</span> <span class="title">java</span>.<span class="title">io</span>.<span class="title">Serializable</span> </span>&#123;     </span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> Singleton INSTANCE = <span class="keyword">new</span> Singleton();     </span><br><span class="line"></span><br><span class="line">   <span class="function"><span class="keyword">protected</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;     </span><br><span class="line"></span><br><span class="line">   &#125;     </span><br><span class="line">   <span class="function"><span class="keyword">private</span> Object <span class="title">readResolve</span><span class="params">()</span> </span>&#123;     </span><br><span class="line">            <span class="keyword">return</span> INSTANCE;     </span><br><span class="line">      &#125;    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>对我来说，我比较喜欢第三种和第五种方式，简单易懂，而且在JVM层实现了线程安全（如果不是多个类加载器环境），一般的情况下，我会使用第三种方式，只有在要明确实现lazy loading效果时才会使用第五种方式，另外，如果涉及到反序列化创建对象时我会试着使用枚举的方式来实现单例，不过，我一直会保证我的程序是线程安全的，而且我永远不会使用第一种和第二种方式，如果有其他特殊的需求，我可能会使用第七种方式，毕竟，JDK1.5已经没有双重检查锁定的问题了。（此处的我是指原文作者）</p>
<p>不过一般来说，第一种不算单例，第四种和第三种就是一种，如果算的话，第五种也可以分开写了。所以说，一般单例都是五种写法。懒汉，恶汉，双重校验锁，枚举和静态内部类。</p>
<h3 id="本人总结"><a href="#本人总结" class="headerlink" title="本人总结"></a>本人总结</h3><p>算是记录设计模式的一个开端，平时只是机械的在用，现在要逐渐的学习。</p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>反序列化错误</title>
    <url>/2019/09/27/%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96%E9%94%99%E8%AF%AF/</url>
    <content><![CDATA[<h1 id="反序列化错误：could-not-deserialize"><a href="#反序列化错误：could-not-deserialize" class="headerlink" title="反序列化错误：could not deserialize"></a>反序列化错误：could not deserialize</h1><blockquote>
<p>每个实体类都加入了Serializable，然后类与类之间有表关联。</p>
</blockquote>
<a id="more"></a>
<h2 id="起源"><a href="#起源" class="headerlink" title="起源"></a>起源</h2><p>一个实体类和另一个实体类的关系为：@OneToMany。在用JPA的findAll方法时，会出现这个错误：<br><strong>org.hibernate.type.SerializationException: could not deserialize</strong><br><strong>java.io.StreamCorruptedException: invalid stream header: 00000000</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">at java.io.ObjectInputStream.readStreamHeader(ObjectInputStream.java:862) ~[na:1.8.0_161]</span><br><span class="line">	at java.io.ObjectInputStream.&lt;init&gt;(ObjectInputStream.java:354) ~[na:1.8.0_161]</span><br><span class="line">	at org.hibernate.internal.util.SerializationHelper$CustomObjectInputStream.&lt;init&gt;(SerializationHelper.java:309) ~[hibernate-core-5.3.11.Final.jar:5.3.11.Final]</span><br><span class="line">	at org.hibernate.internal.util.SerializationHelper$CustomObjectInputStream.&lt;init&gt;(SerializationHelper.java:299) ~[hibernate-core-5.3.11.Final.jar:5.3.11.Final]</span><br><span class="line">	at org.hibernate.internal.util.SerializationHelper.doDeserialize(SerializationHelper.java:218) ~[hibernate-core-5.3.11.Final.jar:5.3.11.Final]</span><br><span class="line">	... 162 common frames omitted</span><br></pre></td></tr></table></figure></p>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><ol>
<li><p>首先检查是否都加上了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后检查是否在加上这个属性之前往表里写入了数据，如果写入了就把数据删除重新写入一下试试。</p>
</li>
<li><p>检查配置文件中的JPA属性</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">jpa:</span><br><span class="line">database-platform: org.hibernate.spatial.dialect.mysql.MySQL8SpatialDialect</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h2><p>在传递rabbitmq的时候出现了一个类似的情况。当我在数据采集项目里发送一个类的集合到MQ队列，在前端接口项目中去接受的时候报了反序列化失败。将两个类的serialVersionUID调成一致的就可以正常接受了。</p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（上）</title>
    <url>/2020/07/04/%E5%9C%A8Java%E9%A1%B9%E7%9B%AE%E4%B8%AD%E6%89%93%E5%8D%B0%E9%94%99%E8%AF%AF%E6%97%A5%E5%BF%97%E7%9A%84%E6%AD%A3%E7%A1%AE%E5%A7%BF%E5%8A%BF%EF%BC%8C%E6%8E%92%E6%9F%A5%E9%97%AE%E9%A2%98%E6%9B%B4%E6%96%B9%E4%BE%BF%EF%BC%8C%E9%9D%9E%E5%B8%B8%E5%AE%9E%E7%94%A8%EF%BC%88%E4%B8%8A%EF%BC%89/</url>
    <content><![CDATA[<h1 id="在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（上）"><a href="#在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（上）" class="headerlink" title="在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（上）"></a>在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（上）</h1><blockquote>
<p>在看公众号的时候发现的，拆成了两篇文章。阅读起来方便一点<br>作者：琴水玉<br>cnblogs.com/lovesqcc/p/4319594.html</p>
</blockquote>
<a id="more"></a>
<p>在程序中打错误日志的主要目标是为更好地排查问题和解决问题提供重要线索和指导。但是在实际中打的错误日志内容和格式变化多样，错误提示上可能残缺不全、没有相关背景、不明其义，使得排查解决问题成为非常不方便或者耗时的操作。</p>
<p>而实际上，如果编程的时候稍加用心，就会减少排查问题的很多无用功。在阐述如何编写有效的错误日志之前，了解错误是怎么产生的， 非常重要。</p>
<h2 id="错误是如何炼成的"><a href="#错误是如何炼成的" class="headerlink" title="错误是如何炼成的"></a>错误是如何炼成的</h2><p>对于当前系统来说， 错误的产生由三个地方引入：</p>
<h3 id="一、上层系统引入的非法参数。"><a href="#一、上层系统引入的非法参数。" class="headerlink" title="一、上层系统引入的非法参数。"></a>一、上层系统引入的非法参数。</h3><p>对于非法参数引入的错误， 可以通过参数校验和前置条件校验来截获错误；</p>
<h3 id="二、与下层系统交互产生的错误。与下层交互产生的错误，-有两种："><a href="#二、与下层系统交互产生的错误。与下层交互产生的错误，-有两种：" class="headerlink" title="二、与下层系统交互产生的错误。与下层交互产生的错误， 有两种："></a>二、与下层系统交互产生的错误。与下层交互产生的错误， 有两种：</h3><ul>
<li>下层系统处理成功了，但是通信出错了， 这样会导致子系统之间的数据不一致。对于这种情况，可以采用超时补偿机制，预先将任务记录下来，通过定时任务在后续将数据订正过来。</li>
<li>通信成功了，但是下层处理出错了。对于这种情况， 需要与下层开发人员沟通， 协调子系统之间的交互；需要根据下层返回的错误码和错误描述做适当的处理或给予合理的提示信息。无论哪一种情况， 都要假设下层系统可靠性一般， 做好出错的设计考虑。</li>
</ul>
<h3 id="三、本层系统处理出错。"><a href="#三、本层系统处理出错。" class="headerlink" title="三、本层系统处理出错。"></a>三、本层系统处理出错。</h3><p>本层系统产生错误的原因：</p>
<h4 id="1-疏忽导致"><a href="#1-疏忽导致" class="headerlink" title="1.疏忽导致"></a>1.疏忽导致</h4><p>疏忽是指程序员能力完全可避免此类错误但实际上没做到。比如将 &amp;&amp; 敲成了 &amp; ， == 敲成了 = ；边界错误， 复合逻辑判断错误等。疏忽要么是程序员注意力不够集中， 比如处于疲倦状态、加班通宵、边开会边写程序；要么是急着实现功能，没有顾及程序的健壮性等。</p>
<ul>
<li><strong>改进措施：使用代码静态分析工具，通过单元测试行覆盖可有效避免此类问题。</strong></li>
</ul>
<h4 id="2-错误与异常处理不够周全导致的"><a href="#2-错误与异常处理不够周全导致的" class="headerlink" title="2.错误与异常处理不够周全导致的"></a>2.错误与异常处理不够周全导致的</h4><p>比如输入问题。计算两个数相加， 不仅要考虑计算溢出问题， 还要考虑输入非法的情形。对于前者，可能通过了解、犯错或经验就可以避免，而对于后者，则必须加以限定，以使之处于我们的智商能够控制的范围内，比如使用正则表达式过滤掉不合法的输入。对于正则表达式必须进行测试。对于不合法输入， 要给出尽可能详细、易懂、友好的提示信息、原因及建议方案。</p>
<ul>
<li><strong>改进措施：尽可能周全地考虑各种错误情形和异常处理。</strong>在实现主流程之后，增加一个步骤：仔细推敲可能的各种错误和异常，返回合理错误码和错误描述。每个接口或模块都有效处理好自己的错误和异常，可有效避免因场景交互复杂导致的bug。</li>
</ul>
<blockquote>
<p>譬如，一个业务用例由场景A.B.C交互完成。实际执行A.B成功了，C失败了，这时B需要根据C返回合理的代码和消息进行回滚并返回给A合理的代码和消息，A根据B的返回进行回滚，并返回给客户端合理的代码和消息。这是一种分段回滚的机制，要求每个场景都必须考虑异常情况下的回滚。</p>
</blockquote>
<h4 id="3-逻辑耦合紧密导致"><a href="#3-逻辑耦合紧密导致" class="headerlink" title="3.逻辑耦合紧密导致"></a>3.逻辑耦合紧密导致</h4><p>由于业务逻辑耦合紧密， 随着软件产品一步步发展， 各种逻辑关系错综复杂， 难以看到全局状况，导致局部修改影响波及到全局范围，造成不可预知的问题。</p>
<ul>
<li><strong>改进措施：编写短函数和短方法，每个函数或方法最好不超过 50 行。</strong> 编写无状态函数和方法， 只读全局状态， 相同的前提条件总是会输出相同的结果， 不会依赖外部状态而变更自己的行为；定义合理的结构、 接口和逻辑段， 使接口之间的交互尽可能正交、低耦合；对于服务层， 尽可能提供简单、正交的接口；持续重构， 保持应用模块化和松耦合， 理清逻辑依赖关系。</li>
</ul>
<p>对于有大量业务接口相互影响的情况， 必须整理各个业务接口的逻辑流程及相互依赖关系， 从整体上进行优化；对于有大量状态的实体， 也需要梳理相关的业务接口， 整理状态之间的转换关系。</p>
<h4 id="4-算法不正确导致。"><a href="#4-算法不正确导致。" class="headerlink" title="4.算法不正确导致。"></a>4.算法不正确导致。</h4><ul>
<li><strong>改进措施：首先将算法从应用中分离出来。</strong> 若算法有多种实现， 可以通过交叉校验的单元测试找出来， 比如排序操作；如果算法具有可逆性质， 可以通过可逆校验的单元测试找出来， 比如加密解密操作。</li>
</ul>
<h4 id="5-相同类型的参数，传入顺序错误导致。"><a href="#5-相同类型的参数，传入顺序错误导致。" class="headerlink" title="5.相同类型的参数，传入顺序错误导致。"></a>5.相同类型的参数，传入顺序错误导致。</h4><p>比如，modifyFlow(int rx, int tx), 实际调用为 modifyFlow(tx,rx)</p>
<ul>
<li><strong>改进措施：尽可能使类型具体化。</strong>该用浮点数就用浮点数， 该用字符串就用字符串， 该用具体对象类型就用具体对象类型；相同类型的参数尽可能错开；如果上述都无法满足， 就必须通过接口测试来验证， 接口参数值务必是不同的。</li>
</ul>
<h4 id="6-空指针异常。"><a href="#6-空指针异常。" class="headerlink" title="6.空指针异常。"></a>6.空指针异常。</h4><p>空指针异常通常是对象没有正确初始化， 或者使用对象之前没有对对象是否非空做检测。</p>
<ul>
<li><strong>改进措施：对于配置对象， 检测其是否成功初始化；对于普通对象， 获取到实体对象使用之前， 检测是否非空。</strong></li>
</ul>
<h4 id="7-网络通信错误。"><a href="#7-网络通信错误。" class="headerlink" title="7.网络通信错误。"></a>7.网络通信错误。</h4><p>网络通信错误通常是因为网络延迟、阻塞或不通导致的错误。网络通信错误通常是小概率事件， 但小概率事件很可能会导致大面积的故障、 难以复现的BUG。</p>
<ul>
<li><strong>改进措施：**</strong>在前一个子系统的结束点和后一个子系统的入口点分别打 INFO 日志。**通过两者的时间差提供一点线索。</li>
</ul>
<h4 id="8-事务与并发错误。"><a href="#8-事务与并发错误。" class="headerlink" title="8.事务与并发错误。"></a>8.事务与并发错误。</h4><p>事务与并发结合在一起， 很容易产生非常难以定位的错误。</p>
<ul>
<li><strong>改进措施：对于程序中的并发操作， 涉及到共享变量及重要状态修改的， 要加 INFO 日志。</strong></li>
</ul>
<h4 id="9-配置错误。"><a href="#9-配置错误。" class="headerlink" title="9.配置错误。"></a>9.配置错误。</h4><ul>
<li><strong>改进措施：在启动应用或启动相应配置时， 检测所有的配置项， 打印相应的INFO日志， 确保所有配置都加载成功。</strong></li>
</ul>
<h4 id="10-业务不熟悉导致的错误。"><a href="#10-业务不熟悉导致的错误。" class="headerlink" title="10.业务不熟悉导致的错误。"></a>10.业务不熟悉导致的错误。</h4><p>在中大型系统， 部分业务逻辑和业务交互都比较复杂， 整个的业务逻辑可能存在于多个开发同学的大脑里， 每个人的认识都不是完整的。这很容易导致业务编码错误。</p>
<ul>
<li><strong>改进措施：通过多人讨论和沟通， 设计正确的业务用例， 根据业务用例来编写和实现业务逻辑；</strong>最终的业务逻辑和业务用例必须完整存档；在业务接口中注明该业务的前置条件、处理逻辑、后置校验和注意事项；当业务变化时， 需要同步更新业务注释；代码REVIEW。<strong>业务注释是业务接口的重要文档， 对业务理解起着重要的缓存作用。</strong></li>
</ul>
<h4 id="11-设计问题导致的错误。"><a href="#11-设计问题导致的错误。" class="headerlink" title="11.设计问题导致的错误。"></a>11.设计问题导致的错误。</h4><p>比如同步串行方式会有性能、响应慢的问题， 而并发异步方式可以解决性能、响应慢的问题， 但会带来安全、正确性的隐患。异步方式会导致编程模型的改变， 新增异步消息推送和接收等新的问题。使用缓存能够提高性能， 但是又会存在缓存更新的问题。</p>
<ul>
<li><strong>改进措施：编写和仔细评审设计文档。</strong>设计文档必须阐述背景、需求、所满足的业务目标、要达到的业务性能指标、可能的影响、设计总体思路、详细方案、预见该方案的优缺点及可能的影响；通过测试和验收， 确保改设计方案确实满足业务目标和业务性能指标。</li>
</ul>
<h4 id="12-未知细节问题导致的错误。"><a href="#12-未知细节问题导致的错误。" class="headerlink" title="12.未知细节问题导致的错误。"></a>12.未知细节问题导致的错误。</h4><p>比如缓冲区溢出、 SQL 注入攻击。从功能上看是没有问题的， 但是从恶意使用上看， 是存在漏洞的。再比如， 选择 jackson 库做 JSON 字符串解析， 默认情况下， 当对象新增字段时会导致解析出错。必须在对象上加 @JsonIgnoreProperties(ignoreUnknown = true) 注解才能正确应对变化。如果选用其他 JSON 库就不一定有这个问题。</p>
<ul>
<li><strong>改进措施：一方面要通过经验积累， 另一方面， 考虑安全问题和例外情况， 选择成熟的经过严格测试的库。</strong></li>
</ul>
<h4 id="13-随时间变化而出现的bug。"><a href="#13-随时间变化而出现的bug。" class="headerlink" title="13.随时间变化而出现的bug。"></a>13.随时间变化而出现的bug。</h4><p>有些解决方案在过去看来是很不错的，但在当前或者未来的情景中可能变得笨拙甚至不中用，也是常见的事情。比如像加密解密算法， 在过去可能认为是完善的， 在破解之后就要慎重使用了。</p>
<ul>
<li><strong>改进措施：关注变化以及漏洞修复消息，及时修正过时的代码、库、行为。</strong></li>
</ul>
<h4 id="14-硬件相关的错误。"><a href="#14-硬件相关的错误。" class="headerlink" title="14.硬件相关的错误。"></a>14.硬件相关的错误。</h4><p>比如内存泄露， 存储空间不足， OutOfMemoryError 等。</p>
<ul>
<li><strong>改进措施：增加对应用系统的 CPU / 内存 / 网络等重要指标的性能监控。</strong></li>
</ul>
<h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4><p>系统出现的常见错误：</p>
<ul>
<li>实体在数据库中的记录不存在， 必须指明是哪个实体或实体标识；</li>
<li>实体配置不正确， 必须指明是哪个配置有问题，正确的配置应该是什么；</li>
<li>实体资源不满足条件， 必须指明当前资源是什么，资源要求是什么；</li>
<li>实体操作前置条件不满足， 必须指明需要满足什么前置条件，当前的状态是什么；</li>
<li>实体操作后置校验不满足， 必须指明需要满足什么后置校验， 当前的状态是什么；</li>
<li>性能问题导致超时， 必须指明是什么导致的性能问题，后续如何优化；</li>
<li>多个子系统交互通信出错导致之间的状态或数据不一致？</li>
</ul>
<p>一般难以定位的错误会出现在比较底层的地方。因为底层无法预知具体的业务场景， 给出的错误消息都是比较通用的。</p>
<p>这就要求在业务上层提供尽可能丰富的线索。错误的产生一定是多个系统或层次交互的过程中在某一层栈上不满足前置条件导致。在编程时， 在每一层栈中尽可能确保所有必须的前置条件满足，尽可能避免错误的参数传递到底层， 尽可能地将错误截获在业务层。</p>
<p><strong>大多数错误都是由多种原因组合产生。**</strong>但每一种错误必定有其原因。<strong>**在解决错误之后， 要深入分析错误是如何发生的， 如何避免这些错误再次发生。</strong>努力就能成功， 但是：反思才能进步 ！推荐：<a href="http://mp.weixin.qq.com/s?__biz=MzI4Njc5NjM1NQ==&amp;mid=2247491358&amp;idx=1&amp;sn=133ee8ee8696c4f39637018a34124fa2&amp;chksm=ebd62032dca1a92444417bfc077b44ce66f73cc844133dd9abf4f13f1b1d7e229afe4aac1924&amp;scene=21#wechat_redirect" target="_blank" rel="noopener">Java优雅的记录日志：log4j实战篇</a> </p>
]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（下）</title>
    <url>/2020/07/04/%E5%9C%A8Java%E9%A1%B9%E7%9B%AE%E4%B8%AD%E6%89%93%E5%8D%B0%E9%94%99%E8%AF%AF%E6%97%A5%E5%BF%97%E7%9A%84%E6%AD%A3%E7%A1%AE%E5%A7%BF%E5%8A%BF%EF%BC%8C%E6%8E%92%E6%9F%A5%E9%97%AE%E9%A2%98%E6%9B%B4%E6%96%B9%E4%BE%BF%EF%BC%8C%E9%9D%9E%E5%B8%B8%E5%AE%9E%E7%94%A8%EF%BC%88%E4%B8%8B%EF%BC%89/</url>
    <content><![CDATA[<h1 id="在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（下）"><a href="#在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（下）" class="headerlink" title="在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（下）"></a>在Java项目中打印错误日志的正确姿势，排查问题更方便，非常实用（下）</h1><blockquote>
<p>在看公众号的时候发现的，拆成了两篇文章。阅读起来方便一点<br>作者：琴水玉<br>cnblogs.com/lovesqcc/p/4319594.html</p>
</blockquote>
<a id="more"></a>
<h2 id="如何编写更容易排查问题的错误日志"><a href="#如何编写更容易排查问题的错误日志" class="headerlink" title="如何编写更容易排查问题的错误日志"></a>如何编写更容易排查问题的错误日志</h2><h3 id="打错误日志的基本原则："><a href="#打错误日志的基本原则：" class="headerlink" title="打错误日志的基本原则："></a>打错误日志的基本原则：</h3><ol>
<li><strong>尽可能完整。</strong>每一条错误日志都完整描述了：什么场景下发生了什么错误， 什么原因（或者哪些可能原因）， 如何解决（或解决提示）；</li>
<li><strong>尽可能具体。</strong>比如 NC 资源不足， 究竟具体指什么资源不足， 是否可以通过程序直接指明；通用错误，比如 VM NOT EXIST ， 要指明在什么场景下发生的，可能便于后续统计的工作。</li>
<li><strong>尽可能直接。</strong>最理想的错误日志应该让人在第一直觉下能够知道是什么原因导致，该怎么去解决，而不是还要通过若干步骤去查找真正的原因。</li>
<li><strong>将已有经验集成直接到系统中。</strong>所有已经解决过的问题及经验都要尽可能以友好的方式集成到系统中，给新进人员更好的提示，而不是埋藏在其他地方。</li>
<li><strong>排版要整洁有序， 格式统一化规范化。</strong>密密麻麻、随笔式的日志看着就揪心， 相当不友好， 也不便于排查问题。</li>
<li><strong>采用多个关键字唯一标识请求</strong>，突出显示关键字：时间、实体标识（比如vmname）、操作名称。</li>
</ol>
<h3 id="排查问题的基本步骤："><a href="#排查问题的基本步骤：" class="headerlink" title="排查问题的基本步骤："></a>排查问题的基本步骤：</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">登录到应用服务器 -&gt; 打开日志文件 -&gt; 定位到错误日志位置 -&gt; 根据错误日志的线索的指导去排查、确认问题和解决问题。</span><br></pre></td></tr></table></figure>
<p>其中：</p>
<ol>
<li><strong>从登陆到打开日志文件：</strong>由于应用服务器有多台， 要逐一登录上去查看实在不方便。 需要编写一个工具放在 AG 上直接在 AG 上查看所有服务器日志， 甚至直接筛选出所需要的错误日志。</li>
<li><strong>定位错误日志位置。</strong> 目前日志的排版密密麻麻，不易定位到错误日志。一般可以先采用”时间”来定位到错误日志的附近前面的地方， 然后使用 实体关键字 / 操作名称 组合来锁定错误日志地方。 根据 requestId 定位错误日志虽然比较符合传统，但是要先找到 requestId , 并且不具有描述性。最好能直接根据时间/内容关键字来定位错误日志位置。</li>
<li><strong>分析错误日志。</strong>错误日志的内容最好能够更加直接明了， 能够明确指明与当前要排查的问题特征是吻合的， 并且给出重要线索。</li>
</ol>
<p>通常， 程序错误日志的问题就是日志内容是针对当前代码情境才能理解，看上去简洁， 但总是写的不全， 半英文格式；一旦离开代码情境， 就很难知道究竟说的是什么， 非要让人思考一下或者去看看代码才能明白日志说的是什么含义。 这不是自己给自己罪受？</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">if ((storageType == StorageType.dfs1 || storageType == StorageType.dfs2)</span><br><span class="line">                &amp;&amp; (zone.hasStorageType(StorageType.io3) || zone.hasStorageType(StorageType.io4))) &#123;</span><br><span class="line">// 进入dfs1 和dfs2 在io3 io4 存储。</span><br><span class="line">&#125; else &#123;</span><br><span class="line">      log.info(&quot;zone storage type not support, zone: &quot; + zone.getZoneId() + &quot;, storageType: &quot;</span><br><span class="line">+ storageType.name());</span><br><span class="line">      throw new BizException(DeviceErrorCode.ZONE_STORAGE_TYPE_NOT_SUPPORT);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>zone 要支持什么 storage type 才是正确的?   Do Not Let Me Think !</p>
<p>错误日志应该做到： 即使离开代码情境，也能清晰地描述发生了什么。</p>
<p>此外，如果能够直接在错误日志中说明清楚原因， 在做巡检日志的时候也可以省些力气。<br>从某种意义上来说， 错误日志也可以是一种非常有益的文档，记录着各种不合法的运行用例。</p>
<h3 id="程序错误日志的内容可能存在如下问题："><a href="#程序错误日志的内容可能存在如下问题：" class="headerlink" title="程序错误日志的内容可能存在如下问题："></a>程序错误日志的内容可能存在如下问题：</h3><h4 id="1-错误日志没有指明错误参数和内容："><a href="#1-错误日志没有指明错误参数和内容：" class="headerlink" title="1. 错误日志没有指明错误参数和内容："></a>1. 错误日志没有指明错误参数和内容：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">catch(Exception ex)&#123;</span><br><span class="line">      log.error(&quot;control ip insert failed&quot;, ex);</span><br><span class="line">      return new ResultSet&lt;AddControlIpResponse&gt;(</span><br><span class="line">ControlIpErrorCode.ERROR_CONTROL_IP_INSERT_FAILURE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>没有指明插入失败的 control ip. 如果加上 control ip 关键字， 更容易搜索和锁定错误。</p>
<p>类似的还有：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;Get some errors when insert subnet and its IPs into database. Add subnet or IP failure.&quot;, e);</span><br></pre></td></tr></table></figure></p>
<p>没有指明是哪个 subnet 的它下属的哪些 IP. 值得注意的是， 要指明这些要额外做一些事情， 可能会稍微影响性能。这时候需要权衡性能和可调试性。</p>
<p><strong>解决方案：</strong>使用 String.format(“Some msg to ErrorObj: %s”, errobj) 方法指明错误参数及内容。<br>这通常要求对 DO 对象编写可读的 toString 方法。</p>
<h4 id="2-错误场景不明确："><a href="#2-错误场景不明确：" class="headerlink" title="2. 错误场景不明确："></a>2. 错误场景不明确：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;nc has exist, nc ip&quot; + request.getIp());</span><br></pre></td></tr></table></figure>
<p>在 createNc 中检测到 NC 已经存在报错。 但是日志上没有指明错误场景， 让人猜测，为什么会报 NC 已存在错误。</p>
<p>可以改为<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;nc has exist when want to create nc, please check nc parameters. Given nc ip: &quot; + request.getIp());</span><br><span class="line">log.error(&quot;[create nc] nc has exist, please check nc parameters. Given nc ip: &quot; + request.getIp());</span><br></pre></td></tr></table></figure></p>
<p>类似的还有：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;not all vm destroyed, nc id &quot; + request.getNcId());</span><br></pre></td></tr></table></figure></p>
<p>可以改为<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;[delete nc] some vms [%s] in the nc are not destroyed. nc id: %s&quot;, vmNames, request.getNcId());</span><br></pre></td></tr></table></figure></p>
<p><strong>解决方案：</strong> 错误消息加上 when 字句， 或者错误消息前加上 【接口名】,  指明错误场景，直接从错误日志就知道明白了。<br>一般能够知道 executor 的可以加上 【接口名】， service 加上 when 字句。</p>
<h4 id="3-内容不明确-或不明其义："><a href="#3-内容不明确-或不明其义：" class="headerlink" title="3. 内容不明确, 或不明其义："></a>3. 内容不明确, 或不明其义：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">if(aliMonitorReporter == null) &#123;</span><br><span class="line">        log.error(&quot;aliMonitorReporter is null!&quot;);</span><br><span class="line">&#125; else &#123;</span><br><span class="line">       aliMonitorReporter.attach(new ThreadPoolMonitor(namePrefix, asynTaskThreadPool.getThreadPoolExecutor()));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>改为<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;aliMonitorReporter is null, probably not initialized properly, please check configuration in file xxx.&quot;);</span><br></pre></td></tr></table></figure></p>
<p>类似的还有：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">if (diskWbps == null &amp;&amp; diskRbps == null &amp;&amp; diskWiops == null    &amp;&amp; diskRiops == null) &#123;</span><br><span class="line">      log.error(&quot;none of attribute is specified for modifying&quot;);</span><br><span class="line">      throw new BizException(DeviceErrorCode.NO_ATTRIBUTE_FOR_MODIFY);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><strong>解决方案：</strong> 更清晰贴切地描述错误内容。</p>
<h4 id="4-排查问题的引导内容不明确："><a href="#4-排查问题的引导内容不明确：" class="headerlink" title="4. 排查问题的引导内容不明确："></a>4. 排查问题的引导内容不明确：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;get gw group ip segment failed. zkPath: &quot; + LockResource.getGwGroupIpSegmnetLockPath(request.getGwGroupId()));</span><br></pre></td></tr></table></figure>
<p>zkPath ?  如何去排查这个问题？ 我该去找谁？ 到哪里去查找更具体的线索？</p>
<p><strong>解决方案：</strong> 加上相应的背景知识和引导排查措施。</p>
<h4 id="5-错误内容不够具体细致："><a href="#5-错误内容不够具体细致：" class="headerlink" title="5. 错误内容不够具体细致："></a>5. 错误内容不够具体细致：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">if (!ncResourceService.isNcResourceEnough(ncResourceDO,    vmResourceCondition)) &#123;</span><br><span class="line">      log.error(&quot;disk space is not enough at vm&apos;s nc, nc id:&quot; + vmDO.getNcId());</span><br><span class="line">      throw new BizException(ResourceErrorCode.ERROR_RESOURCE_NOT_ENOUGH);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>究竟是什么资源不够？ 目前剩余多少？ 现在需要多少？ 值得注意的是， 要指明这些要额外做一些事情， 可能会稍微影响性能。这时候需要权衡性能和可调试性。</p>
<p><strong>解决方案：</strong>  通过改进程序或程序技巧， 尽可能揭示出具体的差异所在， 减少人工比对的操作。</p>
<h4 id="6-半英文句式读起来不够清晰明白，需要思考来拼凑起完整的意思："><a href="#6-半英文句式读起来不够清晰明白，需要思考来拼凑起完整的意思：" class="headerlink" title="6. 半英文句式读起来不够清晰明白，需要思考来拼凑起完整的意思："></a>6. 半英文句式读起来不够清晰明白，需要思考来拼凑起完整的意思：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.warn(&quot;cache status conflict, device id &quot;+deviceDO.getId()+&quot; db status &quot;+deviceDO.getStatus() +&quot;, nc status &quot;+ status);</span><br></pre></td></tr></table></figure>
<p>改为<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.warn(String.format(&quot;[query cache status] device cache status conflicts between regiondb and nc, status of device &apos;%s&apos; in regiondb is %s , but is %s in nc.&quot;, deviceDO.getId(), deviceDO.getStatus(), status));</span><br></pre></td></tr></table></figure></p>
<p><strong>解决方案：</strong> 改为自然可读的英文句式。</p>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>错误日志格式可以为：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">log.error(&quot;[接口名或操作名] [Some Error Msg] happens. [params] [Probably Because]. [Probably need to do].&quot;);</span><br><span class="line"></span><br><span class="line">或者</span><br><span class="line"></span><br><span class="line">log.error(String.format(&quot;[接口名或操作名] [Some Error Msg] happens. [%s]. [Probably Because]. [Probably need to do].&quot;, params));</span><br><span class="line"></span><br><span class="line">或者</span><br><span class="line"></span><br><span class="line">log.error(&quot;[Some Error Msg] happens to 错误参数或内容 when [in some condition]. [Probably Because]. [Probably need to do].&quot;);</span><br><span class="line"></span><br><span class="line">或者</span><br><span class="line"></span><br><span class="line">log.error(String.format(&quot;[Some Error Msg] happens to %s when [in some condition]. [Probably Because]. [Probably need to do].&quot;, parameters));</span><br></pre></td></tr></table></figure></p>
<p>[Probably Reason]. [Probably need to do]. 在某些情况下可以省略； 在一些重要接口和场景下最好能说明一下。</p>
<p>每一条错误日志都是独立的，尽可能完整、具体、直接说明何种场景下发生了什么错误，由什么原因导致，要采用什么措施或步骤。</p>
<p><strong>问题：</strong></p>
<ol>
<li>String.format 的性能会影响打日志吗？ 一般来说， 错误日志应该是比较少的， 使用 String.format 的频度并不会太高，不会对应用和日志造成影响。</li>
<li>开发时间非常紧张时， 有时间去斟酌字句吗？ 建立一个标准化的内容格式，将内容往格式套，可以节省斟酌字句的时间。</li>
<li>什么时候使用 info, warn , error ?<br>info 用于打印程序应该出现的正常状态信息， 便于追踪定位；<br>warn 表明系统出现轻微的不合理但不影响运行和使用；<br>error 表明出现了系统错误和异常，无法正常完成目标操作。</li>
</ol>
<p>错误日志是排查问题的重要手段之一。 当我们编程实现一项功能时， 通常会考虑可能发生的各种错误及相应原因：<br>要排查出相应的原因， 就需要一些关键描述来定位原因。这就会形成三元组：<br>错误现象 -&gt; 错误关键描述 -&gt; 最终的错误原因。<br>需要针对每一种错误尽可能提供相应的错误关键描述，从而定位到相应的错误原因。<br>也就是说，编程的时候，要仔细思考， 哪些描述是非常有利于定位错误原因的， 尽可能将这些描述添加到错误日志中。</p>
<p>文中没有指出的问题或困难，  请提出你的建议。</p>
]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>在linux上安装mbgl-renderer</title>
    <url>/2020/10/10/%E5%9C%A8linux%E4%B8%8A%E5%AE%89%E8%A3%85mbgl-renderer/</url>
    <content><![CDATA[<h1 id="在linux上安装mbgl-renderer"><a href="#在linux上安装mbgl-renderer" class="headerlink" title="在linux上安装mbgl-renderer"></a>在linux上安装mbgl-renderer</h1><blockquote>
<p>docker 安装还是挺方便的。<br>这个软件包主要是用来 java 调用画图。基本上前端可以绘制出的界面，都可以绘制成图片保存。当然，我只是完成我需要的地图叠加。</p>
</blockquote>
<a id="more"></a>
<h2 id="mbgl-renderer-安装"><a href="#mbgl-renderer-安装" class="headerlink" title="mbgl-renderer 安装"></a>mbgl-renderer 安装</h2><p>这个是我们组大佬找到的一种方法。为了应对客户提出的想将看到的前端界面截图保存并做成word文档保存的需求。<br>文档在 github <a href="https://github.com/consbio/mbgl-renderer" target="_blank" rel="noopener">mbgl-renderer</a></p>
<ol>
<li><p>从Docker Hub中获取最新图像：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull consbio/mbgl-renderer:latest</span><br></pre></td></tr></table></figure>
</li>
<li><p>mbgl-server在端口8080的Docker容器中运行（并没有用这个）：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --rm -p 8080:80 consbio/mbgl-renderer</span><br></pre></td></tr></table></figure>
</li>
<li><p>将本地tile目录挂载到/app/tiles容器中，以在服务器或CLI中使用它们（用这个）：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --rm -p 9090:80 -v /root/mbgl:/app/tiles consbio/mbgl-renderer</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="调用绘图程序"><a href="#调用绘图程序" class="headerlink" title="调用绘图程序"></a>调用绘图程序</h2><p>调用代码<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 绘图</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> style 样式</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> bytes</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">byte</span>[] render(JSONObject style) &#123;</span><br><span class="line">        <span class="keyword">if</span> (style == <span class="keyword">null</span> || style.isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> HandleException(<span class="string">"绘图样式表为空"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Map&lt;String, Object&gt; renderParams = <span class="keyword">new</span> HashMap&lt;&gt;(<span class="number">6</span>);</span><br><span class="line">        renderParams.put(<span class="string">"ratio"</span>, <span class="number">2</span>);</span><br><span class="line">        renderParams.put(<span class="string">"width"</span>, <span class="number">1024</span>);</span><br><span class="line">        renderParams.put(<span class="string">"height"</span>, <span class="number">1024</span>);</span><br><span class="line">        renderParams.put(<span class="string">"style"</span>, style.toJSONString());</span><br><span class="line">        renderParams.put(<span class="string">"zoom"</span>, style.getDoubleValue(<span class="string">"zoom"</span>));</span><br><span class="line">        JSONArray centers = style.getJSONArray(<span class="string">"center"</span>);</span><br><span class="line">        renderParams.put(<span class="string">"center"</span>, centers.getDoubleValue(<span class="number">0</span>) + <span class="string">","</span> + centers.getDoubleValue(<span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> HttpUtil.createPost(RENDER_URL)</span><br><span class="line">            .form(renderParams)</span><br><span class="line">            .execute()</span><br><span class="line">            .bodyBytes();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>其中的 <code>JSONObject style</code>是一段前端绘制地图代码。<br>然后再将返回的 byte[] 绘制成图片保存即可<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 根据数据画图</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> bytes 数据</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> filepath 图片全路径，带文件名</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 是否成功</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">drawImage</span><span class="params">(<span class="keyword">byte</span>[] bytes, String filepath)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Path path = Paths.get(filepath);</span><br><span class="line">            FileUtil.mkParentDirs(path.toString());</span><br><span class="line">            <span class="comment">// 图片写入磁盘</span></span><br><span class="line">            Files.write(path, bytes);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            log.error(e.getMessage(), e);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h2><p>后来服务器关了一次机，再运行 docker 时报错<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Is the docker daemon running?</span><br></pre></td></tr></table></figure></p>
<p>启动 docker 即可<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">service docker start</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>mbgl-renderer</tag>
      </tags>
  </entry>
  <entry>
    <title>在windows7上安装docker和hbase镜像和hadoop镜像</title>
    <url>/2019/06/21/%E5%9C%A8windows7%E4%B8%8A%E5%AE%89%E8%A3%85docker%E5%92%8Chbase%E9%95%9C%E5%83%8F/</url>
    <content><![CDATA[<h1 id="在windows7上安装docker和hbase镜像和hadoop镜像"><a href="#在windows7上安装docker和hbase镜像和hadoop镜像" class="headerlink" title="在windows7上安装docker和hbase镜像和hadoop镜像"></a>在windows7上安装docker和hbase镜像和hadoop镜像</h1><blockquote>
<p>在之前的本机安装Hbase环境没有进展后，更换了docker镜像来搭建测试环境。<del>参考<a href="https://www.colabug.com/4772817.html" target="_blank" rel="noopener">基于 Docker 搭建 Mac 本地 HBase 环境</a></del>（原文网址已经404）</p>
</blockquote>
<a id="more"></a>
<h2 id="安装-Docker"><a href="#安装-Docker" class="headerlink" title="安装 Docker"></a>安装 Docker</h2><p>本地环境为windows7 64位系统，直接从官网<a href="https://docs.docker.com/toolbox/toolbox_install_windows/" target="_blank" rel="noopener">https://docs.docker.com/toolbox/toolbox_install_windows/</a> 上面安装<strong>Docker Toolbox</strong>。按照指示一直下一步即可完成安装。</p>
<h3 id="启动-Docker"><a href="#启动-Docker" class="headerlink" title="启动 Docker"></a>启动 Docker</h3><ul>
<li>第一次启动会默认安装一个default命名的虚拟机。因为需要阿里的镜像加速的缘故，我用<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker-machine stop default</span><br><span class="line">docker-machine rm default</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>这两个命令来删除了默认虚拟机。之后再根据阿里的镜像加速配置来操作</p>
<ul>
<li><p>创建一台安装有Docker环境的Linux虚拟机，指定机器名称为default，同时配置Docker加速器地址。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker-machine create --engine-registry-mirror=https://ms4x763y.mirror.aliyuncs.com -d virtualbox default</span><br></pre></td></tr></table></figure>
</li>
<li><p>查看机器的环境配置，并配置到本地，并通过Docker客户端访问Docker服务。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker-machine env default</span><br><span class="line">eval &quot;$(docker-machine env default)&quot;</span><br><span class="line">docker info</span><br></pre></td></tr></table></figure>
</li>
<li><p>因为我本地环境为windows7并且没有装Docker for Windows，所以忽略官网文档其他的操作配置。</p>
</li>
</ul>
<h2 id="安装-Hbase"><a href="#安装-Hbase" class="headerlink" title="安装 Hbase"></a>安装 Hbase</h2><ul>
<li><p>获取容器镜像</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull harisekhon/hbase</span><br></pre></td></tr></table></figure>
</li>
<li><p>启动容器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -d -h myhbase -p 2181:2181 -p 8080:8080 -p 8085:8085 -p 9090:9090 -p 9095:9095 -p 16000:16000 -p 16010:16010 -p 16020:16020 -p 16030:16030 --name hbase1.3.1 harisekhon/hbase</span><br></pre></td></tr></table></figure>
</li>
</ul>
<blockquote>
<ul>
<li>-d: 后台启动。</li>
<li>-h: 容器主机名，必须设置该项并配置 hosts，否则无法连通容器。</li>
<li>-p: 网络端口映射，这里只把要使用的端口（zookeeper端口、HBase Master端口、HBase RegionServer端口等）映射了出来，你可以根据自己需要进行端口映射。</li>
<li>–name: 容器别名。</li>
</ul>
</blockquote>
<p>添加说明：大多数的博客贴在进行到这里的时候都是映射端口为16201和16301，没有16020和16030。经过我查看<a href="http://192.168.99.100:16010/master-status" target="_blank" rel="noopener">http://192.168.99.100:16010/master-status</a> 页面发现我安装的最新hbase2.1.1版本默认启动的是16020和16030。这里特别注意</p>
<h3 id="进入容器"><a href="#进入容器" class="headerlink" title="进入容器"></a>进入容器</h3><ul>
<li>进入hbase命令行<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker exec -it hbase1.3.1 /bin/bash</span><br><span class="line">/hbase/bin/hbase shell</span><br><span class="line">hbase(main):001:0&gt; create &apos;t1&apos;, &#123;NAME =&gt; &apos;f1&apos;, VERSIONS =&gt; 1&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<blockquote>
<p>在执行完第一个命令进入hbase容器后，可以执行<strong>netstat -anp | grep 16000</strong> 来查看hbase绑定的ip 172.17.0.2</p>
</blockquote>
<ul>
<li>运行结果<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Created table t1</span><br><span class="line">Took 2.6696 seconds</span><br><span class="line">=&gt; Hbase::Table - t1</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>这样就完成了整个安装过程。</p>
<h2 id="安装hadoop（没有必要，上面的已经集成了）"><a href="#安装hadoop（没有必要，上面的已经集成了）" class="headerlink" title="安装hadoop（没有必要，上面的已经集成了）"></a>安装hadoop（没有必要，上面的已经集成了）</h2><ul>
<li><p>搜索hadoop镜像</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker search hadoop</span><br></pre></td></tr></table></figure>
</li>
<li><p>选择第一个镜像</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull sequenceiq/hadoop-docker</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>当命令行显示<strong>Status: Downloaded newer image for sequenceiq/hadoop-docker:latest</strong>时候开始下一步。</p>
<ul>
<li><p>启动镜像</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -it --name hadoop sequenceiq/hadoop-docker /etc/bootstrap.sh -bash</span><br></pre></td></tr></table></figure>
</li>
<li><p>测试运行:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd $HADOOP_PREFIX</span><br><span class="line"># run the mapreduce</span><br><span class="line">bin/hadoop jar share/hadoop/mapreduce/hadoop-mapreduce-examples-2.7.0.jar grep input output &apos;dfs[a-z.]+&apos;</span><br><span class="line"></span><br><span class="line"># check the output</span><br><span class="line">bin/hdfs dfs -cat output/*</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="删除容器"><a href="#删除容器" class="headerlink" title="删除容器"></a>删除容器</h2><p>使用以下命令删除单个或多个容器。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker rm &lt;CONTAINER ID|NAME&gt; &lt;CONTAINER ID|NAME&gt;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>在win上使用VMware安装虚拟机运行tippecanoe切片</title>
    <url>/2020/07/31/%E5%9C%A8win%E4%B8%8A%E4%BD%BF%E7%94%A8VMware%E5%AE%89%E8%A3%85%E8%99%9A%E6%8B%9F%E6%9C%BA%E8%BF%90%E8%A1%8Ctippecanoe%E5%88%87%E7%89%87/</url>
    <content><![CDATA[<h1 id="在win上使用VMware安装虚拟机运行tippecanoe切片"><a href="#在win上使用VMware安装虚拟机运行tippecanoe切片" class="headerlink" title="在win上使用VMware安装虚拟机运行tippecanoe切片"></a>在win上使用VMware安装虚拟机运行tippecanoe切片</h1><blockquote>
<p>记录自己安装 tippecanoe 的过程<br>tippecanoe 为生成本地矢量瓦片的开源工具，介绍文章在<a href="https://zhuanlan.zhihu.com/p/31185974" target="_blank" rel="noopener">Mapbox GL JS学习笔记四：开源工具生成本地矢量瓦片</a> 、<a href="https://zhuanlan.zhihu.com/p/31760283" target="_blank" rel="noopener">Mapbox GL JS学习笔记七：mb-util工具使用</a>。<br>步骤为使用 VMware 安装 Linux 虚拟机，将 tippecanoe 部署在虚拟机上。再将启动服务部署在虚拟机上，使用命令调用切片。用另外的 FoxGIS Server Lite 部署在 Windows 主机上来访问切片后的瓦片文件<br>参考文章：<a href="https://jingsam.github.io" target="_blank" rel="noopener">FoxGIS安装部署</a> 、<a href="https://zhuanlan.zhihu.com/p/32306317" target="_blank" rel="noopener">Mapbox GL JS 学习笔记八：tippecanoe在centos上安装以及使用补充说明</a><br>在安装过程中出现错误，无法继续在本机安装。</p>
</blockquote>
<a id="more"></a>
<h2 id="1-安装VMware虚拟机"><a href="#1-安装VMware虚拟机" class="headerlink" title="1.安装VMware虚拟机"></a>1.安装VMware虚拟机</h2><p>从官网（<a href="https://www.vmware.com/cn/products/workstation-pro/workstation-pro-evaluation.html" target="_blank" rel="noopener">试用 VMware Workstation Pro</a>）下载最新版 VMware 。</p>
<p>在<a href="http://mirrors.aliyun.com/centos/7/isos/x86_64/CentOS-7-x86_64-Minimal-2003.iso" target="_blank" rel="noopener">http://mirrors.aliyun.com/centos/7/isos/x86_64/CentOS-7-x86_64-Minimal-2003.iso</a> 下载好 iso 镜像文件。</p>
<p>一路下一步完成 VMware 的安装，创建新的虚拟机。使用 centos7 的镜像包完成安装。</p>
<h2 id="2-在centos上安装tippecanoe"><a href="#2-在centos上安装tippecanoe" class="headerlink" title="2.在centos上安装tippecanoe"></a>2.在centos上安装tippecanoe</h2><p>安装完后尝试 ping 114 ，发现报错<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">connect: Network is unreachable</span><br></pre></td></tr></table></figure></p>
<p>使用<a href="https://blog.csdn.net/yohjob/article/details/90724914" target="_blank" rel="noopener">https://blog.csdn.net/yohjob/article/details/90724914</a> 这个博客的方法解决了。</p>
<h3 id="2-1-解决centos系统网络不通问题"><a href="#2-1-解决centos系统网络不通问题" class="headerlink" title="2.1.解决centos系统网络不通问题"></a>2.1.解决centos系统网络不通问题</h3><ol>
<li><p>进入网卡配置文件所在目录：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd /etc/sysconfig/network-scripts/</span><br></pre></td></tr></table></figure>
</li>
<li><p>编辑网卡配置文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vi ifcfg-ens33</span><br></pre></td></tr></table></figure>
</li>
<li><p>将文件中的最后一行的ONBOOT=no 改为ONBOOT=yes</p>
</li>
<li><p>保存文件并退出vi，重启network</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">service network restart</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h3 id="2-2-安装VMtool"><a href="#2-2-安装VMtool" class="headerlink" title="2.2.安装VMtool"></a>2.2.安装VMtool</h3><p>这个工具安装一下还是很方便的，可以操作的更加流畅。</p>
<ol>
<li>选择 虚拟机 &gt;&gt; 客户机 &gt;&gt; 安装/升级vmwareTools</li>
<li><p>装载cd(挂载到media文件夹下)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mount /dev/cdrom /media</span><br><span class="line">提示mount:block device /dev/sr0 is write-protecter, mounting read-only</span><br><span class="line"></span><br><span class="line">#找到挂载好的压缩包</span><br><span class="line">cd VMware\ Tools/</span><br><span class="line">ls</span><br><span class="line">#将其复制到tmp中</span><br><span class="line">cp VMwareTools-9.0.0. (按Tab补全) /tmp</span><br><span class="line">cd /tmp</span><br><span class="line"></span><br><span class="line">#授权并解压</span><br><span class="line">chmod +x VMwareTools-8.6.0-425873.tar.gz</span><br><span class="line">tar zxf VMwareTools-8.6.0-425873.tar.gz</span><br><span class="line"></span><br><span class="line">#安装</span><br><span class="line">cd vmware-tools-distrib/</span><br><span class="line">./vmware-install.pl</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果出现<code>bash:./vmware-install.pl :/usr/bin/perl:bad interpreter:No such file or directory</code>，输入<code>yum groupinstall &quot;Perl Support&quot;</code>即可。</p>
</li>
<li>一路回车，出现重复现象时注意翻译一下后选择yes或no</li>
</ol>
<h3 id="2-3-安装tippecanoe"><a href="#2-3-安装tippecanoe" class="headerlink" title="2.3.安装tippecanoe"></a>2.3.安装tippecanoe</h3><ol>
<li><p>先更新依赖</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install gcc-c++</span><br><span class="line">yum install sqlite-devel</span><br><span class="line">yum install -y zlib zlib-devel</span><br></pre></td></tr></table></figure>
</li>
<li><p>挂载虚拟机<br>将虚拟机关机后，选择编辑虚拟机设置。<br><img src="/2020/07/31/在win上使用VMware安装虚拟机运行tippecanoe切片/b1.png" alt="b1.png"></p>
</li>
</ol>
<p>在虚拟机设置中选择选项-&gt;共享文件夹<br><img src="/2020/07/31/在win上使用VMware安装虚拟机运行tippecanoe切片/b2.png" alt="b2.png"></p>
<p>选择下一步<br><img src="/2020/07/31/在win上使用VMware安装虚拟机运行tippecanoe切片/b3.png" alt="b3.png"></p>
<p>填写文件夹路径<br><img src="/2020/07/31/在win上使用VMware安装虚拟机运行tippecanoe切片/b4.png" alt="b4.png"></p>
<p>完成<br><img src="/2020/07/31/在win上使用VMware安装虚拟机运行tippecanoe切片/b5.png" alt="b5.png"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/usr/bin/vmhgfs-fuse .host:/vmwareGeojson /mnt/hgfs -o subtype=vmhgfs-fuse,allow_other</span><br><span class="line">/usr/bin/vmhgfs-fuse .host:/vmwareGeojson /mnt/hgfs -o subtype=vmhgfs-fuse,allow_other</span><br></pre></td></tr></table></figure>
<ol start="3">
<li>出现问题<br>用 VMware 安装的 Centos7 进行到这里时无法共享文件夹，参考同事的方法用 virtualBox 安装的Centos7 无法开机。所以放弃这种方式运行。</li>
</ol>
]]></content>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>导入gradle-bin</title>
    <url>/2020/11/14/%E5%AF%BC%E5%85%A5gradle-bin/</url>
    <content><![CDATA[<h1 id="导入gradle-bin"><a href="#导入gradle-bin" class="headerlink" title="导入gradle-bin"></a>导入gradle-bin</h1><blockquote>
<p>简单的记录 gradle-bin 下载失败或者特别慢<br>Download <a href="https://services.gradle.org/distributions/gradle-6.8.2-bin.zip" target="_blank" rel="noopener">https://services.gradle.org/distributions/gradle-6.8.2-bin.zip</a> 失败</p>
</blockquote>
<a id="more"></a>
<h2 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h2><ul>
<li>首先看自己要导入的是哪个版本的包</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Download https://services.gradle.org/distributions/gradle-6.8.2-bin.zip</span><br></pre></td></tr></table></figure>
<ul>
<li>到官网 <a href="https://services.gradle.org/distributions/" target="_blank" rel="noopener">https://services.gradle.org/distributions/</a> 单独下载该版本的包</li>
<li><p>放到自己的 gradle 路径下，默认为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Users\ezio\.gradle\wrapper\dists\gradle-6.8.2-bin\5nlkemqlnno2amj7d1mfn69bt</span><br></pre></td></tr></table></figure>
</li>
<li><p>重新打开项目，会将压缩包解压。</p>
</li>
</ul>
]]></content>
      <tags>
        <tag>Gradle</tag>
      </tags>
  </entry>
  <entry>
    <title>小米note3解锁失败</title>
    <url>/2019/08/10/%E5%B0%8F%E7%B1%B3note3%E8%A7%A3%E9%94%81%E5%A4%B1%E8%B4%A5/</url>
    <content><![CDATA[<h1 id="小米note3解锁失败"><a href="#小米note3解锁失败" class="headerlink" title="小米note3解锁失败"></a>小米note3解锁失败</h1><p>在程序员每天充实的生活中，偶尔也会出一些意外。<br><a id="more"></a></p>
<h2 id="普通的一天"><a href="#普通的一天" class="headerlink" title="普通的一天"></a>普通的一天</h2><p>清晨起床，试图用指纹解锁手机。提示我72小时内需要用手势解锁一次，习惯性的画个圈圈却被告知密码错误。</p>
<p>瞬间开始怀疑自己是不是记错了密码，再次进行各种姿势的解锁，依旧是错误。</p>
<p>点击忘记密码，提示要删除所有个人信息，有点心疼。</p>
<p>无奈关机重启。哎，一下子就可以解锁了。我没记错密码，也没拿错手机。。。这种情况。。。。</p>
]]></content>
      <tags>
        <tag>日常</tag>
      </tags>
  </entry>
  <entry>
    <title>微软 Office Online Server 2016 服务安装部署</title>
    <url>/2020/04/25/%E5%BE%AE%E8%BD%AF%20Office%20Online%20Server%202016%20%E6%9C%8D%E5%8A%A1%E5%AE%89%E8%A3%85%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h1 id="微软-Office-Online-Server-2016-服务安装部署"><a href="#微软-Office-Online-Server-2016-服务安装部署" class="headerlink" title="微软 Office Online Server 2016 服务安装部署"></a>微软 Office Online Server 2016 服务安装部署</h1><blockquote>
<p>结合了几篇文章。每个文章都用了一部分。<br>一、 <a href="https://blog.csdn.net/jiaqu2177/article/details/81945692" target="_blank" rel="noopener">微软 Office Online Server 2016 服务安装部署 + wopi代码实现</a><br>二、<a href="https://www.netnr.com/home/list/114" target="_blank" rel="noopener">Office Online Server 在线编辑Office文档，安装部署</a><br>三、 <a href="https://www.netnr.com/doc/code/4964095842855914510/4935912187725186675" target="_blank" rel="noopener">https://www.netnr.com/doc/code/4964095842855914510/4935912187725186675</a><br>四、 <a href="https://blog.csdn.net/q386815991/article/details/81705128" target="_blank" rel="noopener">office online server2016详细安装步骤及问题总结</a></p>
</blockquote>
<a id="more"></a>
<h2 id="一、转换服务器部署"><a href="#一、转换服务器部署" class="headerlink" title="一、转换服务器部署"></a>一、转换服务器部署</h2><p>我在本机使用vmware打开两个虚拟机，将office online server 和wopi都安装在了转换服务器上，域服务器只提供了一个域。之前大佬曾经尝试过将东西都安装在一个服务器上，没有配置转换服务器。出现了word可以打开，但是没有图片。excel只能打开没有格式的空文件，ppt无法打开的情况。<br>有博客说：</p>
<blockquote>
<p>由于云服务器是克隆副本，加入域会报错：无法完成域加入，原因是试图加入的域的 SID 与本计算机的 SID 相同，解决方法，打开：windows/System32/Sysprep/Sysprep.exe，勾选 通用 重启</p>
</blockquote>
<h3 id="office-online资源"><a href="#office-online资源" class="headerlink" title="office online资源"></a>office online资源</h3><p>office online server 2016的安装包使用的是：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ed2k://|file|cn_office_online_server_last_updated_march_2017_x64_dvd_10245068.iso|730759168|DA70F58CB8FFAF37C02302F2501CE635|/</span><br></pre></td></tr></table></figure></p>
<p>补丁包没有用上，安装的时候报找不到版本。但还是把连接放出来：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https://download.microsoft.com/download/6/B/D/6BD1D664-1212-4AB2-9BE8-447731F2CA0E/wacserver2016-kb4011025-fullfile-x64-glb.exe</span><br></pre></td></tr></table></figure></p>
<p>语言包还是要装的：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://go.microsoft.com/fwlink/p/?LinkId=798136</span><br></pre></td></tr></table></figure></p>
<p>这些资源来自二博客的下图：<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b1.png" alt=""></p>
<h3 id="安装office-online"><a href="#安装office-online" class="headerlink" title="安装office online"></a>安装office online</h3><ol>
<li><p>运行 powershell， 输入命令（Windows Server 2012 R2版本）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Add-WindowsFeature Web-Server,Web-Mgmt-Tools,Web-Mgmt-Console,Web-WebServer,Web-Common-Http,Web-Default-Doc,Web-Static-Content,Web-Performance,Web-Stat-Compression,Web-Dyn-Compression,Web-Security,Web-Filtering,Web-Windows-Auth,Web-App-Dev,Web-Net-Ext45,Web-Asp-Net45,Web-ISAPI-Ext,Web-ISAPI-Filter,Web-Includes,InkandHandwritingServices,NET-Framework-Features,NET-Framework-Core,NET-HTTP-Activation,NET-Non-HTTP-Activ,NET-WCF-HTTP-Activation45,Windows-Identity-Foundation,Server-Media-Foundation</span><br></pre></td></tr></table></figure>
</li>
<li><p>配置环境，一定要按顺序安装<br>.NET Framework 4.5.2</p>
</li>
</ol>
<p><a href="https://go.microsoft.com/fwlink/p/?LinkId=510096" target="_blank" rel="noopener">https://go.microsoft.com/fwlink/p/?LinkId=510096</a></p>
<p>Visual C++ Redistributable Packages for Visual Studio 2013</p>
<p><a href="https://www.microsoft.com/download/details.aspx?id=40784" target="_blank" rel="noopener">https://www.microsoft.com/download/details.aspx?id=40784</a></p>
<p>Visual C++ Redistributable for Visual Studio 2015</p>
<p><a href="https://go.microsoft.com/fwlink/p/?LinkId=620071" target="_blank" rel="noopener">https://go.microsoft.com/fwlink/p/?LinkId=620071</a></p>
<p>Microsoft.IdentityModel.Extention.dll</p>
<p><a href="https://go.microsoft.com/fwlink/p/?LinkId=620072" target="_blank" rel="noopener">https://go.microsoft.com/fwlink/p/?LinkId=620072</a></p>
<ol start="3">
<li>将下载好的office online server安装包解压好，并点击setup.exe开始安装。一路下一步安装就行。</li>
<li>注意事项<ul>
<li>不要在装SQL Server等服务上运行</li>
<li>不要在端口 80、443 或 809 上安装依赖 Web 服务器 (IIS) 角色的任何服务或角色</li>
<li>不要安装任何版本的 Office</li>
<li>不要在域控制器上安装 Office Online Server</li>
<li>简而言之：一台干净的服务器</li>
</ul>
</li>
</ol>
<h3 id="配置启动office-online"><a href="#配置启动office-online" class="headerlink" title="配置启动office online"></a>配置启动office online</h3><p>打开安装好office online的转换服务器，打开powerShell<br>启动服务场<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Import-Module OfficeWebApps</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>如果报错，需要重启</p>
</blockquote>
<p>部署服务器场:<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">New-OfficeWebAppsFarm -InternalURL “http://xx.domin.com” -ExternalUrl “http://192.168.37.138” -AllowHttp –EditingEnabled</span><br></pre></td></tr></table></figure></p>
<p>–InternalURL内部访问地址，一般是<a href="http://office主机名.AD域控地址(如下图)；" target="_blank" rel="noopener">http://office主机名.AD域控地址(如下图)；</a> 全名的查看是在转换服务器的系统属性，计算机名里。</p>
<p>–AllowHttp 是否允许http访问；</p>
<p>–ExternalUrl 外部访问地址，一般是服务器的ip地址；</p>
<p>–EditingEnabled 允许编辑office。</p>
<blockquote>
<p>请以域管理账号登录</p>
</blockquote>
<p>出现如下提示即部署成功<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b2.png" alt=""></p>
<p>这个时候用可以看下有没有启动起来<br>在office online 的浏览器 输入:<a href="http://win-5ldi2svjqoj.test.com/hosting/discovery" target="_blank" rel="noopener">http://win-5ldi2svjqoj.test.com/hosting/discovery</a> 得到以下的结果就ok 了<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b3.png" alt=""><br>当然通过ip地址访问也是ok,<a href="http://192.168.37.138/hosting/discovery" target="_blank" rel="noopener">http://192.168.37.138/hosting/discovery</a><br>当然这两个地址在域控服务器上面访问 也是ok 的</p>
<h3 id="额外配置"><a href="#额外配置" class="headerlink" title="额外配置"></a>额外配置</h3><p>office online的日志地址是<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b5.png" alt=""></p>
<p>安装后的office online server 对大文件会有限制，所以需要配置才能进行访问，具体配置路径如下<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b6.png" alt=""><br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b7.png" alt=""><br>将上面两个文件夹中的settings文件进行修改，在其中填入并保存<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">OpenFromUrlMaxFileSizeInKBytes=(System.Int32)512000</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b8.png" alt=""><br>配置完成后打开CMD命令，输入services.msc打开服务，并找到office online服务。重启即可</p>
<h2 id="安装wopi项目"><a href="#安装wopi项目" class="headerlink" title="安装wopi项目"></a>安装wopi项目</h2><p>本人使用的是<a href="https://github.com/netnr/WopiHost" target="_blank" rel="noopener">https://github.com/netnr/WopiHost</a> 。<br>下载压缩包，解压到任意文件夹中。<br>服务器管理器 -&gt; 工具 -&gt; （iis）管理器 -&gt; 添加了网站。<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b4.png" alt=""><br>图中的接口配置为1080<br>配置好后尝试使用<br><a href="http://192.168.37.138:1080/wopi/files/upload/word.docx/contents" target="_blank" rel="noopener">http://192.168.37.138:1080/wopi/files/upload/word.docx/contents</a><br>可以访问到一个显示文件信息的界面<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b9.png" alt=""></p>
<p>根据第三个博客，将<a href="http://192.168.37.138:1080/wopi/files/upload/word.docx" target="_blank" rel="noopener">http://192.168.37.138:1080/wopi/files/upload/word.docx</a> 用<code>encodeURIComponent</code>重新进行编码。</p>
<p>事例，其中的<code>officeserver</code>是上面的office online信息的访问地址或ip（win-5ldi2svjqoj.test.com 或 192.168.37.138）<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://officeserver/we/WordEditorFrame.aspx?ui=zh-CN&amp;WOPISrc=http%3A%2F%2Fwopiserver%2Fwopi%2Ffiles%2Fupload%2Fdoc%2F2019%2F03%2Fword.docx%3Faccess_token%3D1%26UserId%3D1%26UserName%3D1</span><br><span class="line"></span><br><span class="line">http://officeserver/wv/WordViewer/request.pdf?type=accesspdf&amp;WOPISrc=http%3A%2F%2Fwopiserver%2Fwopi%2Ffiles%2Fupload%2Fdoc%2F2019%2F03%2Fword.docx%3Faccess_token%3D1%26UserId%3D1%26UserName%3D1</span><br></pre></td></tr></table></figure></p>
<p>到这里可以显示下图就算成功。期间出现的一些bug，已经记录在了之前的博客里面。<br><img src="/2020/04/25/微软 Office Online Server 2016 服务安装部署/b10.png" alt=""></p>
]]></content>
      <tags>
        <tag>office</tag>
      </tags>
  </entry>
  <entry>
    <title>排查本地写入sql文件太慢</title>
    <url>/2020/11/07/%E6%8E%92%E6%9F%A5%E6%9C%AC%E5%9C%B0%E5%86%99%E5%85%A5sql%E6%96%87%E4%BB%B6%E5%A4%AA%E6%85%A2/</url>
    <content><![CDATA[<h1 id="排查本地写入sql文件太慢"><a href="#排查本地写入sql文件太慢" class="headerlink" title="排查本地写入sql文件太慢"></a>排查本地写入sql文件太慢</h1><blockquote>
<p>记录一下日常<br>本地写入数据每分钟2000条，所以叫大佬帮忙排查一下。<br>主要原因 binlog 日志、主从复制。</p>
</blockquote>
<a id="more"></a>
<h2 id="查看磁盘情况"><a href="#查看磁盘情况" class="headerlink" title="查看磁盘情况"></a>查看磁盘情况</h2><p>首先怀疑是否是磁盘出了问题，打开任务管理器 -&gt; 性能 -&gt; 资源管理器（或者直接运行命令<code>perfmon.exe</code> 打开）。<br>查看磁盘情况，发现磁盘已经跑满。写入排行里有我的两个 mysql 服务和 binlog 日志服务。<br>由于本机主从复制只是测试用，所以可以关闭。</p>
<h2 id="关闭主从复制"><a href="#关闭主从复制" class="headerlink" title="关闭主从复制"></a>关闭主从复制</h2><p>直接在从库中输入命令即可。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">stop slave;</span><br></pre></td></tr></table></figure></p>
<h2 id="关闭-binlog-日志"><a href="#关闭-binlog-日志" class="headerlink" title="关闭 binlog 日志"></a>关闭 binlog 日志</h2><p>在主库的 my.ini 配置文件里注释配置<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#log-bin=mysql-bin</span><br><span class="line">#binlog_format=mixed</span><br><span class="line">#server-id   = 1</span><br><span class="line">#expire_logs_days = 10</span><br></pre></td></tr></table></figure></p>
<p>在服务里重启 mysql 服务。</p>
<p>成功解决问题。</p>
]]></content>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>无侵入式实现API接口统一</title>
    <url>/2020/08/16/%E6%97%A0%E4%BE%B5%E5%85%A5%E5%BC%8F%E5%AE%9E%E7%8E%B0API%E6%8E%A5%E5%8F%A3%E7%BB%9F%E4%B8%80/</url>
    <content><![CDATA[<h1 id="Spring-Boot-无侵入式-实现-API-接口统一-JSON-格式返回"><a href="#Spring-Boot-无侵入式-实现-API-接口统一-JSON-格式返回" class="headerlink" title="Spring Boot 无侵入式 实现 API 接口统一 JSON 格式返回"></a>Spring Boot 无侵入式 实现 API 接口统一 JSON 格式返回</h1><blockquote>
<p>转载自 <a href="blog.csdn.net/qq_34347620/article/details/102239179">blog.csdn.net/qq_34347620/article/details/102239179</a><br>现在自从增加了每天的通勤路程，感觉每天时间都很紧张。这个文章并没有实际操作一下。</p>
</blockquote>
<a id="more"></a>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><ul>
<li>无侵入式 统一返回JSON格式<ul>
<li>定义JSON格式</li>
<li>定义JavaBean字段</li>
<li>Result实体返回测试</li>
</ul>
</li>
<li>统一返回JSON格式进阶-全局处理(@RestControllerAdvice)<ul>
<li>@ResponseBody继承类</li>
<li>ResponseBodyAdvice继承类</li>
<li>RestControllerAdvice返回测试</li>
</ul>
</li>
<li>统一返回JSON格式进阶-异常处理(@ExceptionHandler))<ul>
<li>异常处理@ResponseStatus(不推荐)</li>
<li>全局异常处理@ExceptionHandler(推荐)</li>
</ul>
</li>
</ul>
<h2 id="无侵入式-统一返回JSON格式"><a href="#无侵入式-统一返回JSON格式" class="headerlink" title="无侵入式 统一返回JSON格式"></a>无侵入式 统一返回JSON格式</h2><p>其实本没有没打算写这篇博客的，但还是要写一下写这篇博客的起因是因为，现在呆着的这家公司居然没有统一的API返回格式?，询问主管他居然告诉我用HTTP状态码就够用了（fxxk），天哪HTTP状态码真的够用吗？<br>在仔细的阅读了项目源码后发现，在API请求的是居然没有业务异常（黑人问好）。好吧 居然入坑了只能遵照项目风格了，懒得吐槽了。<br>因为项目已经开发了半年多了, 要是全部接口都做修改工作量还是挺大的, 只能用这种无侵入式的方案来解决.</p>
<blockquote>
<p>我感觉这种事情会经常碰到，尤其是维护老项目代码。</p>
</blockquote>
<h2 id="定义JSON格式"><a href="#定义JSON格式" class="headerlink" title="定义JSON格式"></a>定义JSON格式</h2><h3 id="定义返回JSON格式"><a href="#定义返回JSON格式" class="headerlink" title="定义返回JSON格式"></a>定义返回JSON格式</h3><p>后端返回给前端一般情况下使用JSON格式, 定义如下<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">"code"</span>: <span class="number">200</span>,</span><br><span class="line">    <span class="string">"message"</span>: <span class="string">"OK"</span>,</span><br><span class="line">    <span class="string">"data"</span>: &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>code: 返回状态码</li>
<li>message: 返回信息的描述</li>
<li>data: 返回值<h2 id="定义JavaBean字段"><a href="#定义JavaBean字段" class="headerlink" title="定义JavaBean字段"></a>定义JavaBean字段</h2><h3 id="定义状态码枚举类"><a href="#定义状态码枚举类" class="headerlink" title="定义状态码枚举类"></a>定义状态码枚举类</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@ToString</span><br><span class="line">@Getter</span><br><span class="line">public enum ResultStatus &#123;</span><br><span class="line"></span><br><span class="line">    SUCCESS(HttpStatus.OK, 200, &quot;OK&quot;),</span><br><span class="line">    BAD_REQUEST(HttpStatus.BAD_REQUEST, 400, &quot;Bad Request&quot;),</span><br><span class="line">    INTERNAL_SERVER_ERROR(HttpStatus.INTERNAL_SERVER_ERROR, 500, &quot;Internal Server Error&quot;),;</span><br><span class="line"></span><br><span class="line">    /** 返回的HTTP状态码, 符合http请求 */</span><br><span class="line">    private HttpStatus httpStatus;</span><br><span class="line">    /** 业务异常码 */</span><br><span class="line">    private Integer code;</span><br><span class="line">    /** 业务异常信息描述 */</span><br><span class="line">    private String message;</span><br><span class="line"></span><br><span class="line">    ResultStatus(HttpStatus httpStatus, Integer code, String message) &#123;</span><br><span class="line">        this.httpStatus = httpStatus;</span><br><span class="line">        this.code = code;</span><br><span class="line">        this.message = message;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>状态码和信息以及http状态码就能一一对应了便于维护, 有同学有疑问了为什么要用到http状态码呀,因为我要兼容项目以前的代码, 没有其他原因, 当然其他同学不喜欢http状态码的可以吧源码中HttpStatus给删除了</p>
<h3 id="定义返回体类"><a href="#定义返回体类" class="headerlink" title="定义返回体类"></a>定义返回体类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Getter</span></span><br><span class="line"><span class="meta">@ToString</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Result</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="comment">/** 业务错误码 */</span></span><br><span class="line">    <span class="keyword">private</span> Integer code;</span><br><span class="line">    <span class="comment">/** 信息描述 */</span></span><br><span class="line">    <span class="keyword">private</span> String message;</span><br><span class="line">    <span class="comment">/** 返回参数 */</span></span><br><span class="line">    <span class="keyword">private</span> T data;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Result</span><span class="params">(ResultStatus resultStatus, T data)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.code = resultStatus.getCode();</span><br><span class="line">        <span class="keyword">this</span>.message = resultStatus.getMessage();</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 业务成功返回业务代码和描述信息 */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Result&lt;Void&gt; <span class="title">success</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Result&lt;Void&gt;(ResultStatus.SUCCESS, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 业务成功返回业务代码,描述和返回的参数 */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">Result&lt;T&gt; <span class="title">success</span><span class="params">(T data)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Result&lt;T&gt;(ResultStatus.SUCCESS, data);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 业务成功返回业务代码,描述和返回的参数 */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">Result&lt;T&gt; <span class="title">success</span><span class="params">(ResultStatus resultStatus, T data)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (resultStatus == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> success(data);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Result&lt;T&gt;(resultStatus, data);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 业务异常返回业务代码和描述信息 */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">Result&lt;T&gt; <span class="title">failure</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Result&lt;T&gt;(ResultStatus.INTERNAL_SERVER_ERROR, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 业务异常返回业务代码,描述和返回的参数 */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">Result&lt;T&gt; <span class="title">failure</span><span class="params">(ResultStatus resultStatus)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> failure(resultStatus, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 业务异常返回业务代码,描述和返回的参数 */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">Result&lt;T&gt; <span class="title">failure</span><span class="params">(ResultStatus resultStatus, T data)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (resultStatus == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> Result&lt;T&gt;(ResultStatus.INTERNAL_SERVER_ERROR, <span class="keyword">null</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Result&lt;T&gt;(resultStatus, data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>因为使用构造方法进行创建对象太麻烦了, 我们使用静态方法来创建对象这样简单明了</p>
<h3 id="Result实体返回测试"><a href="#Result实体返回测试" class="headerlink" title="Result实体返回测试"></a>Result实体返回测试</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/hello"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> HashMap&lt;String, Object&gt; INFO;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        INFO = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        INFO.put(<span class="string">"name"</span>, <span class="string">"galaxy"</span>);</span><br><span class="line">        INFO.put(<span class="string">"age"</span>, <span class="string">"70"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/hello"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title">hello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> INFO;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/result"</span>)</span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="keyword">public</span> Result&lt;Map&lt;String, Object&gt;&gt; helloResult() &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.success(INFO);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>到这里我们已经简单的实现了统一JSON格式了, 但是我们也发现了一个问题了,想要返回统一的JSON格式需要返回Result\&lt;Object>才可以, 我明明返回Object可以了, 为什么要重复劳动, 有没有解决方法, 当然是有的啦, 下面我们开始优化我们的代码吧</p>
<h2 id="统一返回JSON格式进阶-全局处理-RestControllerAdvice"><a href="#统一返回JSON格式进阶-全局处理-RestControllerAdvice" class="headerlink" title="统一返回JSON格式进阶-全局处理(@RestControllerAdvice)"></a>统一返回JSON格式进阶-全局处理(@RestControllerAdvice)</h2><p>我师傅经常告诉我的一句话: “你就是一个小屁孩, 你遇到的问题都已经不知道有多少人遇到过了, 你会想到的问题, 已经有前辈想到过了. 你准备解决的问题, 已经有人把坑填了”。是不是很鸡汤, 是不是很励志, 让我对前辈们充满着崇拜, 事实上他对我说的是: “自己去百度”, 这五个大字, 其实这五个大字已经说明上明的B话了, 通过不断的百度和Google发现了很多的解决方案.</p>
<p>我们都知道使用@ResponseBody注解会把返回Object序列化成JSON字符串,就先从这个入手吧, 大致就是在序列化前把Object赋值给Result\&lt;Object>就可以了, 大家可以观摩<br>org.springframework.web.servlet.mvc.method.annotation.ResponseBodyAdvice<br>和<br>org.springframework.web.bind.annotation.ResponseBody</p>
<h3 id="ResponseBody继承类"><a href="#ResponseBody继承类" class="headerlink" title="@ResponseBody继承类"></a>@ResponseBody继承类</h3><p>我们已经决定从@ResponseBody注解入手了就创建一个注解类继承@ResponseBody, 很干净什么都没有哈哈,@ResponseResultBody 可以标记在类和方法上这样我们就可以跟自由的进行使用了<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(&#123;ElementType.TYPE, ElementType.METHOD&#125;)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> ResponseResultBody &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="ResponseBody继承类-1"><a href="#ResponseBody继承类-1" class="headerlink" title="@ResponseBody继承类"></a>@ResponseBody继承类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ResponseResultBodyAdvice</span> <span class="keyword">implements</span> <span class="title">ResponseBodyAdvice</span>&lt;<span class="title">Object</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Class&lt;? extends Annotation&gt; ANNOTATION_TYPE = ResponseResultBody.class;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 判断类或者方法是否使用了 <span class="doctag">@ResponseResultBody</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(MethodParameter returnType, Class&lt;? extends HttpMessageConverter&lt;?&gt;&gt; converterType)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> AnnotatedElementUtils.hasAnnotation(returnType.getContainingClass(), ANNOTATION_TYPE) || returnType.hasMethodAnnotation(ANNOTATION_TYPE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当类或者方法使用了 <span class="doctag">@ResponseResultBody</span> 就会调用这个方法</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">beforeBodyWrite</span><span class="params">(Object body, MethodParameter returnType, MediaType selectedContentType, Class&lt;? extends HttpMessageConverter&lt;?&gt;&gt; selectedConverterType, ServerHttpRequest request, ServerHttpResponse response)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 防止重复包裹的问题出现</span></span><br><span class="line">        <span class="keyword">if</span> (body <span class="keyword">instanceof</span> Result) &#123;</span><br><span class="line">            <span class="keyword">return</span> body;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Result.success(body);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="RestControllerAdvice返回测试"><a href="#RestControllerAdvice返回测试" class="headerlink" title="RestControllerAdvice返回测试"></a>RestControllerAdvice返回测试</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/helloResult"</span>)</span><br><span class="line"><span class="meta">@ResponseResultBody</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloResultController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> HashMap&lt;String, Object&gt; INFO;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        INFO = <span class="keyword">new</span> HashMap&lt;String, Object&gt;();</span><br><span class="line">        INFO.put(<span class="string">"name"</span>, <span class="string">"galaxy"</span>);</span><br><span class="line">        INFO.put(<span class="string">"age"</span>, <span class="string">"70"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"hello"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> HashMap&lt;String, Object&gt; <span class="title">hello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> INFO;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 测试重复包裹 */</span></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"result"</span>)</span><br><span class="line">    <span class="keyword">public</span> Result&lt;Map&lt;String, Object&gt;&gt; helloResult() &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.success(INFO);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"helloError"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> HashMap&lt;String, Object&gt; <span class="title">helloError</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">"helloError"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"helloMyError"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> HashMap&lt;String, Object&gt; <span class="title">helloMyError</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ResultException();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>是不是很神奇, 直接返回Object就可以统一JSON格式了, 就不用每个返回都返回Result\&lt;T>对象了,直接让SpringMVC帮助我们进行统一的管理, 简直完美。<br>只想看接口哦, helloError和helloMyError是会直接抛出异常的接口,我好像没有对异常返回进行统一的处理哦</p>
<h3 id="统一返回JSON格式进阶-异常处理-ExceptionHandler"><a href="#统一返回JSON格式进阶-异常处理-ExceptionHandler" class="headerlink" title="统一返回JSON格式进阶-异常处理(@ExceptionHandler))"></a>统一返回JSON格式进阶-异常处理(@ExceptionHandler))</h3><p>卧槽, 异常处理, 差点把这茬给忘了, 这个异常处理就有很多方法了,先看看我师傅的处理方式, 我刚拿到这个代码的时候很想吐槽, 对异常类的处理这么残暴的吗, 直接用PrintWriter直接输出结果, 果然是老师傅, 我要是有100个异常类, 不得要写100个 if else了. 赶紧改改睡吧<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyExceptionHandler</span> <span class="keyword">implements</span> <span class="title">HandlerExceptionResolver</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">resolveException</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span></span><br><span class="line"><span class="function"><span class="params">                                         Object handler, Exception ex)</span> </span>&#123;</span><br><span class="line">        PrintWriter out = getPrintWrite(response);</span><br><span class="line">        <span class="keyword">if</span> (ex <span class="keyword">instanceof</span> XXXException) &#123;</span><br><span class="line">            out.write(JsonUtil.formatJson(ResultEnum.PAY_ERROR.getCode(), ex.getMessage()));</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            out.write(JsonUtil.formatJson(ResultEnum.FAIL.getCode(), <span class="string">"服务器异常"</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">null</span> != out) &#123;</span><br><span class="line">            out.close();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> mav;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> PrintWriter <span class="title">getPrintWrite</span><span class="params">(HttpServletResponse response)</span> </span>&#123;</span><br><span class="line">        PrintWriter out = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            response.setHeader(<span class="string">"Content-type"</span>, <span class="string">"text/html;charset=UTF-8"</span>);</span><br><span class="line">            response.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line">            out = response.getWriter();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            log.error(<span class="string">"PrintWriter is exception"</span>, e);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面的代码看看还是没有问题的, 别学过去哦,</p>
<h3 id="异常处理-ResponseStatus-不推荐"><a href="#异常处理-ResponseStatus-不推荐" class="headerlink" title="异常处理@ResponseStatus(不推荐)"></a>异常处理@ResponseStatus(不推荐)</h3><p>@ResponseStatus用法如下,可用在Controller类和Controller方法上以及Exception类上但是这样的工作量还是挺大的<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/error"</span>)</span><br><span class="line"><span class="meta">@ResponseStatus</span>(value = HttpStatus.INTERNAL_SERVER_ERROR, reason = <span class="string">"Java的异常"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloExceptionController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> HashMap&lt;String, Object&gt; INFO;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        INFO = <span class="keyword">new</span> HashMap&lt;String, Object&gt;();</span><br><span class="line">        INFO.put(<span class="string">"name"</span>, <span class="string">"galaxy"</span>);</span><br><span class="line">        INFO.put(<span class="string">"age"</span>, <span class="string">"70"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>()</span><br><span class="line">    <span class="function"><span class="keyword">public</span> HashMap&lt;String, Object&gt; <span class="title">helloError</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">"helloError"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"helloJavaError"</span>)</span><br><span class="line">    <span class="meta">@ResponseStatus</span>(value = HttpStatus.INTERNAL_SERVER_ERROR, reason = <span class="string">"Java的异常"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> HashMap&lt;String, Object&gt; <span class="title">helloJavaError</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">"helloError"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"helloMyError"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> HashMap&lt;String, Object&gt; <span class="title">helloMyError</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> MyException();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@ResponseStatus</span>(value = HttpStatus.INTERNAL_SERVER_ERROR, reason = <span class="string">"自己定义的异常"</span>)</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyException</span> <span class="keyword">extends</span> <span class="title">Exception</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="全局异常处理-ExceptionHandler-推荐"><a href="#全局异常处理-ExceptionHandler-推荐" class="headerlink" title="全局异常处理@ExceptionHandler(推荐)"></a>全局异常处理@ExceptionHandler(推荐)</h3><p>把ResponseResultBodyAdvice类进行改造一下,代码有点多了,<br>主要参考了<br>org.springframework.web.servlet.mvc.method.annotation.ResponseEntityExceptionHandler#handleException()方法,<br>有空可以看一下。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="meta">@RestControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ResponseResultBodyAdvice</span> <span class="keyword">implements</span> <span class="title">ResponseBodyAdvice</span>&lt;<span class="title">Object</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Class&lt;? extends Annotation&gt; ANNOTATION_TYPE = ResponseResultBody.class;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 判断类或者方法是否使用了 <span class="doctag">@ResponseResultBody</span> */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(MethodParameter returnType, Class&lt;? extends HttpMessageConverter&lt;?&gt;&gt; converterType)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> AnnotatedElementUtils.hasAnnotation(returnType.getContainingClass(), ANNOTATION_TYPE) || returnType.hasMethodAnnotation(ANNOTATION_TYPE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 当类或者方法使用了 <span class="doctag">@ResponseResultBody</span> 就会调用这个方法 */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">beforeBodyWrite</span><span class="params">(Object body, MethodParameter returnType, MediaType selectedContentType, Class&lt;? extends HttpMessageConverter&lt;?&gt;&gt; selectedConverterType, ServerHttpRequest request, ServerHttpResponse response)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (body <span class="keyword">instanceof</span> Result) &#123;</span><br><span class="line">            <span class="keyword">return</span> body;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Result.success(body);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 提供对标准Spring MVC异常的处理</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> ex the target exception</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> request the current request</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@ExceptionHandler</span>(Exception.class)</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> ResponseEntity&lt;Result&lt;?&gt;&gt; exceptionHandler(Exception ex, WebRequest request) &#123;</span><br><span class="line">        log.error(<span class="string">"ExceptionHandler: &#123;&#125;"</span>, ex.getMessage());</span><br><span class="line">        HttpHeaders headers = <span class="keyword">new</span> HttpHeaders();</span><br><span class="line">        <span class="keyword">if</span> (ex <span class="keyword">instanceof</span> ResultException) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">this</span>.handleResultException((ResultException) ex, headers, request);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// <span class="doctag">TODO:</span> 2019/10/05 galaxy 这里可以自定义其他的异常拦截</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.handleException(ex, headers, request);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 对ResultException类返回返回结果的处理 */</span></span><br><span class="line">    <span class="keyword">protected</span> ResponseEntity&lt;Result&lt;?&gt;&gt; handleResultException(ResultException ex, HttpHeaders headers, WebRequest request) &#123;</span><br><span class="line">        Result&lt;?&gt; body = Result.failure(ex.getResultStatus());</span><br><span class="line">        HttpStatus status = ex.getResultStatus().getHttpStatus();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.handleExceptionInternal(ex, body, headers, status, request);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 异常类的统一处理 */</span></span><br><span class="line">    <span class="keyword">protected</span> ResponseEntity&lt;Result&lt;?&gt;&gt; handleException(Exception ex, HttpHeaders headers, WebRequest request) &#123;</span><br><span class="line">        Result&lt;?&gt; body = Result.failure();</span><br><span class="line">        HttpStatus status = HttpStatus.INTERNAL_SERVER_ERROR;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.handleExceptionInternal(ex, body, headers, status, request);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * org.springframework.web.servlet.mvc.method.annotation.ResponseEntityExceptionHandler#handleExceptionInternal(java.lang.Exception, java.lang.Object, org.springframework.http.HttpHeaders, org.springframework.http.HttpStatus, org.springframework.web.context.request.WebRequest)</span></span><br><span class="line"><span class="comment">     * &lt;p&gt;</span></span><br><span class="line"><span class="comment">     * A single place to customize the response body of all exception types.</span></span><br><span class="line"><span class="comment">     * &lt;p&gt;The default implementation sets the &#123;<span class="doctag">@link</span> WebUtils#ERROR_EXCEPTION_ATTRIBUTE&#125;</span></span><br><span class="line"><span class="comment">     * request attribute and creates a &#123;<span class="doctag">@link</span> ResponseEntity&#125; from the given</span></span><br><span class="line"><span class="comment">     * body, headers, and status.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">protected</span> ResponseEntity&lt;Result&lt;?&gt;&gt; handleExceptionInternal(</span><br><span class="line">            Exception ex, Result&lt;?&gt; body, HttpHeaders headers, HttpStatus status, WebRequest request) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (HttpStatus.INTERNAL_SERVER_ERROR.equals(status)) &#123;</span><br><span class="line">            request.setAttribute(WebUtils.ERROR_EXCEPTION_ATTRIBUTE, ex, WebRequest.SCOPE_REQUEST);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ResponseEntity&lt;&gt;(body, headers, status);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>Spring boot</tag>
      </tags>
  </entry>
  <entry>
    <title>日常业务和hadoop初学</title>
    <url>/2019/06/01/%E6%97%A5%E5%B8%B8%E4%B8%9A%E5%8A%A1%E5%92%8Chadoop%E5%88%9D%E5%AD%A6/</url>
    <content><![CDATA[<h1 id="日常业务和hadoop初学"><a href="#日常业务和hadoop初学" class="headerlink" title="日常业务和hadoop初学"></a>日常业务和hadoop初学</h1><blockquote>
<p>记录上周遇到的一些业务问题，和在学习hadoop安装中碰到的bug。</p>
</blockquote>
<a id="more"></a>
<h2 id="日常遇到的代码问题"><a href="#日常遇到的代码问题" class="headerlink" title="日常遇到的代码问题"></a>日常遇到的代码问题</h2><ol>
<li>使用idea进行gradle build项目时候遇到警告<br>警告: Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add ‘@EqualsAndHashCode(callSuper=false)’ to your type.<br>在报警告的类里加入‘@EqualsAndHashCode(callSuper=false)注解就可以了。</li>
<li>settings.gradle文件中可以修改打包后的jar包文件名字，记得打包前修改一下。</li>
</ol>
<h2 id="hadoop初学（windows764系统版本）"><a href="#hadoop初学（windows764系统版本）" class="headerlink" title="hadoop初学（windows764系统版本）"></a>hadoop初学（windows764系统版本）</h2><h3 id="安装过程"><a href="#安装过程" class="headerlink" title="安装过程"></a>安装过程</h3><p>参考csdn上的<a href="https://blog.csdn.net/chy2z/article/details/80484848" target="_blank" rel="noopener">Hadoop2.7.6在Windows7单机部署</a>，此文的安装过程。其中遇到了一些问题，记录如下：</p>
<h3 id="错误汇总："><a href="#错误汇总：" class="headerlink" title="错误汇总："></a>错误汇总：</h3><h4 id="使用start-all-cmd命令后产生的错误"><a href="#使用start-all-cmd命令后产生的错误" class="headerlink" title="使用start-all.cmd命令后产生的错误"></a>使用start-all.cmd命令后产生的错误</h4><ol>
<li><p>INFO datanode.DataNode: Shutdown complete.<br>ERROR datanode.DataNode: Exception in secureMain<br>java.io.IOException: <strong>No services to connect, missing NameNode address.</strong><br>at org.apache.hadoop.hdfs.server.datanode.BlockPoolManager.refreshNamenodes(BlockPoolManager.java:165)</p>
</li>
<li><p>INFO namenode.NameNodeUtils: fs.defaultFS is file:///<br>ERROR <strong>namenode.NameNode: Failed to start namenode.</strong><br>java.lang.IllegalArgumentException: Invalid URI for NameNode address (check fs.defaultFS): file:/// has no authority.<br>at org.apache.hadoop.hdfs.DFSUtilClient.getNNAddress(DFSUtilClient.java:697)</p>
</li>
<li><p>error <strong>Couldn’t find a package.json file in “D:\hadoop\hadoop-3.1.1\sbin”</strong><br>info Visit <a href="https://yarnpkg.com/en/docs/cli/run" target="_blank" rel="noopener">https://yarnpkg.com/en/docs/cli/run</a> for documentation about this command.</p>
</li>
<li><p>Error: <strong>JAVA_HOME is incorrectly set.</strong></p>
</li>
</ol>
<h4 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h4><p>第一和第二都是一个原因导致的，是我在配置文件的时候遗漏了core-site.xml这个配置文件所导致的。复制网上的配置后即可解决</p>
<p>第三个错误是因为我将前端的包管理工具yarn配置到了环境变量里。当使用start-all.cmd命令时，其中的yarn被系统判断为前端的yarn导致。删除环境变量中的path变量中的yarn路径，再重启电脑就可以解决这个问题。</p>
<blockquote>
<p>大多数的jdk报错原因都是因为jdk的路径中有空格，或者是hadoop-env.cmd配置文件里的JAVA_HOME=%JAVA_HOME%导致的。</p>
</blockquote>
<p>第四个错误从网上找到两种解决方案：<br>1.用路径替代符C:\PROGRA~1\Java\jdk1.8.0_91<br>PROGRA~1  ===== C:\Program Files 目录的dos文件名模式下的缩写<br>长于8个字符的文件名和文件夹名，都被简化成前面6个有效字符，后面~1，有重名的就 ~2,~3,<br>2.用引号括起来”C:\Program Files”\Java\jdk1.8.0_91。<br>第一个方法经过尝试后依旧不能解决路径空格的问题，第二个可以解决</p>
]]></content>
      <tags>
        <tag>hadoop</tag>
      </tags>
  </entry>
  <entry>
    <title>气象相关MeteoInfo失败使用过程记录</title>
    <url>/2020/05/09/%E6%B0%94%E8%B1%A1%E7%9B%B8%E5%85%B3MeteoInfo%E5%A4%B1%E8%B4%A5%E4%BD%BF%E7%94%A8%E8%BF%87%E7%A8%8B%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="气象相关MeteoInfo失败使用过程记录"><a href="#气象相关MeteoInfo失败使用过程记录" class="headerlink" title="气象相关MeteoInfo失败使用过程记录"></a>气象相关MeteoInfo失败使用过程记录</h1><blockquote>
<p>虽然成功画图，但是依旧是不能够有足够的参数调整。图片一看就是有问题的图，更换文件类型去尝试，未能成功画图。</p>
</blockquote>
<a id="more"></a>
<h2 id="初步安装"><a href="#初步安装" class="headerlink" title="初步安装"></a>初步安装</h2><p>meteoInfo的安装没有多麻烦。直接百度<a href="http://meteothink.org/downloads/index.html" target="_blank" rel="noopener">MeteoInfo</a> 找到压缩包，下载后解压，启动MeteoInfoLab.exe文件即可。</p>
<h2 id="明确目的"><a href="#明确目的" class="headerlink" title="明确目的"></a>明确目的</h2><p>主要目的是为了nc类型文件，micap4类型文件等值线图的绘制。</p>
<h2 id="尝试过程"><a href="#尝试过程" class="headerlink" title="尝试过程"></a>尝试过程</h2><p>一开始是想要寻找现成的脚本运行，然后发现都是有点专业和高级。百度后最快找到的教程是<a href="MeteoInfoLab">MeteoInfoLab脚本汇总贴</a>，在里面寻找到关键字相符的文章后，开始每个语句去试，不知是不是语句太简单的缘故，博主并没有注释。在官网中找到的文档404了。</p>
<h3 id="第一个文件"><a href="#第一个文件" class="headerlink" title="第一个文件"></a>第一个文件</h3><p>尝试了一下<a href="http://bbs.06climate.com/forum.php?mod=viewthread&amp;tid=38489&amp;extra=page%3D1" target="_blank" rel="noopener">MeteoInfoLab脚本示例：创建netCDF文件（合并文件）</a><br>并不能成功的按照步骤来画图，我自己的nc文件并没找到方法读出lat和lon。再次仔细翻看帖子，并没有找到类似的文件解析。最终只好逐渐的一个语句一个语句的去尝试，结果如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">f = addfile(&apos;C:/Users/Administrator/Desktop/nc.nc&apos;)</span><br><span class="line">var = f[&apos;CO&apos;][:]</span><br><span class="line">contourf(var)</span><br><span class="line">print var</span><br></pre></td></tr></table></figure></p>
<p>这个语句可以出图，但是没有边界线。也和之前的图片不同。</p>
<h3 id="第二个文件"><a href="#第二个文件" class="headerlink" title="第二个文件"></a>第二个文件</h3><p>从一个帖子中得知，解压出的sample文件夹有事例文件。取出后用同样的语句进行尝试，这次却没能够重新画图，直接报错。并没有找到原因。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;D:\Downloads\Compressed\MeteoInfo_2.2.1\MeteoInfo\pylib\mipylib\plotlib\miplot.py&quot;, line 2180, in contour</span><br><span class="line">    r = gca.contour(*args, **kwargs)</span><br><span class="line">  File &quot;D:\Downloads\Compressed\MeteoInfo_2.2.1\MeteoInfo\pylib\mipylib\plotlib\_axes.py&quot;, line 2048, in contour</span><br><span class="line">    gdata = np.asgriddata(args[0])</span><br><span class="line">  File &quot;D:\Downloads\Compressed\MeteoInfo_2.2.1\MeteoInfo\pylib\mipylib\numeric\core\numeric.py&quot;, line 2349, in asgriddata</span><br><span class="line">    return data.asgriddata()</span><br><span class="line">  File &quot;D:\Downloads\Compressed\MeteoInfo_2.2.1\MeteoInfo\pylib\mipylib\numeric\core\dimarray.py&quot;, line 667, in asgriddata</span><br><span class="line">    gdata = GridData(self._array, xdata, ydata, self.fill_value, self.proj)</span><br><span class="line">	at org.meteoinfo.data.GridData.&lt;init&gt;(GridData.java:149)</span><br><span class="line"></span><br><span class="line">	at sun.reflect.NativeConstructorAccessorImpl.newInstance0(Native Method)</span><br><span class="line"></span><br><span class="line">	at sun.reflect.NativeConstructorAccessorImpl.newInstance(Unknown Source)</span><br><span class="line"></span><br><span class="line">	at sun.reflect.DelegatingConstructorAccessorImpl.newInstance(Unknown Source)</span><br><span class="line"></span><br><span class="line">	at java.lang.reflect.Constructor.newInstance(Unknown Source)</span><br><span class="line"></span><br><span class="line">	at org.python.core.PyReflectedConstructor.constructProxy(PyReflectedConstructor.java:213)</span><br><span class="line"></span><br><span class="line">java.lang.ArrayIndexOutOfBoundsException: java.lang.ArrayIndexOutOfBoundsException: 12</span><br></pre></td></tr></table></figure></p>
<h3 id="第三个文件"><a href="#第三个文件" class="headerlink" title="第三个文件"></a>第三个文件</h3><p>这次是尝试使用MICAP4类型文件，根据<a href="http://bbs.06climate.com/forum.php?mod=viewthread&amp;tid=45105&amp;extra=page%3D1" target="_blank" rel="noopener">MeteoInfoLab脚本示例：micaps4类数据</a>画出的图片十分的奇怪，直接就可以看出来是个失败的了。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">f = addfile_micaps(&apos;C:\\Users\\Administrator\\Desktop\\20041520.000&apos;)</span><br><span class="line">t1 = f.gettime(0)</span><br><span class="line">data = f[&apos;var&apos;][0,0,:,:]</span><br><span class="line">axesm()</span><br><span class="line">geoshow(&apos;cn_province&apos;)</span><br><span class="line">geoshow(&apos;country&apos;, edgecolor=&apos;k&apos;)</span><br><span class="line">levs = [0.1,2,5,10,20,30,50,80,100,120,150]</span><br><span class="line">cols = makecolors(len(levs)+1, cmap=&apos;MPL_rainbow&apos;)</span><br><span class="line">cols[0] = &apos;w&apos;</span><br><span class="line">layer = imshowm(data, levs, colors=cols)</span><br><span class="line">colorbar(layer, label=&apos;mm&apos;, extendrect=False)</span><br></pre></td></tr></table></figure></p>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>由于时间，以及资料太少了的原因。画图暂停继续研究，试图从客户那边直接要一个程序，我们直接采集图片。</p>
<p>专业的知识还是比较难以搜索，从百度中查找关键词MICAP4只能查到一些莫名其妙的东西。google查meteoInfo也没查出太多有用的东西。这两个关键字大都是指向了气象家园这个论坛。ncl的关键词查询稍稍多了一些，关联到了PyNGL这个技术。然后又牵扯到了python，没用的网页瞬间多了不少呢。</p>
<p>看来专业的知识还是需要花功夫去查一查书籍。直接找脚本还是有些困难的。</p>
]]></content>
      <tags>
        <tag>气象</tag>
      </tags>
  </entry>
  <entry>
    <title>气象相关Python版NCL的安装和未成功使用</title>
    <url>/2020/04/30/%E6%B0%94%E8%B1%A1%E7%9B%B8%E5%85%B3Python%E7%89%88NCL%E7%9A%84%E5%AE%89%E8%A3%85%E5%92%8C%E6%9C%AA%E6%88%90%E5%8A%9F%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h1 id="气象相关Python版NCL的安装和未成功使用"><a href="#气象相关Python版NCL的安装和未成功使用" class="headerlink" title="气象相关Python版NCL的安装和未成功使用"></a>气象相关Python版NCL的安装和未成功使用</h1><blockquote>
<p>业务需要将气象服务文件类型画图，去找了找有关的东西，发现了这个视频简单一些<a href="https://www.bilibili.com/video/av73159125/" target="_blank" rel="noopener">Python版NCL安装及快速入门</a> 。仔细研究了一下发现还是没能入门。但是还是记录一下，之后看看有没有后续研究。<br>这里卡住了之后要去尝试看一下 MeteoInfoLab，暂时停止这个的研究。</p>
</blockquote>
<a id="more"></a>
<h2 id="环境安装"><a href="#环境安装" class="headerlink" title="环境安装"></a>环境安装</h2><p>首先需要一台 linux 机器，本人用的是腾讯云。</p>
<h3 id="1-安装miniconda"><a href="#1-安装miniconda" class="headerlink" title="1. 安装miniconda"></a>1. 安装miniconda</h3><p>百度 minicaonda，打开官网。<a href="https://docs.conda.io/en/latest/miniconda.html" target="_blank" rel="noopener">https://docs.conda.io/en/latest/miniconda.html</a> 找到相对应的安装包。视频中下载的是32位，因为云服务器系统的原因，我使用64位。下载速度有点慢，所以我从本机下载完成后传到准备好的 linux 文件夹里。<br><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b1.png" alt="1"></p>
<p>下载完成后，给安装包赋予执行权限<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">chmod 777 Miniconda3-latest-Linux-x86_64.sh</span><br></pre></td></tr></table></figure></p>
<p>执行安装程序<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash Miniconda3-latest-Linux-x86_64.sh</span><br></pre></td></tr></table></figure></p>
<p>安装时需要同意协议，接着按回车以安装在默认位置<br><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b2.png" alt="2"></p>
<p><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b3.png" alt="3"></p>
<p>输入yes同意安装包初始化 Miniconda。<br><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b4.png" alt="4"></p>
<p>到这里 Miniconda 安装完毕，请注意输出的提示信息 ..bashsrc 被更改。防止一些莫名其妙的错误，重启一下终端。<br><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b5.png" alt="5"></p>
<h3 id="2-使用conda"><a href="#2-使用conda" class="headerlink" title="2. 使用conda"></a>2. 使用conda</h3><p>重启之后我并没有像视频中那样找不到 conda，貌似是直接默认安装到了环境变量中。<br><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b6.png" alt="6"></p>
<p>当命令提示符前面出现“（base）”字样时，我们就已经进入了 conda。使用<code>conda list</code>命令可以查看 conda 的全局环境内装了什么包<br><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b7.png" alt="7"></p>
<p>输入<code>python --version</code>查看一下版本。输入命令来创建一个名为 NCL-py 的 conda 空间并且安装所需的 <code>conda-forge</code> 、<code>xarray</code>、 <code>netcdf4</code> 、<code>scipy</code>、 <code>pyngl</code> 、<code>pynio</code> 、<code>ncl</code> 包，这里有个失误，视频里是<code>_</code>，我打错成了<code>-</code>。不过一个命名没啥影响。安装的中间还是需要同意一下。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">conda create -n NCL-py -c conda-forge xarray netcdf4 scipy pyngl pynio ncl</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b8.png" alt="8"></p>
<p>安装完毕后，输入 conda activate NCL-py 进入我们刚才创建的环境。<br>接下来就要写代码了。<br><img src="/2020/04/30/气象相关Python版NCL的安装和未成功使用/b9.png" alt="9"></p>
<h2 id="代码编写"><a href="#代码编写" class="headerlink" title="代码编写"></a>代码编写</h2><p>这里是卡住我的原因，视频中所提的NC文件我这里并没有。而且我去下载了 Panoply 这个程序后，发现解析出来的nc内容也不一样。</p>
<p>忽略UP主中间的错误尝试，我最后的代码是这样的<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;&quot;&quot;py-NCL画图&quot;&quot;&quot;</span><br><span class="line">import numpy as np</span><br><span class="line">import Ngl, Nio</span><br><span class="line">import netCDF4</span><br><span class="line">f = Nio.open_file(&apos;/root/python3/WRFCHEM_2019110720_CO_f001.nc&apos;)</span><br><span class="line">uwnd = f.variables[&apos;CO&apos;][:]</span><br><span class="line"></span><br><span class="line">wks = Ngl.open_wks(&apos;png&apos;, &apos;NCL-py&apos;)#这里我并没有XMind，直接就选择导出png图片</span><br><span class="line"></span><br><span class="line">res = Ngl.Resources()</span><br><span class="line"></span><br><span class="line">res.cnFillOn = True</span><br><span class="line">res.cnLinesOn = False</span><br><span class="line">res.cnLineLabelsOn = False</span><br><span class="line">res.lbOrientation = &apos;horizontal&apos;</span><br><span class="line"></span><br><span class="line">plot = Ngl.contour(wks, uwnd, res) #原本是画的全球图片，现在只是一个区域</span><br><span class="line"># dataset = netCDF4.Dataset(&apos;/root/python3/WRFCHEM_2019110720_CO_f001.nc&apos;)</span><br><span class="line"># print(dataset.variables.keys())</span><br><span class="line"></span><br><span class="line">Ngl.end()</span><br></pre></td></tr></table></figure></p>
<h3 id="缺失的条件"><a href="#缺失的条件" class="headerlink" title="缺失的条件"></a>缺失的条件</h3><ol>
<li>首先并没有找到nc文件中的经纬度，没有去设置。</li>
<li>对比 Panoply 程序画图后发现上面的程序虽然可以画，但是画出来的图片太粗糙了。而且位置也很奇怪。我不清楚那个画出来的图片是对的。</li>
</ol>
<p>因为工期原因，暂时放弃这种方式解析。</p>
]]></content>
      <tags>
        <tag>气象</tag>
      </tags>
  </entry>
  <entry>
    <title>由lombok引发的一次gradle版本问题</title>
    <url>/2019/08/09/%E7%94%B1lombok%E5%BC%95%E5%8F%91%E7%9A%84%E4%B8%80%E6%AC%A1gradle%E7%89%88%E6%9C%AC%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="由lombok引发的一次gradle版本问题"><a href="#由lombok引发的一次gradle版本问题" class="headerlink" title="由lombok引发的一次gradle版本问题"></a>由lombok引发的一次gradle版本问题</h1><p>在编译打包时，出现了找不到方法的错误，机智的更换了lombok版本。然后在运行时使用@Cacheable注解时就报了这个错误：<strong>cannot deserialize from Object value (no delegate- or property-based Creator)</strong>。<br><a id="more"></a></p>
<h2 id="错误查询思路"><a href="#错误查询思路" class="headerlink" title="错误查询思路"></a>错误查询思路</h2><p>一开始自然是从lombok的版本开始找问题。按照网上的说法从1.6.22换成了最新的版本1.8，可惜不行。</p>
<p>只好呼叫团队大佬出手。一开始的解决思路也是重新build一下项目，删除jar包重新导入。之后因为idea执行时的控制台打印有条数限制，开启了本文件夹中的命令行发现了在执行gradle build时的开头会有一个报错。<strong>lombok.javac.apt.LombokProcessor could not be initialized</strong></p>
<p>再然后大佬就发现了gradle版本兼容问题，把我的项目gradle设置成了4.8.1，全局gradle依旧不变。成功的解决了这个问题</p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>获得腾讯云社区奖品</title>
    <url>/2020/05/30/%E8%8E%B7%E5%BE%97%E8%85%BE%E8%AE%AF%E4%BA%91%E7%A4%BE%E5%8C%BA%E5%A5%96%E5%93%81/</url>
    <content><![CDATA[<h1 id="获得腾讯云社区奖品"><a href="#获得腾讯云社区奖品" class="headerlink" title="获得腾讯云社区奖品"></a>获得腾讯云社区奖品</h1><blockquote>
<p>是开心的一天呀，终于收到了参与的活动奖品。</p>
</blockquote>
<a id="more"></a>
<h2 id="来由"><a href="#来由" class="headerlink" title="来由"></a>来由</h2><p>那是在一个多月之前的一个晚上了，我正打开腾讯云的网页打算去调整一下云服务器，看到了消息里面有一个腾讯云社区的征文活动。</p>
<p>之前我在查询资料的时候也曾查到过腾讯云社区的博客，里面的内容好多都是没有意义的文章。仔细想想我把我写代码的经验传上去，至少是曾经实践过得代码，有点跑通的可能。就按照征文的格式编辑了一下自己之前的部署ftp经验，发了篇博客参加了一下。</p>
<h2 id="经过"><a href="#经过" class="headerlink" title="经过"></a>经过</h2><p>三周前，发现征文活动的获奖名单已经出来了。从头看到尾，发现前面都是腾讯内部的大佬。但惊喜的发现，在末尾还是有我的文章。开开心心的提交了邮编地址，等待着快递的到来。</p>
<h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><p>漫长的三周度过，这周六终于收到了来自深圳南山区腾讯园区的快递。开心的拆开，拍照，发朋友圈。又有了一份写博客的动力。</p>
]]></content>
      <tags>
        <tag>日常</tag>
      </tags>
  </entry>
  <entry>
    <title>简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列</title>
    <url>/2019/12/14/%E7%AE%80%E8%BF%B0%E5%AF%B9%E6%AF%94%20Kafka%E3%80%81RabbitMQ%E3%80%81RocketMQ%E3%80%81ActiveMQ%20%E5%9B%9B%E4%B8%AA%E5%88%86%E5%B8%83%E5%BC%8F%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<h1 id="简述对比-Kafka、RabbitMQ、RocketMQ、ActiveMQ-四个分布式消息队列"><a href="#简述对比-Kafka、RabbitMQ、RocketMQ、ActiveMQ-四个分布式消息队列" class="headerlink" title="简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列"></a>简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列</h1><blockquote>
<p>最近有时间整理一下自己所使用过的技术，大多数时候我都是有这个需求后再去找这个技术去学来使用。除了业务场景，并没有太多的了解。<br>现在大多数人都喜欢把知识分享在公众号上，这篇也是我在公众号上看到后记录过来的。<br>原文：<a href="https://mp.weixin.qq.com/s?__biz=MzAxNjk4ODE4OQ==&amp;mid=2247487283&amp;idx=1&amp;sn=e03ca118ef257ea91e3c9d0178e6037f&amp;chksm=9bed2e41ac9aa75764df6bd037312d0bc6ae051147d47742c914495f245d2af19bc58afed5aa&amp;scene=21#wechat_redirect" target="_blank" rel="noopener">17 个方面，综合对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列</a><br>原文：<a href="https://blog.csdn.net/mrs_haining/article/details/80625833" target="_blank" rel="noopener">为什么分布式一定要有消息队列？</a></p>
</blockquote>
<a id="more"></a>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul>
<li>为何要使用消息队列</li>
<li>资料文档</li>
<li>开发语言</li>
<li>管理界面</li>
<li>支持的协议</li>
<li>消息存储</li>
<li>消息事务</li>
<li>负载均衡</li>
<li>集群方式</li>
<li>可用性</li>
<li>消息重复</li>
<li>吞吐量TPS</li>
<li>订阅形式和消息分发</li>
<li>顺序消息</li>
<li>消息确认</li>
<li>消息回溯</li>
<li>消息重试</li>
<li>并发度<br>本文将从Kafka、RabbitMQ、ZeroMQ、RocketMQ、ActiveMQ 17 个方面综合对比作为消息队列使用时的差异。<br>另外在加入一些我自己在使用和测试中的一些小看法。</li>
</ul>
<h2 id="为什么要使用消息队列"><a href="#为什么要使用消息队列" class="headerlink" title="为什么要使用消息队列"></a>为什么要使用消息队列</h2><p>只答三个最主要的应用场景(不可否认还有其他的，但是只答三个主要的),即以下六个字:解耦、异步、削峰</p>
<h3 id="解耦"><a href="#解耦" class="headerlink" title="解耦"></a>解耦</h3><p>传统模式的缺点：</p>
<ul>
<li>系统间耦合性太强，如下图所示，系统A在代码中直接调用系统B和系统C的代码，如果将来D系统接入，系统A还需要修改代码，过于麻烦！</li>
</ul>
<p><img src="/2019/12/14/简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列/b1.png" alt="传统模式"></p>
<p>中间件模式的的优点：</p>
<ul>
<li>将消息写入消息队列，需要消息的系统自己从消息队列中订阅，从而系统A不需要做任何修改。</li>
</ul>
<p><img src="/2019/12/14/简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列/b2.png" alt="中间件模式"></p>
<h3 id="异步"><a href="#异步" class="headerlink" title="异步"></a>异步</h3><p>传统模式的缺点：</p>
<ul>
<li>一些非必要的业务逻辑以同步的方式运行，太耗费时间。</li>
</ul>
<p><img src="/2019/12/14/简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列/b3.png" alt="传统模式"></p>
<p>中间件模式的的优点：</p>
<ul>
<li>将消息写入消息队列，非必要的业务逻辑以异步的方式运行，加快响应速度</li>
</ul>
<p><img src="/2019/12/14/简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列/b4.png" alt="中间件模式"></p>
<h3 id="削峰"><a href="#削峰" class="headerlink" title="削峰"></a>削峰</h3><p>传统模式的缺点：</p>
<ul>
<li>并发量大的时候，所有的请求直接怼到数据库，造成数据库连接异常</li>
</ul>
<p><img src="/2019/12/14/简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列/b5.png" alt="传统模式"></p>
<p>中间件模式的的优点：</p>
<ul>
<li>系统A慢慢的按照数据库能处理的并发量，从消息队列中慢慢拉取消息。在生产中，这个短暂的高峰期积压是允许的。</li>
</ul>
<p><img src="/2019/12/14/简述对比 Kafka、RabbitMQ、RocketMQ、ActiveMQ 四个分布式消息队列/b6.png" alt="中间件模式"></p>
<h2 id="资料文档"><a href="#资料文档" class="headerlink" title="资料文档"></a>资料文档</h2><p>Kafka：中。有kafka作者自己写的书，网上资料也有一些。<br>rabbitmq：多。有一些不错的书，网上资料多。<br>zeromq：少。没有专门写zeromq的书，网上的资料多是一些代码的实现和简单介绍。<br>rocketmq：少。没有专门写rocketmq的书，网上的资料良莠不齐，官方文档很简洁，但是对技术细节没有过多的描述。<br>activemq：多。没有专门写activemq的书，网上资料多。</p>
<h2 id="开发语言"><a href="#开发语言" class="headerlink" title="开发语言"></a>开发语言</h2><p>Kafka：Scala<br>rabbitmq：Erlang<br>zeromq：c<br>rocketmq：java<br>activemq：java</p>
<h2 id="管理界面"><a href="#管理界面" class="headerlink" title="管理界面"></a>管理界面</h2><p>Kafka：一般<br>rabbitmq：好<br>zeromq：无<br>rocketmq：无<br>activemq：一般</p>
<h2 id="支持的协议"><a href="#支持的协议" class="headerlink" title="支持的协议"></a>支持的协议</h2><p>Kafka：自己定义的一套…（基于TCP）<br>rabbitmq：AMQP<br>zeromq：TCP、UDP<br>rocketmq：自己定义的一套…<br>activemq：OpenWire、STOMP、REST、XMPP、AMQP</p>
<p>这里我要添加一下。AMQP作为一个高级消息队列协议。它主要目的之一是适用于各种流行语言和框架的客户端，实现交互性。实现一种在全行业广泛使用的标准消息中间件技术，以便降低企业和系统集成的开销。</p>
<h2 id="消息存储"><a href="#消息存储" class="headerlink" title="消息存储"></a>消息存储</h2><ul>
<li>Kafka：内存、磁盘、数据库。支持大量堆积。</li>
</ul>
<p>kafka的最小存储单元是分区，一个topic包含多个分区，kafka创建主题时，这些分区会被分配在多个服务器上，通常一个broker一台服务器。分区首领会均匀地分布在不同的服务器上，分区副本也会均匀的分布在不同的服务器上，确保负载均衡和高可用性，当新的broker加入集群的时候，部分副本会被移动到新的broker上。根据配置文件中的目录清单，kafka会把新的分区分配给目录清单里分区数最少的目录。默认情况下，分区器使用轮询算法把消息均衡地分布在同一个主题的不同分区中，对于发送时指定了key的情况，会根据key的hashcode取模后的值存到对应的分区中。</p>
<ul>
<li>rabbitmq：内存、磁盘。支持少量堆积。</li>
</ul>
<p>rabbitmq的消息分为持久化的消息和非持久化消息，不管是持久化的消息还是非持久化的消息都可以写入到磁盘。持久化的消息在到达队列时就写入到磁盘，并且如果可以，持久化的消息也会在内存中保存一份备份，这样可以提高一定的性能，当内存吃紧的时候会从内存中清除。非持久化的消息一般只存在于内存中，在内存吃紧的时候会被换入到磁盘中，以节省内存。</p>
<p>引入镜像队列机制，可将重要队列“复制”到集群中的其他broker上，保证这些队列的消息不会丢失。配置镜像的队列，都包含一个主节点master和多个从节点slave,如果master失效，加入时间最长的slave会被提升为新的master，除发送消息外的所有动作都向master发送，然后由master将命令执行结果广播给各个slave，rabbitmq会让master均匀地分布在不同的服务器上，而同一个队列的slave也会均匀地分布在不同的服务器上，保证负载均衡和高可用性。</p>
<ul>
<li><p>zeromq：消息发送端的内存或者磁盘中。不支持持久化。</p>
</li>
<li><p>rocketmq：磁盘。支持大量堆积。</p>
</li>
</ul>
<p>commitLog文件存放实际的消息数据，每个commitLog上限是1G，满了之后会自动新建一个commitLog文件保存数据。ConsumeQueue队列只存放offset、size、tagcode，非常小，分布在多个broker上。ConsumeQueue相当于CommitLog的索引文件，消费者消费时会从consumeQueue中查找消息在commitLog中的offset，再去commitLog中查找元数据。</p>
<p>ConsumeQueue存储格式的特性，保证了写过程的顺序写盘（写CommitLog文件），大量数据IO都在顺序写同一个commitLog，满1G了再写新的。加上rocketmq是累计4K才强制从PageCache中刷到磁盘（缓存），所以高并发写性能突出。</p>
<ul>
<li>activemq：内存、磁盘、数据库。支持少量堆积。</li>
</ul>
<h2 id="消息事务"><a href="#消息事务" class="headerlink" title="消息事务"></a>消息事务</h2><ul>
<li>Kafka：支持</li>
<li>rabbitmq：支持。客户端将信道设置为事务模式，只有当消息被rabbitMq接收，事务才能提交成功，否则在捕获异常后进行回滚。使用事务会使得性能有所下降</li>
<li>zeromq：不支持 </li>
<li>rocketmq：支持 </li>
<li>activemq：支持</li>
</ul>
<h2 id="负载均衡"><a href="#负载均衡" class="headerlink" title="负载均衡"></a>负载均衡</h2><ul>
<li><p>Kafka：支持负载均衡。</p>
<ol>
<li><p>一个broker通常就是一台服务器节点。对于同一个Topic的不同分区，Kafka会尽力将这些分区分布到不同的Broker服务器上，zookeeper保存了broker、主题和分区的元数据信息。分区首领会处理来自客户端的生产请求，kafka分区首领会被分配到不同的broker服务器上，让不同的broker服务器共同分担任务。<br>每一个broker都缓存了元数据信息，客户端可以从任意一个broker获取元数据信息并缓存起来，根据元数据信息知道要往哪里发送请求。</p>
</li>
<li><p>kafka的消费者组订阅同一个topic，会尽可能地使得每一个消费者分配到相同数量的分区，分摊负载。</p>
</li>
<li><p>当消费者加入或者退出消费者组的时候，还会触发再均衡，为每一个消费者重新分配分区，分摊负载。<br>kafka的负载均衡大部分是自动完成的，分区的创建也是kafka完成的，隐藏了很多细节，避免了繁琐的配置和人为疏忽造成的负载问题。</p>
</li>
<li><p>发送端由topic和key来决定消息发往哪个分区，如果key为null，那么会使用轮询算法将消息均衡地发送到同一个topic的不同分区中。如果key不为null，那么会根据key的hashcode取模计算出要发往的分区。</p>
</li>
</ol>
</li>
<li><p>rabbitmq：对负载均衡的支持不好。</p>
<ol>
<li><p>消息被投递到哪个队列是由交换器和key决定的，交换器、路由键、队列都需要手动创建。<br> rabbitmq客户端发送消息要和broker建立连接，需要事先知道broker上有哪些交换器，有哪些队列。通常要声明要发送的目标队列，如果没有目标队列，会在broker上创建一个队列，如果有，就什么都不处理，接着往这个队列发送消息。假设大部分繁重任务的队列都创建在同一个broker上，那么这个broker的负载就会过大。（可以在上线前预先创建队列，无需声明要发送的队列，但是发送时不会尝试创建队列，可能出现找不到队列的问题，rabbitmq的备份交换器会把找不到队列的消息保存到一个专门的队列中，以便以后查询使用）</p>
<p>使用镜像队列机制建立rabbitmq集群可以解决这个问题，形成master-slave的架构，master节点会均匀分布在不同的服务器上，让每一台服务器分摊负载。slave节点只是负责转发，在master失效时会选择加入时间最长的slave成为master。</p>
<p>当新节点加入镜像队列的时候，队列中的消息不会同步到新的slave中，除非调用同步命令，但是调用命令后，队列会阻塞，不能在生产环境中调用同步命令。</p>
</li>
<li><p>当rabbitmq队列拥有多个消费者的时候，队列收到的消息将以轮询的分发方式发送给消费者。每条消息只会发送给订阅列表里的一个消费者，不会重复。<br>这种方式非常适合扩展，而且是专门为并发程序设计的。<br>如果某些消费者的任务比较繁重，那么可以设置basicQos限制信道上消费者能保持的最大未确认消息的数量，在达到上限时，rabbitmq不再向这个消费者发送任何消息。</p>
</li>
<li><p>对于rabbitmq而言，客户端与集群建立的TCP连接不是与集群中所有的节点建立连接，而是挑选其中一个节点建立连接。<br>但是rabbitmq集群可以借助HAProxy、LVS技术，或者在客户端使用算法实现负载均衡，引入负载均衡之后，各个客户端的连接可以分摊到集群的各个节点之中。</p>
</li>
</ol>
</li>
</ul>
<p>客户端均衡算法：</p>
<ol>
<li><p>轮询法。按顺序返回下一个服务器的连接地址。</p>
</li>
<li><p>加权轮询法。给配置高、负载低的机器配置更高的权重，让其处理更多的请求；而配置低、负载高的机器，给其分配较低的权重，降低其系统负载。</p>
</li>
<li><p>随机法。随机选取一个服务器的连接地址。</p>
</li>
<li><p>加权随机法。按照概率随机选取连接地址。</p>
</li>
<li><p>源地址哈希法。通过哈希函数计算得到的一个数值，用该数值对服务器列表的大小进行取模运算。</p>
</li>
<li><p>最小连接数法。动态选择当前连接数最少的一台服务器的连接地址。</p>
</li>
</ol>
<ul>
<li><p>zeromq：去中心化，不支持负载均衡。本身只是一个多线程网络库。</p>
</li>
<li><p>rocketmq：支持负载均衡。</p>
</li>
</ul>
<p>一个broker通常是一个服务器节点，broker分为master和slave,master和slave存储的数据一样，slave从master同步数据。</p>
<ol>
<li><p>nameserver与每个集群成员保持心跳，保存着Topic-Broker路由信息，同一个topic的队列会分布在不同的服务器上。</p>
</li>
<li><p>发送消息通过轮询队列的方式发送，每个队列接收平均的消息量。发送消息指定topic、tags、keys，无法指定投递到哪个队列（没有意义，集群消费和广播消费跟消息存放在哪个队列没有关系）。</p>
<p>tags选填，类似于 Gmail 为每封邮件设置的标签，方便服务器过滤使用。目前只支 持每个消息设置一个 tag，所以也可以类比为 Notify 的 MessageType 概念。</p>
<p>keys选填，代表这条消息的业务关键词，服务器会根据 keys 创建哈希索引，设置后， 可以在 Console 系统根据 Topic、Keys 来查询消息，由于是哈希索引，请尽可能 保证 key 唯一，例如订单号，商品 Id 等。</p>
</li>
<li><p>rocketmq的负载均衡策略规定：Consumer数量应该小于等于Queue数量，如果Consumer超过Queue数量，那么多余的Consumer 将不能消费消息。这一点和kafka是一致的，rocketmq会尽可能地为每一个Consumer分配相同数量的队列，分摊负载。</p>
</li>
</ol>
<ul>
<li>activemq：支持负载均衡。可以基于zookeeper实现负载均衡。</li>
</ul>
<h2 id="集群方式"><a href="#集群方式" class="headerlink" title="集群方式"></a>集群方式</h2><ul>
<li><p>Kafka：天然的‘Leader-Slave’无状态集群，每台服务器既是Master也是Slave。</p>
<p>分区首领均匀地分布在不同的kafka服务器上，分区副本也均匀地分布在不同的kafka服务器上，所以每一台kafka服务器既含有分区首领，同时又含有分区副本，每一台kafka服务器是某一台kafka服务器的Slave，同时也是某一台kafka服务器的leader。</p>
<p>kafka的集群依赖于zookeeper，zookeeper支持热扩展，所有的broker、消费者、分区都可以动态加入移除，而无需关闭服务，与不依靠zookeeper集群的mq相比，这是最大的优势。</p>
</li>
<li><p>rabbitmq：支持简单集群，’复制’模式，对高级集群模式支持不好。</p>
<p>rabbitmq的每一个节点，不管是单一节点系统或者是集群中的一部分，要么是内存节点，要么是磁盘节点，集群中至少要有一个是磁盘节点。</p>
<p>在rabbitmq集群中创建队列，集群只会在单个节点创建队列进程和完整的队列信息（元数据、状态、内容），而不是在所有节点上创建。</p>
<p>引入镜像队列，可以避免单点故障，确保服务的可用性，但是需要人为地为某些重要的队列配置镜像。</p>
</li>
<li><p>zeromq：去中心化，不支持集群。</p>
</li>
<li><p>rocketmq：常用 多对’Master-Slave’ 模式，开源版本需手动切换Slave变成Master</p>
<p>Name Server是一个几乎无状态节点，可集群部署，节点之间无任何信息同步。</p>
<p>Broker部署相对复杂，Broker分为Master与Slave，一个Master可以对应多个Slave，但是一个Slave只能对应一个Master，Master与Slave的对应关系通过指定相同的BrokerName，不同的BrokerId来定义，BrokerId为0表示Master，非0表示Slave。Master也可以部署多个。每个Broker与Name Server集群中的所有节点建立长连接，定时注册Topic信息到所有Name Server。</p>
<p>Producer与Name Server集群中的其中一个节点（随机选择）建立长连接，定期从Name Server取Topic路由信息，并向提供Topic服务的Master建立长连接，且定时向Master发送心跳。Producer完全无状态，可集群部署。</p>
<p>Consumer与Name Server集群中的其中一个节点（随机选择）建立长连接，定期从Name Server取Topic路由信息，并向提供Topic服务的Master、Slave建立长连接，且定时向Master、Slave发送心跳。Consumer既可以从Master订阅消息，也可以从Slave订阅消息，订阅规则由Broker配置决定。</p>
<p>客户端先找到NameServer, 然后通过NameServer再找到 Broker。</p>
<p>一个topic有多个队列，这些队列会均匀地分布在不同的broker服务器上。rocketmq队列的概念和kafka的分区概念是基本一致的，kafka同一个topic的分区尽可能地分布在不同的broker上，分区副本也会分布在不同的broker上。</p>
<p>rocketmq集群的slave会从master拉取数据备份，master分布在不同的broker上。</p>
</li>
<li><p>activemq：支持简单集群模式，比如’主-备’，对高级集群模式支持不好。</p>
</li>
</ul>
<h2 id="可用性"><a href="#可用性" class="headerlink" title="可用性"></a>可用性</h2><ul>
<li>Kafka：非常高（分布式） </li>
<li>rabbitmq：高（主从） </li>
<li>zeromq：高。</li>
<li>rocketmq：非常高（分布式）</li>
<li>activemq：高（主从）</li>
</ul>
<h2 id="消息重复"><a href="#消息重复" class="headerlink" title="消息重复"></a>消息重复</h2><ul>
<li><p>Kafka：支持at least once、at most once</p>
</li>
<li><p>rabbitmq：支持at least once、at most once</p>
</li>
<li><p>zeromq：只有重传机制，但是没有持久化，消息丢了重传也没有用。既不是at least once、也不是at most once、更不是exactly only once</p>
</li>
<li><p>rocketmq：支持at least once</p>
</li>
<li><p>activemq：支持at least once</p>
</li>
</ul>
<p>这里我添加一下：<br>At most once：最多一次，消息可能会丢失，但不会重复<br>At least once：最少一次，消息不会丢失，可能会重复<br>Exactly once：只且一次，消息不丢失不重复，只且消费一次</p>
<h2 id="吞吐量TPS"><a href="#吞吐量TPS" class="headerlink" title="吞吐量TPS"></a>吞吐量TPS</h2><ul>
<li>Kafka：极大 Kafka按批次发送消息和消费消息。发送端将多个小消息合并，批量发向Broker，消费端每次取出一个批次的消息批量处理。</li>
<li>rabbitmq：比较大 </li>
<li>zeromq：极大 </li>
<li>rocketmq：大 rocketMQ接收端可以批量消费消息，可以配置每次消费的消息数，但是发送端不是批量发送。</li>
<li>activemq：比较大</li>
</ul>
<h2 id="订阅形式和消息分发"><a href="#订阅形式和消息分发" class="headerlink" title="订阅形式和消息分发"></a>订阅形式和消息分发</h2><ul>
<li><p>Kafka：基于topic以及按照topic进行正则匹配的发布订阅模式。<br>【发送】<br>发送端由topic和key来决定消息发往哪个分区，如果key为null，那么会使用轮询算法将消息均衡地发送到同一个topic的不同分区中。如果key不为null，那么会根据key的hashcode取模计算出要发往的分区。<br>【接收】</p>
<ol>
<li><p>consumer向群组协调器broker发送心跳来维持他们和群组的从属关系以及他们对分区的所有权关系，所有权关系一旦被分配就不会改变除非发生再均衡(比如有一个consumer加入或者离开consumer group)，consumer只会从对应的分区读取消息。</p>
</li>
<li><p>kafka限制consumer个数要少于分区个数,每个消息只会被同一个 Consumer Group的一个consumer消费（非广播）。</p>
</li>
<li><p>kafka的 Consumer Group订阅同一个topic，会尽可能地使得每一个consumer分配到相同数量的分区，不同 Consumer Group订阅同一个主题相互独立，同一个消息会被不同的 Consumer Group处理。</p>
</li>
</ol>
</li>
<li><p>rabbitmq：提供了4种：direct, topic ,Headers和fanout。<br>【发送】<br>先要声明一个队列，这个队列会被创建或者已经被创建，队列是基本存储单元。<br>由exchange和key决定消息存储在哪个队列。<br>direct -&gt; 发送到和bindingKey完全匹配的队列。</p>
</li>
</ul>
<p>topic -&gt; 路由key是含有”.”的字符串，会发送到含有“*”、“#”进行模糊匹配的bingKey对应的队列。</p>
<p>fanout -&gt; 与key无关，会发送到所有和exchange绑定的队列</p>
<p>headers -&gt; 与key无关，消息内容的headers属性（一个键值对）和绑定键值对完全匹配时，会发送到此队列。此方式性能低一般不用<br>【接收】<br>rabbitmq的队列是基本存储单元，不再被分区或者分片，对于我们已经创建了的队列，消费端要指定从哪一个队列接收消息。<br>当rabbitmq队列拥有多个消费者的时候，队列收到的消息将以轮询的分发方式发送给消费者。每条消息只会发送给订阅列表里的一个消费者，不会重复。<br>这种方式非常适合扩展，而且是专门为并发程序设计的。<br>如果某些消费者的任务比较繁重，那么可以设置basicQos限制信道上消费者能保持的最大未确认消息的数量，在达到上限时，rabbitmq不再向这个消费者发送任何消息。</p>
<ul>
<li><p>zeromq：点对点(p2p)</p>
</li>
<li><p>rocketmq：基于topic/messageTag以及按照消息类型、属性进行正则匹配的发布订阅模式<br>【发送】<br>发送消息通过轮询队列的方式发送，每个队列接收平均的消息量。发送消息指定topic、tags、keys，无法指定投递到哪个队列（没有意义，集群消费和广播消费跟消息存放在哪个队列没有关系）。<br>tags选填，类似于 Gmail 为每封邮件设置的标签，方便服务器过滤使用。目前只支 持每个消息设置一个 tag，所以也可以类比为 Notify 的 MessageType 概念。<br>keys选填，代表这条消息的业务关键词，服务器会根据 keys 创建哈希索引，设置后， 可以在 Console 系统根据 Topic、Keys 来查询消息，由于是哈希索引，请尽可能 保证 key 唯一，例如订单号，商品 Id 等。<br>【接收】</p>
<ol>
<li>广播消费。一条消息被多个Consumer消费，即使Consumer属于同一个ConsumerGroup，消息也会被ConsumerGroup中的每个Consumer都消费一次。</li>
<li>集群消费。一个 Consumer Group中的Consumer实例平均分摊消费消息。例如某个Topic有 9 条消息，其中一个Consumer Group有3个实例，那么每个实例只消费其中的 3 条消息。即每一个队列都把消息轮流分发给每个consumer。</li>
</ol>
</li>
<li><p>activemq：点对点(p2p)、广播（发布-订阅）</p>
</li>
</ul>
<p>点对点模式，每个消息只有1个消费者；</p>
<p>发布/订阅模式，每个消息可以有多个消费者。</p>
<p>【发送】<br>点对点模式：先要指定一个队列，这个队列会被创建或者已经被创建。</p>
<p>发布/订阅模式：先要指定一个topic，这个topic会被创建或者已经被创建。</p>
<p>【接收】<br>点对点模式：对于已经创建了的队列，消费端要指定从哪一个队列接收消息。</p>
<p>发布/订阅模式：对于已经创建了的topic，消费端要指定订阅哪一个topic的消息。</p>
<h2 id="顺序消息"><a href="#顺序消息" class="headerlink" title="顺序消息"></a>顺序消息</h2><ul>
<li>Kafka：支持。</li>
</ul>
<p>设置生产者的max.in.flight.requests.per.connection为1，可以保证消息是按照发送顺序写入服务器的，即使发生了重试。</p>
<p>kafka保证同一个分区里的消息是有序的，但是这种有序分两种情况</p>
<ol>
<li><p>key为null，消息逐个被写入不同主机的分区中，但是对于每个分区依然是有序的</p>
</li>
<li><p>key不为null , 消息被写入到同一个分区，这个分区的消息都是有序。</p>
</li>
</ol>
<ul>
<li><p>rabbitmq：不支持</p>
</li>
<li><p>zeromq：不支持</p>
</li>
<li><p>rocketmq：支持</p>
</li>
<li><p>activemq：不支持</p>
</li>
</ul>
<h2 id="消息确认"><a href="#消息确认" class="headerlink" title="消息确认"></a>消息确认</h2><ul>
<li><p>Kafka：支持。</p>
<ol>
<li>发送方确认机制</li>
</ol>
</li>
</ul>
<p>ack=0，不管消息是否成功写入分区</p>
<p>ack=1，消息成功写入首领分区后，返回成功</p>
<p>ack=all，消息成功写入所有分区后，返回成功。</p>
<ol start="2">
<li>接收方确认机制</li>
</ol>
<p>自动或者手动提交分区偏移量，早期版本的kafka偏移量是提交给Zookeeper的，这样使得zookeeper的压力比较大，更新版本的kafka的偏移量是提交给kafka服务器的，不再依赖于zookeeper群组，集群的性能更加稳定。</p>
<ul>
<li><p>rabbitmq：支持。</p>
<ol>
<li><p>发送方确认机制，消息被投递到所有匹配的队列后，返回成功。如果消息和队列是可持久化的，那么在写入磁盘后，返回成功。支持批量确认和异步确认。</p>
</li>
<li><p>接收方确认机制，设置autoAck为false，需要显式确认，设置autoAck为true，自动确认。</p>
</li>
</ol>
</li>
</ul>
<p>当autoAck为false的时候，rabbitmq队列会分成两部分，一部分是等待投递给consumer的消息，一部分是已经投递但是没收到确认的消息。如果一直没有收到确认信号，并且consumer已经断开连接，rabbitmq会安排这个消息重新进入队列，投递给原来的消费者或者下一个消费者。</p>
<p>未确认的消息不会有过期时间，如果一直没有确认，并且没有断开连接，rabbitmq会一直等待，rabbitmq允许一条消息处理的时间可以很久很久。</p>
<ul>
<li><p>zeromq：支持。</p>
</li>
<li><p>rocketmq：支持。</p>
</li>
<li><p>activemq：支持。</p>
</li>
</ul>
<h2 id="消息回溯"><a href="#消息回溯" class="headerlink" title="消息回溯"></a>消息回溯</h2><ul>
<li>Kafka：支持指定分区offset位置的回溯。</li>
<li>rabbitmq：不支持</li>
<li>zeromq：不支持</li>
<li>rocketmq：支持指定时间点的回溯。</li>
<li>activemq：不支持</li>
</ul>
<h2 id="消息重试"><a href="#消息重试" class="headerlink" title="消息重试"></a>消息重试</h2><ul>
<li><p>Kafka：不支持，但是可以实现。</p>
</li>
<li><p>kafka支持指定分区offset位置的回溯，可以实现消息重试。</p>
</li>
<li><p>rabbitmq：不支持，但是可以利用消息确认机制实现。</p>
</li>
<li><p>rabbitmq接收方确认机制，设置autoAck为false。</p>
<p>当autoAck为false的时候，rabbitmq队列会分成两部分，一部分是等待投递给consumer的消息，一部分是已经投递但是没收到确认的消息。如果一直没有收到确认信号，并且consumer已经断开连接，rabbitmq会安排这个消息重新进入队列，投递给原来的消费者或者下一个消费者。</p>
</li>
<li><p>zeromq：不支持，</p>
</li>
<li><p>rocketmq：支持。</p>
<p>消息消费失败的大部分场景下，立即重试99%都会失败，所以rocketmq的策略是在消费失败时定时重试，每次时间间隔相同。</p>
<ol>
<li><p>发送端的 send 方法本身支持内部重试，重试逻辑如下：</p>
<ul>
<li><p>至多重试3次；</p>
</li>
<li><p>如果发送失败，则轮转到下一个broker；</p>
</li>
<li><p>这个方法的总耗时不超过sendMsgTimeout 设置的值，默认 10s，超过时间不在重试。</p>
</li>
</ul>
</li>
<li><p>接收端。<br>Consumer 消费消息失败后，要提供一种重试机制，令消息再消费一次。Consumer 消费消息失败通常可以分为以下两种情况：</p>
<ul>
<li><p>由于消息本身的原因，例如反序列化失败，消息数据本身无法处理（例如话费充值，当前消息的手机号被注销，无法充值）等。定时重试机制，比如过 10s 秒后再重试。</p>
</li>
<li><p>由于依赖的下游应用服务不可用，例如 db 连接不可用，外系统网络不可达等。即使跳过当前失败的消息，消费其他消息同样也会报错。这种情况可以 sleep 30s，再消费下一条消息，减轻 Broker 重试消息的压力。</p>
</li>
</ul>
</li>
</ol>
</li>
<li><p>activemq：不支持</p>
</li>
</ul>
<h2 id="并发度"><a href="#并发度" class="headerlink" title="并发度"></a>并发度</h2><ul>
<li><p>Kafka：高</p>
<p>一个线程一个消费者，kafka限制消费者的个数要小于等于分区数，如果要提高并行度，可以在消费者中再开启多线程，或者增加consumer实例数量。</p>
</li>
<li><p>rabbitmq：极高</p>
<p>本身是用Erlang语言写的，并发性能高。</p>
<p>可在消费者中开启多线程，最常用的做法是一个channel对应一个消费者，每一个线程把持一个channel，多个线程复用connection的tcp连接，减少性能开销。</p>
<p>当rabbitmq队列拥有多个消费者的时候，队列收到的消息将以轮询的分发方式发送给消费者。每条消息只会发送给订阅列表里的一个消费者，不会重复。</p>
<p>这种方式非常适合扩展，而且是专门为并发程序设计的。</p>
<p>如果某些消费者的任务比较繁重，那么可以设置basicQos限制信道上消费者能保持的最大未确认消息的数量，在达到上限时，rabbitmq不再向这个消费者发送任何消息。</p>
</li>
<li><p>zeromq：高</p>
</li>
<li><p>rocketmq：高</p>
<ol>
<li><p>rocketmq限制消费者的个数少于等于队列数，但是可以在消费者中再开启多线程，这一点和kafka是一致的，提高并行度的方法相同。</p>
<p>修改消费并行度方法：</p>
<ul>
<li><p>同一个 ConsumerGroup 下，通过增加 Consumer 实例数量来提高并行度，超过订阅队列数的 Consumer实例无效。</p>
</li>
<li><p>提高单个 Consumer 的消费并行线程，通过修改参数consumeThreadMin、consumeThreadMax</p>
</li>
</ul>
</li>
<li><p>同一个网络连接connection，客户端多个线程可以同时发送请求，连接会被复用，减少性能开销。</p>
</li>
</ol>
</li>
<li><p>activemq：高</p>
<p>单个ActiveMQ的接收和消费消息的速度在1万笔/秒（持久化 一般为1-2万， 非持久化 2 万以上），在生产环境中部署10个Activemq就能达到10万笔/秒以上的性能，部署越多的activemq broker 在MQ上latency也就越低，系统吞吐量也就越高。</p>
</li>
</ul>
]]></content>
      <tags>
        <tag>RabbitMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>简单记录JPA自定义删除方法</title>
    <url>/2019/10/20/%E7%AE%80%E5%8D%95%E8%AE%B0%E5%BD%95JPA%E8%87%AA%E5%AE%9A%E4%B9%89%E5%88%A0%E9%99%A4%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<h1 id="简单记录JPA自定义删除方法"><a href="#简单记录JPA自定义删除方法" class="headerlink" title="简单记录JPA自定义删除方法"></a>简单记录JPA自定义删除方法</h1><blockquote>
<p>其实之前使用过多次的时候很是熟悉，但是长期不写后还是要去百度一下配置。干脆就在这里记录一下，下次直接查询方便一些</p>
</blockquote>
<a id="more"></a>
<h2 id="添加注解"><a href="#添加注解" class="headerlink" title="添加注解"></a>添加注解</h2><ol>
<li>需要在删除方法上添加@Modifying</li>
<li>使用时注意添加@Transactional事务控制，否则就会报错</li>
</ol>
<h2 id="更新LazyInitializationException"><a href="#更新LazyInitializationException" class="headerlink" title="更新LazyInitializationException"></a>更新LazyInitializationException</h2><p>使用懒加载注解时也必须添加@Transactional事务控制，否则会报下面的错误<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Caused by: org.hibernate.LazyInitializationException: failed to lazily initialize a collection of role:</span><br></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>jpa</tag>
      </tags>
  </entry>
  <entry>
    <title>记优化主从同步恢复时间的过程</title>
    <url>/2020/06/12/%E8%AE%B0%E4%BC%98%E5%8C%96%E4%B8%BB%E4%BB%8E%E5%90%8C%E6%AD%A5%E6%81%A2%E5%A4%8D%E6%97%B6%E9%97%B4%E7%9A%84%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="记优化主从同步恢复时间的过程"><a href="#记优化主从同步恢复时间的过程" class="headerlink" title="记优化主从同步恢复时间的过程"></a>记优化主从同步恢复时间的过程</h1><blockquote>
<p>这次再次出现了 Mysql 主从同步时，出现了主键冲突错误。当再次采用Data文件夹复制的时候。发现随着二级制日志文件的越来越多，复制的时间也越来越长。在复制恢复的时候无法启动程序，客户感觉体验极差（虽然原因是因为客户那边的服务器断电了，但是技术方面还是可以去避免这种情况的出现）。</p>
</blockquote>
<a id="more"></a>
<h2 id="主从同步的基础原理"><a href="#主从同步的基础原理" class="headerlink" title="主从同步的基础原理"></a>主从同步的基础原理</h2><p>从数据库( Slave )是主数据库的备份，当主数据库( Master )变化时从数据库要更新，这些数据库软件可以设计更新周期。这是提高信息安全的手段。</p>
<p>Mysql 服务器之间的主从同步是基于二进制日志机制，主服务器使用二进制日志来记录数据库的变动情况，从服务器通过读取和执行该日志文件来保持和主服务器的数据一致。</p>
<p>在使用二进制日志时，主服务器的所有操作都会被记录下来，然后从服务器会接收到该日志的一个副本。从服务器可以指定执行该日志中的哪一类事件（譬如只插入数据或者只更新数据），默认会执行日志中的所有语句。</p>
<p>每一个从服务器会记录关于二进制日志的信息：文件名和已经处理过的语句，这样意味着不同的从服务器可以分别执行同一个二进制日志的不同部分，并且从服务器可以随时连接或者中断和服务器的连接。</p>
<p>主服务器和每一个从服务器都必须配置一个唯一的ID号（在 my.ini 文件的[ mysqld ]模块下有一个 server-id 配置项），另外，每一个从服务器还需要通过 CHANGE MASTER TO 语句来配置它要连接的主服务器的 ip 地址，日志文件名称和该日志里面的位置（这些信息存储在主服务器的数据库里）</p>
<p>Mysql 的主从复制涉及到三个线程</p>
<ol>
<li>主节点 binary log dump 线程<br>当从节点连接主节点时，主节点会创建一个 log dump 线程，用于发送 bin-log 的内容。在读取bin-log 中的操作时，此线程会对主节点上的 bin-log 加锁，当读取完成，甚至在发动给从节点之前，锁会被释放。</li>
<li>从节点 I/O 线程<br>当从节点上执行<code>start slave</code>命令之后，从节点会创建一个I/O线程用来连接主节点，请求主库中更新的 bin-log。I/O线程接收到主节点 binlog dump 进程发来的更新之后，保存在本地relay-log 中。</li>
<li>从节点 SQL 线程<br>SQL线程负责读取relay log中的内容，解析成具体的操作并执行，最终保证主从数据的一致性。<br>对于每一个主从连接，都需要三个进程来完成。当主节点有多个从节点时，主节点会为每一个当前连接的从节点建一个 binary log dump 进程，而每个从节点都有自己的 I/O 进程，SQL 进程。从节点用两个线程将从主库拉取更新和执行分成独立的任务，这样在执行同步数据任务的时候，不会降低读操作的性能。比如，如果从节点没有运行，此时I/O进程可以很快从主节点获取更新，尽管 SQL 进程还没有执行。如果在 SQL 进程执行之前从节点服务停止，至少 I/O 进程已经从主节点拉取到了最新的变更并且保存在本地 relay 日志中，当服务再次起来之后，就可以完成数据的同步。</li>
</ol>
<p><img src="/2020/06/12/记优化主从同步恢复时间的过程/b1.png" alt=""></p>
<h2 id="初步尝试，去除二进制文件"><a href="#初步尝试，去除二进制文件" class="headerlink" title="初步尝试，去除二进制文件"></a>初步尝试，去除二进制文件</h2><p>想了想，既然从库复制数据是从二进制文件中读取。那么已经同步完的数据就可以将其二进制文件删除。便在配置文件中加入了<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#设置binlog清理时间</span><br><span class="line">expire_logs_days=7</span><br></pre></td></tr></table></figure></p>
<h2 id="常见处理，跳过错误sql语句"><a href="#常见处理，跳过错误sql语句" class="headerlink" title="常见处理，跳过错误sql语句"></a>常见处理，跳过错误sql语句</h2><p>网上更加常见的一种方式是跳过错误sql：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">stop</span> <span class="keyword">slave</span>;</span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> sql_slave_skip_counter=<span class="number">1</span>;</span><br><span class="line"><span class="keyword">start</span> <span class="keyword">slave</span>;</span><br><span class="line"><span class="keyword">show</span> <span class="keyword">slave</span> <span class="keyword">status</span>;</span><br></pre></td></tr></table></figure></p>
<p>但是这种方式会丢失数据，比如当从库已经占用了 id 为1的主键，主库写入 id 为1和2的数据。当跳过时就会丢失主库 id 为1的记录。</p>
<p>跳过后看似主从同步已经恢复，实际上一旦主库再次操作 id 为1的记录。从库中没有时，依旧会主从同步失败。</p>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>在不丢失数据的情况下出现了主键冲突错误，暂时加快恢复的方式是删除多余的二进制文件。</p>
]]></content>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>记录一次主从同步崩溃后的拯救行动</title>
    <url>/2019/05/08/%E8%AE%B0%E5%BD%95%E4%B8%80%E6%AC%A1%E4%B8%BB%E4%BB%8E%E5%90%8C%E6%AD%A5%E5%B4%A9%E6%BA%83%E5%90%8E%E7%9A%84%E6%8B%AF%E6%95%91%E8%A1%8C%E5%8A%A8/</url>
    <content><![CDATA[<h1 id="记录一次主从同步崩溃后的拯救行动"><a href="#记录一次主从同步崩溃后的拯救行动" class="headerlink" title="记录一次主从同步崩溃后的拯救行动"></a>记录一次主从同步崩溃后的拯救行动</h1><blockquote>
<p>服务器主机出现了问题是一个很头疼的问题。</p>
</blockquote>
<a id="more"></a>
<h2 id="事情起因："><a href="#事情起因：" class="headerlink" title="事情起因："></a>事情起因：</h2><p>白天，我发现远程登录服务器出现了权限问题。经过了大佬一上午加一中午的抢修，服务器好了。数据库的同步却禁不住服务器的变化，需要重新调整。</p>
<h2 id="处理过程："><a href="#处理过程：" class="headerlink" title="处理过程："></a>处理过程：</h2><h3 id="1-首先进行尝试的是跳过一行日志："><a href="#1-首先进行尝试的是跳过一行日志：" class="headerlink" title="1. 首先进行尝试的是跳过一行日志："></a>1. 首先进行尝试的是跳过一行日志：</h3><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&gt; stop slave;</span><br><span class="line"></span><br><span class="line">&gt; SET GLOBAL sql_slave_skip_counter = 1;</span><br><span class="line"></span><br><span class="line">&gt; start slave;</span><br></pre></td></tr></table></figure>
<p>第一次尝试运行后，并没有什么效果。大佬建议我多次尝试，多跳过几行。多次尝试后，宣告GG</p>
<h3 id="2-第二次我尝试重新指向主库最新日志："><a href="#2-第二次我尝试重新指向主库最新日志：" class="headerlink" title="2. 第二次我尝试重新指向主库最新日志："></a>2. 第二次我尝试重新指向主库最新日志：</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">change master to master_host=&apos;ip&apos;, master_user=&apos;username&apos;, master_password=&apos;password&apos;, master_log_file=&apos;mysql-bin.xxxx&apos;, master_log_pos=xxxx;</span><br></pre></td></tr></table></figure>
<p>虽然很开心的看到了slave_sql_running：yes和Slave_IO_Running：yes，但是大佬告诉我中间有可能会缺失记录。</p>
<h3 id="3-第三次只好打开服务器，将mysql的data文件夹内容进行复制。"><a href="#3-第三次只好打开服务器，将mysql的data文件夹内容进行复制。" class="headerlink" title="3. 第三次只好打开服务器，将mysql的data文件夹内容进行复制。"></a>3. 第三次只好打开服务器，将mysql的data文件夹内容进行复制。</h3><p>查询网上的文章，有的作者只是尝试了把数据库的文件夹和 ibdata1 。我在把mysql服务停掉，从库data文件夹压缩备份后进行了尝试。结果启动服务的时候直接报错。</p>
<p>没办法，尝试了一下拷贝整个主库的data文件夹到从库中，成功启动服务。但是在配置主从同步时报错：</p>
<blockquote>
<p>Fatal error: The slave I/O thread stops because master and slave have equal MySQL server UUIDs; these UUIDs must be different for replication to work.</p>
</blockquote>
<p>随机填写auto.cnf文件中保存的UUID后，解决了这个问题。</p>
<h2 id="后记："><a href="#后记：" class="headerlink" title="后记："></a>后记：</h2><p>至此，成功的将主从同步重新配好，在这里记录一下尝试的步骤。以便于下一次恢复时使用。</p>
]]></content>
  </entry>
  <entry>
    <title>转载，为何有的大厂技术弱爆了</title>
    <url>/2020/07/11/%E8%BD%AC%E8%BD%BD%EF%BC%8C%E4%B8%BA%E4%BD%95%E6%9C%89%E7%9A%84%E5%A4%A7%E5%8E%82%E6%8A%80%E6%9C%AF%E5%BC%B1%E7%88%86%E4%BA%86/</url>
    <content><![CDATA[<h1 id="转载，为何有的大厂技术弱爆了"><a href="#转载，为何有的大厂技术弱爆了" class="headerlink" title="转载，为何有的大厂技术弱爆了"></a>转载，为何有的大厂技术弱爆了</h1><blockquote>
<p>这篇文章转自 <a href="https://mp.weixin.qq.com/s/UGVzS86-iSsXqc08eRUrFQ" target="_blank" rel="noopener">简单聊聊为什么有些大公司技术弱爆了？</a><br>这里是想记录一些常见的让自己代码看起来很 low 的点，逐渐的在有时间的情况下把自己的代码写的好一点。<br>当忙起来的时候还真的是忙啊，真的是能完成功能就行，哪里还能想那么多优化，全凭之前的经验，哪个快速开发用哪个。感觉就像是一个无情的打字机器。</p>
</blockquote>
<a id="more"></a>
<p>这是知乎的编程领域下面一个非常火的提问。昨天晚上看到之后比较感兴趣，这里分享一下自己的看法。相信这个大家看到这个话题一定很感兴趣。</p>
<p>问题地址：<a href="https://www.zhihu.com/question/32039226" target="_blank" rel="noopener">https://www.zhihu.com/question/32039226</a> 。</p>
<h2 id="提问内容"><a href="#提问内容" class="headerlink" title="提问内容"></a>提问内容</h2><p>今年年初，到一家互联网公司实习，该公司是国内行业龙头。不过技术和管理方面，却弱爆了。那里的程序员，每天都在看邮件，查问题工单。这些问题，多半是他们设计不当，造成的。<br>代码写的一团糟，全是复制粘贴，连作者都没改，大家普遍不写注释，也不格式化，代码歪歪扭扭。<br>一个项目里，httpclient竟然出现了四种。一种是该公司研发部写的，一种是老版本的开源项目，一种是新版本的开源项目，还有一种是开发人员造的轮子。<br>打接口请求响应日志，竟然不知道用拦截器。打错误日志竟然不打上下文信息，每个人一种日志风格，千奇百怪。许多重要的中间流程，居然不打日志。<br>idea、eclipse、myeclipse的配置文件竟然全部传到项目里去了。<br>该公司混了两年的程序员，跟快递公司做查询接口，竟然不知道加密运单号。<br>所有服务间通讯，都没有设requestId，导致跟踪会话很困难。<br>一个没什么qps的边缘接口，居然做消费者生产者+阻塞队列的异步模式。显得你技术少是不是。不知道异步会增加维护成本，提高测试难度吗？而且，任务队里没有考虑持久化，赶上发布，丢了好多任务。<br>读取一个小小的xml和exc配置文件，居然用流式解析，没见过这么二逼的，真是醉了。<br>做优化全靠拍脑门拍大腿，难道不会用excel分析日志，用jprofile扫项目？一个100以内的常数集合遍历，他也要写个优化算法进去，算法跟业务还搅在一起，一团乱麻。每个人都在嚷嚷性能、算法、分布式计算……<br>几乎没有文档，全靠从代码反推逻辑。<br>有枚举他不用，非要在每个页面上，把枚举值挨个儿写死，知道后面改代码多么费劲吗？<br>欺骗性的变量名，里面存储的是AES加密的，变量名后缀却写成了DES；里面存的是小写字母，却写成upperStr。<br>一个方法十几个参数，有三分之一是极其简略的缩写，注释肯定也没有的。一个类写到三四千行是常事。<br>开发自测，居然要把代码全丢到公共机器上，而且都是走svn，他们把svn当ftp用。svn里面大量的无意义提交，一多半的提交连都编译不过去。我看到有个应届生，改了两句话，马上提交，说是怕代码丢失。<br>一个运行了两年的项目，spring的包扫描明显配错了，有些bean根本扫不进来，居然没有人发现。一半的bean在spring管理下，另一半的bean他们自己写单例模式来实例化。<br>他们用mysql来做审计系统，出报表，有个报表要跑8分钟。原来是有人用字符串来存多值（逗号分隔），sql里写了like，导致没有利用到索引。为什么不用pg，pg在sql编程方面，功能更丰富，更适合做统计，它本身就支持数组。<br>程序员们都是得过且过的态度，怎么把代码灌进去，跑的通测试，就算交差了。<br>为什么大型互联网公司，技术和管理这么差劲，是怎么形成的？（这家公司是卖机票的，没有明确说出公司名字，是怕给自己惹麻烦）</p>
<h2 id="热门回答"><a href="#热门回答" class="headerlink" title="热门回答"></a>热门回答</h2><blockquote>
<p>以下回答来自@陈萌萌。</p>
</blockquote>
<p>题主你看到了很多槽点，但我认为你不能只看到槽点和大概怎么解决。有没有想过怎么改进，如果是你的话你怎么做，这些项目里面临的主要挑 战是什么，次要的挑战又是什么？</p>
<p>不要只告诉我技术A弱爆了，用B就可以完爆这个项目了。<strong>你知道用B的优劣，B的适用场景以及适用B的成本吗？对于一间公司来说，成本是很重要的。</strong></p>
<p>我这里说的成本不是金钱。而是，假如你看不爽一份代码，你打算重构它，你觉得你需要投入多少时间，多少人力？重构之后，又要花费多少时间和人力去升级依赖这份代码的其他项目？不要以为开会无用，老板就只是在天天发邮件。如果你重构了一份代码，不能通过沟通说服其他组去升级他们的组件，又或者你只是重构了一份虽然很丑陋，但其实并没有多少程序依赖它的代码，又又或者你重构了代码只是让代码技术含量更高了，更好看了，却没给公司带来多少收入甚至KPI，那你的工作和成果就很尴尬了。</p>
<p>其实上述也解释了为什么你身边的同事都眼睁睁地看着这些丑陋的shit存在而无动于衷。因为他们也是需要投入成本的。先不论他们个人技术水平高低，<strong>试问谁愿意挑一个又艰难，又不能产生多少效益的任务去做？</strong>当然，你会说，写好代码是程序员的节操。抱歉，<strong>节操多少钱一斤</strong>，北京三环商品房多少钱一平？</p>
<p><strong>编程高手都有真爱，但现实就是编程高手凤毛麟角。我们身边的大部分同事可能只**</strong>是希望养家糊口，他们头上还挂着十几个bug等着修。我们数落他们没追求，但追求从来都不是嘴上说说，吐吐槽就能实现的。**</p>
<p><strong>人心如此，公司也如是。</strong></p>
<p>矛盾分主次，公司的目标都是一样的：用最少的成本投入到最能产生效益的项目中去，或者投入大成本去解决公司最需要解决的问题，这间公司才能继续运作。</p>
<p><strong>所以题主你想想，在你吐槽的个案中，有多少是公司真正关心的？有哪些是你的老板认为可以创造最大效益的？有哪些才是主要矛盾或者挑战需要最牛逼的人挺身而出第一时间解决？去辨别，解决这些关键的问题吧，骚年。必要时带上（忽悠）一队人马（同事）跟你一起干，苟富贵，勿相忘。</strong> 不要像祥林嫂一样，天天抱怨着生活，日日思考着辞职。得罪点说一句：“沦落”到要跟这样的人共事工作，难道自己身上就没有原因？</p>
<p>这个世界有更好的公司，有更牛逼的人。如果你认为解决这间公司的这堆问题不值得，又或者同事实在太不给力，就远走高飞吧。</p>
<p>我以前也跟题主一样，看我第一份正式工作的很多技术环节都相当不爽。这份代码写得丑，那个设计像大学生作品，重要的项目居然连单元测试都没有……<strong>但是我后来反观我自己，并没有发现比起那些丑陋代码和糟糕实现强悍多少。我跟我的同事没有质的区别。</strong> 我笑话他们代码混乱bug不尽，我何尝不是少处理了一个field，倒腾错了一个片段的数据搞到要翻工重跑？在我心底里艹了隔壁组那个“我的程序好像不能跑，你帮我debug下”的同事一千次之后，带我做ML让我倒腾数据并且被我的程序搞坏了几份数据（当然后来搞好了）的T9君在会议上说：“她已经很努力了，我承认我有时候也逼得她太紧，她应该有多些时间的。”</p>
<p>我不是长者，不能share多少人生经验，就留下最后一句话跟诸君共勉（好像有点怪）吧：</p>
<p><strong>我观别人大傻逼，料别人观我亦如是！</strong></p>
<h2 id="JavaGuide（作者）的个人感受"><a href="#JavaGuide（作者）的个人感受" class="headerlink" title="JavaGuide（作者）的个人感受"></a>JavaGuide（作者）的个人感受</h2><p>我觉得其实这事情挺常见的，特别是那种已经做了很久很久的老项目（新起的项目应该很少存在说技术比较 Low的情况）。</p>
<p>我之前接手老项目的时候，也会经常抱怨代码写的是真特么难、分层是真的恶心、使用的框架版本是真特么的老！</p>
<p>但是，到后面自己做一个项目比较久的时候就会发现：很多事情，并不是像你平时写写 Demo 那么简单！ 特别是像大公司的很多项目都比较复杂，项目的代码量也比较庞大。很多时候，你知道某个地方有问题，但是你没办法改，也不敢改啊！</p>
<p>为啥没办法改？ 下面最可能的几个原因大家应该都感同身受吧！</p>
<ol>
<li>没时间和精力（一个接一个的需求）。</li>
<li>向项目经历或者技术Leader提出优化需求之后被驳回。</li>
<li>涉及到的地方太多/工作量太大。</li>
</ol>
<p>为啥不敢改？ 我觉得很大一个原因是害怕背锅。万一改出了问题，那年终奖估计都没有了。</p>
<p>另外，就算是大公司，每个程序员的能力也千差万别，代码能力强的和代码能力差的可能不是一点两点。</p>
<p>最后，根据自己的经历给刚入职场或者即将入职场的小伙伴一个建议。如果你觉得你当前的项目技术很 Low 的话：</p>
<ol>
<li>第一时间记得先换位思考一下，设想假如这个项目是我做的呢（存在即是有原因的）？</li>
<li>然后，你可以试着去找找这个项目的亮点。</li>
<li>最后，如果你想好说可以优化的话，可以找 Leader 和项目经理聊聊，在 Code Review 的时候把自己的想法告诉大家一下。记得一定要说清楚：<ul>
<li>为啥要优化？能带来啥好处？</li>
<li>有没有什么风险？有风险的话怎么避免线上出问题？</li>
<li>涉及到哪些地方的改动？</li>
<li>……</li>
</ul>
</li>
</ol>
<h2 id="我的感受"><a href="#我的感受" class="headerlink" title="我的感受"></a>我的感受</h2><blockquote>
<p>似乎是在套娃。</p>
</blockquote>
<p><strong>我观别人大傻逼，料别人观我亦如是！</strong></p>
<p>前些阵子不是太忙的时候在做算法题，那是真的感觉思路的巧妙。我还在第一层，别人已经到了第五层的快乐。</p>
<p>但是回到现实，最快的速度完成业务需求，最快的速度完成需求更改才是公司需要的东西。至于内部的实现方法，优化的速度，只有在碰到瓶颈的时候（也就是主观上感觉太慢了的时候），才会去花时间优化。不然，等着拉长项目时间赔钱么。</p>
<p>也是自己对于一些设计模式和底层原理并不是太熟练，第一时间想到的层次太低。对项目需求的解析不是太准确，出现了预估工时太短的问题。工作经验的积累还是要记得及时的整理，新的东西也要敲一敲，熟练起来。、</p>
<p>总是有优秀的人在前面。</p>
]]></content>
      <tags>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>遇到的bug修复</title>
    <url>/2019/05/08/%E9%81%87%E5%88%B0%E7%9A%84bug%E4%BF%AE%E5%A4%8D/</url>
    <content><![CDATA[<h1 id="业务中遇到的bug积累之A-bean-with-that-name-has-already-been-defined-in-file"><a href="#业务中遇到的bug积累之A-bean-with-that-name-has-already-been-defined-in-file" class="headerlink" title="业务中遇到的bug积累之A bean with that name has already been defined in file"></a>业务中遇到的bug积累之A bean with that name has already been defined in file</h1><blockquote>
<p>这个BUG的全部信息为The bean ‘jwtAuthenticationTokenFilter’, defined in class path resource [config/SecurityConfig.class], could not be registered. A bean with that name has already been defined in file [E:\idea-workspace-web\out\production\classes\secruity\JwtAuthenticationTokenFilter.class] and overriding is disabled.</p>
</blockquote>
<a id="more"></a>
<p>一开始我只是发现了这里是因为bean的重名导致这种现象，并没有找到原因。</p>
<p>还好我们团队的技术大牛伸出援手，指出了问题的所在：在修改Spring Boot Gradle Plugin版本之前并没有出现这种状况。在版本2.0.4之前可以有以下这种技术写法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JwtAuthenticationTokenFilter</span> <span class="keyword">extends</span> <span class="title">OncePerRequestFilter</span> </span>&#123;</span><br><span class="line">	<span class="comment">//业务代码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> JwtAuthenticationTokenFilter <span class="title">jwtAuthenticationTokenFilter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">new</span> JwtAuthenticationTokenFilter();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>旧版本支持在两个地方同时注册同名的bean，不会报出今天的这个错误。而2.1.4则是修复了这个问题，必须要在这两个bean中做出选择，保留其一。</p>
]]></content>
      <tags>
        <tag>gradle</tag>
      </tags>
  </entry>
  <entry>
    <title>部署geojson切片服务</title>
    <url>/2020/08/08/%E9%83%A8%E7%BD%B2geojson%E5%88%87%E7%89%87%E6%9C%8D%E5%8A%A1/</url>
    <content><![CDATA[<h1 id="部署geojson文件矢量切片服务"><a href="#部署geojson文件矢量切片服务" class="headerlink" title="部署geojson文件矢量切片服务"></a>部署geojson文件矢量切片服务</h1><blockquote>
<p>客户答应的 Linux 虚拟机到了，终于可以直接部署 tippecanoe 了。<br>原本的 Geojson 文件太大了，还是需要切片。</p>
</blockquote>
<a id="more"></a>
<h2 id="安装-tippecanoe"><a href="#安装-tippecanoe" class="headerlink" title="安装 tippecanoe"></a>安装 tippecanoe</h2><p>登录 Linux 服务器</p>
<ol>
<li>准备好 tippecanoe 源码。</li>
<li>将服务器的 yum 源更改为 阿里源</li>
<li><p>下载依赖</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install gcc-c++</span><br><span class="line">yum install sqlite-devel</span><br><span class="line">yum install -y zlib zlib-devel</span><br></pre></td></tr></table></figure>
</li>
<li><p>将源码解压好后，在 tippecanoe 文件夹内</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">make -j</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>完成<br>具体的命令行参数参考：<a href="https://github.com/mapbox/tippecanoe" target="_blank" rel="noopener">https://github.com/mapbox/tippecanoe</a></p>
<h2 id="安装-FoxGIS-Server-Lite"><a href="#安装-FoxGIS-Server-Lite" class="headerlink" title="安装 FoxGIS Server Lite"></a>安装 FoxGIS Server Lite</h2><p>直接部署在了原本的 win 服务器上，通过 FTP 读取 tippecanoe 做好的矢量地图文件。</p>
<p>通过<a href="https://jingsam.github.io/foxgis-server-lite/#/deploy" target="_blank" rel="noopener">https://jingsam.github.io/foxgis-server-lite/#/deploy</a> 下载 win 包，直接启动 exe 文件即可。</p>
<h3 id="02-23更新"><a href="#02-23更新" class="headerlink" title="02-23更新"></a>02-23更新</h3><p>如果出现了环境问题，可以使用线上代码解决<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https://gitee.com/jingsam/foxgis-server-lite.git map-server</span><br><span class="line">cd map-server</span><br><span class="line">yarn install</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure></p>
<h2 id="部署调用命令-jar-包"><a href="#部署调用命令-jar-包" class="headerlink" title="部署调用命令 jar 包"></a>部署调用命令 jar 包</h2><p>这个程序是负责调用 Linux 中的切片程序，只需要放在 Linux 任意文件夹中开启来就完事了。只是一个开发接口调用命令而已。</p>
<h2 id="处理中间关联程序"><a href="#处理中间关联程序" class="headerlink" title="处理中间关联程序"></a>处理中间关联程序</h2><p>切片程序部署在 Linux 服务器上，将原本的 geojson 文件通过 FTP上传到 Linux 文件夹中。<br>再访问接口来启动切片命令，收到完成回复后，将 FTP 中切好的文件拿回 win 服务器。<br>放在 FoxGIS Server Lite 中，得到映射的 url 。</p>
<p>注意中间我关闭了 Linux 服务器的防火墙和 SeLinux，部署 FTP 的方式是用的之前的文章。</p>
]]></content>
      <tags>
        <tag>Geojson</tag>
      </tags>
  </entry>
</search>
